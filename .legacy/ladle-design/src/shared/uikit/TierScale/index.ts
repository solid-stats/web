// Pips lives inside TierScale (architecture.md leaf-granularity, D-02) and is
// re-exported here so the sibling TierChip can render the same discrete meter.
export { Pips, TierScale } from "./TierScale";
