// Entry for the shared state-matrix story helper. Internal helper (underscore-
// prefixed): consumed by family stories, NOT graduated into the package public
// barrel (src/index.ts).
export { StateCell, StateMatrix } from "./StateMatrix";
