// Pagination slice entry (KIT-02). The inert cursor affordance (Назад / Дальше +
// end-of-list) — graduated as `Pagination`. Presentational-only (D-01): no server,
// no cursor engine; the controlled props are the durable interface the v1.0 cursor
// swaps into.
export { Pagination } from "./Pagination";
