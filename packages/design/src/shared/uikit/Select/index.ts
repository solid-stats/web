// KIT-05 Select slice barrel (Plan 03-03). `Select` (the typed-value option control) +
// its `SelectOption` option type graduate; the `select` tv() recipe stays internal.
export type { SelectOption } from "./Select";
export { Select } from "./Select";
