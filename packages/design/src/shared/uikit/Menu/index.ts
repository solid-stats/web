// KIT-06 Menu slice barrel (Plan 03-06). `Menu` (the trigger-menu overlay) + its public
// `MenuItemData` item type graduate; the `menu` tv() recipe stays internal.
export type { MenuItemData } from "./Menu";
export { Menu } from "./Menu";
