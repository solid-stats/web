// KIT-06 Tabs slice barrel (Plan 03-06). `Tabs` (the roving-tabindex tab set) + its public
// `TabData` tab type graduate; the `tabs` tv() recipe stays internal.
export type { TabData } from "./Tabs";
export { Tabs } from "./Tabs";
