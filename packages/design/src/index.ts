// @solid-stats/design — UIKIT public barrel (the package `exports` map "." entry
// resolves here, D-05). Empty in Phase 1 (D-07: zero components). Components
// graduate into this barrel in later phases; the Smoke catalog story is NOT a
// UIKIT export and is intentionally absent here.
export {};
