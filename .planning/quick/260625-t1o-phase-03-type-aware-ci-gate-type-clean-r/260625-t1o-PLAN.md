---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 01
quick: 260625-t1o
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/src/shared/uikit/NavBar/NavBar.tsx
  - packages/design/src/shared/uikit/NavBar/NavBar.stories.tsx
  - packages/design/src/shared/uikit/MobileTabBar/MobileTabBar.tsx
  - packages/design/src/shared/uikit/MobileTabBar/MobileTabBar.stories.tsx
  - packages/design/src/shared/uikit/AppShell/AppShell.tsx
  - packages/design/src/shared/uikit/Table/Table.tsx
  - packages/design/src/shared/uikit/Table/AutoTable.tsx
  - packages/design/src/shared/uikit/Table/Table.stories.tsx
  - packages/design/src/shared/uikit/Toast/Toast.stories.tsx
  - packages/design/src/shared/uikit/ToastManager/ToastManager.tsx
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
  - packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx
  - packages/design/src/shared/uikit/Button/Button.stories.tsx
  - packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.stories.tsx
  - packages/design/src/shared/uikit/EmptyState/EmptyState.tsx
  - packages/design/src/shared/uikit/ErrorState/ErrorState.tsx
  - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx
  - packages/design/src/css-modules.d.ts
  - packages/design/tsconfig.json
  - packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts
  - packages/design/src/shared/uikit/_i18n/index.ts
  - .github/workflows/check.yml
  - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
  - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-VERIFICATION.md
autonomous: true
requirements: [KIT-08]

must_haves:
  truths:
    - "`pnpm check` exits 0 with the type-aware Oxlint/TS-Go checker active (typeCheck:true) — 0 type errors across packages."
    - "A misspelled or unknown `i18n._({ id })` is a compile error: the committed `@ts-expect-error` oracle is GREEN while the augmentation holds and turns RED (unused-directive) if it ever breaks."
    - "The Ladle CSS side-effect imports type-check (ambient `*.css` module decl present and inside the include)."
    - "CI runs `pnpm check` on every push and pull_request (`.github/workflows/check.yml` present)."
    - "The no-direct-vite-dep boundary and the Ladle build are intact (no `vite` dep added; `ladle:build` still produces meta.json)."
  artifacts:
    - packages/design/src/css-modules.d.ts
    - packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts
    - .github/workflows/check.yml
  key_links:
    - "packages/design/src/shared/uikit/_i18n/lingui.d.ts (Register.messageIds augmentation) ← exercised by → typed-key.oracle.ts"
    - "packages/design/vite.config.ts (no-direct-vite-dep boundary) ← preserved by → tsconfig config-file exclusion"
    - "root vite.config.ts (typeCheck:true) ← invoked by → .github/workflows/check.yml `pnpm check`"
---

<objective>
Close Phase-03 SC#4 (KIT-08): the typed message-id contract (`lingui.d.ts` augments
`@lingui/core` `Register.messageIds = keyof typeof STRINGS`) is only enforced by a full type
check. The root `vite.config.ts` already makes `pnpm check` type-aware (typeCheck:true) and
`@types/react@19` is installed (HEAD e72894c) — so the type check now runs and surfaces 33 REAL
pre-existing type errors that were always latent but never checked.

This plan makes `pnpm check` exit 0 by fixing those errors as CLASSES (no per-line
suppressions), adds a committed `@ts-expect-error` regression oracle so the typed-key gate
cannot silently regress, adds the GitHub Actions CI that runs `pnpm check` on every PR/push,
and records the SC#4 resolution in the phase-03 artifacts.

Purpose: make the KIT-08 typed-key contract a permanent, exercised CI gate — not a structurally
declared but unverified one.

Output: a type-clean repo, the oracle file, the CI workflow, and the updated UAT/VERIFICATION
entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md

# The typed-key contract under test and its config
@packages/design/src/shared/uikit/_i18n/lingui.d.ts
@packages/design/src/shared/uikit/_i18n/i18n.ts
@packages/design/src/shared/uikit/_i18n/index.ts
@packages/design/src/shared/uikit/_i18n/catalogs.test.ts
@packages/design/tsconfig.json
@tsconfig.base.json
@packages/design/vite.config.ts
@vite.config.ts
@package.json

# CONVENTIONS — read the SKILL.md AND every named reference (a subagent reads ONLY named files)
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/typescript.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/tests.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
@.agents/skills/solidstats-shared-testing-standards/SKILL.md
@.agents/skills/solidstats-shared-planning-standards/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `pnpm check` type-clean — fix the 33 errors as classes + ambient CSS decl + config-file exclusion</name>
  <files>
    packages/design/src/shared/uikit/NavBar/NavBar.tsx,
    packages/design/src/shared/uikit/MobileTabBar/MobileTabBar.tsx,
    packages/design/src/shared/uikit/AppShell/AppShell.tsx,
    packages/design/src/shared/uikit/Table/Table.tsx,
    packages/design/src/shared/uikit/Table/AutoTable.tsx,
    packages/design/src/shared/uikit/Table/Table.stories.tsx,
    packages/design/src/shared/uikit/Toast/Toast.stories.tsx,
    packages/design/src/shared/uikit/ToastManager/ToastManager.tsx,
    packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx,
    packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx,
    packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx,
    packages/design/src/shared/uikit/Button/Button.stories.tsx,
    packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.stories.tsx,
    packages/design/src/shared/uikit/EmptyState/EmptyState.tsx,
    packages/design/src/shared/uikit/ErrorState/ErrorState.tsx,
    packages/design/src/shared/uikit/Skeleton/Skeleton.tsx,
    packages/design/src/css-modules.d.ts,
    packages/design/tsconfig.json
  </files>
  <action>
First run `./node_modules/.bin/vp check packages` to see the live error set (33 errors at plan
time). Fix each as a CLASS per typescript.md + component-shape.md; NO per-line `// @ts-expect-error`
or `any`, NO disabling typeCheck, NO `!` non-null on indexed access.

CLASS A — `Promise<ReactNode>` story-helper return types (the ~10 TS2322 in Table.stories.tsx +
the 1 in AsyncBoundary.stories.tsx + the TS2769 "(density) => ..." mismatch at Table.stories.tsx:300).
Root cause: helpers are typed `ReturnType<Story>`. `Story extends React.FC` and in `@types/react@19`
a function component returns `ReactNode | Promise<ReactNode>`, so `ReturnType<Story>` includes
`Promise<ReactNode>` — not a valid JSX child nor a `(density: TableDensity) => ReactNode` value.
These helpers are SYNCHRONOUS (each returns one element/fragment). Fix: type each as
`ReactElement` imported as a type from "react" (`import type { ReactElement } from "react"`).
Apply to every synchronous helper currently annotated `ReturnType<Story>`: `dataTable`,
`VolumeCaption`, `renderRows` (the `(density: TableDensity) => ReactElement` inside AutoDensity —
this also resolves the AutoTable `rows` prop TS2769), `LoadingTable`, and in AsyncBoundary.stories.tsx
`readyContent`. Keep the exported `Story`-typed stories themselves as `Story` (their return is genuinely
the FC return). Do not touch the actual `AutoTable` `rows` prop type unless the mismatch persists —
typing `renderRows` as `ReactElement` is the assignable side; if `AutoTable.rows` is declared
`(density: TableDensity) => ReactNode`, that already accepts `ReactElement`.

CLASS B — `exactOptionalPropertyTypes` (the ~16 TS2375 + the 2 TS2322 on Skeleton/EmptyState/ErrorState
props). An optional prop declared `prop?: T` rejects an explicit `T | undefined` argument under
`exactOptionalPropertyTypes:true`. Per typescript.md ("handle the `T | undefined`") + component-shape
(props are genuinely optional-or-absent on these presentational primitives), apply ONE consistent
pattern, do not mix ad-hoc per call site:
  (B1) For a primitive's OWN optional prop that legitimately accepts an absent value, widen the prop
       declaration to `prop?: T | undefined` (explicit `| undefined`). Apply to the offending props on:
       EmptyState (`totalCount`), ErrorState (`contact`), Skeleton (`density`), Toast (`action`),
       StatTile (`delta`), DataTrustBanner (`label`), Table (`onSort`), Th (`numeric`, `onSort` — the
       Table.tsx `<Th>` callsite), NavBar/Link forwarded props (`href`, `active`, `aria-current`,
       `data-state`, `roleExtras`), Button (`type`, `className` at the Button.stories callsite),
       AutoTable→Table (`onSort`). Where the prop is forwarded via `Omit`/reused base props, widen at
       the base declaration only (component-shape: a wrapper reuses base props, does not re-declare).
  (B2) Where a PARENT computes a maybe-undefined value and forwards it (AsyncBoundary →
       EmptyState/ErrorState/Skeleton, ToastManager → Toast `action`, AppShell → NavBar props),
       once the target prop is widened per B1 the call type-checks; do NOT additionally branch/omit.
Decide B1-vs-omit once and document the choice in the SUMMARY. Prefer B1 (widen the declared optional
prop) as the uniform pattern here — these are leaf presentational primitives whose optional props
model genuine absence; omit-when-undefined at every callsite would scatter the same decision across
many files. Keep prop order system-first per component-shape; do not reorder existing props.

CLASS C — `noUncheckedIndexedAccess` (TS2532 ×2: `ONE[0].icon` in NavBar.stories.tsx:76 and
MobileTabBar.stories.tsx:61). Guard the index access — do NOT `!`. Read the local `ONE` definition;
if it is a non-empty literal tuple, derive the icon from a named element (e.g. a `const [first] = ONE`
destructure with a guard, or reference the source nav item directly) so the value is `T` not
`T | undefined`. Pick whichever reads cleanest given how `ONE` is built; no `!`, no `as`.

CLASS D — CSS side-effect imports (TS2882 ×2 in packages/design/.ladle/components.tsx:
`../src/styles/fonts.css` and `./tailwind.css`). Create ONE ambient decl file
`packages/design/src/css-modules.d.ts` declaring a side-effect module for `*.css` (a bare
`declare module "*.css";` ambient block — no exports needed for side-effect imports). It must be
INSIDE the type-check include: it sits under `src` (already in `include`), so no include change is
needed for the decl itself — but confirm `.ladle/components.tsx` (under `.ladle`, also in `include`)
now resolves both imports. Do not add a CSS-modules `:export` shape; these are plain side-effect
stylesheets.

CLASS E — config files outside the type-check scope (TS2307 `vite` in packages/design/vite.config.ts:12,
and the TS2769 in packages/design/playwright.config.ts:36). The no-direct-vite-dep architecture
(vite.config.ts header + architecture.md: Ladle owns the bundled Vite, packages/design must not add a
direct `vite` dep) means `import type { UserConfig } from "vite"` cannot resolve — and SHOULD not, by
design. PREFER excluding the config files from the type-check scope over adding a `vite` dep. In
packages/design/tsconfig.json add an `"exclude"` for the root config files: `vite.config.ts`,
`playwright.config.ts`, and `.ladle/config.mjs` is `.mjs` (not type-checked) — exclude `vite.config.ts`
and `playwright.config.ts`. Note `include` is `["src", ".ladle"]`; `vite.config.ts`/`playwright.config.ts`
live at the package ROOT (outside both include globs) yet are still being type-checked — confirm whether
they are pulled in transitively; if `exclude` does not remove them because they are not in `include`,
the correct fix is they should already be out of scope — re-run check to confirm which mechanism applies
and apply the minimal one (exclude entry) that drops both config-file errors WITHOUT adding a `vite`
dep and WITHOUT excluding any `src`/`.ladle` source. Keep the `paths` block and the Lingui workaround
comment in tsconfig.json untouched. Do NOT touch packages/design/vite.config.ts's resolve.alias or the
root vite.config.ts.

After all classes: re-run `./node_modules/.bin/vp check packages` until it reports 0 errors. Then run
the full unit + e2e + ladle:build gates (see verify) to prove nothing regressed — the prop-widening must
not change any runtime behavior (widening `?: T` to `?: T | undefined` is type-only).
  </action>
  <verify>
    <automated>./node_modules/.bin/vp check packages</automated>
    Exits 0 (0 type errors, formatting + lint clean). Equivalent: `pnpm check` exits 0.
    Also: `pnpm --filter @solid-stats/design test` (186 unit tests pass),
    `pnpm --filter @solid-stats/design test:e2e` (344 e2e pass),
    `pnpm --filter @solid-stats/design ladle:build` (meta.json produced — no-direct-vite-dep + Ladle
    build intact).
  </verify>
  <done>
    `./node_modules/.bin/vp check packages` (and `pnpm check`) exits 0 with the type-aware checker
    active. All 33 errors fixed as classes — no `// @ts-expect-error`/`any`/`!`-on-index, no disabled
    typeCheck, no added `vite` dep. Ambient `packages/design/src/css-modules.d.ts` present and inside
    the include. Unit/e2e/ladle:build all green. theme.css generation untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the @ts-expect-error typed-key regression oracle + the CI workflow</name>
  <files>
    packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts,
    packages/design/src/shared/uikit/_i18n/index.ts,
    .github/workflows/check.yml
  </files>
  <action>
ORACLE (KIT-08 SC#4 regression guard). Create
`packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts`. It must be inside the type-check
include (it sits under `src`, already in `include` — confirm). Semantics: while the `lingui.d.ts`
augmentation holds, `i18n._({ id: "<unknown>" })` is a compile error; a single `@ts-expect-error`
directive immediately above it SUPPRESSES that error → green. If the augmentation ever breaks (id
widens back to `string`), the call no longer errors → the `@ts-expect-error` becomes an unused
directive → tsgolint reports it → red. This is the ONLY sanctioned suppression in the repo.

Content shape (directive prose, executor writes the code): import the runtime `i18n` from `./i18n`.
Export a single bare const that references `i18n._` with a deliberately unknown id, the line directly
above the call carrying `// @ts-expect-error unknown message id must be a compile error (KIT-08 SC#4)`.
Keep it side-effect-light: a plain exported `const` whose initializer is the `i18n._({ id: "no.such.key.exists", message: "" })` call (the bound `message` satisfies the runtime signature). It must NOT
break runtime/Ladle — a bare exported const is tree-shakeable and harmless; do not call it, do not
import it from any story or runtime path. Add a head comment explaining the green/red semantics so a
future reader does not "fix" the intentional bad id. Place + name per tests.md / localization.md (the
`_i18n` segment owns the typed-key contract; `.oracle.ts` names its purpose). Re-export is NOT required,
but if the file is tree-shaken out of the type-check graph it would not be evaluated — it is reachable
because it is in the `include` glob (tsconfig type-checks every included file, not only imported ones).
To be certain it is exercised, add a type-only re-export line to `packages/design/src/shared/uikit/_i18n/index.ts`
ONLY IF needed to keep it in graph — prefer NOT polluting the public barrel; the `include` glob alone
keeps it checked, so default to NOT touching index.ts unless a check run shows the oracle file is
skipped. Verify the oracle is actually evaluated: temporarily flip the bad id to a real STRINGS key,
re-run check, confirm the `@ts-expect-error` then reports UNUSED (proving the directive is live), then
restore the bad id and confirm green.

CI (none exists). Create `.github/workflows/check.yml` (remote = github solid-stats/web). Trigger on
`pull_request` and `push`. Single job `check` on `ubuntu-latest`:
  1. `actions/checkout@v4`
  2. enable corepack + pin pnpm via the `packageManager` field (`corepack enable`) — package.json pins
     `pnpm@11.6.0`; do NOT hardcode a second pnpm version, let corepack read the pin.
  3. `actions/setup-node@v4` with `node-version: 25` (mirror engines `>=25 <26`) and `cache: pnpm`.
  4. `pnpm install --frozen-lockfile`
  5. `pnpm check`  ← the real gate (gen-theme drift + design.md lint + type-aware `vp check`).
Keep it minimal and deterministic; the Playwright/e2e matrix is out of scope for this gate workflow
(this plan wires the type-aware lane only). Mirror engines from package.json (node >=25 <26, pnpm
11.6.0). Use `actions/setup-node` pnpm cache (requires corepack-enabled pnpm before the cache step, or
order the steps so pnpm exists when setup-node resolves the cache — enable corepack, then setup-node
with cache:pnpm, then install).
  </action>
  <verify>
    <automated>./node_modules/.bin/vp check packages</automated>
    Exits 0 WITH the oracle file present (the `@ts-expect-error` is consumed → green). Prove the oracle
    is live: `grep -n "ts-expect-error" packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts`
    returns exactly one directive. Workflow lints as valid YAML:
    `node -e "const y=require('node:fs').readFileSync('.github/workflows/check.yml','utf8'); if(!/pnpm check/.test(y)||!/pull_request/.test(y)) throw new Error('check.yml missing gate or trigger')"`.
  </automated>
  <done>
    `packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts` exists with exactly one
    `@ts-expect-error` directive guarding an unknown `i18n._({ id })`; `vp check` is green with it
    present and was proven live (flip-to-real-key shows the directive go UNUSED, then restored).
    `.github/workflows/check.yml` exists, triggers on push + pull_request, and runs `pnpm check` on
    Node 25 / pnpm 11.6 (corepack). No public-barrel pollution unless the include-glob check required it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the SC#4 resolution in the phase-03 UAT + VERIFICATION artifacts</name>
  <files>
    .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md,
    .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-VERIFICATION.md
  </files>
  <action>
Record the decision factually (no scope language). The recorded UAT decision: a PERMANENT CI
type-aware gate, NOT structural-only.

In 03-UAT.md test #1 ("SC#4 typed-key gate (KIT-08)"): set `result:` to passed/resolved and add a
short resolution note: the type-aware gate is now permanent — root `vite.config.ts` `typeCheck:true`
makes `pnpm check` (and CI) run the full TS-Go/tsgolint type check; the repo is type-clean
(`pnpm check` exits 0); a committed `@ts-expect-error` oracle
(`packages/design/src/shared/uikit/_i18n/typed-key.oracle.ts`) regression-guards the contract; CI
(`.github/workflows/check.yml`) runs `pnpm check` on every push + PR. Update the `## Summary` counts
(passed +1, pending -1) and the `## Current Test` block to reflect resolution. Leave tests #2–#5
(visual QUAL passes) as pending — this plan does not touch them.

In 03-VERIFICATION.md: update the SC#4 entry (frontmatter `behavior_unverified_items` /
`human_verification`, the Observable-Truths row `SC#4`, the "Specific Probe Results → KIT-08" section,
and the "Human Verification Required → 1" section) from PRESENT_BEHAVIOR_UNVERIFIED to VERIFIED, citing:
the now-active type-aware gate, the 0-error `pnpm check`, the oracle file, and the CI workflow. Keep the
prose factual and minimal; do not rewrite unrelated sections. Update `behavior_unverified: 1` → `0` and
`status: human_needed` → `verified` in the frontmatter if (and only if) SC#4 was the sole open item —
it is, per the report. Adjust `score` annotation prose so it reads 9/9 truths VERIFIED (drop the
"1 behavior-unverified" qualifier).
  </action>
  <verify>
    <automated>grep -n "typed-key.oracle.ts\|check.yml\|VERIFIED" .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-VERIFICATION.md | head</automated>
    Confirms the SC#4 entry now cites the oracle + CI and reads VERIFIED. Also
    `grep -n "result:" .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md`
    shows test #1 no longer `[pending]`.
  </automated>
  <done>
    03-UAT.md test #1 records the permanent-CI-type-aware-gate resolution (result passed, summary
    counts updated); 03-VERIFICATION.md SC#4 reads VERIFIED citing the oracle file + CI workflow +
    0-error `pnpm check`; both kept factual; tests #2–#5 untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | The GitHub Actions workflow installs deps and runs the gate; a compromised/over-broad workflow could execute arbitrary install scripts. |
| type-only oracle → runtime/Ladle | The oracle file lives in `src`; it must stay side-effect-light so it never reaches the Ladle bundle or runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03q-01 | Tampering | `.github/workflows/check.yml` deps install | medium | mitigate | `pnpm install --frozen-lockfile` (no lockfile drift); pin actions to major (`@v4`); corepack reads the `packageManager` pin (no arbitrary pnpm version). No new packages added by this plan. |
| T-03q-02 | Tampering | typed-key gate silent regression | high | mitigate | The committed `@ts-expect-error` oracle turns RED (unused directive) the moment the augmentation breaks — the gate cannot silently revert to `string` ids. |
| T-03q-03 | Tampering | suppression sprawl | low | accept | Exactly one sanctioned `@ts-expect-error` (the oracle); the class fixes use no suppressions. Reviewer/CI catches any new blanket suppression. |
| T-03q-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan (no new deps; `@types/react` already added at HEAD e72894c). No legitimacy gate needed. |
</threat_model>

<verification>
- `pnpm check` exits 0 (gen-theme drift clean + design.md lint clean + type-aware `vp check` with 0
  type errors).
- `pnpm --filter @solid-stats/design test` — 186 unit tests pass.
- `pnpm --filter @solid-stats/design test:e2e` — 344 e2e pass.
- `pnpm --filter @solid-stats/design ladle:build` — meta.json produced (no-direct-vite-dep + Ladle
  build intact).
- The oracle is proven live (flip-to-real-key → directive UNUSED → restore → green).
- `.github/workflows/check.yml` triggers on push + pull_request and runs `pnpm check`.
- No new `vite` dependency in packages/design; theme.css generation untouched.
</verification>

<success_criteria>
- `pnpm check` exits 0 with `typeCheck:true` active across packages (0 type errors).
- An unknown `i18n._({ id })` is a compile error, guarded by the single committed `@ts-expect-error`
  oracle.
- CI (`.github/workflows/check.yml`) runs `pnpm check` on every push + PR.
- Phase-03 UAT #1 + VERIFICATION SC#4 record the permanent CI type-aware gate resolution as
  VERIFIED.
- All fixes are class-level; the only suppression in the repo is the intentional oracle directive.
</success_criteria>

<output>
Create `.planning/quick/260625-t1o-phase-03-type-aware-ci-gate-type-clean-r/260625-t1o-SUMMARY.md` when done.
</output>
