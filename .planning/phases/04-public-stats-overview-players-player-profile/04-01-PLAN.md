---
phase: 04-public-stats-overview-players-player-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/src/surfaces/public-stats/_fixtures/publicStats.ts
  - packages/design/src/surfaces/public-stats/_fixtures/publicStats.test.ts
  - packages/design/src/surfaces/public-stats/_fixtures/index.ts
  - packages/design/src/surfaces/public-stats/_harness/PublicStatsSurfaceHarness.tsx
  - packages/design/src/surfaces/public-stats/_harness/PublicStatsSurfaceHarness.stories.tsx
  - packages/design/src/surfaces/public-stats/_harness/index.ts
  - packages/design/src/shared/uikit/_fixtures/strings.ts
autonomous: true
requirements: [SURF-01, SURF-02, SURF-03, QUAL-01, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "The public-stats trio reads one canonical fixture graph for Overview, Players, and Player Profile per D-03/D-04."
    - "The shared surface harness renders shell, loading, freshness, provenance, and state/data-volume cells for later surface stories per D-05/D-07."
    - "RU and EN strings needed by Phase 4 exist at parity before surface components consume them."
  artifacts:
    - path: "packages/design/src/surfaces/public-stats/_fixtures/publicStats.ts"
      provides: "Canonical Phase 4 public-stats fixture graph"
      exports: ["PUBLIC_STATS", "PUBLIC_STATS_VOLUMES", "PUBLIC_STATS_PERIODS", "toPlayerCompactRow", "toTierCell"]
    - path: "packages/design/src/surfaces/public-stats/_harness/PublicStatsSurfaceHarness.tsx"
      provides: "Shared Ladle surface frame for public-stats stories"
      exports: ["PublicStatsSurfaceHarness", "PublicStatsStateMatrix"]
  key_links:
    - from: "packages/design/src/surfaces/public-stats/_fixtures/publicStats.ts"
      to: "packages/design/src/shared/uikit/_fixtures/roster.ts"
      via: "imports ROSTER, OVERVIEW_PLAYERS, scoreOf, kdOf"
      pattern: "(ROSTER|OVERVIEW_PLAYERS|scoreOf|kdOf)"
    - from: "packages/design/src/surfaces/public-stats/_harness/PublicStatsSurfaceHarness.tsx"
      to: "packages/design/src/shared/uikit/AppShell/AppShell.tsx"
      via: "wraps every surface in AppShell"
      pattern: "AppShell"
---

<objective>
Create the shared Phase 4 data and story harness that every public-stats surface uses.

Purpose: prevent fixture drift across SURF-01/SURF-02/SURF-03 while preserving the design-only Ladle boundary from D-01/D-02.
Output: canonical fixtures, invariant tests, shared shell/state/trust harness, and Phase 4 string keys.
</objective>

## Phase Goal

**As a** SolidGames visitor, **I want to** inspect the Stats Overview, Players list, and Player Profile with one consistent stats model, **so that** I can trust ranks, tiers, loading states, and provenance across the public stats surface.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-stats-overview-players-player-profile/04-CONTEXT.md
@.planning/phases/04-public-stats-overview-players-player-profile/04-RESEARCH.md
@.planning/phases/04-public-stats-overview-players-player-profile/04-PATTERNS.md
@.planning/phases/04-public-stats-overview-players-player-profile/04-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/domain-rules.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
</context>

## Artifacts this phase produces

- `PUBLIC_STATS`, `PUBLIC_STATS_VOLUMES`, `PUBLIC_STATS_PERIODS`, `toPlayerCompactRow`, `toTierCell`, `PublicStatsPeriod`, `PublicStatsPlayer`, `PublicStatsProfile`, `PublicStatsOverview`
- `PublicStatsSurfaceHarness`, `PublicStatsStateMatrix`, `PublicStatsStoryFrame`, `PublicStatsLang`
- New `STRINGS` keys for public-stats headings, controls, state copy, entry labels, provenance explanation labels, and profile tab labels
- New paths under `packages/design/src/surfaces/public-stats/_fixtures/` and `_harness/`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the canonical fixture and i18n invariant tests</name>
  <files>packages/design/src/surfaces/public-stats/_fixtures/publicStats.test.ts, packages/design/src/surfaces/public-stats/_fixtures/index.ts</files>
  <read_first>
    @packages/design/src/shared/uikit/_fixtures/roster.ts
    @packages/design/src/shared/uikit/_fixtures/tiers.ts
    @packages/design/src/shared/uikit/_fixtures/_fixtures.test.ts
    @packages/design/src/shared/uikit/_fixtures/tiers.test.ts
    @packages/design/src/shared/uikit/_i18n/catalogs.test.ts
    @.planning/phases/04-public-stats-overview-players-player-profile/04-CONTEXT.md
    @.planning/phases/04-public-stats-overview-players-player-profile/04-PATTERNS.md
  </read_first>
  <behavior>
    - Test that Vasiliy is rank #1 in overview, players list, and profile context per D-04.
    - Test every exported player recomputes `score` via `scoreOf` and `kd` via `kdOf` per D-03/D-04.
    - Test every score and K/D tier is derived with `tierFor(metric, value, SS_BASELINE, period)` for both `rotation` and `alltime`.
    - Test data-volume exports include `empty`, `few`, `many`, and `limitReached` for Overview, Players, and Profile.
    - Test every new public-stats `STRINGS` key resolves to non-empty RU and EN.
  </behavior>
  <action>Create a Vitest suite for the Phase 4 fixture graph before implementing it. Import only from existing fixture seeds and the local fixture barrel created by this plan. Keep the tests behavior-oriented; do not snapshot generated objects. Cite D-03/D-04 in test descriptions or comments only where the invariant would otherwise be non-obvious.</action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- publicStats.test.ts</automated>
  </verify>
  <acceptance_criteria>The command fails before `publicStats.ts` exists and passes after Task 2; failure assertions name formula, tier, rank, volume, or locale parity drift.</acceptance_criteria>
  <done>The red tests define the shared public-stats graph contract without adding production fixture logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared public-stats fixture graph and Phase 4 strings</name>
  <files>packages/design/src/surfaces/public-stats/_fixtures/publicStats.ts, packages/design/src/surfaces/public-stats/_fixtures/index.ts, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    @packages/design/src/shared/uikit/_fixtures/roster.ts
    @packages/design/src/shared/uikit/_fixtures/tiers.ts
    @packages/design/src/shared/uikit/_fixtures/strings.ts
    @packages/design/src/shared/uikit/Table/Table.tsx
    @packages/design/src/shared/uikit/CompactRow/CompactRow.tsx
    @.planning/phases/04-public-stats-overview-players-player-profile/04-UI-SPEC.md
  </read_first>
  <action>Implement `publicStats.ts` as the single Phase 4 fixture source per D-03/D-04. Import `ROSTER`, `OVERVIEW_PLAYERS`, `scoreOf`, `kdOf`, `SS_BASELINE`, and `tierFor`; do not import frozen hi-fi reference code. Export typed overview, players, and profile view data with raw counters plus derived display fields, period metadata, freshness/provenance objects, and volume variants. Add public-stats copy to `STRINGS` in the existing bilingual shape only; `catalogs.ts` must continue deriving RU/EN from `STRINGS` without local edits. Keep all player/profile concepts under `surfaces/public-stats`, not `shared/uikit`, per D-01/D-02.</action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- publicStats.test.ts</automated>
  </verify>
  <acceptance_criteria>Fixture tests pass; a targeted `rg` check confirms public-stats source files do not import frozen hi-fi reference code; `rg 'Vasiliy' packages/design/src/surfaces/public-stats/_fixtures/publicStats.ts` shows the shared graph keeps him as the top reference.</acceptance_criteria>
  <done>Overview, Players, and Profile can consume one typed graph with formula-derived stats, period-aware tiers, data volumes, and RU/EN copy.</done>
</task>

<task type="auto">
  <name>Task 3: Build the shared public-stats surface harness story</name>
  <files>packages/design/src/surfaces/public-stats/_harness/PublicStatsSurfaceHarness.tsx, packages/design/src/surfaces/public-stats/_harness/PublicStatsSurfaceHarness.stories.tsx, packages/design/src/surfaces/public-stats/_harness/index.ts</files>
  <read_first>
    @packages/design/src/shared/uikit/AppShell/AppShell.tsx
    @packages/design/src/shared/uikit/AppShell/AppShell.stories.tsx
    @packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
    @packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.tsx
    @packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.tsx
    @packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx
    @packages/design/src/shared/uikit/_i18n/i18n.ts
    @packages/design/src/surfaces/public-stats/_fixtures/publicStats.ts
  </read_first>
  <action>Create a surface-only harness that composes `AppShell`, `AsyncBoundary`, `FreshnessPill`, `ProvenanceLine`, and the existing state-matrix helpers per D-05/D-07. The harness must accept `lang`, `activeKey`, `state`, `children`, and trust/provenance props; it must resolve copy through `i18n._({ id, values })` at the surface/story layer and pass plain strings into UIKIT primitives. Add a Ladle story proving success, loading, empty, error, offline, reconnecting, and stale states inside the public-stats shell. Do not export the harness from `packages/design/src/index.ts`.</action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test && pnpm --filter @solid-stats/design test:e2e --grep "public-stats"</automated>
  </verify>
  <acceptance_criteria>The harness story appears in Ladle meta after build; catalog tests include it automatically; `data-app-shell`, `data-async-boundary`, freshness, and provenance hooks are visible in the story DOM.</acceptance_criteria>
  <done>Later Phase 4 surface stories can mount inside one shared shell/state/trust contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture data to rendered story | Mock stats and copied labels cross into public UI; they must not imply unsupported certainty. |
| Story controls to components | Ladle args change public-stats states; they must remain inert design controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `publicStats.ts` formulas | medium | mitigate | Recompute Score/K/D and tiers in Vitest from canonical helpers per D-03/D-04. |
| T-04-02 | Information Disclosure | profile/identity fixture data | low | mitigate | Keep story identity data mocked and mask any SteamID-like value to last four if introduced. |
| T-04-03 | Spoofing | external/profile links in fixtures | low | mitigate | Use known-safe inert/story hrefs or `https://sg.zone/profile/<nick>` only; no preview/fetch behavior. |
| T-04-SC | Tampering | package installs | high | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
Run `pnpm --filter @solid-stats/design test -- publicStats.test.ts`, then `pnpm --filter @solid-stats/design test` after the harness lands.
</verification>

<success_criteria>
The shared fixture graph and harness exist, all invariant tests pass, and no Phase 4 production file imports frozen hi-fi code or adds domain UI to `shared/uikit`.
</success_criteria>

<output>
Create `.planning/phases/04-public-stats-overview-players-player-profile/04-01-SUMMARY.md` when done.
</output>
