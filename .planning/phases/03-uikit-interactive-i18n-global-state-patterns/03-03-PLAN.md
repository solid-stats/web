---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/design/src/shared/uikit/Select/Select.tsx
  - packages/design/src/shared/uikit/Select/select.ts
  - packages/design/src/shared/uikit/Select/Select.stories.tsx
  - packages/design/src/shared/uikit/Select/index.ts
  - packages/design/src/shared/uikit/Stepper/Stepper.tsx
  - packages/design/src/shared/uikit/Stepper/stepper.ts
  - packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
  - packages/design/src/shared/uikit/Stepper/index.ts
  - packages/design/src/index.ts
  - packages/design/tests/keyboard.spec.ts
autonomous: true
requirements: [KIT-05, QUAL-01, QUAL-02, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "Select opens a popover of options with full keyboard (arrows / Home / End / type-ahead), no trap; the active option is cyan paired with the selected state (never color-alone); placeholder is selectPlaceholder; options typed by their value union (KIT-05, SC#1, a11y.md)."
    - "Stepper renders its value in tabular mono (14px / 400, alignment from the mono face); inc/dec controls are ≥44px and carry stepperIncrement/stepperDecrement aria-labels (KIT-05, UI-SPEC)."
    - "Both compose under the Field wrapper for label/error/required/disabled; every Select/Stepper story is axe-clean + 44px (QUAL-03)."
  artifacts:
    - "packages/design/src/shared/uikit/Select/Select.tsx + select.ts — Ark Select wrapped in a per-part tv() recipe"
    - "packages/design/src/shared/uikit/Stepper/Stepper.tsx + stepper.ts — Ark NumberInput, mono value, ≥44px aria-labelled triggers"
    - "Select.stories.tsx + Stepper.stories.tsx — StateMatrix + Playground, ×4 data-volume for Select options"
  key_links:
    - "Select.Root value/onValueChange controlled → typed value union; active option cyan + aria-selected"
    - "NumberInput.Root value(string)/onValueChange → mono Input part; inc/dec triggers get aria-label props the slice does not invent"

premises:
  - claim: "Ark Select exposes Root/Trigger/Positioner/Content/Item parts with className per part and full keyboard nav built in"
    src: 03-RESEARCH.md#Don't-Hand-Roll
    verify: "node -e \"require.resolve('@ark-ui/react/select')\""
  - claim: "Ark NumberInput value is a STRING via value/onValueChange; parts Root/Label/Control/Input/IncrementTrigger/DecrementTrigger"
    src: 03-RESEARCH.md#Code-Examples
    verify: "node -e \"require.resolve('@ark-ui/react/number-input')\""
  - claim: "the Field wrapper from Plan 03-02 is the label/error seam Select/Stepper nest under"
    src: packages/design/src/shared/uikit/Field/Field.tsx
    verify: "test -f packages/design/src/shared/uikit/Field/Field.tsx"

carried_forward:
  - claim: "merge-free /lite — hold mutually-exclusive utilities as variants, not a passed className"
    src: .planning/STATE.md#Accumulated-Context
  - claim: "the popover surface uses surface-1 + border-2 + --shadow-md (floating UI = the only shadow use)"
    src: packages/design/src/styles/theme.css#L111
---

<objective>
Ship the second form-family slice group (KIT-05): `Select` (Ark Select — typed-value options, keyboard nav, cyan active option) and `Stepper` (Ark NumberInput — tabular-mono value, ≥44px aria-labelled inc/dec). Both nest under the `Field` wrapper from Plan 03-02 for their label/error/required/disabled broadcast.

Purpose: KIT-05 needs selects and steppers as keyboard-accessible, focus-managed form controls (SC#1). Ark owns the roving-tabindex / type-ahead / Home-End semantics — the slice only adds the `tv()`-per-part recipe + `data-*` hooks + aria-labels (the D-01 boundary; Don't-Hand-Roll). The ×4 data-volume states (empty / few / many / limit-reached option lists) and the ×7 component states are demonstrated per slice (QUAL-01, D-07).
Output: `Select` + `Stepper` slices (component + recipe + stories + barrel); the overlay/keyboard specs for select-popover-no-trap extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@packages/design/src/shared/uikit/Field/Field.tsx
@packages/design/src/shared/uikit/Button/control.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select — Ark Select per-part recipe, typed value union, cyan active option</name>
  <files>packages/design/src/shared/uikit/Select/Select.tsx, packages/design/src/shared/uikit/Select/select.ts, packages/design/src/shared/uikit/Select/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/control.ts (the tv() recipe shape + /lite + token-only literals)
    - packages/design/src/shared/uikit/Field/Field.tsx (the wrapper Select nests under for its label/error)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Don't-Hand-Roll — Ark Select owns arrow/Home/End/type-ahead/wrap + aria-expanded/controls; anti-pattern — className per part, not asChild)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-05 → Select contract: options typed by value union not SelectOption<string>; placeholder selectPlaceholder; active option cyan; popover content surface-1 + border-2 + --shadow-md; full keyboard, no trap)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (custom listbox needs role/aria/focus + keyboard; prefer Ark primitive)
  </read_first>
  <action>
    Create `Select/select.ts`: a `tv({ slots: { trigger, positioner, content, item, itemIndicator, ... } })` — one slot per Ark Select anatomy part [src: 03-PATTERNS.md#tv-recipe-per-part], `className` per part NEVER `asChild` for styling [src: 03-RESEARCH.md#Anti-Patterns-to-Avoid]. Trigger styled like the input recipe family (surface-2 fill, border hairline → focus glow), ≥44px (`min-h-11`); content popover on `bg-surface-1 border border-border-2 rounded-lg shadow-md`; item hover `surface-3`, the active/selected item `text-primary` paired with an `itemIndicator` check icon (cyan never the only carrier — selected state shows the indicator) [src: 03-UI-SPEC.md#KIT-05]. `/lite`, literal strings, emitted tokens only.

    Create `Select/Select.tsx`: a GENERIC named `Select<TValue extends string>` with controlled `value: TValue`/`onValueChange`, a typed `options: readonly { value: TValue; label: string }[]` prop (typed by the value union, NOT `SelectOption<string>`) [src: 03-UI-SPEC.md#KIT-05], `placeholder: string` (resolved string, not a key), `disabled?`, `data-select` hook. Render Ark `Select.Root`/`Control`/`Trigger`/`Positioner`/`Content`/`Item`/`ItemIndicator` with the recipe slots; rely on Ark for keyboard nav + `aria-expanded`/`aria-controls` (do not hand-roll) [src: a11y.md#Keyboard-&-focus]. No i18n import. Add `Select/index.ts` exporting `Select` and the option type.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Select is a generic typed-value control with a per-part tv() recipe (no asChild styling), a cyan-active option paired with an indicator icon, an input-family trigger, and Ark-owned keyboard nav; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stepper — Ark NumberInput, tabular-mono value, ≥44px aria-labelled triggers</name>
  <files>packages/design/src/shared/uikit/Stepper/Stepper.tsx, packages/design/src/shared/uikit/Stepper/stepper.ts, packages/design/src/shared/uikit/Stepper/index.ts</files>
  <read_first>
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (### Ark NumberInput → Stepper code example: value is a STRING via value/onValueChange; inc/dec triggers need aria-label; Input uses the mono/stat role font-mono text-base tabular-nums)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-05 → Stepper: value tabular mono — alignment from the mono face not weight; inc/dec ≥44px with aria-label; discretion own slice vs NumberInput variant)
    - packages/design/src/shared/uikit/Button/Button.tsx (the aria-label-prop precedent — the slice does not invent the accessible name for icon-only triggers)
  </read_first>
  <behavior>
    - Test (in Stepper.stories or a stepper.test.ts pure-logic): the stepper input recipe `toContain('font-mono')` and `toContain('tabular-nums')` (value aligns via the mono face) [src: 03-UI-SPEC.md#Typography].
    - Test: the recipe carries `min-h-11` on the inc/dec trigger slots (≥44px floor) [src: a11y.md#Targets-&-motion].
  </behavior>
  <action>
    Create `Stepper/stepper.ts`: a `tv({ slots: { control, input, incrementTrigger, decrementTrigger, ... } })` for the Ark NumberInput parts; the `input` slot is the mono/stat role (`font-mono text-base tabular-nums text-text-primary`), the inc/dec trigger slots are ≥44px (`min-h-11`) with the canonical `focus-visible:shadow-(--shadow-ring)` ring [src: 03-RESEARCH.md#Code-Examples]. `/lite`, literal, emitted tokens.

    Create `Stepper/Stepper.tsx`: a named `Stepper` over Ark `NumberInput.Root`/`Control`/`Input`/`IncrementTrigger`/`DecrementTrigger`. Value is a STRING (Ark recommendation) via controlled `value`/`onValueChange` [src: 03-RESEARCH.md#Code-Examples]. The inc/dec triggers are icon-only (Lucide Plus/Minus) and require `incrementAria`/`decrementAria` string props the story passes from `stepperIncrement`/`stepperDecrement` — the slice does NOT invent them [src: component-shape.md#Icons-&-controls]. `min`/`max`/`step` honored; `disabled?`; `data-stepper` hook; no i18n import. Add `Stepper/index.ts`.

    Discretion recorded (D-05 family): `Stepper` is its own slice (not a NumberInput variant) — it has a distinct mono-value contract and its own aria-labelled triggers; planner's call [src: 03-CONTEXT.md#Claude's-Discretion].
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- stepper && pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Stepper renders a tabular-mono value with ≥44px aria-labelled inc/dec triggers over Ark NumberInput; the mono + 44px contract is pinned; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Select + Stepper stories (×4 data-volume), barrel graduation, select keyboard spec GREEN</name>
  <files>packages/design/src/shared/uikit/Select/Select.stories.tsx, packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx, packages/design/src/index.ts, packages/design/tests/keyboard.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/Button.stories.tsx (the StateMatrix + Playground two-mode pattern)
    - packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx (StateCell — reuse verbatim, data-state-cell labels)
    - packages/design/tests/keyboard.spec.ts (the Menu/Select aria-expanded/controls + trap-free Tab cycle scaffolds from Plan 03-01 — extend with the Select story id here)
    - packages/design/src/index.ts (region-comment barrel append)
  </read_first>
  <action>
    Create `Select.stories.tsx` (title `KIT-05 Form / Select`) and `Stepper.stories.tsx` (title `KIT-05 Form / Stepper`), each two-mode (D-02): a `StateMatrix` grid + a `Playground`. For Select, demonstrate the ×4 data-volume states (empty option list / few / many / limit-reached) [src: 03-UI-SPEC.md#Per-Component-States] and a FORCED-OPEN cell via Ark `open`/`defaultOpen` (the static axe-gate mechanism — no Ladle hack) [src: 03-RESEARCH.md#Pitfall-2]. Compose both under the `Field` wrapper to show the label/error/required states. Resolve all copy via `i18n._({ id })` (e.g. `selectPlaceholder`, `stepperIncrement`, `stepperDecrement`, `fieldRequired`) and pass plain string props. RU-longest-label check at 360 floor (QUAL-05).

    Append to `src/index.ts` a region `// ---- KIT-05 Form family — Select + Stepper (Wave 3 / Plan 03-03) ----` exporting `Select` (+ its option type) and `Stepper`.

    Extend the Plan-03-01 Menu/Select keyboard spec block in `tests/keyboard.spec.ts` to point at the forced-open Select story: assert the trigger carries `aria-expanded` toggling false→true and `aria-controls` pointing at the listbox, arrow-key moves the highlighted option, and Tab does not trap inside the open popover (bounded loop) — turning that scaffold GREEN for Select. (The Menu portion stays RED until Wave 6.)
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design ladle:build && pnpm --filter @solid-stats/design test:e2e -- keyboard.spec.ts catalog.spec.ts</automated>
  </verify>
  <done>Select + Stepper each have a StateMatrix (incl. Select forced-open + ×4 data-volume) + Playground rendering bilingually under Field; both are exported from the barrel; catalog axe/44px passes and the Select aria-expanded/controls + no-trap keyboard spec is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story fixture → Select options / Stepper value | Author-controlled fixture data; no untrusted input, no network/server-2/auth (v0.1 presentational Ladle). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Tampering | Select option label / Stepper aria interpolation | low | accept | Labels are author-controlled fixtures rendered via JSX (auto-escaped); no dangerouslySetInnerHTML. |
| T-03-03-02 | Denial of service | Stepper min/max bounds | low | accept | Ark NumberInput clamps to min/max client-side as UX only; the real numeric gate is server-2 (v1.0). Presentational primitive. |
| T-03-SC | Tampering | npm installs | n/a (no installs this plan) | mitigate | Deps gated by the Plan 03-01 blocking-human checkpoint; this plan adds no packages. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design test` green (stepper mono/44px contract).
- `pnpm --filter @solid-stats/design test:e2e -- catalog.spec.ts keyboard.spec.ts` green — axe + 44px on Select/Stepper; Select aria-expanded/controls + no-trap GREEN.
- `tsc --noEmit` green; no arbitrary values.
</verification>

<success_criteria>
- Select is a generic typed-value control with keyboard nav, cyan active option + indicator, ×4 data-volume states.
- Stepper renders a tabular-mono value with ≥44px aria-labelled triggers.
- Both nest under Field and are graduated into the barrel; the Select keyboard contract is GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-03-SUMMARY.md` when done.
</output>
