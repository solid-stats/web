---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/package.json
  - packages/design/src/shared/uikit/_i18n/catalogs.ts
  - packages/design/src/shared/uikit/_i18n/i18n.ts
  - packages/design/src/shared/uikit/_i18n/lingui.d.ts
  - packages/design/src/shared/uikit/_i18n/catalogs.test.ts
  - packages/design/src/shared/uikit/_i18n/index.ts
  - packages/design/src/shared/uikit/_fixtures/strings.ts
  - packages/design/.ladle/components.tsx
  - packages/design/.ladle/config.mjs
  - packages/design/src/index.ts
autonomous: false
requirements: [KIT-08, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "Every Ladle story can resolve any catalogued string in RU or EN via the Lingui i18n instance; toggling the Ladle locale control re-renders every story bilingually (D-04, KIT-08, SC#2)."
    - "A reference to a message id not present in STRINGS is a tsc error (typed keys; KIT-08, SC#4)."
    - "RU plural strings render one/few/many correctly through ICU, never string concatenation (QUAL-05, localization.md)."
  artifacts:
    - "packages/design/src/shared/uikit/_i18n/catalogs.ts — ru + en {id→ICU message} maps derived from STRINGS"
    - "packages/design/src/shared/uikit/_i18n/i18n.ts — the loaded+activated @lingui/core i18n instance (RU primary)"
    - "packages/design/src/shared/uikit/_i18n/lingui.d.ts — Register augmentation making messageIds = keyof typeof STRINGS"
    - "packages/design/.ladle/components.tsx — GlobalProvider wraps children in I18nProvider, reads the locale control"
    - "@ark-ui/react + @lingui/core + @lingui/react in packages/design/package.json dependencies; @lingui/cli rejected for v0.1 (hand-authored maps)"
  key_links:
    - "GlobalProvider (.ladle/components.tsx) → i18n.activate(locale) → I18nProvider → every story re-renders in the chosen locale"
    - "STRINGS (_fixtures/strings.ts) → catalogs.ts derive → both ru/en maps AND the lingui.d.ts Register union (one source)"

premises:
  - claim: "@ark-ui/react@5.37.2 and @lingui/core@6.4.0 resolve on React 19.2 / Node 25.9 / pnpm 11.6"
    src: 03-RESEARCH.md#Standard-Stack
    verify: "node -v && pnpm -v && npm view @ark-ui/react version"
  - claim: "STRINGS is a typed Record<string, Bilingual> with RU+EN for every key and a derived StringKey union"
    src: packages/design/src/shared/uikit/_fixtures/strings.ts#L18
    verify: "grep -n 'export const STRINGS' packages/design/src/shared/uikit/_fixtures/strings.ts && grep -n 'export type StringKey' packages/design/src/shared/uikit/_fixtures/strings.ts"
  - claim: "the Ladle GlobalProvider currently receives only { children } and imports fonts.css then tailwind.css (import-once order)"
    src: packages/design/.ladle/components.tsx#L14
    verify: "grep -n 'GlobalProvider' packages/design/.ladle/components.tsx"
  - claim: "@lingui/cli + extract/compile pipeline is NOT installed; v0.1 hand-authors the catalog maps (Open Question 1 resolution)"
    src: 03-RESEARCH.md#Open-Questions
    verify: "node -e \"const p=require('./packages/design/package.json');process.exit(p.devDependencies&&p.devDependencies['@lingui/cli']?1:0)\""

carried_forward:
  - claim: "a class present only on the GlobalProvider wrapper (outside @source ../src) is tree-shaken by Tailwind v4 — keep new utility classes inside src/ recipes, verify paint in a real browser"
    src: .planning/STATE.md#Accumulated-Context
---

<objective>
Install the two brief-locked libraries (Ark UI, Lingui) and stand up the typed RU/EN i18n harness (KIT-08) plus the Ladle language switcher — the foundation every Wave 2–7 story resolves strings through. This is the enabler wave: it adds the runtime deps behind a blocking human-verify legitimacy checkpoint, migrates the existing Phase-2 `STRINGS` seed into Lingui runtime catalogs (RU primary, EN at parity, RU one/few/many via ICU), makes a missing message id a `tsc` error via the `Register` augmentation, and wires the locale toggle into the Ladle GlobalProvider so every catalogued story renders bilingually.

Purpose: KIT-08 is the seam every interactive primitive's story depends on — strings resolve in the story (not in the primitive, per the uikit boundary), so the harness must exist before any form/overlay story can render copy. The language switcher closes SC#2.
Output: `@ark-ui/react` + `@lingui/*` in package.json; the `_i18n/` harness (catalogs, instance, typed Register, barrel); the Ladle locale control + I18nProvider; the i18n exports in the public barrel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-install package-legitimacy checkpoint (re-run the seam now that network is available)</name>
  <read_first>
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (## Package Legitimacy Audit — the in-sandbox seam timed out on registry I/O, NOT on a bad verdict; verdicts are OK from direct npm view + provenance)
    - packages/design/package.json (current deps: lucide-react, react@19.2.0, react-dom, tailwind-variants only — @ark-ui/react and @lingui/* are absent)
  </read_first>
  <action>Blocking human checkpoint — see what-built / how-to-verify / resume-signal below. No code is written in this task; it gates the first `pnpm add` in Task 2.</action>
  <what-built>
    Nothing is installed yet. RESEARCH flagged that the automated `package-legitimacy check` seam timed out on registry I/O in-sandbox, so the automated gate was left un-run. Before the first `pnpm add`, re-run the seam and confirm the exact patch pins. The threat register lists T-03-SC (supply-chain Tampering) for npm installs — this checkpoint is its mitigation.
  </what-built>
  <how-to-verify>
    1. Run: `gsd-tools query package-legitimacy check --ecosystem npm @ark-ui/react @lingui/core @lingui/react`
    2. Run: `npm view @ark-ui/react version` (expect 5.37.2 or a newer 5.x), `npm view @lingui/core version` (expect 6.4.0 or newer 6.x), `npm view @lingui/react version`, `npm view @lingui/react peerDependencies` (confirm `@lingui/core` lockstep on the same 6.x line).
    3. Confirm `@ark-ui/react` peerDependencies allow `react >=18` (React 19.2 in range): `npm view @ark-ui/react peerDependencies`.
    4. Confirm each package's npm page shows the known org (`github.com/chakra-ui/ark` for Ark; `github.com/lingui/js-lingui` for Lingui) — visit npmjs.com/package/@ark-ui/react and npmjs.com/package/@lingui/core.
    5. Record the exact resolved patch versions (these become the install pins in Task 2).
  </how-to-verify>
  <resume-signal>Type "approved" with the confirmed patch pins (e.g. ark 5.37.2, lingui 6.4.x), or describe any [SUS]/[SLOP] verdict to halt.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install Ark UI + Lingui and build the runtime i18n harness (catalogs, instance, typed Register)</name>
  <files>packages/design/package.json, packages/design/src/shared/uikit/_i18n/catalogs.ts, packages/design/src/shared/uikit/_i18n/i18n.ts, packages/design/src/shared/uikit/_i18n/lingui.d.ts, packages/design/src/shared/uikit/_i18n/catalogs.test.ts, packages/design/src/shared/uikit/_i18n/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/_fixtures/strings.ts (the STRINGS Record<string, Bilingual> + the derived StringKey union at L129 — the migration source; DO NOT delete it, derive from it)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Pattern 3 — the runtime-mode catalog/instance/Register recipe; Pitfall 1 — macros silently no-op without a babel plugin, so RUNTIME mode only; Pitfall 6 — RU one/few/many in the ICU string)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md (### _i18n/catalogs.ts + i18n.ts + lingui.d.ts — the derive-from-STRINGS pattern, the boundary rule)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md (typed keys = tsc error; ICU plural not concatenation; RU/EN parity)
  </read_first>
  <behavior>
    - Test (catalogs.test.ts, vitest pure-logic): for every key in STRINGS, `ru[key]` and `en[key]` are both defined and non-empty (RU/EN parity proof) [src: localization.md#Keys-&-usage].
    - Test: the ru and en maps have exactly the same key set as STRINGS (`Object.keys(ru)` deep-equals `Object.keys(en)` deep-equals `Object.keys(STRINGS)`).
    - Test: a plural-bearing message id (the `filteredEmpty`/`emptyFilteredBody` style key carrying `{n}`) resolves through `i18n._({ id, values: { n } })` to three DISTINCT RU forms for n=1, n=2, n=5 (RU one/few/many actually differ — never concatenation) [src: 03-RESEARCH.md#Pitfall-6]. If no RU plural key exists in the migrated STRINGS yet, this test asserts against a new `replayCount`-style ICU key added to STRINGS in this task.
  </behavior>
  <action>
    Install the runtime deps into `packages/design` at the patch pins confirmed in Task 1 [src: 03-RESEARCH.md#Installation]: `pnpm --filter @solid-stats/design add @ark-ui/react@<pin> @lingui/core@<pin> @lingui/react@<pin>`. Do NOT add `@lingui/cli` / `@lingui/babel-plugin-lingui-macro` / `@lingui/swc-plugin` — v0.1 uses RUNTIME mode with hand-authored maps (Open Question 1 resolution; macros would need a babel plugin in the Ladle Vite build — Pitfall 1) [src: 03-RESEARCH.md#Pitfall-1]. After install, re-run the legitimacy seam non-interactively as a record: `gsd-tools query package-legitimacy check --ecosystem npm @ark-ui/react @lingui/core @lingui/react`.

    Add `_i18n/catalogs.ts`: import `STRINGS` from `../_fixtures/strings`; export `ru` and `en` as `{ id → ICU message string }` maps built with `Object.fromEntries(Object.entries(STRINGS).map(([k, v]) => [k, v.ru|v.en]))` [src: 03-PATTERNS.md#_i18n]. Any plural copy carries ICU `{n, plural, one{…} few{…} many{…} other{…}}` directly in the RU string — never `count + " " + word` [src: localization.md#Keys-&-usage]. If the migrated STRINGS lacks a real RU plural exemplar, ADD one bilingual ICU plural key to `_fixtures/strings.ts` in Task 3 and exercise it here (the harness must prove RU one/few/many works now per D-03).

    Add `_i18n/i18n.ts`: import `{ i18n }` from `@lingui/core`; call `i18n.load({ ru, en })` then `i18n.activate("ru")` (RU primary, D-03); `export { i18n }` [src: 03-RESEARCH.md#Pattern-3].

    Add `_i18n/lingui.d.ts`: `import type { STRINGS } from "../_fixtures/strings"`; `declare module "@lingui/core" { interface Register { messageIds: keyof typeof STRINGS } }` — this makes a missing/renamed id a `tsc` error (the localization.md typed-key rule; SC#4) [src: 03-PATTERNS.md#_i18n].

    Add `_i18n/index.ts` re-exporting `i18n` and the `ru`/`en` catalogs for the GlobalProvider and stories. The primitive boundary rule (carry forward, do NOT violate in later waves): strings resolve in the *story* via `i18n._({ id, message, values })` and pass as a plain `message: string` prop; NO i18n import inside any `shared/uikit` primitive [src: architecture.md#Uikit-vs-feature-UI].
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- catalogs.test.ts</automated>
  </verify>
  <done>ru/en maps derive from STRINGS with full key parity; the i18n instance loads+activates RU; a missing message id fails tsc via Register; the RU plural exemplar renders distinct one/few/many; @ark-ui/react + @lingui/core + @lingui/react are in package.json, @lingui/cli is not.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the Ladle language switcher + I18nProvider and graduate the i18n seam into the barrel</name>
  <files>packages/design/.ladle/components.tsx, packages/design/.ladle/config.mjs, packages/design/src/index.ts, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    - packages/design/.ladle/components.tsx (the existing GlobalProvider — receives { children }, imports fonts.css then tailwind.css; PRESERVE that import order and the bg-bg-0 wrapper — import-once discipline)
    - packages/design/.ladle/config.mjs (the addons block: theme disabled, width enabled with the 360 floor — leave both untouched; the disabled theme addon is the global-toggle precedent)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Pattern 4 — GlobalProvider reads globalState locale control + I18nProvider; Open Question 2 — custom addon button vs global-control arg, both stable in 5.1.1)
    - packages/design/src/index.ts (the barrel — region-comment-per-wave precedent; underscore-prefixed helpers are NOT graduated, but the i18n PUBLIC seam — the i18n instance + types — IS, since Phases 4–9 consume it)
  </read_first>
  <action>
    In `.ladle/components.tsx`: extend the single `Provider` to accept `{ globalState, children }`, read the locale value (`(globalState.control?.["locale"]?.value as "ru" | "en") ?? "ru"`), call `i18n.activate(locale)`, and wrap the existing `<div className="bg-bg-0 text-text-primary font-body min-h-screen p-4">` in `<I18nProvider i18n={i18n}>` from `@lingui/react` [src: 03-RESEARCH.md#Pattern-4]. Keep the `fonts.css` → `tailwind.css` import order exactly; the I18nProvider wraps but does not reorder imports.

    In `.ladle/config.mjs`: add a `locale` global control (RU↔EN) alongside the existing `width` addon — leave `theme` and `width` untouched. Choose the custom-addon-button-or-global-control mechanism (Claude's discretion, D-04; RESEARCH Open Question 2 recommends the custom addon button mirroring the disabled `theme` precedent) so the value is readable as `globalState.control["locale"]` in the Provider [src: 03-RESEARCH.md#Open-Questions]. Default `ru`.

    In `src/index.ts`: append a new region `// ---- KIT-08 i18n harness (Wave 1 / Plan 03-01) ----` graduating the PUBLIC i18n seam — `export { i18n } from "./shared/uikit/_i18n"` and the `Bilingual`/`StringKey` types if Phases 4–9 need them. The `_i18n/catalogs` maps + `_fixtures/STRINGS` + `_state-matrix` stay INTERNAL (underscore-prefix rule — not graduated) [src: 03-PATTERNS.md#src/index.ts]. Note in the region comment that v1.0 graduates this same instance under the `/ru` `/en` router (D-03 graduation seam).

    If Task 2 added a new ICU plural exemplar key, ensure it lives in `_fixtures/strings.ts` (RU one/few/many + EN one/other) so the existing `StringKey` derive and the `Register` union both see it.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit && pnpm --filter @solid-stats/design ladle:build</automated>
  </verify>
  <done>The Ladle GlobalProvider activates the chosen locale and wraps stories in I18nProvider; a locale control toggles RU↔EN; the i18n instance is exported from the barrel; `tsc` and `ladle build` are green.</done>
</task>

<task type="auto">
  <name>Task 4: Wave-0 test scaffolds — extend keyboard.spec.ts + cls.spec.ts with the per-component behavior specs the later waves fill</name>
  <files>packages/design/tests/keyboard.spec.ts, packages/design/tests/cls.spec.ts</files>
  <read_first>
    - packages/design/tests/keyboard.spec.ts (the per-story `page.goto('/?story=<id>&mode=preview')` + `data-*` selector + boundingBox/getComputedStyle oracle pattern — keyboard.spec.ts:14-49,155-173 — the template to copy)
    - packages/design/tests/cls.spec.ts (the reserved-height box-match oracle — DataTrustBanner/Skeleton/Table proofs — the template AsyncBoundary reuses)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (## Validation Architecture → Wave 0 Gaps + the Phase Requirements → Test Map: overlay Esc-close/return-focus, Menu aria-expanded/controls, Tabs roving tabindex, trap-free Tab cycle, Field.ErrorText live-region; AsyncBoundary reserved-height six states)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-VALIDATION.md (## Wave 0 Requirements)
  </read_first>
  <action>
    Extend `tests/keyboard.spec.ts` with `test.describe` blocks (skeleton specs that reference the story ids the later waves will create) for the overlay + form behavior the generic `catalog.spec.ts` cannot express [src: 03-RESEARCH.md#Validation-Architecture]: (a) Dialog — Esc closes + focus returns to the trigger; (b) Menu — trigger carries `aria-expanded` toggling false→true and `aria-controls` pointing at the panel id; (c) Tabs — arrow-key roving tabindex moves the active tab; (d) trap-free Tab cycle — Tab from the last focusable inside an open overlay does not get trapped (bounded loop, fails not hangs); (e) Field — forced-invalid story exposes `Field.ErrorText` with `aria-live` and the error is associated to the control. Reference the planned story ids (e.g. `kit-06-overlay--dialog--playground`, `kit-05-form--field--matrix`) so the spec is RED until the matching wave lands. Do NOT use `.skip` — the spec must fail (RED) until its story exists, per the Nyquist contract; a Wave-0 scaffold that is green is a no-op.

    Extend `tests/cls.spec.ts` with an `AsyncBoundary CLS = 0` describe: all six states (loading/empty/error/offline/reconnecting/stale) reserve the SAME box height as the `ready` content slot — copy the `boundingBox().height` match oracle from the DataTrustBanner proof. Reference the planned `surf-18-global-state--asyncboundary--cls` story id (RED until Wave 7) [src: 03-RESEARCH.md#Wave-0-Gaps].

    Mark these as the explicit Wave-0 dependency set in a top-of-file comment so the executor of each later wave knows which spec block its story must turn GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>keyboard.spec.ts carries RED overlay/form behavior specs and cls.spec.ts a RED AsyncBoundary reserved-height spec, each referencing the story id its owning wave will create; the spec files type-check; the specs are wired to fail until their stories land (no `.skip`, no green no-op).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | `pnpm add` pulls third-party code into `node_modules` and the lockfile — the one real attack surface this phase introduces (supply chain). No app/network/server-2/auth boundary exists in v0.1 (presentational Ladle only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | `pnpm add @ark-ui/react @lingui/*` | high | mitigate | Task 1 blocking-human legitimacy checkpoint re-runs `package-legitimacy check` + `npm view` provenance before the first install; exact patch pins recorded; checkpoint is never auto-approvable. |
| T-03-01 | Tampering | Lingui ICU message interpolation in catalogs | low | accept | Runtime mode interpolates `{n}`/`{id}` as text via Lingui's CLDR formatter; React JSX auto-escapes; no `dangerouslySetInnerHTML`, no HTML in messages. v0.1 fixture strings are author-controlled, not user input. |
| T-03-02 | Information disclosure | i18n catalog content | low | accept | Catalogs are static UI copy (no secrets, no PII); shipped in the client bundle by design. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design test` green (catalogs parity + RU plural).
- `pnpm --filter @solid-stats/design exec tsc --noEmit` green — and a deliberately-wrong message id fails tsc (typed-key proof).
- `pnpm --filter @solid-stats/design ladle:build` green; the locale control toggles RU↔EN in the built catalog.
- The Wave-0 behavior specs are RED (referencing not-yet-existing stories) — confirming the Nyquist scaffold is wired, not a no-op.
</verification>

<success_criteria>
- KIT-08 harness exists: RU primary + EN parity catalogs derived from STRINGS, a loaded/activated i18n instance, typed keys (missing id = tsc error), RU one/few/many via ICU.
- The Ladle language switcher toggles every story bilingually (SC#2).
- The i18n seam is graduated into the barrel; the Wave-0 keyboard/cls scaffolds are in place for Waves 2–7.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-01-SUMMARY.md` when done.
</output>
