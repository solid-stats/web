---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
  - packages/design/tests/file-upload-preview.spec.ts
autonomous: true
gap_closure: true
requirements: [KIT-05]
must_haves:
  truths:
    - "An accepted image file renders EXACTLY ONE preview node (the <img>), never the <img> + the ImageUp fallback side by side."
    - "A non-image accepted file (if ever accepted) renders the fallback icon, never an empty/double preview — the branch is on file.type, not a `.*` catch-all."
    - "The new file-upload-preview regression test FAILS on the pre-fix code (two preview nodes per image) and PASSES after the fix (one)."
    - "pnpm check + pnpm --filter @solid-stats/design test:e2e stay green."
  artifacts:
    - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
    - packages/design/tests/file-upload-preview.spec.ts
  key_links:
    - "FileUpload accepted-row render → a single ItemPreview branched on file.type → exactly one preview node per file."
---

<objective>
Close GAP-05 (blocker): FileUpload renders TWO previews for an image. `FileUpload.tsx` renders `<ItemPreview type="image/*">` (the `<img>`) AND `<ItemPreview type=".*">` (the `ImageUp` fallback) as siblings; Ark filters by `file.type.match(props.type)` and `.*` is a catch-all that ALSO matches images, so both render for any image. Ark provides no negative filter — this is our misuse [src: 03-UAT.md#GAP-05] [src: 03-UAT-VISUAL-FINDINGS.md#FU3].

Purpose: render exactly one preview per accepted file — the `<img>` for images, the icon fallback only for non-images.

Output: a single-preview FileUpload + a regression test that fails on the double-preview pre-fix code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-CONTEXT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md

# CONVENTIONS — SKILL.md INDEX + named references
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/tests.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
@.agents/skills/solidstats-shared-testing-standards/SKILL.md

# Source the executor edits / depends on
@packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
@packages/design/src/shared/uikit/FileUpload/fileUpload.ts
@packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx
</context>

<premises>
  - claim: FileUpload renders two ItemPreview siblings per accepted file — `type="image/*"` (the img) and `type=".*"` (the ImageUp fallback) — and `.*` matches images too, so both render
    src: FileUpload.tsx#L131
    verify: grep -n "ItemPreview\|type=\".\\*\"\|type=\"image/\\*\"" packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
  - claim: the accepted default is PNG/JPEG/WebP (images only) — so in v0.1 every accepted file is an image, and a non-image fallback only ever fires if the accept allowlist widens
    src: fileUpload.ts#L29
    verify: grep -n "ACCEPTED_IMAGE_TYPES\|ACCEPT_DEFAULT" packages/design/src/shared/uikit/FileUpload/fileUpload.ts
  - claim: the FileUpload story drives accepted rows from controlled `files` fixtures (image/png Files), so a Playwright story-drive can assert preview-node count deterministically
    src: FileUpload.stories.tsx#L38
    verify: grep -n "imageFile\|FEW_FILES\|files=" packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx
</premises>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — regression spec asserting exactly one preview node per accepted image</name>
  <files>packages/design/tests/file-upload-preview.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx — the `few`/`many` Matrix cells render accepted image rows from controlled fixtures; pick a story id that shows accepted image rows (e.g. `kit-05-form--fileupload--matrix`). Note the styled preview slot class (`itemPreview`) and the `<img>` (`itemPreviewImage`) so the test can count preview nodes per row.
    - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx — confirm the two-sibling ItemPreview shape so the test counts what the bug produces (2 per image now, 1 after).
    - @.agents/skills/solidstats-frontend-react-tests/SKILL.md — Playwright-against-Ladle; assert the rendered DOM count, not internals.
  </read_first>
  <behavior>
    - Test 1 (one preview per image): for an accepted image row, count the preview nodes (the styled `itemPreview` slot or the `<img>`/fallback-icon pair) and assert exactly ONE preview is rendered per file row. On the pre-fix code this is 2 (img + ImageUp fallback) → fails RED.
    - Test 2 (the preview is the image, not the fallback icon): assert an accepted image row's single preview contains the `<img>` (the image branch), not the `ImageUp` placeholder icon — so the fix kept the right branch.
  </behavior>
  <action>
    Create the Playwright-against-Ladle regression for GAP-05. Drive the FileUpload Matrix story (`?story=kit-05-form--fileupload--matrix&mode=preview`), wait on the `[data-file-upload]` / accepted-row hook, scope to one accepted image row, and count the preview nodes within it (the `itemPreview` slot node, or count `img` + the fallback-icon together). Assert exactly one preview per accepted file row. [src: 03-UAT.md#GAP-05]

    This MUST FAIL on the pre-fix code (two ItemPreview siblings per image render → count 2). Run it RED before Task 2. No `.skip`. Commit RED: `test(03-10): add failing FileUpload single-preview regression`.

    Do NOT inline code fences in this action — describe the locator/count assertions in prose; mirror the responsive/cls spec harness idiom.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test:e2e -- file-upload-preview.spec.ts</automated>
  </verify>
  <done>The spec runs (no `.skip`) and FAILS on the current double-preview code.</done>
  <acceptance_criteria>
    - The test counts rendered preview nodes per accepted image row.
    - It fails RED (count 2) on pre-fix code.
    - It asserts the surviving preview is the `<img>` branch.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — render a single ItemPreview branched on file.type</name>
  <files>packages/design/src/shared/uikit/FileUpload/FileUpload.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx — the accepted-row map rendering the two ItemPreview siblings; the `fileKey`, `styles.itemPreview()`, `styles.itemPreviewImage()` usage to preserve.
    - packages/design/src/shared/uikit/FileUpload/fileUpload.ts — the object-URL lifecycle is owned ENTIRELY by Ark's `ItemPreviewImage` (create+revoke in its effect); keep the image branch using `ItemPreviewImage` so that lifecycle is preserved.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md — JSX conditionals use explicit ternaries returning null; stable keys.
  </read_first>
  <action>
    Render exactly ONE preview per accepted file, branched on `file.type` instead of the `.*` catch-all sibling [src: 03-UAT.md#GAP-05]. For an image file (`file.type` starts with `image/`), render the `<ItemPreview type="image/*">` with `ItemPreviewImage` (keeping Ark's object-URL create+revoke lifecycle intact). For a non-image file, render a single fallback preview with the `ImageUp` (or appropriate non-image) icon — gated so it NEVER co-renders with the image preview. Remove the unconditional `<ItemPreview type=".*">` sibling that double-renders. Branch in the row map on `file.type`, or use a single ItemPreview whose content switches on `file.type` — whichever keeps the Ark preview-image lifecycle owned by Ark (do not hand-roll createObjectURL/revokeObjectURL).

    Keep the slice i18n-free (copy stays plain props), keep the `data-file-upload` hook, keep stable `fileKey` keys, keep tokens-only styling. `pnpm check` stays green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- file-upload-preview.spec.ts</automated>
  </verify>
  <done>Exactly one preview renders per accepted image (the `<img>`); the Task-1 spec is GREEN; `pnpm check` exits 0.</done>
  <acceptance_criteria>
    - The `.*` catch-all double-preview is gone.
    - Image files render the `<img>` via `ItemPreviewImage` (Ark keeps the URL lifecycle).
    - Non-image files (if ever accepted) render a single fallback, never co-rendered with the image.
    - No arbitrary values; slice stays i18n-free; `pnpm check` green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → preview | Object-URL preview of an uploaded file; lifecycle (create+revoke) stays owned by Ark's ItemPreviewImage — the SVG/XSS allowlist (PNG/JPEG/WebP, no SVG) in fileUpload.ts is unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10-01 | Information disclosure / leak | preview object-URL | low | mitigate | Keep the image branch on Ark's `ItemPreviewImage` so the create+revoke lifecycle stays owned by Ark — no hand-rolled URL ledger is introduced. The SVG-exclusion allowlist is untouched (no new accept types). No package installs. |
</threat_model>

<verification>
- `pnpm check` green.
- `pnpm --filter @solid-stats/design test:e2e` green (incl. file-upload-preview spec).
- Manual confirm in `ladle dev`: an accepted image shows one thumbnail, not two.
</verification>

<success_criteria>
GAP-05 closed: one preview per accepted file, guarded by a test that fails on the pre-fix double-preview code.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-10-SUMMARY.md` when done.
</output>
