---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/design/src/shared/uikit/Field/Field.tsx
  - packages/design/src/shared/uikit/Field/field.ts
  - packages/design/src/shared/uikit/Field/Field.stories.tsx
  - packages/design/src/shared/uikit/Field/index.ts
  - packages/design/src/shared/uikit/Input/Input.tsx
  - packages/design/src/shared/uikit/Input/input.ts
  - packages/design/src/shared/uikit/Input/input.test.ts
  - packages/design/src/shared/uikit/Input/Input.stories.tsx
  - packages/design/src/shared/uikit/Input/index.ts
  - packages/design/src/index.ts
  - packages/design/tests/keyboard.spec.ts
autonomous: true
requirements: [KIT-05, QUAL-01, QUAL-02, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "A Field renders a real visible <label> associated to its control, and when invalid shows Field.ErrorText in an aria-live region paired with a Lucide icon (never color-alone), sitting by the field with recovery copy (KIT-05, SC#1, a11y.md)."
    - "An Input fills surface-2 with a border-1 hairline that brightens to border-2 on hover and goes primary-border + ring-glow on focus-visible; disabled = surface-1 + text-subtle + 0.6 opacity (UI-SPEC input recipe, DESIGN.md input)."
    - "The forced-invalid StateMatrix cell (Field.Root invalid) is axe-clean and announces the error; the catalog spec passes axe + 44px on every Field/Input story (QUAL-03, SC#1)."
  artifacts:
    - "packages/design/src/shared/uikit/Field/Field.tsx + field.ts — the shared label+error+live-region wrapper over Ark Field.Root"
    - "packages/design/src/shared/uikit/Input/Input.tsx + input.ts — the Ark-Field-styled text input"
    - "Field.stories.tsx + Input.stories.tsx — StateMatrix (forced states incl. invalid) + Playground (Ladle args)"
  key_links:
    - "Field.Root invalid → broadcasts to the nested Input via Ark context → Field.ErrorText renders the announced error (forced-invalid cell needs no Ladle hack)"
    - "story resolves strings via i18n._({id}) → passes message: string props → Field/Input primitives stay i18n-free (uikit boundary)"

premises:
  - claim: "@ark-ui/react is installed and Field.Root broadcasts invalid/required/disabled to the nested control via context; Field.ErrorText renders only when invalid"
    src: 03-RESEARCH.md#Pattern-2
    verify: "node -e \"require.resolve('@ark-ui/react/field')\""
  - claim: "the i18n harness + Register typing exists from Plan 03-01 so stories resolve strings"
    src: packages/design/src/shared/uikit/_i18n/i18n.ts
    verify: "test -f packages/design/src/shared/uikit/_i18n/i18n.ts"
  - claim: "all input/ring/surface tokens the recipe needs are already emitted in theme.css (no gen-theme.mjs extension)"
    src: packages/design/src/styles/theme.css#L114
    verify: "grep -E 'shadow-ring-glow|color-surface-2|color-primary-border|color-border-1' packages/design/src/styles/theme.css"

carried_forward:
  - claim: "tailwind-variants/lite is merge-free — hold mutually-exclusive utilities as variants or use the FORCED_STATE !-override; a passed className does not beat a base by class-attribute order"
    src: .planning/STATE.md#Accumulated-Context
  - claim: "icon-only controls need an aria-label prop the slice does not invent (Button precedent)"
    src: packages/design/src/shared/uikit/Button/Button.tsx#L31
---

<objective>
Ship the first form-family slice group (KIT-05): the shared `Field` wrapper (visible label + inline error + `aria-live` live-region over Ark `Field.Root`) and the `Input` text control styled per the DESIGN.md `input` recipe. `Field` is the seam every other form control (`Select`, `Stepper`, `FileUpload`) nests under for its label/error/required/disabled broadcast — so it ships first.

Purpose: KIT-05 forms must have visible labels/errors, inline validation, and live-region behavior (SC#1). Each slice is the thin Ark wrapper styling Ark's anatomy parts with a `tv()`-per-part recipe consuming `@theme` tokens (the D-01 props-down presentational boundary — controlled props in, `data-*` hooks, no business/i18n imports inside). `Field.Root invalid` is the forced-validation StateMatrix cell (axe gate, no Ladle hack). Every story demonstrates the ×7 component states / ×5 scenario endings / ×4 data-volume where applicable (D-07). The `Field` live-region is the presentational seam v1.0's TanStack Form composes — this phase ships the primitive, not a wired mutation (forms.md).
Output: `Field` + `Input` slices (component + `tv()` recipe + stories + barrel exports); the forced-invalid live-region keyboard spec turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/forms.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@packages/design/src/shared/uikit/Button/Button.tsx
@packages/design/src/shared/uikit/Button/control.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Field wrapper — label + error + aria-live over Ark Field.Root</name>
  <files>packages/design/src/shared/uikit/Field/Field.tsx, packages/design/src/shared/uikit/Field/field.ts, packages/design/src/shared/uikit/Field/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/Button.tsx (the controlled-props + className-passthrough + data-* + no-i18n-import component shell to copy)
    - packages/design/src/shared/uikit/Button/control.ts (the tv() recipe shape, /lite import, token-only literals, the slots-vs-flat distinction)
    - packages/design/src/shared/uikit/Toast/Toast.tsx (the icon+message never-color-alone pattern + role discipline — mirror for Field.ErrorText)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Pattern 2 — Field.Root broadcasts invalid/required/disabled; Field.ErrorText is the announced error; pair aria-live + a Lucide icon)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md (### Field wrapper specifically)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (visible labels, errors announced near the field with recovery guidance, no naked inputs)
  </read_first>
  <action>
    Create `Field/field.ts`: a `tv({ slots: { ... } })` recipe (one slot per Ark Field anatomy part used — `root`, `label`, `helperText`, `errorText`) consuming ONLY emitted `@theme` tokens [src: 03-PATTERNS.md#tv-recipe-per-part], `/lite` import, literal class strings (so the `@source` scan picks them up). Label uses the UI-SPEC `label` typography role (12px / 600 / uppercase / letterSpacing.label) [src: 03-UI-SPEC.md#Typography]; helper/caption text `text-muted`; error text `text-loss` paired with the icon (color is never the only carrier) [src: a11y.md#Structure-&-contrast].

    Create `Field/Field.tsx`: a named function `Field` with system-props-first `Props` (`className?`, then `label: string`, `helperText?: string`, `errorText?: string`, `invalid?: boolean`, `required?: boolean`, `disabled?: boolean`, `children: ReactNode`) [src: component-shape.md#Shape]. Render Ark `Field.Root` with `invalid`/`required`/`disabled` (broadcast to the nested control via Ark context — the forced-invalid cell just passes `invalid`) [src: 03-RESEARCH.md#Pattern-2]. Render `Field.Label` (real associated `<label>`), `{children}` (the control), conditional `Field.HelperText` (explicit ternary returning null — never leak a bare value) [src: component-shape.md#Shape], and `Field.ErrorText` with `aria-live="polite"` preceded by a Lucide icon (e.g. `CircleAlert`) so the error is announced and never color-alone [src: 03-PATTERNS.md#Field-wrapper-specifically]. NO i18n import — `label`/`errorText`/`helperText` arrive as plain strings resolved in the story [src: architecture.md#Uikit-vs-feature-UI]. `data-field` test hook. Add `Field/index.ts` exporting `Field`.

    Discretion (D-05 family): one shared `Field` wrapper (not split per control) — it is the reuse seam Select/Stepper/FileUpload nest under; planner's call recorded.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Field renders an associated label, conditional helper text, and an aria-live error region paired with an icon; invalid/required/disabled broadcast via Ark Field.Root; no i18n import; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Input control — the DESIGN.md input recipe over Ark Field</name>
  <files>packages/design/src/shared/uikit/Input/Input.tsx, packages/design/src/shared/uikit/Input/input.ts, packages/design/src/shared/uikit/Input/input.test.ts, packages/design/src/shared/uikit/Input/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/control.ts (the recipe shape + the control.test.ts contract-test precedent in the sibling file)
    - packages/design/src/shared/uikit/Button/control.test.ts (the pure-logic recipe assertion pattern — toContain on resolved utilities — to mirror for input.test.ts)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-05 Form family → Input contract: surface-2 fill, border-1 → border-2 hover → primary-border + ring-glow focus-visible; text-subtle placeholder; disabled = surface-1 + text-subtle + 0.6 opacity)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Project Constraints — input/dialog/popover components.* recipes are NOT emitted; style via tv() against emitted base tokens, do NOT extend gen-theme.mjs)
  </read_first>
  <behavior>
    - Test (input.test.ts, vitest pure-logic): the resolved input recipe `toContain('bg-surface-2')`, `toContain('border-border-1')` (resting hairline), `toContain('hover:border-border-2')` (hover brighten), `toContain('focus-visible:border-primary-border')` + `toContain('focus-visible:shadow-(--shadow-ring-glow)')` (focus glow) [src: 03-UI-SPEC.md#KIT-05].
    - Test: the disabled variant `toContain('bg-surface-1')`, `toContain('text-text-subtle')`, `toContain('opacity-60')` [src: 03-UI-SPEC.md#KIT-05].
    - Test: placeholder uses `placeholder:text-text-subtle` (the one sanctioned text-subtle use).
    - Test: NO arbitrary value (no `[` bracket utility) appears in the resolved string [src: styling.md].
  </behavior>
  <action>
    Create `Input/input.ts`: a `tv()` recipe for the input control mapping the DESIGN.md `input` recipe literally onto emitted tokens [src: 03-UI-SPEC.md#KIT-05] — `bg-surface-2`, `border border-border-1`, `rounded-sm`, body text role; `hover:border-border-2`; `focus-visible:border-primary-border focus-visible:shadow-(--shadow-ring-glow) focus-visible:outline-none`; `placeholder:text-text-subtle`; a `disabled` variant `bg-surface-1 text-text-subtle opacity-60`. `/lite`, literal strings, no arbitrary values, no gen-theme.mjs change (tokens already emitted) [src: 03-RESEARCH.md#Project-Constraints].

    Create `Input/Input.tsx`: a named `Input` wrapping the Ark Field input part (or a native input styled with the recipe and `Field.Root`-context-aware), controlled `value`/`onValueChange`-shaped props (or `value`/`onChange`), system-props-first, `data-input` hook, `disabled` honored, no i18n import. Placeholder arrives as a plain `placeholder?: string` prop. Add `Input/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- input.test.ts</automated>
  </verify>
  <done>The input recipe resolves the DESIGN.md input tokens (fill/hover/focus-glow/disabled/placeholder) with zero arbitrary values; input.test.ts pins the contract; the Input consumes Field context.</done>
</task>

<task type="auto">
  <name>Task 3: Field + Input stories, barrel graduation, and turn the forced-invalid keyboard spec GREEN</name>
  <files>packages/design/src/shared/uikit/Field/Field.stories.tsx, packages/design/src/shared/uikit/Input/Input.stories.tsx, packages/design/src/index.ts, packages/design/tests/keyboard.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/Button.stories.tsx (the StateMatrix/StateCell grid + Playground args two-mode pattern, D-02)
    - packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx (StateMatrix + StateCell — reuse verbatim; data-state-cell labels the catalog spec asserts against; min-h-11 already there)
    - packages/design/tests/keyboard.spec.ts (the Field forced-invalid live-region describe added in Plan 03-01 Wave-0 — currently RED; this task makes it GREEN by creating the referenced story id)
    - packages/design/src/index.ts (the region-comment-per-wave barrel append precedent)
  </read_first>
  <action>
    Create `Field.stories.tsx` (title `KIT-05 Form / Field`) and `Input.stories.tsx` (title `KIT-05 Form / Input`), each with the two D-02 modes [src: 03-PATTERNS.md#stories]: (1) a static `StateMatrix` grid using `StateCell` from `_state-matrix` covering the ×7 component states (enabled / hover / pressed / focused / selected / disabled / loading where applicable) AND the ×5 scenario endings for the Field — crucially a FORCED-INVALID cell passing `Field.Root invalid` with an `errorText` (the axe + live-region gate cell; no Ladle hack) [src: 03-RESEARCH.md#Pitfall-2]; (2) a `Playground` with Ladle args. Resolve every string via `i18n._({ id })` in the story (e.g. `fieldRequired`, `fieldErrorRequired`, `selectPlaceholder`) and pass as plain `message`/`label`/`errorText` props — the primitive stays i18n-free [src: architecture.md#Uikit-vs-feature-UI]. Give the forced-invalid cell a stable story id matching the one referenced by the Wave-0 keyboard spec (e.g. `kit-05-form--field--matrix`). Include a RU-longest-label check at the 360 floor for the longest field copy (QUAL-05 clip check) [src: 03-UI-SPEC.md#Copywriting-Contract].

    Append to `src/index.ts` a region `// ---- KIT-05 Form family — Field + Input (Wave 2 / Plan 03-02) ----` exporting `Field` and `Input` (and their public prop types). The `tv()` recipes + `_state-matrix` stay internal.

    Update the Plan-03-01 Field forced-invalid live-region spec block in `tests/keyboard.spec.ts` to point at the real story id and assert: `Field.ErrorText` is present, carries `aria-live`, is associated to the control (aria-describedby/aria-errormessage), and the error text is the resolved copy — turning that RED block GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design ladle:build && pnpm --filter @solid-stats/design test:e2e -- keyboard.spec.ts catalog.spec.ts</automated>
  </verify>
  <done>Field + Input each have a StateMatrix (incl. a forced-invalid axe/live-region cell) + Playground story rendering bilingually; both are exported from the barrel; the catalog axe/44px gate passes on the new stories and the forced-invalid live-region keyboard spec is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story fixture → Field/Input props | Props are author-controlled fixture strings resolved from the i18n catalog. No untrusted input, no network, no server-2, no auth — presentational Ladle only (v0.1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Tampering | error/label/placeholder copy interpolation | low | accept | Strings are author-controlled fixtures rendered via JSX (auto-escaped); no dangerouslySetInnerHTML, no HTML in messages (V5). |
| T-03-02-02 | Information disclosure | error message content | low | accept | errorText carries only localized user-facing copy (no stack/PII); system errors (id+contact) are a distinct SURF-18 pattern, not a field error. |
| T-03-SC | Tampering | npm installs | n/a (no installs this plan) | mitigate | Deps were gated by the Plan 03-01 blocking-human legitimacy checkpoint; this plan adds no new packages. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design test` green (input recipe contract).
- `pnpm --filter @solid-stats/design test:e2e -- catalog.spec.ts keyboard.spec.ts` green — axe-clean + 44px on Field/Input stories incl. the forced-invalid cell; the live-region spec is GREEN.
- `tsc --noEmit` green; no arbitrary Tailwind value in the recipes.
</verification>

<success_criteria>
- Field provides the shared visible-label + announced-error + live-region seam; Input matches the DESIGN.md input recipe (fill/hover/focus-glow/disabled).
- The forced-invalid axe/live-region gate passes with no Ladle hack (Ark Field.Root invalid).
- Field + Input render bilingually and are graduated into the barrel.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-02-SUMMARY.md` when done.
</output>
