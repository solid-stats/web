---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - packages/design/src/shared/uikit/Dialog/Dialog.tsx
  - packages/design/src/shared/uikit/Dialog/dialog.ts
  - packages/design/src/shared/uikit/Dialog/Dialog.stories.tsx
  - packages/design/src/shared/uikit/Dialog/index.ts
  - packages/design/src/shared/uikit/Popover/Popover.tsx
  - packages/design/src/shared/uikit/Popover/popover.ts
  - packages/design/src/shared/uikit/Popover/Popover.stories.tsx
  - packages/design/src/shared/uikit/Popover/index.ts
  - packages/design/src/index.ts
  - packages/design/tests/keyboard.spec.ts
autonomous: true
requirements: [KIT-06, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "Dialog traps focus inside while open, returns focus to the trigger on close, closes on Esc, inerts the main content, and carries a localized title (real heading) + a dialogClose-aria close affordance over an overlay scrim (KIT-06, SC#1, a11y.md)."
    - "Popover is focus-managed, returns focus, closes on Esc, has no trap; both overlays animate transform/opacity only (CLS = 0) and use the floating-UI shadow tokens (KIT-06, QUAL-04)."
    - "The forced-open StateMatrix cell uses Ark open/defaultOpen (no Ladle hack) and is axe-clean even with the portal content (QUAL-03); a destructive-confirmation Dialog story defaults focus to the safe action (WCAG 3.3.6)."
  artifacts:
    - "packages/design/src/shared/uikit/Dialog/Dialog.tsx + dialog.ts — Ark Dialog per-part recipe (backdrop/positioner/content/title/close)"
    - "packages/design/src/shared/uikit/Popover/Popover.tsx + popover.ts — Ark Popover per-part recipe"
    - "Dialog.stories.tsx (incl. destructive-confirm pattern) + Popover.stories.tsx — StateMatrix forced-open + Playground"
  key_links:
    - "Dialog.Root open/onOpenChange controlled + lazyMount + unmountOnExit → forced-open cell passes open; Portal mounts to body but classes stay literal in src/ recipes (@source scan)"
    - "story resolves title/close-aria/body via i18n._ → plain string props → overlay primitives stay i18n-free"

premises:
  - claim: "Ark Dialog provides trapFocus, focus-return, Esc-close, portal, and inert background natively; open/defaultOpen is the forced-open mechanism"
    src: 03-RESEARCH.md#Don't-Hand-Roll
    verify: "node -e \"require.resolve('@ark-ui/react/dialog')\""
  - claim: "Ark Popover returns focus, Esc-closes, no trap; uses the popover recipe (surface-1 + border-2 + --shadow-md)"
    src: 03-UI-SPEC.md#KIT-06
    verify: "node -e \"require.resolve('@ark-ui/react/popover')\""
  - claim: "portal content escapes the @source scan ONLY for dynamically-built class strings — literal tv() classes under src/ ARE scanned (the Phase-1 dark-base lesson)"
    src: 03-RESEARCH.md#Pitfall-3
    verify: "grep -n '@source' packages/design/.ladle/tailwind.css"
  - claim: "the overlay/keyboard spec scaffolds (Dialog Esc-close + return-focus, trap-free Tab) exist RED from Plan 03-01"
    src: packages/design/tests/keyboard.spec.ts
    verify: "grep -n 'Dialog' packages/design/tests/keyboard.spec.ts"

carried_forward:
  - claim: "a class only on the GlobalProvider was tree-shaken — verify portal content actually paints in a real-browser Playwright check"
    src: .planning/STATE.md#Accumulated-Context
---

<objective>
Ship the first overlay-family slice group (KIT-06): `Dialog` (modal — focus trap, return-focus, Esc, scrim, inert background, localized title) and `Popover` (non-modal — focus-managed, return-focus, Esc, no trap). Ark owns the accessible behavior; the slice adds the `tv()`-per-part recipe + `data-*` hooks + aria props. Dialog also ships the destructive-confirmation pattern story (reversible/checked/confirmed, WCAG 3.3.6) that Phases 8–9 compose.

Purpose: KIT-06 overlays must be keyboard-accessible, focus-managed, no traps (SC#1). Each is the thin Ark wrapper styling Ark's parts with a `tv()`-per-part recipe (the D-01 boundary — controlled props, `data-*` hooks, no i18n inside), demonstrating the ×7 states / ×5 endings (D-07). `open`/`defaultOpen` is the forced-open StateMatrix mechanism (axe gate, no Ladle hack). Overlays animate transform/opacity only for CLS = 0 (QUAL-04). Portal content must actually paint (the Phase-1 tree-shake lesson → real-browser check).
Output: `Dialog` + `Popover` slices (component + recipe + stories + barrel); the Dialog Esc-close + return-focus + trap-free keyboard specs turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@packages/design/src/shared/uikit/Button/Button.tsx
@packages/design/src/shared/uikit/Button/control.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dialog — Ark Dialog per-part recipe, focus trap, return-focus, Esc, scrim, localized title</name>
  <files>packages/design/src/shared/uikit/Dialog/Dialog.tsx, packages/design/src/shared/uikit/Dialog/dialog.ts, packages/design/src/shared/uikit/Dialog/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/control.ts (the recipe shape + canonical ring; the close + confirm/cancel render the shared Button)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Pattern 1 — the Dialog per-part code example: Root open/onOpenChange lazyMount unmountOnExit, Portal, Backdrop, Positioner, Content, Title, CloseTrigger; Don't-Hand-Roll — Ark owns trapFocus/return/Esc/portal; Pitfall 3 — portal content + @source)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md (### Ark slice wrapper — the Dialog component-shell + tv-recipe-per-part code)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-06 → Dialog contract: surface-1 + border-2 + rounded-xl + --shadow-lg, overlay scrim, portal, real heading title, controlled open + lazy mount, focus trap inside + return on close + Esc, inert main, dialogClose aria; Copywriting Contract — destructive-confirmation pattern story, confirm uses loss + verb+noun label, focus defaults to cancel)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (modal uses Ark Dialog with portal, localized title, controlled open/mount, lazy mount, inert main)
  </read_first>
  <action>
    Create `Dialog/dialog.ts`: a `tv({ slots: { backdrop, positioner, content, title, body } })` over Ark Dialog parts [src: 03-PATTERNS.md#Ark-slice-wrapper]. backdrop `fixed inset-0 bg-overlay`; positioner `fixed inset-0 flex items-center justify-center p-4`; content `bg-surface-1 border border-border-2 rounded-xl shadow-lg p-6`; title the dialog-title typography role (18px / 600) [src: 03-UI-SPEC.md#Typography]. Animate transform/opacity only — NO width/height animation (CLS = 0) [src: 03-UI-SPEC.md#KIT-06]. `/lite`, literal class strings so the Portal'd content is still `@source`-scanned (Pitfall 3) [src: 03-RESEARCH.md#Pitfall-3].

    Create `Dialog/Dialog.tsx`: a named `Dialog` with controlled `open`/`onOpenChange`, `title: string`, `closeAria: string`, `children`, `className?`, system-props-first. Render `Dialog.Root` (`open`, `onOpenChange`, `lazyMount`, `unmountOnExit`) → `Portal` → `Backdrop`/`Positioner`/`Content`/`Title`/`CloseTrigger`. The CloseTrigger is icon-only (Lucide X) and takes the `closeAria` prop — the slice does not invent the name [src: component-shape.md#Icons-&-controls]. `data-dialog` hook. NO i18n import — title/close-aria/body arrive as plain strings resolved in the story [src: architecture.md#Uikit-vs-feature-UI]. Ark owns the focus trap, return-focus, Esc, and inert background — do not hand-roll [src: 03-RESEARCH.md#Don't-Hand-Roll]. Add `Dialog/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Dialog renders the per-part recipe (scrim/positioner/content/title/close) over Ark Dialog with controlled open + lazyMount + unmountOnExit; the close affordance takes a localized aria; Ark owns trap/return/Esc/inert; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Popover — Ark Popover per-part recipe, focus-managed, no trap</name>
  <files>packages/design/src/shared/uikit/Popover/Popover.tsx, packages/design/src/shared/uikit/Popover/popover.ts, packages/design/src/shared/uikit/Popover/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Dialog/Dialog.tsx (the just-built per-part shell — Popover mirrors it minus the focus trap)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-06 → Popover contract: surface-1 + border-2 + rounded-lg + --shadow-md; focus-managed, returns focus, Esc closes, no trap)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Pattern 1 — controlled open; Pitfall 3 — portal + @source)
  </read_first>
  <action>
    Create `Popover/popover.ts`: a `tv({ slots: { positioner, content, title, arrow } })` over Ark Popover parts — content `bg-surface-1 border border-border-2 rounded-lg shadow-md p-4` (the popover recipe, --shadow-md is the floating-UI elevation) [src: 03-UI-SPEC.md#KIT-06]. Animate transform/opacity only. `/lite`, literal, emitted tokens.

    Create `Popover/Popover.tsx`: a named `Popover` with controlled `open`/`onOpenChange`, a `trigger` slot (renders the shared Button or a passed element), `children` body, optional `title`, `className?`. Render Ark `Popover.Root`/`Trigger`/`Positioner`/`Content` — Ark owns return-focus + Esc-close + no-trap (do not hand-roll). `data-popover` hook; no i18n import. Add `Popover/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Popover renders the popover recipe (surface-1 + border-2 + rounded-lg + --shadow-md) over Ark Popover, focus-managed with no trap, transform/opacity animation only; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Dialog + Popover stories (forced-open + destructive-confirm), barrel, Dialog/trap keyboard specs GREEN</name>
  <files>packages/design/src/shared/uikit/Dialog/Dialog.stories.tsx, packages/design/src/shared/uikit/Popover/Popover.stories.tsx, packages/design/src/index.ts, packages/design/tests/keyboard.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/Button.stories.tsx (StateMatrix + Playground two-mode pattern)
    - packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx (StateCell reuse, data-state-cell labels)
    - packages/design/tests/keyboard.spec.ts (the Dialog Esc-close + return-focus + trap-free Tab cycle scaffolds from Plan 03-01 — currently RED; create the matching story ids here)
    - packages/design/src/index.ts (region-comment barrel append)
  </read_first>
  <action>
    Create `Dialog.stories.tsx` (title `KIT-06 Overlay / Dialog`) two-mode (D-02): a `StateMatrix` with a FORCED-OPEN cell via Ark `open`/`defaultOpen` (the static axe-gate mechanism — the open overlay renders deterministically, no Ladle hack) [src: 03-RESEARCH.md#Pitfall-2], plus a dedicated DESTRUCTIVE-CONFIRMATION story (reversible/checked/confirmed, WCAG 3.3.6): the confirm button uses the `loss` color + a Lucide icon + an explicit verb+noun label, and focus DEFAULTS to the safe (cancel) action [src: 03-UI-SPEC.md#Copywriting-Contract]; and an interactive `Playground` (trigger button opens the real Dialog). Create `Popover.stories.tsx` (title `KIT-06 Overlay / Popover`) two-mode with a forced-open cell + Playground. Resolve copy via `i18n._({ id })` — `dialogClose`, `formSubmit`/`formCancel`, a destructive verb+noun — and pass plain string props. Give the forced-open Dialog cell + the Playground stable ids matching the Wave-0 keyboard spec references. RU-longest-label check at 360 floor (QUAL-05).

    Append to `src/index.ts` a region `// ---- KIT-06 Overlay family — Dialog + Popover (Wave 5 / Plan 03-05) ----` exporting `Dialog` and `Popover` (+ public types).

    Turn the Plan-03-01 Dialog spec block GREEN in `tests/keyboard.spec.ts` against the real story ids: Esc closes the open Dialog AND focus returns to the trigger; Tab from the last focusable inside the open Dialog cycles back inside (trap-free in the sense of no escape to the inert background, bounded loop — fails not hangs); the forced-open content actually paints (a real-browser computed-style check, the tree-shake guard) [src: 03-RESEARCH.md#Pitfall-3]. The Popover return-focus/Esc/no-trap assertions are added here too.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design ladle:build && pnpm --filter @solid-stats/design test:e2e -- keyboard.spec.ts catalog.spec.ts cls.spec.ts</automated>
  </verify>
  <done>Dialog + Popover each have a forced-open StateMatrix + Playground; Dialog adds the destructive-confirm story with focus on cancel; both are graduated into the barrel; the catalog axe/44px gate, the Dialog Esc-close + return-focus + trap behavior, and the portal-paints check are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story fixture → Dialog/Popover content | Author-controlled fixture strings; no untrusted input, no network/server-2/auth (v0.1 presentational Ladle). Portal mounts to document.body — the only structural note (styling, not security). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Tampering | dialog title / body interpolation | low | accept | Strings are author-controlled fixtures via JSX (auto-escaped); no dangerouslySetInnerHTML, no HTML in messages. |
| T-03-05-02 | Repudiation (UX safety) | destructive confirm default focus | low | mitigate | The destructive-confirm story defaults focus to the safe (cancel) action and labels the verb+noun (WCAG 3.3.6 Error Prevention) — a UX-safety pattern, not a security control, but it prevents accidental destructive activation Phases 8–9 compose. |
| T-03-SC | Tampering | npm installs | n/a (no installs this plan) | mitigate | Deps gated by the Plan 03-01 blocking-human checkpoint. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design test:e2e -- catalog.spec.ts keyboard.spec.ts cls.spec.ts` green — axe-clean on the forced-open overlays; Dialog Esc-close + return-focus + trap behavior GREEN; portal content paints; transform/opacity-only (no CLS).
- `tsc --noEmit` green; no arbitrary values.
</verification>

<success_criteria>
- Dialog is a modal with focus trap, return-focus, Esc, scrim, inert background, localized title, and a destructive-confirm pattern (focus on cancel).
- Popover is focus-managed with no trap; both animate transform/opacity only (CLS = 0).
- The forced-open axe gate uses Ark open/defaultOpen (no Ladle hack); portal content paints; both are graduated into the barrel.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-05-SUMMARY.md` when done.
</output>
