---
phase: 01-workspace-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/gen-theme.mjs
  - packages/design/src/styles/theme.css
  - src/styles/theme.css
files_removed:
  - src/styles/theme.css
autonomous: true
requirements: [WS-03, DS-01, DS-02, DS-03]

must_haves:
  truths:
    - "`pnpm gen-theme` writes the generated @theme to packages/design/src/styles/theme.css (the relocated OUT_PATH)."
    - "Regenerating theme.css from a clean tree produces no git diff (committed == regenerated — the drift gate)."
    - "The old top-level src/styles/theme.css no longer exists (relocated, not duplicated)."
    - "The data-trust vocabulary exists as first-class @theme state/color tokens (freshness up-to-date/stale/offline/reconnecting + known/unknown/conflict), resolved from DESIGN.md components.* — present in theme.css where they were absent before (DS-03)."
    - "No Russian display strings appear as @theme token values (translatable copy stays in DESIGN.md annotations / i18n — D-12)."
    - "Paired --text-* / --text-*--line-height tokens are present and no arbitrary token values exist (DS-01)."
  artifacts:
    - path: "packages/design/src/styles/theme.css"
      provides: "The relocated, regenerated @theme block (the @solid-stats/design ./theme.css export target)"
      contains: "@theme"
    - path: "scripts/gen-theme.mjs"
      provides: "OUT_PATH relocated + a bounded components.* data-trust emit (D-09 + D-12)"
      contains: "packages/design"
  key_links:
    - from: "scripts/gen-theme.mjs"
      to: "DESIGN.md"
      via: "reads components.badge-freshness.states.* + badge-known/unknown/conflict and resolves {colors.*} like the existing elevation ring resolve (D-12)"
      pattern: "components|badge-freshness|freshness"
    - from: "scripts/gen-theme.mjs"
      to: "packages/design/src/styles/theme.css"
      via: "OUT_PATH = join(ROOT, packages, design, src, styles, theme.css) (D-09)"
      pattern: "OUT_PATH.*packages.*design"
---

<objective>
Move the token pipeline into the workspace and close the DS-03 gap. Two coupled changes to the root `gen-theme.mjs`: (1) relocate `OUT_PATH` (L26) to `packages/design/src/styles/theme.css` so the generated `@theme` lands in the design package (D-09 / WS-03), and (2) extend the generator to emit the `components.*` data-trust recipes as first-class `@theme` state/color tokens (D-12 — the one substantive code task this phase), resolving `{colors.*}` / `{rounded.*}` references the same way the existing elevation ring section does. Then regenerate, prove zero drift, and remove the orphan top-level `src/styles/theme.css`.

Purpose: DS-03 / ROADMAP criterion #3 require the data-trust vocabulary as first-class tokens — and RESEARCH Pitfall 2 confirmed by grep that `gen-theme.mjs` never reads `components.*`, so the freshness/known/unknown/conflict tokens are absent from `theme.css` today (0 hits). This plan makes them present. `theme.css` stays generated, never hand-edited (D-06).
Output: The relocated + extended generator and its regenerated `theme.css` carrying the data-trust tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md
@DESIGN.md
@scripts/gen-theme.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate OUT_PATH + emit data-trust tokens (D-09 + D-12)</name>
  <read_first>
    - scripts/gen-theme.mjs (OUT_PATH L26 — the one path that changes; buildTheme destructure L173; the elevation {colors.*} resolve pattern L260-264; the block() helper L167-170)
    - DESIGN.md (components.badge-freshness.states.* L330-354; provenance-line L406-411; badge-known L412-418; badge-unknown L419-425; badge-conflict L426-432; the semantic color tokens win/warn/loss/info + -weak/-border L34-45)
    - .planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md (D-09, D-12, D-10 no-arbitrary-values; Claude's Discretion — gate ordering run-then-check)
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (Pitfall 2 — the load-bearing DS-03 gap; A2 named-state-tokens resolution; A4 the YAML parser already handles nested maps)
    - .claude/skills/solidstats-frontend-react-design/references/design-system.md (where the token values flow from; the design.md lint/diff rationale)
  </read_first>
  <files>scripts/gen-theme.mjs</files>
  <behavior>
    - Generating from DESIGN.md writes to packages/design/src/styles/theme.css (NOT the old src/styles path).
    - theme.css contains named freshness state tokens for all four states: up-to-date, stale, offline, reconnecting — each as a fill/text/border triple resolved from the win/warn/loss/info + -weak/-border colors per DESIGN.md badge-freshness.states.
    - theme.css contains named known / unknown / conflict data-trust tokens resolved from DESIGN.md badge-known/unknown/conflict.
    - The {colors.*} and {rounded.*} reference tokens are resolved to their literal hex/rgba values (no unresolved "{colors.win}" strings leak into theme.css).
    - NO Russian string («Актуально» etc.) appears as a token VALUE — those are i18n copy (D-12), documented only in DESIGN.md annotations.
    - The existing output (colors, paired text scales, breakpoints, containers, elevation) is unchanged byte-for-byte except for the new data-trust section.
  </behavior>
  <action>
    In `scripts/gen-theme.mjs`: change `OUT_PATH` (L26) from `join(ROOT, "src", "styles", "theme.css")` to `join(ROOT, "packages", "design", "src", "styles", "theme.css")` — leave `DESIGN_PATH` (L25) unchanged (root stays root, D-09).
    Extend `buildTheme()` to read the `components` section (add `components` to the L173 destructure) and emit a bounded set of named data-trust custom properties (D-12 / A2). Choose stable token names under a `--color-freshness-*` namespace for the four `badge-freshness.states` (`up-to-date` / `stale` / `offline` / `reconnecting`) and a `--color-trust-*` (or `--color-known/unknown/conflict`) namespace for `badge-known` / `badge-unknown` / `badge-conflict`. For each, emit the fill + text + border the recipe references, resolving `{colors.*}` / `{rounded.*}` exactly as the elevation section already resolves `{colors.bg-0}` etc. (L260-264 pattern — extend the `resolve` approach or add a small generic `{colors.NAME}` / `{rounded.NAME}` resolver). Emit these in their own `block()` section (e.g. "Data-trust state tokens (freshness · known/unknown/conflict)"). Do NOT emit any Russian display string as a value — the labels are i18n copy deferred to Phase 3 (D-12). Keep the file dependency-free (A4 — the parser already handles the nested `states:` map). Do NOT hand-edit `packages/design/src/styles/theme.css` — it is regenerated in Task 2.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('scripts/gen-theme.mjs','utf8'); if (!/OUT_PATH\s*=\s*join\(ROOT,\s*\"packages\",\s*\"design\"/.test(s)) { console.error('OUT_PATH not relocated'); process.exit(1); } if (!/components/.test(s)) { console.error('components.* not read'); process.exit(1); } console.log('generator change OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `gen-theme.mjs` `OUT_PATH` points at `packages/design/src/styles/theme.css`; `DESIGN_PATH` is unchanged.
    - `buildTheme()` destructure includes `components` and the new section emits the four freshness state tokens + the known/unknown/conflict tokens.
    - The emit resolves `{colors.*}` / `{rounded.*}` to literal values (a grep of the eventual theme.css for `{colors.` finds nothing in the data-trust section).
    - No Russian string is written as a token value anywhere in the generator's output.
    - The file remains dependency-free (no new imports / node_modules).
  </acceptance_criteria>
  <done>The generator writes to the package path and emits the data-trust vocabulary as resolved @theme state tokens, with Russian copy excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate, drift-gate, remove orphan src/styles</name>
  <read_first>
    - scripts/gen-theme.mjs (the run section L325-336 — reads DESIGN.md, writes OUT_PATH, prints the token summary)
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (Pitfall 5 — run-then-check ordering + git diff --exit-code drift assertion; Runtime State Inventory — remove the orphan top-level src/styles after relocation; Validation Map WS-03)
    - src/styles/theme.css (the current generated output being relocated)
  </read_first>
  <files>packages/design/src/styles/theme.css, src/styles/theme.css</files>
  <action>
    Run `pnpm gen-theme` (or `node scripts/gen-theme.mjs`) to write `packages/design/src/styles/theme.css`. Commit that generated file. Then assert drift-freedom: re-run the generator and confirm `git diff --exit-code packages/design/src/styles/theme.css` is clean (committed == regenerated — Pitfall 5 / WS-03 drift gate). Delete the now-orphan top-level `src/styles/theme.css` and remove the empty `src/styles/` (and `src/`) directory — nothing live references the old path (RESEARCH Runtime State Inventory: only `gen-theme.mjs` L26 did, now relocated). Confirm the generated file carries the data-trust tokens (the DS-03 grep that returned 0 before this phase) and the paired `--text-*--line-height` tokens (DS-01).
  </action>
  <verify>
    <automated>pnpm gen-theme && git diff --exit-code packages/design/src/styles/theme.css && test ! -e src/styles/theme.css && grep -Eq -- '--color-freshness-(up-to-date|stale|offline|reconnecting)' packages/design/src/styles/theme.css && grep -Eq -- '--text-[a-z0-9]+--line-height' packages/design/src/styles/theme.css && ! grep -q '{colors\.' packages/design/src/styles/theme.css</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm gen-theme` writes `packages/design/src/styles/theme.css`; an immediate re-run leaves a clean `git diff --exit-code` (no drift).
    - `src/styles/theme.css` no longer exists (relocated).
    - The generated theme.css contains the four `--color-freshness-*` tokens and the known/unknown/conflict tokens (DS-03 satisfied — was 0 hits before).
    - The generated theme.css contains paired `--text-*--line-height` tokens (DS-01).
    - No unresolved `{colors.` reference leaks into the generated theme.css.
  </acceptance_criteria>
  <done>theme.css is regenerated at the package path, drift-free, carries the data-trust + paired-line-height tokens, and the orphan top-level copy is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Supply-chain / build-integrity only (no runtime surface). The one boundary specific to this plan is the build script itself.

| Boundary | Description |
|----------|-------------|
| DESIGN.md → gen-theme.mjs → theme.css | Arbitrary-code execution path: the root build script reads DESIGN.md and writes the consumed theme.css |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering | `scripts/gen-theme.mjs` (build script writing consumed CSS) | mitigate | Generator stays dependency-free (no new imports / network); output is gated by `git diff --exit-code` (drift detection) and `design.md lint` (plan 05) so an unexpected theme.css change is visible in review. |
| T-1-06 | Information Disclosure | Russian product copy leaking into the token layer | mitigate | D-12 rule enforced: no display string becomes a token value; acceptance criteria grep rejects it. (Not security-sensitive, but a correctness boundary the gate protects.) |
</threat_model>

<verification>
- `pnpm gen-theme` writes `packages/design/src/styles/theme.css`; re-run is drift-free (`git diff --exit-code`).
- `src/styles/theme.css` removed.
- theme.css carries `--color-freshness-*` (×4) + known/unknown/conflict tokens and paired `--text-*--line-height`.
- No `{colors.` reference remains unresolved in theme.css.
</verification>

<success_criteria>
WS-03 (DESIGN.md → packages/design theme.css via gen-theme.mjs, single-source, old src/styles relocated), DS-01 (no arbitrary values; paired line-height), DS-02 (the gunmetal/cyan/Saira/IBM-Plex recipes carried through), and DS-03 (data-trust vocabulary as first-class tokens — the closed gap) are satisfied. D-09 and D-12 are both implemented.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-design-system-foundation/01-03-SUMMARY.md` when done.
</output>
