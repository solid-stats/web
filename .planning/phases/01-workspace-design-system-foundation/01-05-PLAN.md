---
phase: 01-workspace-design-system-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - package.json
  - .design/README.md
  - .design/_ds/
  - .design/hifi/
  - .design/wireframes/
autonomous: true
requirements: [WS-05]

must_haves:
  truths:
    - "The root `pnpm check` runs gen-theme, then design.md lint, then the resolved toolchain gate (vp check or the primitive contingency), and exits 0."
    - "`design.md lint DESIGN.md` exits 0 with zero error-severity findings (warnings allowed — the 7 -weak contrast warnings are documented false positives)."
    - "Regenerating theme.css inside the gate leaves no drift (git diff --exit-code)."
    - "`.design/` is a frozen reference archive EXCEPT .design/CLAUDE.md and .design/MIGRATION.md, which stay live and authoritative (D-11)."
    - "No .design/hifi/*.jsx or wireframe is ported into packages/ — they stay reference-only."
  artifacts:
    - path: "package.json"
      provides: "The root `check` script with the resolved WS-05 gate tail wired (run-then-check ordering)"
      contains: "gen-theme"
    - path: ".design/README.md"
      provides: "Archive pointer noting .design/ is frozen reference and the canonical SoT is /DESIGN.md (D-11)"
      contains: "DESIGN.md"
  key_links:
    - from: "package.json check script"
      to: ".planning/phases/01-workspace-design-system-foundation/TOOLCHAIN.md"
      via: "wires the exact gate command resolved in plan 02 (vp check OR oxlint && oxfmt --check && tsgo --noEmit)"
      pattern: "check"
    - from: "package.json check script"
      to: "DESIGN.md"
      via: "design.md lint DESIGN.md gates the SoT (exit 0 / zero error-severity)"
      pattern: "lint:design|design.md lint"
---

<objective>
Close the phase: wire the resolved WS-05 toolchain gate into the root `check` script in run-then-check order (`gen-theme` → drift check → `design.md lint` → the resolved `vp check`/primitive gate) and prove it green across the workspace; then execute the `.design/` freeze (D-11) — archive everything except `.design/CLAUDE.md` + `.design/MIGRATION.md`, which stay live and authoritative.

Purpose: WS-05 ("the lint/format/type-check toolchain runs green across the workspace; design.md lint gates DESIGN.md") and the D-11 freeze boundary. This is the phase gate — after it, `/gsd-verify-work` runs against a green workspace.
Output: The finalized root `check` script + the frozen `.design/` archive with its pointer README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-workspace-design-system-foundation/TOOLCHAIN.md
@.design/MIGRATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire + prove the WS-05 green gate (run-then-check)</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/TOOLCHAIN.md (the resolved gate command from plan 02 — vp check OR the primitive triplet)
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (Pitfall 1 — design.md lint exits 0 with 86 warnings, gate on exit 0 / zero error-severity NOT zero findings; Pitfall 5 — run-then-check + git diff drift; Code Examples — root package.json check script; Validation Map WS-05)
    - .planning/phases/01-workspace-design-system-foundation/01-VALIDATION.md (Full suite command; sampling; the warnings-allowed note)
    - package.json (the placeholder `check` script from plan 01 to finalize)
    - .claude/skills/solidstats-frontend-react-conventions/SKILL.md (the vp check toolchain that WS-05 points at)
  </read_first>
  <files>package.json</files>
  <action>
    Finalize the root `check` script (run-then-check ordering, Pitfall 5): `pnpm gen-theme && git diff --exit-code packages/design/src/styles/theme.css && pnpm lint:design && <RESOLVED_GATE>` where `<RESOLVED_GATE>` is the exact command recorded in TOOLCHAIN.md by plan 02 (`vp check` OR `oxlint && oxfmt --check . && tsgo --noEmit`). The `git diff --exit-code` step fails the gate if the committed theme.css drifted from DESIGN.md (catches "edited DESIGN.md, forgot to regen"). Keep `lint:design` = `design.md lint DESIGN.md`; the gate keys on EXIT 0 / zero error-severity, NOT zero findings — the 86 warnings (incl. 7 false-positive `-weak` contrast warnings) are expected and must NOT fail the gate (Pitfall 1). If a strict warning-count is ever wanted, pipe `--format json` and fail only on `findings[].severity === "error"` — but do NOT add `--max-warnings 0` or grep for "0 findings".
    Run `pnpm check` from the root and confirm exit 0 across the workspace (gen-theme drift-free, design.md lint zero errors, the toolchain gate green). If `vp check`/primitives surface real findings in the scaffold files, fix them (formatting/lint) until green — WS-05 requires green, not waivers.
  </action>
  <verify>
    <automated>pnpm check && npx @google/design.md lint DESIGN.md; echo "lint exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0 across the workspace.
    - The `check` script runs gen-theme FIRST, then the `git diff --exit-code` drift assertion, then `lint:design`, then the resolved toolchain gate (the exact TOOLCHAIN.md command).
    - `design.md lint DESIGN.md` exits 0; the script does NOT use `--max-warnings 0` or a "zero findings" grep (warnings allowed).
    - No `vp check` / primitive findings remain unaddressed (green, not waived).
  </acceptance_criteria>
  <done>The workspace gate is green: gen-theme drift-free, DESIGN.md lints to zero errors, and the lint/format/type-check toolchain passes — WS-05 satisfied under the resolved path.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze .design/ (D-11)</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md (D-11 freeze boundary — CLAUDE.md + MIGRATION.md stay live; hifi/wireframes/_ds/app are reference-only, never ported)
    - .design/MIGRATION.md (step 5 freeze: keep CLAUDE.md as living companion; hifi/wireframes/_ds become reference + a README pointing to canonical /DESIGN.md; the dead --container:1240 survives only here)
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (anti-pattern: porting any .design/hifi/*.jsx — frozen reference only)
  </read_first>
  <files>.design/README.md</files>
  <action>
    Add (or update) `.design/README.md` declaring `.design/` a FROZEN reference archive: state that the canonical token source of truth is the repo-root `/DESIGN.md` (regenerated to `packages/design/src/styles/theme.css` via `scripts/gen-theme.mjs`), that `hifi/`, `wireframes/`, `_ds/`, `app/`, `screenshots/`, `uploads/`, `export/`, and `support.js` are visual reference ONLY (fake-stack plain CSS — never ported, MIGRATION D1/D2), and that the ONLY live, authoritative files in `.design/` are `CLAUDE.md` (the running per-surface companion + domain-truth home, D-11) and `MIGRATION.md`. Do NOT delete or move the archived directories (preserve completed work — they are reference); do NOT touch `.design/CLAUDE.md` or `.design/MIGRATION.md` (they stay live). Do NOT copy any `.design/hifi/*.jsx` or wireframe into `packages/` (anti-pattern).
  </action>
  <verify>
    <automated>test -f .design/README.md && grep -q "DESIGN.md" .design/README.md && test -f .design/CLAUDE.md && test -f .design/MIGRATION.md && ! find packages -name '*.jsx' 2>/dev/null | grep -q . && ! grep -rilq "container: 1240\|--container: 1240" packages 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `.design/README.md` exists and points to `/DESIGN.md` as the canonical SoT, marking hifi/wireframes/_ds/app/etc. reference-only.
    - `.design/CLAUDE.md` and `.design/MIGRATION.md` are untouched (still present, still authoritative).
    - No `.jsx` from `.design/hifi/` is copied into `packages/`.
    - The dead `--container: 1240` does not appear anywhere under `packages/` (it survives only in frozen `.design/`).
  </acceptance_criteria>
  <done>`.design/` is a frozen reference archive with a canonical-SoT pointer; CLAUDE.md + MIGRATION.md stay live; nothing was ported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Supply-chain / build-integrity only. This plan's boundary is the CI gate itself (it executes the resolved toolchain).

| Boundary | Description |
|----------|-------------|
| root `check` script → toolchain | The gate runs gen-theme + design.md lint + the resolved vp/primitive toolchain; all are audited tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Tampering | the `check` gate (executes build + lint tools) | mitigate | The gate runs only audited tools (plan 01/02 legitimacy audits); the `git diff --exit-code` drift step makes any unexpected theme.css change fail the gate, surfacing tampering with the SoT→output pipeline. |
| T-1-SC | Tampering | npm/pnpm installs (whole-phase supply-chain) | mitigate | Exact-pin + committed lockfile (plan 01) + legitimacy audit (RESEARCH) + the blocking human checkpoints on the four dev-deps (plan 01 T-1-01) and the unconfirmed `vp` (plan 02 T-1-03). No `[ASSUMED]`/`[SUS]` package was auto-approved. |
</threat_model>

<verification>
- `pnpm check` exits 0 across the workspace (gen-theme drift-free, design.md lint zero errors, toolchain green).
- `design.md lint DESIGN.md` exits 0 (warnings allowed).
- `.design/README.md` points to `/DESIGN.md`; CLAUDE.md + MIGRATION.md untouched; no `.jsx` ported into `packages/`.
</verification>

<success_criteria>
WS-05 (toolchain green across the workspace; design.md lint gates DESIGN.md at exit 0 / zero error-severity) is satisfied and the `.design/` freeze boundary (D-11) is in place. The phase gate is green for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-design-system-foundation/01-05-SUMMARY.md` when done.
</output>
