---
phase: 02-uikit-structural-data-display-primitives
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - packages/design/src/shared/uikit/Table/index.ts
  - packages/design/src/shared/uikit/Table/Table.tsx
  - packages/design/src/shared/uikit/Table/Th.tsx
  - packages/design/src/shared/uikit/Table/TableRow.tsx
  - packages/design/src/shared/uikit/Table/Table.stories.tsx
  - packages/design/src/shared/uikit/DensityToggle/index.ts
  - packages/design/src/shared/uikit/DensityToggle/DensityToggle.tsx
  - packages/design/src/shared/uikit/DensityToggle/DensityToggle.stories.tsx
  - packages/design/src/shared/uikit/CompactRow/index.ts
  - packages/design/src/shared/uikit/CompactRow/CompactRow.tsx
  - packages/design/src/shared/uikit/CompactRow/CompactRow.stories.tsx
  - packages/design/src/shared/uikit/Pagination/index.ts
  - packages/design/src/shared/uikit/Pagination/Pagination.tsx
  - packages/design/src/shared/uikit/Pagination/Pagination.stories.tsx
  - packages/design/src/index.ts
autonomous: true
requirements: [KIT-02, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]

must_haves:
  truths:
    - "Table is sticky-header scroll-in-card with a colgroup of fixed widths + a reserved scroll height + a virtualization-ready fixed ROW_H (52/44) row model with spacer rows — CLS = 0; presentational-only, no @tanstack/react-table or react-virtual (KIT-02 / D-01 / QUAL-04)"
    - "Th is a plain <button> with a Lucide sort arrow + aria-sort (asc/desc/none); sort state is a controlled prop, no overlay/engine (KIT-02 / QUAL-03)"
    - "TableRow: whole row is the click zone with a focusable anchor in the player-name cell (Tab reaches it; SR traverses rows); selected row = primary-weak fill + inset 2px cyan left-edge marker + aria-selected — never fill-only (KIT-02 / QUAL-03)"
    - "Long nickname/squad cells truncate+title (ellipsis) — never clip; CompactRow at 360px stacks label-over-value (KIT-02 / QUAL-02)"
    - "DensityToggle switches ROW_H comfortable 52 / compact 44 via a controlled prop; the loading state is a Skeleton row reproducing the exact colgroup+header+row dims (KIT-02 / QUAL-04)"
    - "Mobile (< md) = CompactRow top-N + показать ещё · N, secondary columns dropped, NO horizontal/nested scroll at 360px; total count in the caption (KIT-02 / QUAL-02)"
    - "All rows render from the single _fixtures roster (Vasiliy #1, tier-colored Счёт/K-D with pips); ×5 endings + ×4 data-volumes demonstrated; axe-clean / keyboard / ≥44px / RU + EN (QUAL-01 / QUAL-06 / QUAL-03 / QUAL-05)"
    - "The family graduates into the @solid-stats/design barrel"
  artifacts:
    - path: "packages/design/src/shared/uikit/Table/Table.tsx"
      provides: "Sticky-header scroll-in-card, colgroup, reserved height, virtualization-ready ROW_H + spacer rows"
      contains: "colgroup"
    - path: "packages/design/src/shared/uikit/Table/Th.tsx"
      provides: "Sortable header: plain button + Lucide arrow + aria-sort, controlled sort prop"
      contains: "aria-sort"
    - path: "packages/design/src/shared/uikit/Table/TableRow.tsx"
      provides: "Row: whole-row click zone + focusable name-cell anchor; selected = primary-weak + inset cyan edge + aria-selected"
      contains: "aria-selected"
    - path: "packages/design/src/shared/uikit/DensityToggle/DensityToggle.tsx"
      provides: "Controlled density toggle switching ROW_H 52/44"
      contains: "export function DensityToggle"
    - path: "packages/design/src/shared/uikit/CompactRow/CompactRow.tsx"
      provides: "Mobile compact row: label-over-value stack, secondary cols dropped, no h-scroll"
      contains: "export function CompactRow"
    - path: "packages/design/src/shared/uikit/Pagination/Pagination.tsx"
      provides: "Pagination / cursor affordance (inert — no server), Prev/Next + end-of-list"
      contains: "export function Pagination"
    - path: "packages/design/src/index.ts"
      provides: "Barrel graduating the KIT-02 data-table family"
      contains: "Table"
  key_links:
    - from: "packages/design/src/shared/uikit/Table/Table.tsx"
      to: "packages/design/src/shared/uikit/_fixtures/index.ts"
      via: "renders rows from the canonical roster (Vasiliy #1, consistent Score/K-D)"
      pattern: "ROSTER"
    - from: "packages/design/src/shared/uikit/Table/Table.tsx"
      to: "packages/design/src/shared/uikit/Skeleton/index.ts"
      via: "loading state uses the Skeleton table variant reproducing colgroup+header+row dims"
      pattern: "Skeleton"
    - from: "packages/design/src/shared/uikit/Table/Table.stories.tsx"
      to: "packages/design/src/shared/uikit/_state-matrix/index.ts"
      via: "demonstrates ×4 data-volume + ×5 ending states via StateMatrix / story args"
      pattern: "StateMatrix|StateCell"

premises:
  - claim: D-01 — Phase 2 ships presentational-only; NO @tanstack/react-table, NO @tanstack/react-virtual; sort/density/selection/pagination are controlled props (the durable interface for the v1.0 engine swap)
    src: 02-CONTEXT.md#D-01
    verify: sed -n '33,44p' .planning/phases/02-uikit-structural-data-display-primitives/02-CONTEXT.md
  - claim: DESIGN.md authors table-header (L370), table-row (L377, selected = inset 2px cyan left-edge), table-row-zebra (L386), table-cell-numeric (L388)
    src: DESIGN.md#components
    verify: grep -nE '^\s+(table-header|table-row|table-row-zebra|table-cell-numeric):' DESIGN.md
  - claim: full-row click zone needs a focusable anchor in the name cell so Tab reaches it and a SR can traverse rows (checker rec #1); long values truncate+tooltip never clip (checker rec #2)
    src: 02-UI-SPEC.md#Checker-Recommendations
    verify: sed -n '246,253p' .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md
  - claim: the Skeleton table variant (Plan 03) reproduces the exact colgroup+header+N×ROW_H for the CLS-0 loading state
    src: 02-03-SUMMARY.md
    verify: ls packages/design/src/shared/uikit/Skeleton

carried_forward:
  - claim: ROW_H is fixed by density (52/44), not padding — a fixed-height row model so the virtualization-ready spacer math and CLS = 0 hold
    src: 02-UI-SPEC.md#Spacing-Scale
  - claim: spacer/viewport/reserved heights are computed dynamic values via inline style (not tokens); the row/cell visual classes stay literal token utilities
    src: 02-RESEARCH.md#Pattern-5
---

<objective>
The KIT-02 data-table family — the heaviest slice: `Table` (with `Th`/`TableRow` as leaf components), `DensityToggle`, `CompactRow`, and `Pagination`/`CursorAffordance`. Built last so it composes the proven Skeleton (table variant, Plan 03), the tier-colored cells + pips (Plan 05), and the single fixture roster. Presentational-only (D-01): the full VISUAL contract as markup + tokens + fixture rows passed as props — sort/density/selection/pagination are controlled props (the durable interface the v1.0 TanStack/server engine swaps into without changing the visual contract). [src: 02-CONTEXT.md#D-01] [src: 02-UI-SPEC.md#KIT-02] [src: mvp_mode_active]

Acceptance test for this slice: after it completes, a Phase-4+ surface-builder can import one more reviewed family (`Table`, `Th`, `TableRow`, `DensityToggle`, `CompactRow`, `Pagination`) from `@solid-stats/design` — completing the structural & data-display catalog for the whole phase.

Purpose: a CLS = 0 sticky-header scroll-in-card with a virtualization-ready row model, a keyboard/SR-traversable full-row click zone, and a mobile compact-row with no horizontal scroll — the data surface every public-stats page (Phases 4-7) composes. [src: .design/CLAUDE.md#L150-184]
Output: four colocated slices (Th/TableRow nested in Table per architecture.md leaf-granularity), each a StateMatrix / data-volume story, graduated into the barrel and design-review APPROVED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md
@.design/CLAUDE.md
@packages/design/src/styles/theme.css
@packages/design/src/shared/uikit/_fixtures/index.ts
@.planning/phases/02-uikit-structural-data-display-primitives/02-05-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

No runtime trust boundary — presentational fixture-only catalog. Sort/density/selection/pagination are controlled props with no engine and no server — no data-access surface this phase. [src: 02-RESEARCH.md#Security-Domain]

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | row values + RU+EN headers/captions rendered from fixtures | mitigate | All values/copy via JSX auto-escaping; never `dangerouslySetInnerHTML`; static fixtures (V5). [src: 02-RESEARCH.md#Security-Domain] |
| T-02-02 | Tampering | Lucide sort arrows + pagination icons | mitigate | Icons via `lucide-react` components, never raw SVG strings (V14). [src: 02-RESEARCH.md#Security-Domain] |
| — | Spoofing / Repudiation / Info-disclosure / DoS / Elevation | — | N/A | No auth/session/network/persistence/privilege; no real sort/pagination engine this phase (controlled props only). [src: 02-RESEARCH.md#Security-Domain] |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Table + Th + TableRow (CLS-0 scroll-in-card, controlled sort, full-row click zone)</name>
  <files>packages/design/src/shared/uikit/Table/index.ts, packages/design/src/shared/uikit/Table/Table.tsx, packages/design/src/shared/uikit/Table/Th.tsx, packages/design/src/shared/uikit/Table/TableRow.tsx, packages/design/src/shared/uikit/Table/Table.stories.tsx</files>
  <read_first>
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (Pattern 5 CLS-0 table: colgroup + fixed ROW_H + reserved viewport + skeleton match; virtualization-ready spacer rows; selected-row inset cyan edge; Pitfall 5 full-row click zone + name-cell anchor; Pitfall 6 truncate+title)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-02 visual contract, sortable Th, selected row, states ×7, click zone, numeric cells)
    - DESIGN.md (table-header L370, table-row L377 incl. selected inset 2px cyan, table-row-zebra L386, table-cell-numeric L388)
    - .design/hifi/players.jsx (L11, L170-213 row-model SEMANTICS only — rebuild, never import, D-11)
    - packages/design/src/shared/uikit/_fixtures/index.ts (ROSTER + scoreOf/kdOf/tierFor — tier-colored Счёт/K-D cells)
    - packages/design/src/shared/uikit/Skeleton/index.ts (the table-variant skeleton for the loading state — reuse, do not re-build)
    - packages/design/src/shared/uikit/TierChip/index.ts and TierScale (pips in numeric cells)
    - packages/design/src/shared/uikit/_state-matrix/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (native table + focusable name-cell anchor; aria-sort; aria-selected; never color-alone; focus not obscured behind sticky header)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (reserved scroll height; CLS = 0; virtualization-ready but NOT virtualized this phase)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md (controlled-prop primitive; leaf granularity Th/TableRow inside Table)
  </read_first>
  <behavior>
    - Table renders `<table className="table-fixed">` + a `<colgroup>` of fixed `<col>` widths + a `<thead className="sticky top-0">` (table-header on surface-2) inside an `overflow-y-auto` viewport of reserved height; rows are fixed `ROW_H` (52/44 by density prop); top/bottom spacer rows frame the visible window (virtualization-ready, but NOT virtualized this phase — D-01); loading swaps in the Skeleton table variant reproducing the exact colgroup+header+N×ROW_H (CLS = 0).
    - Th renders a plain `<button>` inside the `<th>` with a Lucide arrow (arrow-up/arrow-down/none) + `aria-sort` (ascending/descending/none); sort state is a controlled prop (parent owns it) — no overlay/menu, no engine.
    - TableRow: the whole row is the click target with a focusable `<a>`/`<button>` in the player-name cell (Tab reaches the anchor, the row delegates the pointer click to it); selected row = `primary-weak` fill + an inset 2px cyan left-edge marker + `aria-selected` — never fill-only; long name/squad cells `overflow-hidden text-ellipsis whitespace-nowrap` + `title` (never clip); numeric cells `table-cell-numeric` (tabular mono, right-aligned, signed deltas), Счёт/K-D tier-colored with pips paired (never color-alone).
    - Table story demonstrates the ×7 row states (enabled/hover surface-3/pressed/focused ring-not-hidden/selected/disabled/loading skeleton) + the ×4 data-volume states (empty → EmptyState / few / many capped window with total in caption / limit-reached) + the ×5 endings, all from the roster.
  </behavior>
  <action>
    Create the `Table` slice with `Th` and `TableRow` as nested leaf components (architecture.md leaf-granularity; D-02 is the intended shape, not a rigid file count). Build the CLS-0 contract per RESEARCH Pattern 5: `table-fixed` + `<colgroup>` fixed widths + sticky `table-header` (L370) + reserved viewport height; fixed `ROW_H` (52/44) + top/bottom spacer rows (virtualization-ready — but DO NOT install/use `@tanstack/react-table` or `@tanstack/react-virtual`, D-01); the loading state reuses the Plan-03 Skeleton table variant (do not rebuild it). Spacer/viewport/reserved heights are computed dynamic values via inline style; the row/cell visual classes stay literal token utilities [src: 02-RESEARCH.md#Pattern-5].

    `Th` = a plain `<button>` + Lucide arrow + `aria-sort`, sort state a controlled prop (no overlay/menu/engine). `TableRow` = whole-row click zone with a focusable name-cell `<a>`/`<button>` (checker rec #1 — Tab reaches the anchor, SR traverses rows); selected = `primary-weak` + inset `2px 0 0 primary` left-edge + `aria-selected` (recipe `table-row` L383, never fill-only); long name/squad cells truncate+`title` (checker rec #2, never clip); numeric cells `table-cell-numeric` (L388) tier-colored Счёт/K-D with pips (never color-alone). Focus ring must not be hidden under the sticky header (WCAG 2.4.12) [src: a11y.md].

    Render rows from `_fixtures/ROSTER` (Vasiliy #1) + `tierFor` for cell tiers; copy (headers, caption total, empty/error copy) from `_fixtures/STRINGS` (RU + EN). Rebuild from `.design/hifi/players.jsx` SEMANTICS — never import (D-11). Story: StateMatrix / story args for the ×7 + ×4 + ×5 states. Keep all class strings literal [src: 01-04-SUMMARY.md#deviations].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts tests/keyboard.spec.ts tests/cls.spec.ts -g "table"</automated>
  </verify>
  <acceptance_criteria>
    - Table has a `<colgroup>` of fixed widths + sticky `<thead>` + reserved viewport; the loading skeleton's table `boundingBox().height` equals the data table's (CLS = 0).
    - Th renders a plain `<button>` + Lucide arrow + `aria-sort` (asc/desc/none); sort is a controlled prop (grep confirms no @tanstack/react-table import).
    - TableRow: the name-cell anchor is keyboard-focusable (Tab reaches it, keyboard spec); selected row carries `aria-selected` + the inset cyan edge (not fill-only); long cells use `text-ellipsis` + `title`.
    - No @tanstack/react-table or @tanstack/react-virtual imported (grep returns 0); no arbitrary Tailwind values / raw hex (only computed dynamic heights via inline style).
    - Rows render from `_fixtures/ROSTER` (Vasiliy #1); axe no serious/critical; RU + EN headers/caption.
  </acceptance_criteria>
  <done>Table/Th/TableRow deliver the full CLS-0 visual contract with controlled sort, a keyboard/SR full-row click zone, and tier-colored numeric cells — presentational-only, no engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DensityToggle + CompactRow (mobile) + Pagination/CursorAffordance</name>
  <files>packages/design/src/shared/uikit/DensityToggle/index.ts, packages/design/src/shared/uikit/DensityToggle/DensityToggle.tsx, packages/design/src/shared/uikit/DensityToggle/DensityToggle.stories.tsx, packages/design/src/shared/uikit/CompactRow/index.ts, packages/design/src/shared/uikit/CompactRow/CompactRow.tsx, packages/design/src/shared/uikit/CompactRow/CompactRow.stories.tsx, packages/design/src/shared/uikit/Pagination/index.ts, packages/design/src/shared/uikit/Pagination/Pagination.tsx, packages/design/src/shared/uikit/Pagination/Pagination.stories.tsx</files>
  <read_first>
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-02 DensityToggle controlled ROW_H 52/44; CompactRow top-N + показать ещё · N, secondary cols dropped, no h-scroll, label-over-value at 360px; Pagination/CursorAffordance inert; Copywriting Contract: density toggle, show-more, pagination Назад/Дальше)
    - .design/CLAUDE.md (L165-184 mobile = top-20 + incremental show more · N, never dumps; desktop virtualizes — N/A this phase as controlled prop)
    - packages/design/src/shared/uikit/Table/TableRow.tsx (the row visual contract CompactRow mirrors at 360px)
    - packages/design/src/shared/uikit/_fixtures/index.ts and _state-matrix/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (@container reflow; no arbitrary values; tv())
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (44px targets; keyboard; never color-alone)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (no nested scroll on mobile; reserved space)
  </read_first>
  <behavior>
    - DensityToggle switches ROW_H comfortable 52 / compact 44 via a controlled prop (Nano-store-shaped in v1.0; here a controlled prop / story arg); ≥44px hit area; RU labels Плотность: Обычная / Компактная.
    - CompactRow is the mobile (< md) layout: top-N rows + a `показать ещё · N` expander, secondary columns dropped (e.g. max-dist), label-over-value stack at the 360px floor, NO horizontal scroll, NO nested scroll (the page scrolls).
    - Pagination/CursorAffordance renders Prev/Next (Назад/Дальше) + an end-of-list affordance; inert this phase (no server) — the active state uses the cyan accent; ≥44px targets.
    - Stories: each a StateMatrix of states + a Playground; CompactRow story verified at 360px (no h-scroll).
  </behavior>
  <action>
    Create the `DensityToggle` slice (a controlled toggle switching ROW_H 52/44 — a container class the Table consumes; ≥44px hit area; RU/EN labels from `_fixtures/STRINGS`). Create the `CompactRow` slice — the mobile layout: top-N + a `показать ещё · N` expander, secondary columns dropped, label-over-value stack at 360px, NO horizontal/nested scroll [src: 02-UI-SPEC.md#KIT-02]; reflow keyed off `@container` not viewport [src: styling.md]. Create the `Pagination` slice (Prev/Next + end-of-list cursor affordance, inert — no server; cyan active state; ≥44px). Pull copy from `_fixtures/STRINGS` (RU + EN). Reuse the `tv()`/StateMatrix patterns; keep class strings literal [src: 01-04-SUMMARY.md#deviations].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts tests/responsive.spec.ts -g "densitytoggle|compactrow|pagination"</automated>
  </verify>
  <acceptance_criteria>
    - DensityToggle switches ROW_H 52/44 via a controlled prop (≥44px hit area, axe clean); RU + EN labels.
    - CompactRow at 360px: `scrollWidth <= clientWidth` (no horizontal scroll), no nested scroll, secondary columns dropped, label-over-value stack; the `показать ещё · N` expander present.
    - Pagination renders Назад/Дальше + end-of-list; targets ≥ 44px; cyan active state (never color-alone — carries a label/icon).
    - No arbitrary Tailwind values / raw hex (grep returns 0); axe no serious/critical.
  </acceptance_criteria>
  <done>DensityToggle (controlled 52/44), CompactRow (mobile, no h-scroll, top-N + show-more), and Pagination (inert cursor affordance) are catalogued and axe/responsive-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Graduate the KIT-02 family into the barrel + per-family design-review + full-phase gate</name>
  <files>packages/design/src/index.ts</files>
  <read_first>
    - packages/design/src/index.ts (barrel after all prior families — append the KIT-02 block to a distinct region; this completes the phase catalog)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-02 full contract)
    - .agents/skills/solidstats-frontend-react-design-review/SKILL.md (seven pillars)
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (Pillar 4 states/data-volumes; Pillar 5 responsiveness/360px no h-scroll/sticky scroll-in-card; Pillar 2 CLS=0 skeleton match; Pillar 3 full-row keyboard + aria-sort/aria-selected; Pillar 6 tier coloring + tabular mono + mock-numbers-obey-formulas)
    - .agents/skills/solidstats-shared-review-standards/SKILL.md (report format + verdict)
  </read_first>
  <action>
    Append named re-exports of the KIT-02 slices (`Table`, `Th`, `TableRow`, `DensityToggle`, `CompactRow`, `Pagination`/`CursorAffordance`) to `packages/design/src/index.ts` in a distinct block below the prior families (append-only — this completes the Phase-2 catalog). Keep Smoke + `_fixtures` + `_state-matrix` absent.

    Run the per-family design-review (`solidstats-frontend-react-design-review` seven pillars + checklist) against the KIT-02 stories at the project breakpoints (esp. 360px no horizontal scroll, sticky scroll-in-card on desktop, CLS = 0 skeleton match, full-row keyboard traversal, aria-sort/aria-selected, tier-colored numerics, mock numbers obey the formulas / Vasiliy #1). Fix the CLASS of any finding; map into the shared-review-standards report format.

    Then confirm the full-phase gate: the complete Vitest + Playwright matrix green and every family (KIT-01/02/03/04/07) graduated into the barrel and design-review APPROVED — the phase is ready for `/gsd-verify-work`.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && pnpm exec vitest run && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `import { Table, Th, TableRow, DensityToggle, CompactRow, Pagination } from "@solid-stats/design"` resolves (barrel grep confirms the KIT-02 names alongside all prior families — the full Phase-2 catalog is graduated).
    - The full `playwright test` matrix is green (axe = 0 serious/critical, 44px, keyboard, CLS = 0, responsive 360px) across all families; full Vitest green.
    - `pnpm check` (root) exits 0.
    - `solidstats-frontend-react-design-review` returns APPROVE for the KIT-02 family.
  </acceptance_criteria>
  <done>The KIT-02 data-table family is importable from @solid-stats/design and passes design-review; the full Phase-2 structural & data-display catalog is complete and green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @solid-stats/design exec playwright test` green for KIT-02 (axe = 0, 44px, keyboard full-row, CLS = 0 skeleton match, responsive 360px no h-scroll) + the full-phase matrix.
- No @tanstack/react-table or @tanstack/react-virtual imported anywhere (D-01).
- Barrel exports the KIT-02 slices plus all prior families (complete catalog); helpers/Smoke absent.
- `solidstats-frontend-react-design-review` APPROVE; full Vitest green; root `pnpm check` exits 0.
</verification>

<success_criteria>
The KIT-02 data-table family (Table, Th, TableRow, DensityToggle, CompactRow, Pagination) is catalogued as colocated Ladle stories: a CLS = 0 sticky-header scroll-in-card with a colgroup + reserved height + virtualization-ready fixed-ROW_H + spacer rows (presentational-only, no engine), controlled-prop sort (aria-sort) / density (52/44) / selection (primary-weak + inset cyan edge + aria-selected, never fill-only), a keyboard/SR-traversable full-row click zone, truncate+title long cells, a mobile CompactRow with no horizontal scroll at 360px + top-N + показать ещё, an inert Pagination/cursor affordance, all rows from the single fixture roster (Vasiliy #1, tier-colored), ×5 endings + ×4 data-volumes, axe-clean / keyboard / ≥44px / RU + EN, graduated into the barrel and design-review APPROVED — completing the Phase-2 structural & data-display catalog.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created this phase):
- Components: `Table`, `Th` (nested), `TableRow` (nested), `DensityToggle`, `CompactRow`, `Pagination`/`CursorAffordance`.
- Barrel exports: the KIT-02 names appended to `packages/design/src/index.ts` (completing the full Phase-2 catalog).
- Stories: one colocated `*.stories.tsx` per slice (Matrix + Playground / data-volume states).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-06-SUMMARY.md` when done
</output>
