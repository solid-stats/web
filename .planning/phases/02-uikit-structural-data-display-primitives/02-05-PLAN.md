---
phase: 02-uikit-structural-data-display-primitives
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - packages/design/src/shared/uikit/StatTile/index.ts
  - packages/design/src/shared/uikit/StatTile/StatTile.tsx
  - packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx
  - packages/design/src/shared/uikit/MiniStatGrid/index.ts
  - packages/design/src/shared/uikit/MiniStatGrid/MiniStatGrid.tsx
  - packages/design/src/shared/uikit/MiniStatGrid/MiniStatGrid.stories.tsx
  - packages/design/src/shared/uikit/TierChip/index.ts
  - packages/design/src/shared/uikit/TierChip/TierChip.tsx
  - packages/design/src/shared/uikit/TierChip/TierChip.stories.tsx
  - packages/design/src/shared/uikit/TierScale/index.ts
  - packages/design/src/shared/uikit/TierScale/TierScale.tsx
  - packages/design/src/shared/uikit/TierScale/TierScale.stories.tsx
  - packages/design/src/shared/uikit/Sparkline/index.ts
  - packages/design/src/shared/uikit/Sparkline/Sparkline.tsx
  - packages/design/src/shared/uikit/Sparkline/Sparkline.stories.tsx
  - packages/design/src/index.ts
autonomous: true
requirements: [KIT-03, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]

must_haves:
  truths:
    - "StatTile renders hero Score/K-D in tabular-mono stat-xl with a signed delta colored win/loss paired with trending-up/down Lucide icon — never color-alone (KIT-03 / QUAL-03)"
    - "MiniStatGrid is an even grid (no orphan tiles) following metric priority; every value tabular mono (KIT-03)"
    - "TierChip/TierScale/Pips are population-derived from SS_BASELINE with baseline passed explicitly; show level name + entry threshold (≥2.4 ХОРОШО); tier color paired with the level word — never color-alone (KIT-03 / QUAL-06 / D-04)"
    - "Sparkline is dependency-free (DOM bars filled with var(--color-chart-*)/tier tokens, computed % height via inline style); chart aria-hidden + figcaption sr-only summary; motion honors prefers-reduced-motion; fixed height = CLS 0 (KIT-03 / D-03 / QUAL-04)"
    - "All stat data comes from the single _fixtures roster/baseline — Vasiliy #1, internally consistent with Score/K-D formulas (QUAL-06)"
    - "Every stat story is axe-clean, ≥44px on any interactive tile, RU + EN from _fixtures/strings (QUAL-03 / QUAL-05)"
    - "The family graduates into the @solid-stats/design barrel"
  artifacts:
    - path: "packages/design/src/shared/uikit/StatTile/StatTile.tsx"
      provides: "Hero stat tile, stat-xl tabular, signed delta + trend icon"
      contains: "tabular-nums"
    - path: "packages/design/src/shared/uikit/MiniStatGrid/MiniStatGrid.tsx"
      provides: "Even mini-stat grid, no orphan tiles, metric priority"
      contains: "export function MiniStatGrid"
    - path: "packages/design/src/shared/uikit/TierChip/TierChip.tsx"
      provides: "Population-derived tier chip: level name + entry threshold, color paired with word"
      contains: "tierFor"
    - path: "packages/design/src/shared/uikit/TierScale/TierScale.tsx"
      provides: "Tier scale + Pips discrete level indicator"
      contains: "tierFor"
    - path: "packages/design/src/shared/uikit/Sparkline/Sparkline.tsx"
      provides: "Dependency-free DOM-bar sparkline, aria-hidden + sr-only summary, motion-reduce, fixed height"
      contains: "aria-hidden"
    - path: "packages/design/src/index.ts"
      provides: "Barrel graduating the KIT-03 stat-primitive family"
      contains: "StatTile"
  key_links:
    - from: "packages/design/src/shared/uikit/TierChip/TierChip.tsx"
      to: "packages/design/src/shared/uikit/_fixtures/index.ts"
      via: "calls tierFor(metric, value, baseline) with baseline passed explicitly"
      pattern: "tierFor"
    - from: "packages/design/src/shared/uikit/Sparkline/Sparkline.tsx"
      to: "packages/design/src/styles/theme.css"
      via: "bar fill is a token class (bg-chart-1/tier-*); height is a computed % via inline style"
      pattern: "chart-|tier-"
    - from: "packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx"
      to: "packages/design/src/shared/uikit/_fixtures/index.ts"
      via: "renders Score/K-D from the canonical roster (Vasiliy #1)"
      pattern: "ROSTER|scoreOf|kdOf"

premises:
  - claim: theme.css ships --color-chart-1..5 + --color-grid-line (chart-1 cyan, chart-2 green) for the Sparkline
    src: packages/design/src/styles/theme.css#L52-57
    verify: grep -nE 'color-chart-[1-5]|grid-line' packages/design/src/styles/theme.css
  - claim: DESIGN.md authors stat-tile (L394); SS_BASELINE thresholds + tierFor live in _fixtures (Plan 01)
    src: DESIGN.md#components
    verify: grep -nE '^\s+stat-tile:' DESIGN.md; ls packages/design/src/shared/uikit/_fixtures/tiers.ts
  - claim: Sparkline bar height set via inline style reading a computed % is a legitimate dynamic value (styling.md), NOT an arbitrary themable value; the bar FILL must still be a token class
    src: 02-RESEARCH.md#Pattern-4
    verify: sed -n '248,264p' .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md
  - claim: sr-only utility is available (Wave 0 confirmed/added it) for the Sparkline figcaption summary
    src: 02-01-SUMMARY.md
    verify: grep -c 'sr-only' packages/design/.ladle/tailwind.css packages/design/src/styles/theme.css

carried_forward:
  - claim: tier baseline must be passed explicitly to tier calls, never mutate a module-global (D-04 — Vitest already proves tierFor is pure)
    src: 02-01-SUMMARY.md
  - claim: keep class strings literal in tv(); the bar-fill token class must be literal so the @source scan emits it
    src: 01-04-SUMMARY.md#deviations
---

<objective>
The KIT-03 stat-primitive family — `StatTile` (hero Score/K-D), `MiniStatGrid`, `TierChip`/`TierScale`/`Pips`, and the dependency-free `Sparkline`. This is the headline-data vocabulary; built after the structural families so it consumes the proven `_fixtures` roster/baseline + `tierFor` (already Vitest-proven pure) and the StateMatrix helper. [src: 02-UI-SPEC.md#KIT-03] [src: mvp_mode_active]

Acceptance test for this slice: after it completes, a Phase-4+ surface-builder can import one more reviewed family (`StatTile`, `MiniStatGrid`, `TierChip`, `TierScale`, `Pips`, `Sparkline`) from `@solid-stats/design`, with all numbers internally consistent (Vasiliy #1) via the single fixture source.

Purpose: population-derived tiers (never hardcoded cutoffs), tabular-mono numerals, and a microchart that holds CLS = 0 and never imports a second color system. [src: .design/CLAUDE.md#L94-101] [src: 02-CONTEXT.md#D-03]
Output: five colocated slices (Pips lives inside TierScale per architecture.md leaf-granularity), each a StateMatrix + Playground story, graduated into the barrel and design-review APPROVED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md
@.design/CLAUDE.md
@packages/design/src/styles/theme.css
@packages/design/src/shared/uikit/_fixtures/index.ts
@.planning/phases/02-uikit-structural-data-display-primitives/02-04-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

No runtime trust boundary — presentational fixture-only catalog. [src: 02-RESEARCH.md#Security-Domain]

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | stat values + RU+EN tier/threshold labels rendered from fixtures | mitigate | All copy/values via JSX auto-escaping; never `dangerouslySetInnerHTML`; static fixtures (V5). [src: 02-RESEARCH.md#Security-Domain] |
| T-02-02 | Tampering | Lucide trend icons + the Sparkline bars | mitigate | Icons via `lucide-react` components; the Sparkline is dependency-free DOM bars (no charting lib, no raw SVG strings) (V14, D-03). [src: 02-RESEARCH.md#Security-Domain] |
| — | Spoofing / Repudiation / Info-disclosure / DoS / Elevation | — | N/A | No auth/session/network/persistence/privilege this phase. [src: 02-RESEARCH.md#Security-Domain] |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StatTile + MiniStatGrid</name>
  <files>packages/design/src/shared/uikit/StatTile/index.ts, packages/design/src/shared/uikit/StatTile/StatTile.tsx, packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx, packages/design/src/shared/uikit/MiniStatGrid/index.ts, packages/design/src/shared/uikit/MiniStatGrid/MiniStatGrid.tsx, packages/design/src/shared/uikit/MiniStatGrid/MiniStatGrid.stories.tsx</files>
  <read_first>
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-03 StatTile recipe stat-tile, stat-xl tabular, signed delta + trend icon; MiniStatGrid even grid, metric priority Игры→Убийства→ТК→Смерти→K/D→Счёт; Typography roles stat/stat-xl)
    - DESIGN.md (recipe stat-tile L394-403; typography roles)
    - .design/CLAUDE.md (L90-95 metric priority + formulas; hero = Score+K/D, rest = even mini-grid)
    - .design/hifi/player.jsx (L234-266 hero tiles / mini-stat grid SEMANTICS only — rebuild, never import, D-11)
    - packages/design/src/shared/uikit/_fixtures/index.ts (ROSTER, scoreOf, kdOf — render Vasiliy #1 hero values)
    - packages/design/src/shared/uikit/_state-matrix/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (tabular-nums, no arbitrary values, tv())
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (never color-alone; reserved dims)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md (named fn + typed Props; reserve dimensions; Lucide locals)
  </read_first>
  <behavior>
    - StatTile renders a `label` (text-muted) + a `stat-xl` tabular-mono value (text-primary) + an optional signed delta (`+12`/`−3`) colored win/loss paired with `trending-up`/`trending-down` — never color-alone; recipe `stat-tile` (surface-1 + border-1 + rounded-md); reserved dimensions (CLS = 0).
    - Score + K/D are the two hero tiles side-by-side (naturally-equal — allowed).
    - MiniStatGrid renders an even column grid (no orphan tile) following metric priority (Игры → Убийства → ТК → Смерти → K/D → Счёт), each value tabular mono; reflows column count by container width.
    - Stories: StatTile Matrix (×7 states where interactive; otherwise display states + loading skeleton dims) + the two hero tiles; MiniStatGrid Matrix (few/many data-volume + empty reserved-height placeholder) + Playground; values from the roster (Vasiliy #1).
  </behavior>
  <action>
    Create the `StatTile` slice (recipe `stat-tile` L394) — `label` + `stat-xl` tabular-mono value + signed delta colored win/loss with `trending-up`/`trending-down` Lucide icon (never color-alone, a11y.md); reserve the tile dims (CLS = 0; loading = Skeleton tile variant from KIT-07). Create the `MiniStatGrid` slice — an even grid (no orphan tiles — keep the count even for the column count) in metric-priority order, each value tabular mono, reflowing by container width [src: 02-UI-SPEC.md#KIT-03]. Pull hero Score/K-D values + labels from `_fixtures` (ROSTER + `scoreOf`/`kdOf`, Vasiliy #1) and copy from `_fixtures/STRINGS` (RU + EN). Rebuild from `.design/hifi/player.jsx` SEMANTICS — never import (D-11). Stories: StateMatrix + Playground. Keep class strings literal [src: 01-04-SUMMARY.md#deviations].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts -g "stattile|ministatgrid"</automated>
  </verify>
  <acceptance_criteria>
    - StatTile renders the hero value in tabular-mono `stat-xl` with a signed delta + trend icon (never color-alone); axe no serious/critical.
    - MiniStatGrid has an even tile count (no orphan) in metric-priority order; every value tabular mono.
    - Hero Score/K-D values match the `_fixtures` roster (Vasiliy #1) — story renders the canonical numbers.
    - No arbitrary Tailwind values / raw hex (grep returns 0); RU + EN labels render.
  </acceptance_criteria>
  <done>StatTile (hero, tabular, signed delta + icon) and MiniStatGrid (even, metric priority) are catalogued and axe-clean, sourced from the single fixture roster.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TierChip + TierScale/Pips + Sparkline (dependency-free)</name>
  <files>packages/design/src/shared/uikit/TierChip/index.ts, packages/design/src/shared/uikit/TierChip/TierChip.tsx, packages/design/src/shared/uikit/TierChip/TierChip.stories.tsx, packages/design/src/shared/uikit/TierScale/index.ts, packages/design/src/shared/uikit/TierScale/TierScale.tsx, packages/design/src/shared/uikit/TierScale/TierScale.stories.tsx, packages/design/src/shared/uikit/Sparkline/index.ts, packages/design/src/shared/uikit/Sparkline/Sparkline.tsx, packages/design/src/shared/uikit/Sparkline/Sparkline.stories.tsx</files>
  <read_first>
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-03 Tiers D-04 population-derived, level name + entry threshold ≥2.4 ХОРОШО, never color-alone; Sparkline D-03 dependency-free, aria-hidden + figure summary, prefers-reduced-motion, fixed height)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (Pattern 4 Sparkline DOM bars + figure/sr-only summary + motion-reduce; D-04 baseline passed explicitly)
    - packages/design/src/shared/uikit/_fixtures/tiers.ts (tierFor, SS_BASELINE — call with baseline passed explicitly)
    - packages/design/src/styles/theme.css (L52-57 --color-chart-* + grid-line for the Sparkline; tier color tokens)
    - .design/hifi/players.jsx (L18-40 pips/zones/bars SEMANTICS only — rebuild, never import, D-11)
    - .design/CLAUDE.md (L97-101 population tiers, Score reaches отлично, K/D tops at хорошо)
    - packages/design/src/shared/uikit/_state-matrix/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (chart figure + hidden text summary; never color-alone; reduced-motion)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (fixed wrapper height → CLS 0; animate transform/opacity only)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (no arbitrary values; computed dynamic dimension via inline style is the sanctioned escape; bar fill must be a token class)
  </read_first>
  <behavior>
    - TierChip calls `tierFor(metric, value, baseline)` (baseline passed explicitly) and renders the level name (ниже/норма/хорошо/отлично) + its entry threshold (e.g. `≥2.4 ХОРОШО`), tier color paired with the level word — never color-alone.
    - TierScale renders the zone scale; Pips (a discrete 4-level indicator) lives inside TierScale (leaf-granularity per architecture.md — exported via TierScale's index if reused). Selected/active zone is the player's tier.
    - Sparkline renders DOM bars (`<i>`/`<span>`) inside a fixed-height row, each bar's height a computed `%` via inline style, the bar FILL a token class (`bg-chart-1`/`tier-*`); the chart wrapper is `aria-hidden` with a `<figcaption className="sr-only">` value summary; grow motion behind `motion-reduce:`; fixed height → CLS = 0.
    - Stories: TierChip/TierScale Matrix (all four levels + threshold labels) + Playground; Sparkline Matrix (empty flat baseline / few points / many points / clamped range data-volume ×4) + Playground; tier values from the roster.
  </behavior>
  <action>
    Create the `TierChip` slice calling `tierFor(metric, value, baseline)` with `baseline` passed explicitly (D-04 — the fn is already Vitest-proven pure) and rendering the level name + entry threshold (`≥2.4 ХОРОШО`), tier-derived color paired with the level word (never color-alone, a11y.md). Create the `TierScale` slice (the zone scale + the `Pips` discrete 4-level indicator nested inside it per architecture.md leaf-granularity — D-02 is the intended shape, not a rigid file count; export Pips via TierScale's index only if a sibling needs it). Create the `Sparkline` slice (D-03, dependency-free): DOM bars in a fixed-height row, bar height a computed `%` via inline `style` (the sanctioned dynamic-dimension escape — NOT an arbitrary themable value), bar FILL a token class (`bg-chart-1`/`tier-*`, must be literal so the scan emits it); wrapper `aria-hidden="true"` + a `<figcaption className="sr-only">` carrying the value summary (the Wave-0 sr-only utility); grow motion behind `motion-reduce:`; fixed wrapper height → CLS = 0 [src: 02-RESEARCH.md#Pattern-4]. NO recharts/visx/d3.

    Pull tier metric values from the `_fixtures` roster (Vasiliy #1; Score reaches отлично, K/D tops at хорошо) and labels from `_fixtures/STRINGS` (RU + EN). Rebuild from `.design/hifi/players.jsx` SEMANTICS — never import (D-11). Stories: StateMatrix + Playground.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts tests/cls.spec.ts -g "tierchip|tierscale|sparkline"</automated>
  </verify>
  <acceptance_criteria>
    - TierChip renders the level name + entry threshold (`≥2.4 ХОРОШО`) with the tier color paired with the word (never color-alone); `tierFor` is called with `baseline` passed explicitly (grep confirms `tierFor(` with a baseline arg).
    - Sparkline: no charting dependency imported (grep the slice for recharts/visx/d3 returns 0); the chart wrapper is `aria-hidden` + has a `sr-only` `<figcaption>` summary; bar fill is a token class (no inline hex); the Sparkline `boundingBox().height` is fixed across data-volume states (CLS = 0).
    - Sparkline grow motion is gated behind `motion-reduce:` (grep the component).
    - All families pass axe (no serious/critical) + 44px geometry; RU + EN labels render.
  </acceptance_criteria>
  <done>TierChip/TierScale/Pips (population-derived, baseline explicit, never color-alone) and Sparkline (dependency-free, aria-hidden + sr-only summary, CLS = 0, reduced-motion) are catalogued and axe-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Graduate the KIT-03 family into the barrel + per-family design-review</name>
  <files>packages/design/src/index.ts</files>
  <read_first>
    - packages/design/src/index.ts (barrel after KIT-04/07/01 graduations — append the KIT-03 block to a distinct region)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-03 full contract)
    - .agents/skills/solidstats-frontend-react-design-review/SKILL.md (seven pillars)
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (Pillar 6 tier coloring + tabular mono + never-color-alone + mock-numbers-obey-formulas; Pillar 2 CLS=0; Pillar 3 charts figure+summary)
    - .agents/skills/solidstats-shared-review-standards/SKILL.md (report format + verdict)
  </read_first>
  <action>
    Append named re-exports of the KIT-03 slices (`StatTile`, `MiniStatGrid`, `TierChip`, `TierScale`, `Sparkline`; `Pips` only if exported standalone) to `packages/design/src/index.ts` in a distinct block below the prior families (append-only). Keep Smoke + `_fixtures` + `_state-matrix` absent.

    Run the per-family design-review (`solidstats-frontend-react-design-review` seven pillars + checklist) against the KIT-03 stories at the project breakpoints. Confirm: tabular mono + right-aligned numerals, tier color paired with the level word (never color-alone), the Sparkline figure + hidden summary + CLS = 0, mock numbers obey the Score/K-D formulas and never outrank the real leaders (Vasiliy #1), RU + EN natural (threshold labels not clipped at 360px). Fix the CLASS of any finding; map into the shared-review-standards report format.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `import { StatTile, MiniStatGrid, TierChip, TierScale, Sparkline } from "@solid-stats/design"` resolves (barrel grep confirms the names alongside the prior families).
    - The full `playwright test` suite is green for KIT-03 (axe + 44px + CLS = 0 on StatTile/Sparkline).
    - `pnpm check` (root) exits 0.
    - `solidstats-frontend-react-design-review` returns APPROVE for the KIT-03 family.
  </acceptance_criteria>
  <done>The KIT-03 stat-primitive family is importable from @solid-stats/design and passes design-review.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @solid-stats/design exec playwright test` green for KIT-03 (axe = 0, 44px, Sparkline/StatTile CLS = 0).
- Barrel exports the KIT-03 slices plus all prior families; helpers/Smoke absent.
- Tier values + Score/K-D internally consistent (Vasiliy #1) from the single fixture source.
- `solidstats-frontend-react-design-review` APPROVE; root `pnpm check` exits 0.
</verification>

<success_criteria>
The KIT-03 stat-primitive family (StatTile, MiniStatGrid, TierChip, TierScale, Pips, Sparkline) is catalogued as colocated Ladle stories: hero tiles in tabular-mono with signed delta + trend icon, even mini-grid, population-derived tiers (baseline explicit, level name + threshold, never color-alone), a dependency-free CLS=0 Sparkline with an aria-hidden chart + sr-only summary honoring reduced-motion, all numbers internally consistent from the single fixture roster (Vasiliy #1), axe-clean / ≥44px, RU + EN, graduated into the barrel and design-review APPROVED.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created this phase):
- Components: `StatTile`, `MiniStatGrid`, `TierChip`, `TierScale`, `Pips` (nested in TierScale), `Sparkline`.
- Barrel exports: the KIT-03 names appended to `packages/design/src/index.ts`.
- Stories: one colocated `*.stories.tsx` per slice (Matrix + Playground).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-05-SUMMARY.md` when done
</output>
