---
phase: 02-uikit-structural-data-display-primitives
plan: 10
type: execute
wave: 4
depends_on: ["02-09"]
gap_closure: true
autonomous: true
requirements: [KIT-02, QUAL-02, QUAL-03]
files_modified:
  - packages/design/src/shared/uikit/Table/TableRow.tsx
  - packages/design/src/shared/uikit/Table/Table.stories.tsx
  - packages/design/src/shared/uikit/CompactRow/CompactRow.tsx
  - packages/design/src/shared/uikit/CompactRow/CompactRow.stories.tsx
  - packages/design/tests/keyboard.spec.ts

must_haves:
  truths:
    - "GAP-09: the SELECTED row no longer breaks the table-fixed column layout — the left-edge marker is an inset box-shadow on the row (box-shadow: inset 2px 0 0 var(--color-primary)) instead of a positioned <tr> + ::before; columns stay aligned with the colgroup"
    - "GAP-10: the FOCUSED row state is visibly distinct from ENABLED — a real focus-within row treatment (surface lift + the focus ring not obscured by the sticky header, WCAG 2.4.12), and the forced 'focused' catalog state maps to the SAME utilities"
    - "GAP-12: CompactRow/DataVolumes renders as full-width labelled sections at a real mobile width (≤384px column) — rows actually render, not empty tall boxes in a narrow StateMatrix cell"
    - "GAP-13: the inline name in CompactRow drops min-h-11 so name + squad stack tightly; the ≥44px hit area comes from the ROW (the anchor already stretches via after:inset-0)"
  artifacts:
    - path: "packages/design/src/shared/uikit/Table/TableRow.tsx"
      provides: "Selected via inset box-shadow (no positioned <tr>) + real focus-within row treatment"
      contains: "shadow"
    - path: "packages/design/src/shared/uikit/CompactRow/CompactRow.tsx"
      provides: "Tight name+squad stack (min-h-11 off the inline name; hit area on the row)"
  key_links:
    - from: "packages/design/tests/keyboard.spec.ts"
      to: "packages/design/src/shared/uikit/Table/TableRow.tsx"
      via: "keyboard.spec asserts the selected row carries aria-selected (kept) + columns stay aligned"
      pattern: "aria-selected"
---

<objective>
KIT-02 data-table row + compact visuals — close GAP-09, GAP-10, GAP-12, GAP-13. The
shipped TableRow (02-06) gives the selected row a `position:relative` `<tr>` + an
absolutely-positioned `before:` cyan bar, which breaks the `table-fixed` column layout
(only the selected row shifts/clips); the focused row state is pixel-identical to
enabled (no real `focus-within` treatment despite the comment claiming one); the
CompactRow/DataVolumes story renders empty tall boxes (the wide row crammed into a
narrow StateMatrix cell); and the CompactRow name carries `min-h-11` which pushes the
squad ~44px down.

Honor D-01 / D-11: re-implement with project tokens, diff against the hi-fi semantics,
no arbitrary values, keep the three redundant selected signals + the full-row anchor.

Purpose: GAP-09/10/12/13 closure (KIT-02 + QUAL-02 + QUAL-03).
Output: a selected-row inset box-shadow marker (columns aligned); a real focus-within
row treatment; a full-width CompactRow/DataVolumes story; a tight name+squad stack.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-CONTEXT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-06-SUMMARY.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-09-SUMMARY.md

# The BINDING data-table semantic/layout reference (D-11 — diff, never port).
@.design/hifi/players.jsx
@.design/CLAUDE.md
@DESIGN.md

# Conventions the executor MUST follow.
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/state.md
@.agents/skills/solidstats-frontend-react-design-review/references/checklist.md
</context>

<artifacts_this_phase_produces>
**Changed internals (no public prop changes):**
- `TableRow` selected marker → an inset box-shadow on the row (or first cell), `before:` + `relative` dropped from the `<tr>`. The three redundant signals (primary-weak fill + edge marker + aria-selected) are KEPT.
- `TableRow` gains a real `focus-within:` row treatment; the forced `focused` catalog state maps to the same utilities.
- `CompactRow` inline name drops `min-h-11` (the ≥44px hit area stays on the row via the anchor's `after:inset-0` stretch).
- `Table.stories.tsx` / `CompactRow.stories.tsx`: the CompactRow DataVolumes states render as full-width labelled sections at a ≤384px column.

**No barrel changes.** **No new STRINGS.**
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GAP-09 — selected-row inset box-shadow (columns aligned, no positioned <tr>)</name>
  <files>packages/design/src/shared/uikit/Table/TableRow.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/Table/TableRow.tsx L64-86 (the `row` tv() recipe: the `selected` variant gives the `<tr>` `before:absolute before:... before:bg-primary` + the base carries `relative` — the GAP-09 root cause: an abspos child in a `<tr>` breaks `table-fixed` widths on only the selected row).
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md GAP-09 (the diagnosed fix: render the marker WITHOUT positioning the `<tr>` — use `box-shadow: inset 2px 0 0 var(--color-primary)` on the row/first cell; drop `relative` + `before:`; keep the three signals).
    - packages/design/src/styles/theme.css (the `--color-primary` token the inset shadow reads — the sanctioned CSS-var escape, like `--shadow-ring`).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (never color-alone — keep aria-selected + the primary-weak fill).
  </read_first>
  <action>
    GAP-09: replace the selected-row left-edge marker. Drop `relative` from the row base and the `before:absolute … before:bg-primary` bar from the `selected` variant. Render the marker as an inset box-shadow on the row (or the first cell): `box-shadow: inset 2px 0 0 var(--color-primary)` — read the `--color-primary` @theme var (the sanctioned escape; NOT a raw hex, NOT an arbitrary Tailwind value). Keep the `primary-weak` fill and `aria-selected` (the three redundant signals — never fill-only). Because the marker no longer positions the `<tr>`, the `table-fixed` colgroup widths hold on the selected row.

    Verify columns stay aligned with the colgroup on the selected row (the Счёт/K-D columns no longer clip). Cite GAP-09 in the recipe comment and remove the now-false "positioned before bar" wording.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test catalog.spec.ts keyboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'before:absolute' packages/design/src/shared/uikit/Table/TableRow.tsx` returns ZERO in the selected variant (the positioned bar is gone); the selected marker is an inset box-shadow reading `var(--color-primary)`.
    - The selected row keeps `aria-selected` + the `bg-primary-weak` fill (never fill-only) — keyboard.spec `tr[aria-selected="true"]` still passes.
    - The selected row's cells stay aligned with the colgroup — assert via a Playwright check that the selected row's cell x-positions match a non-selected row's (no rightward shift / no clipped trailing column).
    - `grep -rE 'bg-\[|p-\[|text-\[|#[0-9A-Fa-f]{6}' packages/design/src/shared/uikit/Table/TableRow.tsx` — ZERO (the inset shadow uses `var(--color-primary)`, not a raw hex / arbitrary value).
  </acceptance_criteria>
  <done>The selected row's left-edge marker is an inset box-shadow (no positioned <tr>); columns stay aligned with the colgroup; the three redundant selected signals are kept.</done>
</task>

<task type="auto">
  <name>Task 2: GAP-10 — a real focus-within row treatment</name>
  <files>packages/design/src/shared/uikit/Table/TableRow.tsx, packages/design/src/shared/uikit/Table/Table.stories.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/Table/TableRow.tsx L59-86 (the `row` recipe: `focused: ""` empty + the base has NO `focus-within:` utility despite the comment claiming a focus-within lift — the GAP-10 root cause).
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md GAP-10 (the diagnosed fix: add `focus-within:` row styling — surface lift + the focus ring not obscured by the sticky header, WCAG 2.4.12 — and map the forced `focused` catalog state to the SAME utilities; remove the misleading comment).
    - packages/design/src/shared/uikit/Table/Table.tsx L116 (the sticky `<thead>` — confirm the focus ring on a focused row is not hidden beneath it).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (visible focus; focus not obscured by sticky headers — 2.4.12).
  </read_first>
  <action>
    GAP-10: give the row a real focus treatment. Add a `focus-within:` row utility to the base (a surface lift, e.g. `focus-within:bg-surface-3` plus a visible cyan indication that is NOT obscured by the sticky header — WCAG 2.4.12; the anchor's own focus ring stays). Map the forced `focused` catalog state (`state: { focused: ... }`) to the SAME utilities so the static matrix cell visibly differs from `enabled`. Remove the misleading "focus-within lift" comment now that it is real. Keep the anchor focus ring on the name anchor (the keyboard-reachable affordance).

    Update Table.stories.tsx so the RowStates `focused` cell visibly differs from `enabled` (it renders the forced focus utilities).
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test catalog.spec.ts keyboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'focus-within:' packages/design/src/shared/uikit/Table/TableRow.tsx` matches (a real focus-within row treatment exists).
    - The forced `focused` state maps to the same utilities (the `state.focused` recipe entry is non-empty and equals the focus-within utilities) — the RowStates `focused` cell visibly differs from `enabled` (assert the rendered class/bg differs).
    - Live keyboard focus on a row's name anchor lifts the row (focus-within) and the ring is not hidden under the sticky header — assert via a Playwright focus + visibility/position check.
    - axe serious/critical = 0; the misleading comment is removed.
  </acceptance_criteria>
  <done>The focused row state is visibly distinct from enabled (real focus-within lift, ring not obscured by the sticky header); the forced catalog state maps to the same utilities.</done>
</task>

<task type="auto">
  <name>Task 3: GAP-12/13 — full-width CompactRow/DataVolumes + tight name+squad stack; design-review vs hi-fi</name>
  <files>packages/design/src/shared/uikit/CompactRow/CompactRow.tsx, packages/design/src/shared/uikit/CompactRow/CompactRow.stories.tsx, packages/design/tests/keyboard.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/CompactRow/CompactRow.tsx L54-65 (the name anchor carries `min-h-11` which inflates the name block + pushes the squad ~44px down — GAP-13; the hit area should come from the row, the anchor already stretches via `after:inset-0`).
    - packages/design/src/shared/uikit/CompactRow/CompactRow.stories.tsx (the DataVolumes story rendering the wide CompactList inside narrow StateMatrix cells — GAP-12: empty tall boxes, caption wraps to 3 lines).
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md GAP-12 + GAP-13 (the diagnosed fixes: render the data-volume states as full-width labelled sections at a ≤384px column — mirror the Table DataVolumes/RowStates full-width pattern the prior wave already used; drop `min-h-11` from the inline name; keep ≥44px hit area on the row).
    - .design/CLAUDE.md §13 (no nested scroll on mobile; top-N + show-more; drop secondary columns — the binding mobile model).
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (the seven-pillar diff-vs-hi-fi review; Pillar 5 the 360px floor).
  </read_first>
  <action>
    GAP-13: drop `min-h-11` from the CompactRow inline name anchor so name + squad stack tightly. Keep the ≥44px hit area on the ROW (the anchor's `after:inset-0` already stretches the hit area across the row; ensure the ROW itself is ≥44px tall via its padding/content, not by inflating the name). Tighten the row's vertical rhythm.

    GAP-12: rebuild the CompactRow DataVolumes story to render each data-volume state (few / many / limit / single) as a FULL-WIDTH labelled section at a real mobile width (a ≤384px column wrapper), NOT inside the shared StateMatrix grid — mirror the Table DataVolumes/RowStates full-width pattern (the same fix the prior wave applied to the row-states / MiniStatGrid). Keep the `data-compact-list` / `data-show-more` hooks the responsive.spec asserts.

    Final: run the `solidstats-frontend-react-design-review` seven pillars DIFFING the reworked TableRow + CompactRow against the binding `.design/hifi/players.jsx` (selected marker without breaking columns, real focus state, mobile compact-row stack, top-N + show-more). Record the hi-fi diff in the SUMMARY. Acceptance = APPROVE with the diff noted. Fix findings and re-run the gate.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && pnpm exec vitest run && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'min-h-11' packages/design/src/shared/uikit/CompactRow/CompactRow.tsx` does NOT appear on the inline name anchor (the name no longer carries the 44px floor); the row keeps the ≥44px hit area (assert the CompactRow box height ≥44px via catalog geometry while name+squad stack tightly).
    - The CompactRow/DataVolumes story renders actual rows in each data-volume cell at the ≤384px column (no empty tall boxes) — assert `[data-compact-row]` count > 0 in each labelled state cell; the caption does not wrap to 3 lines.
    - responsive.spec CompactRow 360px no-h-scroll / no-nested-scroll + show-more present — still green.
    - The design-review verdict is APPROVE with the `.design/hifi/players.jsx` diff recorded in the SUMMARY (selected marker / focus state / mobile stack all matched-or-justified).
    - `pnpm exec playwright test` + `pnpm exec vitest run` green; root `pnpm check` exit 0; 0 arbitrary values across TableRow + CompactRow.
  </acceptance_criteria>
  <done>The CompactRow name+squad stack tightly with the hit area on the row; the CompactRow/DataVolumes story renders full-width sections with real rows; the row + compact visuals pass the seven-pillar design-review diffed against the binding hi-fi, recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational data-table catalog; no untrusted input / data / network. Inherits the Phase-2 boundary set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | Dropping the positioned marker losing the never-color-alone selected signal | mitigate | The keyboard.spec aria-selected check + the design-review Pillar 3 (never color-alone: fill + edge + aria-selected) run as the gate; a fill-only selected state fails. |
| T-02-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (token-only CSS changes + story restructure). |
</threat_model>

<verification>
- `cd packages/design && pnpm exec ladle build` — green.
- `pnpm exec playwright test` — full matrix green (selected columns aligned; focus-within distinct; CompactRow DataVolumes renders rows; 360px no-h-scroll; axe serious/critical = 0; keyboard aria-selected intact).
- `pnpm exec vitest run` — green.
- Root `pnpm check` — exit 0.
- No-arbitrary-values grep across TableRow + CompactRow — ZERO.
- design-review APPROVE with the `.design/hifi/players.jsx` diff in the SUMMARY.
</verification>

<success_criteria>
- GAP-09 closed: selected row uses an inset box-shadow marker; columns stay aligned with the colgroup; three redundant signals kept.
- GAP-10 closed: focused row state visibly distinct from enabled (real focus-within, ring not obscured).
- GAP-12 closed: CompactRow/DataVolumes renders full-width sections with real rows at the mobile floor.
- GAP-13 closed: name+squad stack tightly; ≥44px hit area on the row.
- Design-review APPROVE diffed against the binding hi-fi; full gate green.
</success_criteria>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-10-SUMMARY.md` when done.
</output>
