---
phase: 02-uikit-structural-data-display-primitives
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/design/src/shared/uikit/FreshnessPill/index.ts
  - packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.tsx
  - packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.stories.tsx
  - packages/design/src/shared/uikit/ProvenanceLine/index.ts
  - packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.tsx
  - packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.stories.tsx
  - packages/design/src/shared/uikit/TrustBadge/index.ts
  - packages/design/src/shared/uikit/TrustBadge/TrustBadge.tsx
  - packages/design/src/shared/uikit/TrustBadge/TrustBadge.stories.tsx
  - packages/design/src/shared/uikit/DataTrustBanner/index.ts
  - packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx
  - packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.stories.tsx
  - packages/design/src/shared/uikit/InlineReviewRow/index.ts
  - packages/design/src/shared/uikit/InlineReviewRow/InlineReviewRow.tsx
  - packages/design/src/shared/uikit/InlineReviewRow/InlineReviewRow.stories.tsx
  - packages/design/src/index.ts
autonomous: true
requirements: [KIT-04, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "FreshnessPill renders the 4 states (Актуально / Данные устаревают / Связь потеряна / Переподключение) each with its Lucide icon + literal word — never color-alone (KIT-04 / QUAL-03)"
    - "ProvenanceLine renders `посчитано из N реплеев · <freshness> · Как считается` with a cyan focusable `Как считается` link (KIT-04)"
    - "Unknown is the literal amber word + circle-help icon — never 0 or — ; Conflict is amber + triangle-alert; Known is surface-2 + circle-check (KIT-04 / QUAL-03)"
    - "Stale/Offline/Reconnecting banners reserve their height (CLS = 0) and pair icon + text — never color-alone (KIT-04 / QUAL-04)"
    - "InlineReviewRow is a quiet amber `на проверке` inline footnote with a request link — never a filled banner (KIT-04)"
    - "Every data-trust story is axe-clean (serious/critical), keyboard-operable, with ≥44px interactive targets, and renders RU + EN from _fixtures/strings (QUAL-03 / QUAL-05)"
    - "Reconnecting spinner and any motion honor prefers-reduced-motion (QUAL-03)"
    - "The family graduates into the @solid-stats/design barrel (importable by Phase-4+ surfaces)"
  artifacts:
    - path: "packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.tsx"
      provides: "4-state freshness pill (model C), compound border token via inline style"
      contains: "tv("
    - path: "packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.tsx"
      provides: "Provenance line (model A) with cyan Как считается link"
      contains: "export function ProvenanceLine"
    - path: "packages/design/src/shared/uikit/TrustBadge/TrustBadge.tsx"
      provides: "Known/Unknown/Conflict badges — literal word + Lucide icon, amber for unknown/conflict"
      contains: "tv("
    - path: "packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx"
      provides: "Stale/Offline/Reconnecting banners, reserved height, icon+text"
      contains: "tv("
    - path: "packages/design/src/shared/uikit/InlineReviewRow/InlineReviewRow.tsx"
      provides: "Quiet amber на проверке inline footnote + request link"
      contains: "export function InlineReviewRow"
    - path: "packages/design/src/index.ts"
      provides: "Barrel graduating the KIT-04 data-trust family"
      contains: "FreshnessPill"
  key_links:
    - from: "packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.tsx"
      to: "packages/design/src/styles/theme.css"
      via: "consumes compound --color-freshness-*-border via inline style (sanctioned escape hatch)"
      pattern: "var\\(--color-freshness"
    - from: "packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.stories.tsx"
      to: "packages/design/src/shared/uikit/_state-matrix/index.ts"
      via: "lays out the 4 states + ×7 states in a StateMatrix"
      pattern: "StateMatrix|StateCell"
    - from: "packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.stories.tsx"
      to: "packages/design/src/shared/uikit/_fixtures/index.ts"
      via: "renders RU+EN copy + N from the fixtures string map"
      pattern: "STRINGS|from \"\\.\\.\\/_fixtures\""

premises:
  - claim: theme.css ships --color-freshness-{up-to-date,stale,offline,reconnecting}-{fill,text,border} + --color-{known,unknown,conflict}-{fill,text,border} + --color-provenance-{fg,link}, resolved today
    src: packages/design/src/styles/theme.css#L135-158
    verify: grep -nE 'color-(freshness|known|unknown|conflict|provenance)' packages/design/src/styles/theme.css
  - claim: the data-trust recipes (badge-freshness L330, provenance-line L406, badge-known/unknown/conflict L412-432, inline-review-row L433) are authored in DESIGN.md
    src: DESIGN.md#components
    verify: grep -nE '^\s+(badge-freshness|provenance-line|badge-known|badge-unknown|badge-conflict|inline-review-row):' DESIGN.md
  - claim: Smoke.stories.tsx is the direct precedent for the compound-border inline-style escape hatch on freshness tokens
    src: packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx#L73-85
    verify: sed -n '73,85p' packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx
  - claim: the only Unknown with backing today is commander_side_stats.unknown_outcomes; the per-player coverage panel "B" is NOT in the data model and is NOT built this phase
    src: 02-CONTEXT.md#D-05
    verify: sed -n '108,137p' .design/CLAUDE.md

carried_forward:
  - claim: compound *-border token is `1px solid …` — consume via inline style reading the custom property, NOT an arbitrary value; the reviewer/linter must not flag it
    src: 01-04-SUMMARY.md#key-decisions
  - claim: keep every class literal (tv() holds them), all stories/components under src/, never compute class names — else Tailwind v4 tree-shakes the data-state utility
    src: 01-04-SUMMARY.md#deviations
---

<objective>
The KIT-04 data-trust component family — the FIRST family vertical slice, chosen first because it is the EXACT in-repo analog (the Smoke story already proves the freshness-pill + compound-token escape hatch) and the lowest-risk markup with the highest token reuse, so it proves the Wave-0 harness works end-to-end on a real family (first real family story passing axe in the Playwright catalog gate). [src: 02-PATTERNS.md#KIT-04-data-trust] [src: mvp_mode_active]

Acceptance test for this slice: after it completes, a Phase-4+ surface-builder can import one reviewed family (`FreshnessPill`, `ProvenanceLine`, `TrustBadge`, `DataTrustBanner`, `InlineReviewRow`) from `@solid-stats/design` than before.

Purpose: data trust is a designed layer, not a badge — provenance, freshness, and honest Known/Unknown/Conflict/stale states are first-class, every value traceable, never color-alone. [src: solidstats-frontend-react-design#Non-negotiable-design-rules]
Output: five colocated slices (each `index.ts` + `*.tsx` + `*.stories.tsx`), each story a StateMatrix + Playground, all graduated into the barrel and passing axe/keyboard/44px + design-review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md
@.design/CLAUDE.md
@packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx
@packages/design/src/styles/theme.css
@.planning/phases/02-uikit-structural-data-display-primitives/02-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

No runtime trust boundary — presentational fixture-only catalog (no auth/network/input/persistence/secrets). [src: 02-RESEARCH.md#Security-Domain]

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | RU+EN strings + N rendered in pills/banners/provenance | mitigate | All copy via JSX auto-escaping; never `dangerouslySetInnerHTML`; strings are static literals from `_fixtures` (V5). [src: 02-RESEARCH.md#Security-Domain] |
| T-02-02 | Tampering | Lucide trust icons (circle-dot, wifi-off, refresh-cw, circle-help, triangle-alert, circle-check) | mitigate | Icons via `lucide-react` components, never raw SVG strings (V14). [src: 02-RESEARCH.md#Security-Domain] |
| — | Spoofing / Repudiation / Info-disclosure / DoS / Elevation | — | N/A | No auth/session/network/persistence/privilege this phase. [src: 02-RESEARCH.md#Security-Domain] |

V4 Access Control: role-aware enforcement is N/A (no RBAC this phase; the КС-surface unknown_outcomes that backs Unknown is a Phase-6 surface concern — Phase 2 ships only the visual badge).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FreshnessPill (model C) + ProvenanceLine (model A)</name>
  <files>packages/design/src/shared/uikit/FreshnessPill/index.ts, packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.tsx, packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.stories.tsx, packages/design/src/shared/uikit/ProvenanceLine/index.ts, packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.tsx, packages/design/src/shared/uikit/ProvenanceLine/ProvenanceLine.stories.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx (the EXACT freshness-pill precedent: FRESHNESS_STATES + compound-border inline style; tokens-only discipline)
    - packages/design/src/styles/theme.css (L135-158 the resolved freshness + provenance tokens to consume)
    - DESIGN.md (recipe `badge-freshness` L330-354 the 4 states + icons circle/circle-dot/wifi-off/refresh-cw; `provenance-line` L406-411 the `·` separator + cyan link)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-04 FreshnessPill + ProvenanceLine contracts; Copywriting Contract rows)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (Pattern 1 state-matrix+args; Pattern 3 compound-token inline style)
    - packages/design/src/shared/uikit/_fixtures/index.ts (STRINGS RU+EN map)
    - packages/design/src/shared/uikit/_state-matrix/index.ts (StateMatrix/StateCell)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (never color-alone; reduced-motion; figure/link semantics)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (no arbitrary values; tv(); the var() escape hatch)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md (named fn + typed Props; system props first; Lucide PascalCase locals; type="button")
  </read_first>
  <behavior>
    - FreshnessPill accepts a `state` prop of the 4-member union and renders `rounded-full` with the matching Lucide icon + the literal RU/EN word; reconnecting shows a spinning refresh-cw gated behind motion-reduce.
    - The compound `--color-freshness-<state>-border` is applied via inline style reading the custom property (Smoke precedent); fill/text likewise; no arbitrary Tailwind value.
    - ProvenanceLine renders `посчитано из N реплеев · <freshness> · Как считается` (or the EN mirror) with `·` separators, `text-muted` `2xs`, and a focusable cyan `Как считается` link (`type="button"` or `<a>`); the freshness segment embeds a FreshnessPill state label.
    - The FreshnessPill story exports a Matrix (all 4 states as StateCells) + a Playground (args/argTypes inline-radio over the 4 states); ProvenanceLine story shows the 4 freshness variants + the N-pluralization (RU one/few/many handled manually via the map) + RU and EN.
  </behavior>
  <action>
    Create the `FreshnessPill` slice: a named-function component with a typed `Props` (system props first, then `state`), a `tv()` recipe holding the literal token utilities, and the compound `--color-freshness-*` tokens consumed via inline `style` reading `var(--color-freshness-<state>-{fill,text,border})` (the sanctioned escape hatch, Smoke precedent — NOT an arbitrary value) [src: 01-04-SUMMARY.md#key-decisions]. Icons are `lucide-react` PascalCase locals (`circle`/`circle-dot`/`wifi-off`/`refresh-cw` per recipe L330), `aria-hidden` decorative with the literal word carrying meaning (never color-alone) [src: a11y.md]. Reconnecting motion behind `motion-reduce:` [src: design-system.md#Motion].

    Create the `ProvenanceLine` slice: a named-function component rendering the model-A recipe `provenance-line` (L406) — `text-muted` `2xs`, `·` separators, the embedded freshness segment, and a cyan focusable `Как считается` link (`--color-provenance-link`); copy + the `N реплеев` pluralization pulled from `_fixtures/STRINGS` (D-07 manual RU one/few/many). Do NOT build the per-player coverage panel "B" (not in the data model, D-05).

    Each story: `default` export `{ title: "KIT-04 Data-trust / FreshnessPill" }` (and ProvenanceLine), a `Matrix` story laying the states out via `StateMatrix`/`StateCell`, and a `Playground` with `args`/`argTypes`. Pull all copy from `_fixtures/STRINGS` — render both RU and EN. Keep all class strings literal inside `tv()` so the Tailwind v4 `@source ../src` scan emits them [src: 01-04-SUMMARY.md#deviations].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts -g "freshnesspill|provenanceline"</automated>
  </verify>
  <acceptance_criteria>
    - The FreshnessPill Matrix story renders all 4 states each with an icon + literal word; axe reports no serious/critical (`playwright test tests/catalog.spec.ts` for the freshness stories exits 0).
    - ProvenanceLine renders `посчитано из N реплеев · <freshness> · Как считается` with a keyboard-focusable cyan link (Tab reaches it; ≥44px hit area).
    - `grep -rE 'bg-\[|p-\[|text-\[|#[0-9A-Fa-f]{6}' packages/design/src/shared/uikit/FreshnessPill packages/design/src/shared/uikit/ProvenanceLine` returns no arbitrary value / raw hex (only `var(--color-*)` inline styles allowed).
    - Both stories render RU and EN from `_fixtures/STRINGS`.
  </acceptance_criteria>
  <done>FreshnessPill (4 states, never color-alone, reduced-motion) and ProvenanceLine (model A, cyan link) are catalogued and axe-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrustBadge (Known/Unknown/Conflict), DataTrustBanner (Stale/Offline/Reconnecting), InlineReviewRow</name>
  <files>packages/design/src/shared/uikit/TrustBadge/index.ts, packages/design/src/shared/uikit/TrustBadge/TrustBadge.tsx, packages/design/src/shared/uikit/TrustBadge/TrustBadge.stories.tsx, packages/design/src/shared/uikit/DataTrustBanner/index.ts, packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx, packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.stories.tsx, packages/design/src/shared/uikit/InlineReviewRow/index.ts, packages/design/src/shared/uikit/InlineReviewRow/InlineReviewRow.tsx, packages/design/src/shared/uikit/InlineReviewRow/InlineReviewRow.stories.tsx</files>
  <read_first>
    - DESIGN.md (recipes `badge-known` L412, `badge-unknown` L419, `badge-conflict` L426, `inline-review-row` L433; icons circle-check / circle-help / triangle-alert)
    - packages/design/src/styles/theme.css (L148-158 known/unknown/conflict/provenance tokens)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-04 badges + banners + InlineReviewRow contracts; Copywriting Contract: Unknown/Conflict/Known/banners/inline-review rows)
    - .design/CLAUDE.md (L108-137 the A/C-not-B model; Unknown = literal amber word, never 0/—; InlineReviewRow is a footnote not a banner)
    - packages/design/src/shared/uikit/FreshnessPill/FreshnessPill.tsx (the tv()+compound-token pattern just established — reuse it)
    - packages/design/src/shared/uikit/_state-matrix/index.ts and _fixtures/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (banners reserve space + live-region intent; never color-alone)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/errors.md (stale/offline/timeout data explicitly labeled)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (reserve final dimensions → CLS = 0)
  </read_first>
  <behavior>
    - TrustBadge accepts a `kind` of `known|unknown|conflict`; Unknown/Conflict are amber with `circle-help`/`triangle-alert` + the literal word (`Неизвестно`/`Конфликт`), never `0`/`—`; Known is `surface-2` + `circle-check` + `Известно`.
    - DataTrustBanner accepts `kind` of `stale|offline|reconnecting`; each reserves a fixed height (CLS = 0 — the empty/absent state still occupies the same box), pairs a Lucide icon + literal banner text; reconnecting motion behind motion-reduce.
    - InlineReviewRow renders a quiet amber `на проверке` row (transparent bg, `triangle-alert`, a request link) — visually a footnote inside a list, never a filled banner.
    - Each story is a StateMatrix of its variants + a Playground; CLS proof: a "reserved/empty" StateCell shows the box at the same height as the filled banner.
  </behavior>
  <action>
    Create the `TrustBadge` slice (a single component with a `kind` variant via `tv()`, recipes `badge-known`/`badge-unknown`/`badge-conflict` L412-432) — literal word + Lucide icon, amber for unknown/conflict, `rounded-xs` (badge, not pill); NEVER render `0`/`—` for Unknown [src: .design/CLAUDE.md#L108-137]. Create the `DataTrustBanner` slice (a `kind` variant: stale/offline/reconnecting) reserving a fixed height at every breakpoint (CLS = 0, performance.md), icon + text, reconnecting spinner behind `motion-reduce:`. Create the `InlineReviewRow` slice (recipe `inline-review-row` L433) — transparent-bg amber footnote + `на проверке` + a request link (`type="button"`/`<a>`, ≥44px touch hit area), explicitly NOT a filled banner and not pinned to the bottom of a stretched column [src: 02-UI-SPEC.md#KIT-04].

    Reuse the FreshnessPill `tv()` + compound-`var()` pattern from Task 1 — do not re-invent it. Pull all copy from `_fixtures/STRINGS` (RU + EN). Each story: a Matrix (variants + a reserved/empty cell for the banner CLS proof) + a Playground (`args`/`argTypes`).

    Do NOT build the per-player coverage/conflict panel "B" — it is not in the data model (deferred) [src: 02-CONTEXT.md#Deferred-Ideas].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts -g "trustbadge|datatrustbanner|inlinereviewrow"</automated>
  </verify>
  <acceptance_criteria>
    - TrustBadge Unknown renders the amber literal word `Неизвестно` + `circle-help` icon — grep confirms no `>0<` or `—` placeholder in the Unknown rendering.
    - DataTrustBanner: the reserved/empty StateCell `boundingBox().height` equals the filled banner's height (CLS = 0) — assert in the cls.spec or the catalog spec.
    - All three families pass axe (no serious/critical) and the 44px geometry check via `playwright test tests/catalog.spec.ts`.
    - No arbitrary Tailwind values / raw hex (grep returns 0; only `var(--color-*)` inline styles).
  </acceptance_criteria>
  <done>TrustBadge (never 0/—), DataTrustBanner (reserved height, CLS=0), and InlineReviewRow (footnote not banner) are catalogued and axe-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Graduate the KIT-04 family into the barrel + per-family design-review</name>
  <files>packages/design/src/index.ts</files>
  <read_first>
    - packages/design/src/index.ts (the current barrel — `export {}`; replace with named graduations, Smoke + _fixtures + _state-matrix stay absent)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-04 full contract — the design-review reference)
    - .agents/skills/solidstats-frontend-react-design-review/SKILL.md (the seven pillars — the gate this family must pass)
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (Pillar 6 data-trust items; Unknown = literal word never 0/—; Pillar 3 a11y; Pillar 5 responsiveness/360px)
    - .agents/skills/solidstats-shared-review-standards/SKILL.md (severity buckets + verdict — the report format the review uses)
  </read_first>
  <action>
    Replace the barrel `export {}` in `packages/design/src/index.ts` with named re-exports of the five KIT-04 slices via their `index.ts` entries (`FreshnessPill`, `ProvenanceLine`, `TrustBadge`, `DataTrustBanner`, `InlineReviewRow`) [src: 02-CONTEXT.md#Integration-Points]. Keep the Smoke story and the `_fixtures`/`_state-matrix` helpers absent from the barrel. Append-only this family's export block (later family plans append their own blocks to distinct regions — barrel writes are serialized by wave).

    Then run the per-family design-review: apply `solidstats-frontend-react-design-review` (the seven pillars + checklist) against the KIT-04 stories at the project breakpoints (esp. the 360px floor for the RU `Данные устаревают` clip check, checker rec #3) and map findings into the shared-review-standards report format. Fix the CLASS of any finding across the family, not the single line. RU clip / shorter-variant copy changes are routed back, not invented silently [src: 02-UI-SPEC.md#Checker-Recommendations].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `import { FreshnessPill, ProvenanceLine, TrustBadge, DataTrustBanner, InlineReviewRow } from "@solid-stats/design"` resolves (barrel grep confirms all five named exports).
    - The full `playwright test` suite (axe + 44px + keyboard + cls + responsive at 360px) is green for the KIT-04 stories.
    - `pnpm check` (root) exits 0.
    - `solidstats-frontend-react-design-review` returns APPROVE for the KIT-04 family (findings, if any, fixed at the class level; RU clip checked at 360px).
  </acceptance_criteria>
  <done>The KIT-04 data-trust family is importable from @solid-stats/design and passes design-review; the harness is proven end-to-end on the first real family.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @solid-stats/design exec playwright test` green for the KIT-04 stories (axe serious/critical = 0, 44px geometry, keyboard, CLS = 0 banners, 360px no-clip).
- Barrel exports all five KIT-04 slices; `_fixtures`/`_state-matrix`/Smoke absent.
- `solidstats-frontend-react-design-review` APPROVE for the family.
- Root `pnpm check` exits 0.
</verification>

<success_criteria>
The KIT-04 data-trust family (FreshnessPill, ProvenanceLine, TrustBadge, DataTrustBanner, InlineReviewRow) is catalogued as colocated Ladle stories, each demonstrating its states via StateMatrix, never color-alone, Unknown never 0/—, banners CLS = 0, RU + EN from the fixtures map, axe-clean / keyboard-operable / ≥44px, graduated into the barrel, and design-review APPROVED — proving the Wave-0 harness end-to-end on the first real family.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created this phase):
- Components: `FreshnessPill`, `ProvenanceLine`, `TrustBadge` (Known/Unknown/Conflict variants), `DataTrustBanner` (Stale/Offline/Reconnecting variants), `InlineReviewRow`.
- Barrel exports: the five names above graduated into `packages/design/src/index.ts`.
- Stories: one colocated `*.stories.tsx` per slice (Matrix + Playground).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-02-SUMMARY.md` when done
</output>
