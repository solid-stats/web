---
phase: 02-uikit-structural-data-display-primitives
plan: 11
type: execute
wave: 5
depends_on: ["02-09"]
gap_closure: true
autonomous: true
requirements: [KIT-03, KIT-07, QUAL-04, QUAL-05]
files_modified:
  - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx
  - packages/design/src/shared/uikit/Skeleton/Skeleton.stories.tsx
  - packages/design/src/shared/uikit/StatTile/StatTile.tsx
  - packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx
  - packages/design/src/shared/uikit/_fixtures/strings.ts
  - packages/design/tests/cls.spec.ts
  - packages/design/.ladle/tailwind.css

must_haves:
  truths:
    - "GAP-15: Skeleton has a SWEEP shimmer — a gradient ::after/overlay translated via transform: translateX(...) (transform-only → performance.md), motion-reduce → static; applied to all variants (text/tile/table), replacing the opacity-only pulse"
    - "GAP-16: a delta-bearing StatTile's loading skeleton reserves the delta row (a withDelta skeleton variant, or StatTile always reserves the delta line) so a delta tile is NOT taller than its skeleton → CLS = 0 on load"
    - "GAP-16: cls.spec asserts the delta-tile skeleton box height == the final delta-tile box height"
    - "GAP-18: outcome copy is unified to 'W'/'L' for BOTH ru and en (gaming shorthand); the intentional non-translation is documented for QUAL-05"
  artifacts:
    - path: "packages/design/src/shared/uikit/Skeleton/Skeleton.tsx"
      provides: "Sweep-shimmer (transform-only gradient overlay, motion-reduce static) on all variants"
      contains: "translate"
    - path: "packages/design/src/shared/uikit/_fixtures/strings.ts"
      provides: "outcomeWin/outcomeLoss unified to W/L in ru + en"
      contains: "outcomeWin"
  key_links:
    - from: "packages/design/tests/cls.spec.ts"
      to: "packages/design/src/shared/uikit/StatTile/StatTile.tsx"
      via: "cls.spec asserts delta-tile skeleton box == final delta-tile box (CLS = 0)"
      pattern: "delta"
---

<objective>
Stat / feedback / copy fixes — close GAP-15, GAP-16, GAP-18. The shipped Skeleton (02-03)
only pulses opacity, which is barely perceptible on the dark gunmetal (the hi-fi
`players.css` uses a sweep shimmer); a delta-bearing StatTile (02-05) is taller than its
loading skeleton (which reserves only label + value, no delta row) → CLS when delta tiles
load; and the Badge outcome copy is asymmetric in RU («П» vs «пор.») — the user wants the
W/L shorthand in both languages.

Honor D-11 (diff against the hi-fi sweep semantics, re-implement with project tokens) and
performance.md (transform-only animation, motion-reduce static). Honor D-07 (the RU+EN
placeholder map; W/L is an intentional non-translation documented for QUAL-05).

Purpose: GAP-15 (KIT-07 + QUAL-04), GAP-16 (KIT-03 + QUAL-04), GAP-18 (KIT-07 + QUAL-05).
Output: a sweep-shimmer Skeleton across all variants; a delta-row-reserving StatTile
skeleton + a cls.spec delta assertion; unified W/L outcome copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-CONTEXT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-09-SUMMARY.md

# The BINDING skeleton sweep-shimmer reference (D-11 — diff, never port).
@.design/hifi/players.css
@.design/hifi/kit.css
@DESIGN.md

# Conventions the executor MUST follow.
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md
@.agents/skills/solidstats-frontend-react-design-review/references/checklist.md
</context>

<artifacts_this_phase_produces>
**Changed internals (no public prop changes to Skeleton's text/table variants):**
- `Skeleton` shimmer → a transform-only sweep (gradient `::after`/overlay translated via `translateX`), `motion-reduce:` static, across text/tile/table variants.
- `Skeleton` `tile` variant gains a `withDelta` option (reserves the delta row) — or `StatTile` always reserves the delta line height. The chosen approach is documented.
- `StatTile` loading placeholder reserves the delta row for delta-bearing tiles.

**Changed copy:** `outcomeWin` / `outcomeLoss` in `_fixtures/strings.ts` → `{ ru: "W", en: "W" }` / `{ ru: "L", en: "L" }` (the documented intentional non-translation).

**New cls.spec assertion:** delta-tile skeleton box height == final delta-tile box height.

**Possible new sweep keyframe** in `.ladle/tailwind.css` (if the sweep needs a `@keyframes` not expressible as a stock Tailwind animation utility) — token-driven, transform-only.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GAP-15 — sweep-shimmer Skeleton (transform-only, motion-reduce static) across all variants</name>
  <files>packages/design/src/shared/uikit/Skeleton/Skeleton.tsx, packages/design/src/shared/uikit/Skeleton/Skeleton.stories.tsx, packages/design/.ladle/tailwind.css</files>
  <read_first>
    - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx L29-43, L71-78 (the current `shimmer = "... motion-safe:animate-pulse motion-reduce:animate-none"` opacity pulse — replace with a sweep across text/tile/table).
    - .design/hifi/players.css (the BINDING sweep: `.sk::after { animation: sk-sweep 1.25s ... } @keyframes sk-sweep { 100% { transform: translateX(100%) } }` + reduced-motion off — a moving shine bar; diff the semantics, re-implement with project tokens).
    - .design/hifi/kit.css (the shimmer/sweep recipe context).
    - packages/design/.ladle/tailwind.css (where a `@keyframes sk-sweep` + the utility would live if a stock Tailwind animation cannot express the transform sweep — token-driven, NOT an arbitrary value).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (animate transform/opacity ONLY; respect prefers-reduced-motion via `motion-reduce:`).
  </read_first>
  <action>
    GAP-15: replace the opacity pulse with a sweeping shine. Give the shimmer block a relative overflow-hidden container and an `::after`/overlay gradient bar translated via `transform: translateX(...)` (transform-only → performance.md compliant) on a loop, dropping to a STATIC block under `motion-reduce:`. Diff against the hi-fi `sk-sweep` keyframe semantics (re-implement, do not port). If a stock Tailwind animation utility cannot express the sweep, add a `@keyframes sk-sweep` + the matching utility class in `.ladle/tailwind.css` (token-driven; the gradient stops read surface tokens / `--color-*`, NOT raw hex / arbitrary values). Apply the sweep to ALL Skeleton variants (text / tile / table) — so the dull loading look on Table + StatTile is fixed too. Keep the reserved dimensions identical (the sweep is an overlay — it must not change any box height, CLS = 0).

    Update Skeleton.stories.tsx to show the sweep on each variant (and a motion-reduce note). Cite GAP-15 in the recipe comment.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test cls.spec.ts catalog.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'translateX\|translate-x\|sk-sweep' packages/design/src/shared/uikit/Skeleton/Skeleton.tsx packages/design/.ladle/tailwind.css` matches (a transform-based sweep exists); `grep -n 'animate-pulse' packages/design/src/shared/uikit/Skeleton/Skeleton.tsx` returns ZERO (the opacity pulse is replaced).
    - The sweep animates `transform` only (no width/height/top/left/margin animation) — verify the keyframe/utility animates transform; `motion-reduce:` drops it to a static block.
    - The Skeleton box dimensions are unchanged (the existing cls.spec skeleton-box == final-box equality still holds — the sweep is an overlay, CLS = 0).
    - `grep -rE 'bg-\[|#[0-9A-Fa-f]{6}' packages/design/src/shared/uikit/Skeleton/Skeleton.tsx packages/design/.ladle/tailwind.css` — ZERO raw hex / arbitrary values (gradient stops read tokens).
    - axe serious/critical = 0 on the Skeleton stories.
  </acceptance_criteria>
  <done>The Skeleton shimmers with a transform-only sweep across all variants, static under reduced motion, with unchanged reserved dimensions (CLS = 0).</done>
</task>

<task type="auto">
  <name>Task 2: GAP-16 — StatTile delta-row skeleton reserve + cls.spec delta assertion</name>
  <files>packages/design/src/shared/uikit/StatTile/StatTile.tsx, packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx, packages/design/src/shared/uikit/Skeleton/Skeleton.tsx, packages/design/tests/cls.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/StatTile/StatTile.tsx L54-75 (a tile WITH a `delta` renders label + value + a delta line; the loading placeholder must match its height).
    - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx L93-107 (the `tile` variant reserves only `label line + value block` — no delta row → a delta tile is taller than its skeleton, GAP-16).
    - packages/design/src/shared/uikit/StatTile/StatTile.stories.tsx (the loading / delta stories — add a delta-tile + its matching skeleton for the cls proof).
    - packages/design/tests/cls.spec.ts (the existing CLS proofs — ADD a delta-tile skeleton box == final delta-tile box height assertion).
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md GAP-16 (the diagnosed fix: reserve the delta row in the tile skeleton when the tile has a delta — a `withDelta` skeleton prop/variant — OR have StatTile always reserve the delta line height; add the cls assertion).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (skeleton reserves the FINAL dimensions; CLS = 0).
  </read_first>
  <action>
    GAP-16: make the delta-bearing StatTile's loading skeleton reserve the delta row. Choose (cite GAP-16): EITHER add a `withDelta` option to the Skeleton `tile` variant that reserves an extra delta-line shimmer matching the StatTile delta line height, OR have StatTile always reserve the delta line height (a fixed-height delta slot whether or not a delta is present) so the tile skeleton (label + value) already matches. Prefer the `withDelta` skeleton variant so a no-delta tile stays compact. Ensure the reserved delta-line height EXACTLY equals the StatTile delta line's rendered height (the `deltaTv` text-sm line + its gap).

    Add a StatTile cls proof story (a delta tile above its matching `withDelta` skeleton) and a cls.spec assertion: the delta-tile skeleton box height == the final delta-tile box height (CLS = 0 on load). Cite GAP-16 in the test comment.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test cls.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - cls.spec asserts the delta-tile skeleton box height == the final delta-tile box height (grep `delta` in cls.spec shows the new assertion) — green.
    - A delta-bearing StatTile and its loading skeleton have EQUAL box heights (no CLS on load) — the proof story passes.
    - A no-delta StatTile and the plain `tile` skeleton still match (no regression to the existing tile-variant equality).
    - `grep -rE 'h-\[|#[0-9A-Fa-f]{6}' packages/design/src/shared/uikit/{StatTile,Skeleton}/` — ZERO arbitrary heights / raw hex.
  </acceptance_criteria>
  <done>The delta-tile loading skeleton reserves the delta row so a delta tile is not taller than its skeleton (CLS = 0), proven by a cls.spec delta box-equality assertion.</done>
</task>

<task type="auto">
  <name>Task 3: GAP-18 — unify outcome copy to W/L in both languages; design-review vs hi-fi</name>
  <files>packages/design/src/shared/uikit/_fixtures/strings.ts, packages/design/src/shared/uikit/Badge/Badge.stories.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/_fixtures/strings.ts L69-70 (`outcomeWin = { ru: "П", en: "W" }`, `outcomeLoss = { ru: "пор.", en: "L" }` — the asymmetric RU pair to unify).
    - packages/design/src/shared/uikit/Badge/Badge.stories.tsx L15-16 (the badge-outcome story consuming `outcomeWin`/`outcomeLoss`).
    - DESIGN.md L295-303 (`badge-outcome-win` / `badge-outcome-loss` recipes — "pair with label W/L" — the W/L shorthand is the recipe's own intent).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md (RU+EN keyed strings; an intentional non-translation must be documented).
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (Pillar 6 RU+EN natural + the diff-vs-hi-fi review).
  </read_first>
  <action>
    GAP-18: unify the outcome labels to the W/L gaming shorthand in BOTH languages — `outcomeWin: { ru: "W", en: "W" }`, `outcomeLoss: { ru: "L", en: "L" }` (the DESIGN.md `badge-outcome-*` recipe already names "W/L"). Document the intentional non-translation in the strings.ts comment for QUAL-05 (the W/L shorthand is deliberately not localized — gaming shorthand, recipe-sanctioned), so the QUAL-05 parity intent stays explicit (parity is satisfied: both languages carry the same shorthand by design).

    Final: run the `solidstats-frontend-react-design-review` seven pillars on the Skeleton (sweep), StatTile (delta reserve), and Badge (W/L copy), DIFFING the sweep shimmer against the binding `.design/hifi/players.css` / `kit.css` and the badge copy against the DESIGN.md `badge-outcome-*` recipe. Record the hi-fi/recipe diff in the SUMMARY. Acceptance = APPROVE with the diff noted. Fix findings and re-run the gate.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && pnpm exec vitest run && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'outcomeWin\|outcomeLoss' packages/design/src/shared/uikit/_fixtures/strings.ts` shows `outcomeWin: { ru: "W", en: "W" }` and `outcomeLoss: { ru: "L", en: "L" }`; the strings.ts comment documents the intentional non-translation for QUAL-05.
    - The Badge outcome stories render W / L in both languages; the Vitest STRINGS parity proof still holds (`satisfies Record<string, Bilingual>`).
    - The design-review verdict is APPROVE with the `.design/hifi/players.css` sweep diff + the DESIGN.md `badge-outcome-*` copy diff recorded in the SUMMARY.
    - `pnpm exec playwright test` + `pnpm exec vitest run` green; root `pnpm check` exit 0; 0 arbitrary values across Skeleton / StatTile / Badge.
  </acceptance_criteria>
  <done>Outcome copy is unified to W/L in both languages with the intentional non-translation documented; the Skeleton sweep + StatTile delta reserve + Badge copy pass the seven-pillar design-review diffed against the binding hi-fi/recipe, recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational stat/feedback/copy catalog; no untrusted input / data / network. Inherits the Phase-2 boundary set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Tampering | The sweep overlay accidentally changing a reserved box height (CLS regression) | mitigate | cls.spec skeleton-box == final-box equality + the no-scrollbar check (from 02-09) re-run as the gate; a height change fails. |
| T-02-11-02 | Tampering | A non-transform animation slipping in (performance.md violation) | mitigate | The design-review Pillar 2 (transform/opacity-only) + the grep for `animate-pulse` removal run as the gate. |
| T-02-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (CSS keyframe + copy change only). |
</threat_model>

<verification>
- `cd packages/design && pnpm exec ladle build` — green.
- `pnpm exec playwright test` — full matrix green (Skeleton sweep transform-only; StatTile delta cls box-equality; Badge W/L; axe serious/critical = 0; the existing CLS proofs intact).
- `pnpm exec vitest run` — green (STRINGS parity).
- Root `pnpm check` — exit 0.
- `grep animate-pulse Skeleton.tsx` ZERO; no-arbitrary-values grep ZERO.
- design-review APPROVE with the `.design/hifi/players.css` + DESIGN.md recipe diff in the SUMMARY.
</verification>

<success_criteria>
- GAP-15 closed: a transform-only sweep shimmer across all Skeleton variants, static under reduced motion, dimensions unchanged.
- GAP-16 closed: the delta-tile loading skeleton reserves the delta row (CLS = 0); cls.spec asserts the delta box-equality.
- GAP-18 closed: outcome copy unified to W/L in both languages, intentional non-translation documented for QUAL-05.
- Design-review APPROVE diffed against the binding hi-fi/recipe; full gate green.
</success_criteria>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-11-SUMMARY.md` when done.
</output>
