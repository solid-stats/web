---
phase: 02-uikit-structural-data-display-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/package.json
  - packages/design/vitest.config.ts
  - packages/design/playwright.config.ts
  - packages/design/tests/globalSetup.ts
  - packages/design/tests/catalog.spec.ts
  - packages/design/src/shared/uikit/_fixtures/index.ts
  - packages/design/src/shared/uikit/_fixtures/tiers.ts
  - packages/design/src/shared/uikit/_fixtures/roster.ts
  - packages/design/src/shared/uikit/_fixtures/strings.ts
  - packages/design/src/shared/uikit/_fixtures/_fixtures.test.ts
  - packages/design/src/shared/uikit/_fixtures/tiers.test.ts
  - packages/design/src/shared/uikit/_state-matrix/index.ts
  - packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx
  - packages/design/.ladle/tailwind.css
  - package.json
autonomous: false
requirements: [QUAL-03, QUAL-04, QUAL-05, QUAL-06]
user_setup: []

must_haves:
  truths:
    - "Score/K-D formulas and population tiers are internally consistent; Vasiliy is index 0 (#1) in the canonical roster; no generated player outranks a real Overview leader (QUAL-06)"
    - "Tier level is population-derived from SS_BASELINE[period] with baseline passed explicitly; no module-global mutation; the level name carries its entry threshold (e.g. ≥2.4 ХОРОШО) (QUAL-06 / D-04)"
    - "Vitest runs green against the fixture module via `pnpm --filter @solid-stats/design test`"
    - "Playwright drives the built Ladle catalog one story at a time via meta.json + ?story=&mode=preview + [data-storyloaded] and the suite runs (axe + 44px on the Smoke story prove the harness end-to-end) (QUAL-03)"
    - "The RU+EN placeholder string map exports both languages for every Phase-2 copy element (QUAL-05)"
    - "A reusable StateMatrix/StateCell helper renders one labelled cell per state for every later story to consume uniformly (QUAL-01 enabler)"
  artifacts:
    - path: "packages/design/src/shared/uikit/_fixtures/index.ts"
      provides: "Single shared fixture module barrel: SS_BASELINE, roster, formulas, strings, tier fn"
      exports: ["SS_BASELINE", "ROSTER", "STRINGS", "tierFor", "scoreOf", "kdOf"]
    - path: "packages/design/src/shared/uikit/_fixtures/tiers.ts"
      provides: "Pure population-derived tier function (baseline passed explicitly)"
      contains: "export function tierFor"
    - path: "packages/design/src/shared/uikit/_fixtures/roster.ts"
      provides: "Canonical roster: 10 Overview players verbatim + deterministic tail; Vasiliy #1"
      contains: "export const ROSTER"
    - path: "packages/design/src/shared/uikit/_fixtures/strings.ts"
      provides: "Hardcoded bilingual RU+EN placeholder string map"
      contains: "export const STRINGS"
    - path: "packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx"
      provides: "StateMatrix grid + StateCell labelled-cell helper for stories"
      contains: "export function StateMatrix"
    - path: "packages/design/vitest.config.ts"
      provides: "Vitest wiring for the fixture/tier unit tests"
      contains: "defineConfig"
    - path: "packages/design/playwright.config.ts"
      provides: "Playwright wiring + webServer serving the built Ladle catalog"
      contains: "webServer"
    - path: "packages/design/tests/catalog.spec.ts"
      provides: "Per-story axe + 44px geometry harness iterating meta.json"
      contains: "AxeBuilder"
  key_links:
    - from: "packages/design/tests/catalog.spec.ts"
      to: "packages/design/tests/globalSetup.ts"
      via: "reads the meta.json story list produced by globalSetup (LADLE_META env or temp file)"
      pattern: "LADLE_META|globalSetup"
    - from: "packages/design/src/shared/uikit/_fixtures/_fixtures.test.ts"
      to: "packages/design/src/shared/uikit/_fixtures/index.ts"
      via: "imports the roster + formulas + tier fn and asserts consistency"
      pattern: "from \"\\.\\/index\"|from \"\\.\\.\\/_fixtures\""

premises:
  - claim: packages/design has @ladle/react 5.1.1 + tailwindcss 4.3.1 + @tailwindcss/vite 4.3.1 installed; no test tooling exists yet
    src: packages/design/package.json#devDependencies
    verify: cat packages/design/package.json
  - claim: .ladle/tailwind.css carries `@import` of theme.css + `@source ../src` — the only reason Tailwind v4 scans the package; new data-state utilities must reach this scan
    src: 01-04-SUMMARY.md#key-decisions
    verify: cat packages/design/.ladle/tailwind.css
  - claim: theme.css has no `sr-only` utility today (Sparkline accessible summary needs it; Tailwind v4 may or may not ship it)
    src: 02-RESEARCH.md#A3
    verify: grep -c 'sr-only' packages/design/src/styles/theme.css
  - claim: the data-trust @theme tokens (freshness/known/unknown/conflict/provenance) are resolved in theme.css L135-158 and available today
    src: packages/design/src/styles/theme.css#L135-158
    verify: grep -n 'color-freshness-up-to-date-fill' packages/design/src/styles/theme.css
  - claim: SS_BASELINE values and the Score/K-D formulas live in .design/CLAUDE.md (rebuild natively — hi-fi tiers.js is frozen, D-11)
    src: .design/CLAUDE.md#L94-101
    verify: sed -n '94,101p' .design/CLAUDE.md

carried_forward:
  - claim: Ladle sets Vite root to its bundled app dir in node_modules, so a class built by string concatenation or living outside src/ is tree-shaken — keep every class literal (tv() holds them) and all stories/components under src/
    src: 01-04-SUMMARY.md#deviations
  - claim: the compound data-trust `*-border` token is `1px solid …` — consume it via inline style reading the custom property (the sanctioned escape hatch, Smoke precedent), NOT an arbitrary value
    src: 01-04-SUMMARY.md#key-decisions
---

<objective>
Wave 0 enabler for the Phase-2 UIKIT catalog: install the test/runtime dev-deps, wire Vitest + Playwright-against-Ladle into `packages/design` (which has no test tooling today), and build the two shared modules every family slice depends on — the single canonical `_fixtures` module (D-06) and the `_state-matrix` story helper (RESEARCH Pattern 1). This is the thin walking-skeleton-style enabler; it is NOT a horizontal "foundation" dumping ground.

The first family slice (Plan 02, data-trust) proves the harness end-to-end. To make that real here, this plan wires the Playwright catalog spec and proves it runs against the existing Smoke story (axe + 44px geometry), and proves the Vitest fixture-consistency suite green — so by the end of Wave 0 the gate exists and demonstrably executes.

Purpose: every stat/table/tier/data-trust/feedback story imports its rows, baseline, formulas, and copy from one place (so "Vasiliy #1 everywhere" and the Score/K-D consistency hold across tiles, tables, and chips), and every story lays out its ×7 states + ×4 data-volume cells through one `StateMatrix` so the catalog is uniform and the Playwright spec has a predictable structure to assert against. [src: 02-CONTEXT.md#D-06] [src: 02-RESEARCH.md#Recommended-Project-Structure]
Output: dev-deps installed + pinned, `vitest.config.ts`, `playwright.config.ts`, `tests/globalSetup.ts`, `tests/catalog.spec.ts`, the `_fixtures` module (+ its two Vitest specs), the `_state-matrix` helper, and the `sr-only` base-layer utility confirmed/added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-CONTEXT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-VALIDATION.md
@.design/CLAUDE.md
@packages/design/package.json
@packages/design/.ladle/config.mjs
@packages/design/.ladle/tailwind.css
@packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → packages/design dev-deps | new packages enter the build at install time (the only untrusted surface this phase) |

No runtime trust boundary exists: Phase 2 is a fixture-only presentational catalog — no auth, no network, no user input, no persistence, no secrets. [src: 02-RESEARCH.md#Security-Domain]

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (`vitest`, `@playwright/test`, `@axe-core/playwright`, `tailwind-variants`, `lucide-react`) | mitigate | Package Legitimacy Gate: blocking human-verify checkpoint for the two not-yet-in-lockfile packages (`tailwind-variants`, `lucide-react`) before install; pin exact versions; the other three are first-tier (RESEARCH audit OK). No postinstall-network deps. [src: 02-RESEARCH.md#Package-Legitimacy-Audit] |
| T-02-01 | Tampering | fixture/i18n string rendering | mitigate | All copy rendered via JSX (auto-escaped); never `dangerouslySetInnerHTML`; the RU+EN map is static literals (V5). [src: 02-RESEARCH.md#Security-Domain] |
| T-02-02 | Tampering | icons | mitigate | Icons via `lucide-react` components, never raw SVG strings (V14). [src: 02-RESEARCH.md#Security-Domain] |
| — | Spoofing / Repudiation / Info-disclosure / DoS / Elevation | — | N/A | No auth, sessions, network, persistence, or privileged operations exist this phase. [src: 02-RESEARCH.md#Security-Domain] |

ASVS L2 applicable categories: V5 (input/output rendering — JSX escaping), V14 (config/dependencies — legitimacy + pinning). V2/V3/V4/V6 are N/A (no auth/session/access-control/crypto this phase).
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — confirm tailwind-variants + lucide-react before install</name>
  <files>(none — gate precedes the install in Task 2)</files>
  <what-built>Nothing yet — this checkpoint precedes the install. Three of the five new dev-deps (`vitest@4.1.9`, `@playwright/test@1.61.0`, `@axe-core/playwright@4.11.3`) are first-tier packages already verified in the RESEARCH legitimacy audit. The two NOT yet in the lockfile — `tailwind-variants` and `lucide-react` — must have their exact latest versions confirmed legitimate before they enter the build (T-02-SC). [src: 02-RESEARCH.md#Package-Legitimacy-Audit]</what-built>
  <read_first>
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (§ Package Legitimacy Audit, § Standard Stack installation block)
  </read_first>
  <action>
    BLOCKING human-verify gate (never auto-approvable — `workflow.auto_advance` is ignored for a legitimacy checkpoint). Before any install runs in Task 2, confirm the two packages not yet in the lockfile (T-02-SC): run `npm view tailwind-variants version` and `npm view lucide-react version` (or check npmjs.com), confirm the source repos are `github.com/heroui-inc/tailwind-variants` and `github.com/lucide-icons/lucide` (matching the RESEARCH audit), confirm neither carries a network-touching postinstall script, and report the two exact versions to pin. The other three dev-deps are first-tier and already audited OK. Do NOT proceed to Task 2 until approved.
  </action>
  <how-to-verify>
    1. Run `npm view tailwind-variants version` and `npm view lucide-react version` (or visit npmjs.com/package/tailwind-variants and npmjs.com/package/lucide-react) and confirm: source repo is `github.com/heroui-inc/tailwind-variants` and `github.com/lucide-icons/lucide` respectively, both actively maintained with substantial weekly downloads.
    2. Confirm neither has a network-touching postinstall script.
    3. Report the two exact versions to pin.
  </how-to-verify>
  <verify>Human confirms the two source repos match the RESEARCH audit and reports the two exact pinned versions.</verify>
  <acceptance_criteria>
    - The two exact versions are stated and confirmed legitimate (not [SLOP]/[SUS]); their source repos match the RESEARCH audit. This checkpoint is never auto-approvable.
  </acceptance_criteria>
  <done>Both packages confirmed legitimate (source repos match RESEARCH); the two exact versions to pin are recorded; Task 2 may proceed.</done>
  <resume-signal>Type "approved" with the two pinned versions, or describe issues.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install dev-deps + wire Vitest, Playwright, and the catalog harness; confirm sr-only</name>
  <files>packages/design/package.json, packages/design/vitest.config.ts, packages/design/playwright.config.ts, packages/design/tests/globalSetup.ts, packages/design/tests/catalog.spec.ts, packages/design/.ladle/tailwind.css, package.json</files>
  <read_first>
    - packages/design/package.json (the exports map + scripts to preserve — do NOT add a direct `vite` dep; Ladle owns the single bundled Vite)
    - packages/design/.ladle/tailwind.css (the `@source ../src` scan seam — add `sr-only` here in the base layer if theme.css lacks it; verify pattern from premise)
    - packages/design/.ladle/config.mjs (the story glob + dark-only addon config the harness serves)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (§ Code Examples — the Playwright per-story a11y+44px harness L356-404; § Validation Architecture full-suite command)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-VALIDATION.md (Test Infrastructure + Per-Task Verification Map)
    - .agents/skills/solidstats-frontend-react-tests/SKILL.md (the Vitest/Playwright runner split + the Ladle story-is-the-unit harness; no RTL render-and-assert-DOM)
    - .agents/skills/solidstats-shared-testing-standards/SKILL.md (RITE, AAA, determinism — no real sleep/wall-clock waiting)
  </read_first>
  <behavior>
    - Vitest config resolves `*.test.ts` under `packages/design/src` and runs in node/jsdom-free mode (pure logic only — no DOM render).
    - Playwright config's `webServer` serves the BUILT Ladle catalog (`ladle build` then a static serve, or `ladle preview`) at a fixed port with `baseURL` set; CI determinism preferred (Open Q1 → `ladle build` + serve). [src: 02-RESEARCH.md#Open-Questions]
    - `globalSetup.ts` reads the catalog `meta.json` (native fetch or fs after build) and exposes the story-key list to the spec (`LADLE_META` env or a temp file) — no `sync-fetch` dep unless unavoidable. [src: 02-RESEARCH.md#Standard-Stack]
    - `catalog.spec.ts` iterates every story key: navigates `?story=<key>&mode=preview`, waits for `[data-storyloaded]`, runs `AxeBuilder.withTags("wcag2a","wcag2aa","wcag22aa").analyze()` and blocks serious/critical; asserts every visible `a,button,[role=button],input` has `boundingBox()` ≥ 44×44. Proves itself against the existing `smoke--tokens` story.
  </behavior>
  <action>
    Install the five dev/runtime deps at the package: dev-deps `vitest`, `@playwright/test`, `@axe-core/playwright` (versions from RESEARCH); runtime/style deps `tailwind-variants`, `lucide-react` (pinned exact versions from Task 1). Run `pnpm --filter @solid-stats/design exec playwright install` for the browsers. Add `test` (Vitest run, watch off) and `test:e2e` (`ladle build` then `playwright test`) scripts to `packages/design/package.json`, preserving the existing `exports` map and `ladle`/`ladle:build` scripts; do NOT add a direct `vite` dependency [src: 01-04-SUMMARY.md#key-decisions].

    Author `vitest.config.ts` (defineConfig, include `src/**/*.test.ts`, node environment — pure logic only). Author `playwright.config.ts` per the RESEARCH sketch: `testDir: ./tests`, `webServer` building+serving the Ladle catalog at a fixed port, `use.baseURL`, and the CI project matrix (chromium + firefox + webkit + a 360px mobile viewport + reduced-motion + forced-colors where feasible) per the frontend-tests CI gate [src: solidstats-frontend-react-tests#CI-matrix]. Author `tests/globalSetup.ts` (read `meta.json` → story list) and `tests/catalog.spec.ts` (the per-story axe + 44px harness from RESEARCH L356-393), scoped to iterate `meta.json` so it stays in sync.

    Confirm `sr-only`: grep theme.css; if absent, add a `.sr-only` utility in the `@layer base` of `.ladle/tailwind.css` (the Sparkline accessible summary in Plan 05 needs it) [src: 02-RESEARCH.md#A3]. Compose the package `test`/`test:e2e` into the workspace check flow without breaking the existing root `pnpm check` (gen-theme → drift → design.md lint → vp check); keep `vp check packages scripts` scope intact [src: 01-05-SUMMARY.md#patterns-established].

    Do NOT touch `.ladle/config.mjs` story glob beyond confirming `addons.a11y.enabled` (dev aid, non-blocking) and do NOT touch the `@source ../src` line semantics — only append the `sr-only` rule if needed.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @solid-stats/design exec ladle build` exits 0 and emits `meta.json`.
    - `pnpm --filter @solid-stats/design exec playwright test tests/catalog.spec.ts` runs and the `smoke--tokens` story passes axe (no serious/critical) and the 44px geometry check — proving the harness end-to-end against a real story.
    - `package.json` retains the `exports` map (`./theme.css` + `.`) and the `ladle`/`ladle:build` scripts; no direct `vite` dep was added.
    - `grep -c 'sr-only' packages/design/.ladle/tailwind.css packages/design/src/styles/theme.css` returns ≥ 1 across the two (sr-only available).
    - Root `pnpm check` still exits 0.
  </acceptance_criteria>
  <done>The Vitest + Playwright-against-Ladle harness exists and demonstrably runs; the five deps are installed at pinned versions; sr-only is available; the workspace gate is unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The single canonical _fixtures module + _state-matrix helper (Vitest-proven)</name>
  <files>packages/design/src/shared/uikit/_fixtures/index.ts, packages/design/src/shared/uikit/_fixtures/tiers.ts, packages/design/src/shared/uikit/_fixtures/roster.ts, packages/design/src/shared/uikit/_fixtures/strings.ts, packages/design/src/shared/uikit/_fixtures/_fixtures.test.ts, packages/design/src/shared/uikit/_fixtures/tiers.test.ts, packages/design/src/shared/uikit/_state-matrix/index.ts, packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx</files>
  <read_first>
    - .design/CLAUDE.md (L94-101 formulas + SS_BASELINE; L169-174 roster "Vasiliy #1 everywhere"; L93/L97 metric priority)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (§ Fixtures & Domain Consistency; § Copywriting Contract — the RU+EN rows verbatim)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-PATTERNS.md (§ No Analog Found — tiers/roster rebuild rules; § Single canonical fixture source)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (Pattern 1 state-matrix + StateCell shape L163-198; Validation Architecture L483-484 the unit assertions)
    - packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx (the FRESHNESS_STATES literal shape + tokens-only discipline; the grid markup the StateMatrix mirrors)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md (shared/uikit slice rules; _fixtures/_state-matrix as generic helpers importing nothing page/business)
    - .agents/skills/solidstats-shared-ts-standards/SKILL.md (§B type over interface, no any/!/as; noUncheckedIndexedAccess; §F es-toolkit/type-fest before hand-rolling)
    - .agents/skills/solidstats-shared-testing-standards/SKILL.md (AAA, strong oracle, test.each tables, deterministic)
  </read_first>
  <behavior>
    - `scoreOf(p)` returns `(kills − TK) / (games + deathsFromTk)` and `kdOf(p)` returns `(kills − TK) / (deaths + deathsFromTk)` exactly per the canonical formulas; Vitest asserts these on known inputs.
    - `tierFor(metric, value, baseline)` derives `низкий|базовый|хороший|элитный` (low/base/good/elite) from `baseline.by[period][metric]` thresholds with baseline passed explicitly; Vitest asserts a value of 2.4 against the rotation score baseline returns `хороший` with entry threshold `≥2.4`, and asserts calling `tierFor` does not mutate the passed baseline object.
    - `ROSTER` head (first 10, sorted by Score desc) equals the 10 Overview players verbatim; Vitest asserts the head matches and `ROSTER[0]` is Vasiliy; asserts no generated tail player's Score exceeds any real-leader Score; the tail descends to negative Score (more TK than kills).
    - `STRINGS` exposes `{ ru, en }` for every Copywriting-Contract element; Vitest asserts both keys exist for each (parity), without asserting prose values are "correct" (that is design-review, not a unit test).
    - `StateMatrix`/`StateCell` render a labelled grid of children using tokens-only utilities (no arbitrary values); each `StateCell` carries its `label` text so the Playwright spec can assert named cells exist.
  </behavior>
  <action>
    Build the `_fixtures` module as four files re-exported through `index.ts`: `tiers.ts` (the `SS_BASELINE` model with `by.rotation` `{ score:{base:1.00,good:2.40,elite:4.00}, kd:{base:1.00,good:3.40,elite:6.80} }` and `by.alltime` `{ score:{base:1.00,good:3.00,elite:5.00}, kd:{base:1.00,good:5.00,elite:10.00} }` rebuilt natively from the frozen hi-fi semantics — NEVER import `.design/hifi/tiers.js`, D-11; the pure `tierFor(metric, value, baseline)` fn returning the level + entry threshold, baseline passed explicitly, no global mutation, D-04); `roster.ts` (the canonical roster: the 10 Overview players verbatim at the head + a deterministic generated tail down to negative Score, deaths scaling with games, `ROSTER[0]` = Vasiliy, no generated player outscoring a real leader); `strings.ts` (the hardcoded bilingual RU+EN map keyed per the Copywriting Contract — render-via-JSX literals, never a real i18n harness, D-07); `index.ts` re-exporting `SS_BASELINE`, `ROSTER`, `STRINGS`, `tierFor`, `scoreOf`, `kdOf`. Use `type` not `interface`, no `any`/`!`/`as`, and reach for `es-toolkit` before hand-rolling a sort/groupBy [src: solidstats-shared-ts-standards#F].

    Write `_fixtures.test.ts` (formula + roster + Vasiliy-#1 + parity assertions) and `tiers.test.ts` (population-derived level + entry threshold + no-mutation), using `test.each` tables where the assertion logic is identical [src: solidstats-shared-testing-standards#G]. These are the QUAL-06 consistency proofs.

    Build `_state-matrix/StateMatrix.tsx` (`StateMatrix` grid wrapper + `StateCell` with a visible `label`) + `index.ts` entry, tokens-only (mirror the Smoke grid utilities `flex`/`gap-*`/`bg-surface-*`/`border-border-1`/`rounded-*`/`text-*`), importing nothing page/business/localization [src: architecture.md#Uikit-vs-feature]. Do NOT graduate `_fixtures` or `_state-matrix` into the public barrel `src/index.ts` (underscore-prefixed internal helpers, like the Smoke story which is not a barrel export) [src: 02-PATTERNS.md#barrel].
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec vitest run src/shared/uikit/_fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @solid-stats/design test` (Vitest) exits 0; `_fixtures.test.ts` asserts `score = (kills−TK)/(games+dftk)`, `kd = (kills−TK)/(deaths+dftk)`, `ROSTER` head == the 10 Overview players, `ROSTER[0]` is Vasiliy (index 0), and no tail player outscores a real leader.
    - `tiers.test.ts` asserts a rotation score of 2.4 → `хороший` / threshold `≥2.4` and that `tierFor` does not mutate the passed baseline.
    - `STRINGS` has both `ru` and `en` for every Copywriting-Contract element (Vitest parity assertion).
    - `StateMatrix`/`StateCell` use only token utilities (no `bg-[`/`p-[`/`text-[` arbitrary values — grep returns 0); `_fixtures` and `_state-matrix` are absent from `src/index.ts`.
    - `pnpm check` (root) exits 0.
  </acceptance_criteria>
  <done>The single fixture source proves domain consistency in Vitest, and the StateMatrix helper is ready for every family story to consume.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @solid-stats/design test` (Vitest fixtures/tiers) exits 0.
- `pnpm --filter @solid-stats/design exec ladle build && pnpm --filter @solid-stats/design exec playwright test tests/catalog.spec.ts` runs; the Smoke story passes axe + 44px (harness proven end-to-end).
- Root `pnpm check` exits 0 (gate unbroken).
- No arbitrary Tailwind values in `_state-matrix`; `_fixtures`/`_state-matrix` not in the barrel.
</verification>

<success_criteria>
The Phase-2 harness exists and demonstrably executes (Vitest green; Playwright runs the catalog and the Smoke story passes axe + 44px); the single canonical `_fixtures` module proves Score/K-D + tier + Vasiliy-#1 consistency (QUAL-06); the RU+EN map covers every Phase-2 copy element (QUAL-05); the `StateMatrix` helper is ready for uniform ×7/×4 state demonstration (QUAL-01 enabler); the five dev/runtime deps are installed at pinned, legitimacy-confirmed versions.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (Wave 0 enablers — exclude from drift verification):
- Config/wiring: `packages/design/vitest.config.ts`, `packages/design/playwright.config.ts`, `packages/design/tests/globalSetup.ts`, `packages/design/tests/catalog.spec.ts`; `package.json` (root) check-flow touch; `packages/design/package.json` deps + `test`/`test:e2e` scripts; `.ladle/tailwind.css` `sr-only` rule.
- `_fixtures` module exports: `SS_BASELINE`, `ROSTER`, `STRINGS`, `tierFor`, `scoreOf`, `kdOf` (+ `_fixtures.test.ts`, `tiers.test.ts`).
- `_state-matrix` helper exports: `StateMatrix`, `StateCell`.
- New dev/runtime deps: `vitest`, `@playwright/test`, `@axe-core/playwright`, `tailwind-variants`, `lucide-react`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-01-SUMMARY.md` when done
</output>
