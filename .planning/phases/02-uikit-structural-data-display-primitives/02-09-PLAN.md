---
phase: 02-uikit-structural-data-display-primitives
plan: 09
type: execute
wave: 3
depends_on: ["02-07"]
gap_closure: true
autonomous: true
requirements: [KIT-02, QUAL-02, QUAL-04]
files_modified:
  - packages/design/src/shared/uikit/Table/Table.tsx
  - packages/design/src/shared/uikit/Table/Table.stories.tsx
  - packages/design/src/shared/uikit/Pagination/Pagination.tsx
  - packages/design/src/shared/uikit/Pagination/Pagination.stories.tsx
  - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx
  - packages/design/src/shared/uikit/DensityToggle/index.ts
  - packages/design/src/shared/uikit/DensityToggle/DensityToggle.tsx
  - packages/design/src/shared/uikit/DensityToggle/DensityToggle.stories.tsx
  - packages/design/src/shared/uikit/_fixtures/strings.ts
  - packages/design/tests/cls.spec.ts
  - packages/design/tests/responsive.spec.ts
  - packages/design/src/index.ts

must_haves:
  truths:
    - "GAP-06: the DensityToggle component + story + barrel export are DELETED; table density derives automatically from the @container (comfortable at ≥@md-class width, compact below); ROW_H 52/44 and the controlled density prop on Table are kept (the auto-resolver feeds it)"
    - "GAP-07: Pagination is a REAL pager — an 'N–M из total' range/page indicator, end-of-list = a DISABLED Next button (never a bare «Это всё» text), Prev disabled at the start; controlled props, no engine (D-01)"
    - "GAP-08: the reserved viewport height accounts for borders (border-box math) so header + N rows fit EXACTLY with no overflow, for BOTH the data table and the Skeleton table variant; the skeleton never scrolls"
    - "GAP-11: the Table/RowStates loading cell renders the actual loading Skeleton (shimmer placeholder), not real data"
    - "GAP-14: few vs limit-reached read differently — few = N of a larger set (caption shows N of total); limit-reached = an explicit end-of-list cue"
  artifacts:
    - path: "packages/design/src/shared/uikit/Pagination/Pagination.tsx"
      provides: "Real pager: N–M of total range + disabled Next at end-of-list"
      contains: "disabled"
    - path: "packages/design/src/shared/uikit/Table/Table.tsx"
      provides: "Border-box viewport height (no stray scroll) + auto-density resolver"
  key_links:
    - from: "packages/design/tests/cls.spec.ts"
      to: "packages/design/src/shared/uikit/Table/Table.tsx"
      via: "cls.spec asserts scrollHeight <= clientHeight on the data table AND the skeleton table variant (no scrollbar)"
      pattern: "scrollHeight"
    - from: "packages/design/src/index.ts"
      to: "packages/design/src/shared/uikit/DensityToggle"
      via: "the DensityToggle barrel export is REMOVED"
      pattern: "DensityToggle"
---

<objective>
KIT-02 data-table geometry + controls — close GAP-06, GAP-07, GAP-08, GAP-11, GAP-14.
The shipped table (02-06) diverged from the binding hi-fi `.design/hifi/players.jsx`:
density was an invented toggle (hi-fi derives it from screen); the pager shows neither
pages nor total and renders a bare «Это всё» text instead of a disabled control; every
table (and the skeleton) has a stray ~1–2px scrollbar from un-accounted borders; the
RowStates loading cell shows real data; and few vs limit-reached are indistinguishable.

Honor D-01 (no engine): density / pagination stay CONTROLLED props — no
`@tanstack/react-table`, no `@tanstack/react-virtual`. Honor D-11: diff against the
hi-fi for semantics, re-implement with project tokens (no arbitrary values).

Purpose: GAP-06/07/08/11/14 closure (KIT-02 + QUAL-02 + QUAL-04).
Output: DensityToggle removed + auto density; a real Pagination pager; border-box
viewport math (no stray scroll, skeleton never scrolls); the loading cell shows the
skeleton; distinct few/limit cues; cls.spec asserts no-scrollbar on both tables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-CONTEXT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-06-SUMMARY.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-07-SUMMARY.md

# The BINDING data-table semantic/layout reference (D-11 — diff, never port).
@.design/hifi/players.jsx
@.design/CLAUDE.md
@DESIGN.md

# Conventions the executor MUST follow.
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/state.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-design-review/references/checklist.md
</context>

<artifacts_this_phase_produces>
**Removed exports:** `DensityToggle` (GAP-06) — the component dir, story, and barrel export are deleted.
**Kept:** `ROW_H`, the `TableDensity` type, and the controlled `density` prop on `Table` (the auto-resolver feeds it).
**Changed contract:** `Pagination` props gain a `rangeLabel` / total model (`N–M из total`); the end-of-list renders a disabled Next, not a text marker.
**New STRINGS keys (RU+EN):** `paginationRange` (`{from}–{to} из {total}` / `{from}–{to} of {total}`); an end-of-list is now the disabled Next state (the `paginationEnd` text key is dropped from the pager UI — it MAY stay as a caption string if still used; otherwise remove it).
**New/changed cls.spec assertions:** `scrollHeight <= clientHeight` on the data table viewport AND the Skeleton table variant (no scrollbar), plus the existing box-equality.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GAP-08 — border-box viewport math (no stray scroll on table + skeleton); GAP-11 loading cell shows skeleton</name>
  <files>packages/design/src/shared/uikit/Table/Table.tsx, packages/design/src/shared/uikit/Skeleton/Skeleton.tsx, packages/design/src/shared/uikit/Table/Table.stories.tsx, packages/design/tests/cls.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Table/Table.tsx L76-103 (the `viewportStyle = HEADER_H + visibleRows*ROW_H` reservation that does NOT account for the per-row + thead `border-b` 1px each under `border-collapse` — the GAP-08 root cause).
    - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx L109-149 (the table-variant skeleton with its own bordered header + N body rows — overflows the same way; it must NEVER scroll).
    - packages/design/src/shared/uikit/Table/Table.stories.tsx (the RowStates story — the loading cell calls `dataTable(...)` WITHOUT the `loading` flag, GAP-11; fix it to pass `loading` or render the Skeleton table variant directly).
    - packages/design/tests/cls.spec.ts L63-91 (the Table CLS box-equality test — ADD a no-scrollbar assertion `scrollHeight <= clientHeight` on the data table viewport AND the skeleton table variant).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (CLS = 0; reserved space; no layout-property animation).
  </read_first>
  <action>
    GAP-08: make the reserved viewport height account for the borders so header + N rows fit EXACTLY with no overflow. Choose the border-box-correct approach (cite GAP-08): EITHER add the header + per-row `border-b` 1px total into the computed `viewportStyle` height, OR drop the inner borders from the height-bearing boxes (e.g. use a non-collapsing border model / `box-border` on the row boxes / move the row separators to a non-height-adding technique). Apply the SAME fix to the Skeleton table variant (its header + body rows) so the skeleton box stays equal to the data box AND never scrolls. Keep the colgroup widths + ROW_H geometry as computed inline-style values (the sanctioned escape — not themable tokens).

    GAP-11: fix the Table/RowStates story's loading cell to render the actual loading state — pass `loading` to the Table (or render `<Skeleton variant="table">` directly) so the catalog shows the shimmer placeholder, not the Vasiliy data row.

    Strengthen cls.spec.ts (cite GAP-08): keep the existing skeleton-box == data-box equality, and ADD `scrollHeight <= clientHeight` (no scrollbar) on BOTH the data-table viewport (`[data-table-viewport]`) and the Skeleton table variant container.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test cls.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - cls.spec asserts `scrollHeight <= clientHeight` on the data-table viewport AND the skeleton table variant (grep `scrollHeight` in cls.spec shows the new assertions) — green.
    - The existing skeleton-box == data-box height + width equality still holds (CLS = 0).
    - The Table/RowStates loading cell renders `[data-skeleton="table"]` (the shimmer), NOT a `[data-table-row]` data row (assert in the story / catalog).
    - `grep -rE 'bg-\[|p-\[|h-\[|w-\[|#[0-9A-Fa-f]{6}' packages/design/src/shared/uikit/{Table,Skeleton}/` — ZERO arbitrary values introduced (computed geometry stays inline-style).
  </acceptance_criteria>
  <done>The data table and the Skeleton table variant fit header + N rows with no stray scrollbar (cls.spec asserts no overflow on both), the CLS box-equality still holds, and the RowStates loading cell shows the skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: GAP-07/14 — a real Pagination pager + distinct few/limit cues</name>
  <files>packages/design/src/shared/uikit/Pagination/Pagination.tsx, packages/design/src/shared/uikit/Pagination/Pagination.stories.tsx, packages/design/src/shared/uikit/Table/Table.stories.tsx, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    - .design/hifi/players.jsx (the BINDING table semantics: desktop = capped virtualized scroll + total in the caption; mobile = show-more — the user decision is to KEEP a pager but make it real, option B, NOT the hi-fi no-pager; diff for the total-in-caption idea).
    - packages/design/src/shared/uikit/Pagination/Pagination.tsx (refactored onto Button in 02-07; now make it a real pager — add the range/total indicator + the disabled-Next end state).
    - packages/design/src/shared/uikit/_fixtures/strings.ts L62-67 (the existing `paginationPrev`/`paginationNext`/`paginationEnd` — add `paginationRange` `{from}–{to} из {total}`; the bare «Это всё» text marker is REPLACED by a disabled Next, so it is no longer rendered as a control).
    - packages/design/src/shared/uikit/Table/Table.stories.tsx (the DataVolumes few/limit cells — GAP-14: make them read differently via the caption total).
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UAT.md GAP-07 + GAP-14 (the exact target: N–M of total; disabled Next at end; Prev disabled at start; few = N of total in caption; limit = explicit end cue).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (disabled controls keep their label; never icon/color-alone; aria-disabled).
  </read_first>
  <action>
    GAP-07: rebuild Pagination as a REAL pager (controlled, no engine — D-01). Add a range/page indicator showing `N–M из total` (a `from`/`to`/`total` prop model + the `paginationRange` STRING). Render the end-of-list state as a DISABLED Next `<Button variant="secondary" disabled>` (keep the `data-page-next` hook on it; never a bare «Это всё» `<span>`). Prev is `<Button variant="secondary" disabled={!hasPrev}>` (disabled at the start). Keep the controlled `hasPrev`/`hasNext` props; add the `from`/`to`/`total` props. Surface the total here AND/OR in the table caption (the few/limit cue, see GAP-14). Update Pagination.stories.tsx to show start / middle / end-of-list states (Prev-disabled, both-enabled, Next-disabled) with the range indicator, RU+EN.

    GAP-14: make the data-volume states read differently in Table.stories.tsx — `few` = a few of a larger set (the caption / range shows `N из total` where total > N); `limit-reached` = an explicit "all N shown / end of list" cue (the Next button disabled + the range showing the full span). Fold the total into the caption (the `.design` total-in-caption model).
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test catalog.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-page-end' packages/design/src/shared/uikit/Pagination/Pagination.tsx` returns ZERO (the bare end-of-list `<span>` text marker is gone); the end state is a disabled Next `<button>` carrying `data-page-next` + `disabled`.
    - The Pagination renders a `N–M из total` range indicator (the `paginationRange` STRING is rendered; grep the story / component for the range model).
    - The DataVolumes `few` and `limit-reached` cells render distinct cues (few shows N of a larger total; limit shows the end-of-list / all-shown cue) — assert in the story.
    - axe serious/critical = 0 on the Pagination + Table stories; the disabled Next keeps its label + `aria-disabled` (never icon/color-alone).
    - `grep -rE 'bg-\[|p-\[|text-\[|#[0-9A-Fa-f]{6}' packages/design/src/shared/uikit/Pagination/` — ZERO arbitrary values.
  </acceptance_criteria>
  <done>Pagination is a real controlled pager — a range/total indicator + a disabled Next at end-of-list (no bare text marker) + Prev disabled at start; few vs limit-reached read differently via the caption total.</done>
</task>

<task type="auto">
  <name>Task 3: GAP-06 — drop DensityToggle, auto density from @container; design-review vs hi-fi</name>
  <files>packages/design/src/shared/uikit/DensityToggle/index.ts, packages/design/src/shared/uikit/DensityToggle/DensityToggle.tsx, packages/design/src/shared/uikit/DensityToggle/DensityToggle.stories.tsx, packages/design/src/shared/uikit/Table/Table.tsx, packages/design/src/shared/uikit/Table/Table.stories.tsx, packages/design/src/index.ts, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    - .design/hifi/players.jsx L318 (`const density = (device === 'desktop' && !winMobile) ? 'comfortable' : 'compact'` — the BINDING auto-density model: derived from device/screen, NO toggle).
    - packages/design/src/shared/uikit/DensityToggle/ (the whole slice — component + index.ts + story — to DELETE per GAP-06).
    - packages/design/src/shared/uikit/Table/Table.tsx (the controlled `density` prop — KEPT; an auto-resolver feeds it from the @container, the same way AppShell reflows).
    - packages/design/src/index.ts L72 (the `DensityToggle` barrel export — REMOVE it).
    - packages/design/src/shared/uikit/_fixtures/strings.ts L52-61 (the `densityToggle`/`densityGroup`/`densityComfortable`/`densityCompact` strings — the toggle-specific ones may be removed if no longer referenced; verify no other consumer).
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (prefer @container over viewport branching — the auto-density derives from the container, like the nav-shell reflow).
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (the seven-pillar diff-vs-hi-fi review).
  </read_first>
  <action>
    GAP-06: DELETE the DensityToggle slice (component, index.ts, story) and remove its barrel export from index.ts. Make table density derive automatically from the @container (comfortable at a desktop-class container width ≥ the table's `@md`-class breakpoint, compact below) — keyed off `@container` per styling.md, mirroring the hi-fi `density` derivation (diff against `players.jsx` L318). Keep `ROW_H` 52/44 and the controlled `density` prop on `Table` (the auto-resolver passes the resolved value in). Implement the resolver as a small `@container`-driven mechanism: either two reserved-height Table renders gated by `@container` utility visibility (the catalog-friendly, CLS-safe approach mirroring AppShell's `@md:block`/`@md:hidden`), or a documented container-width hook if CSS cannot express the row-height swap — prefer the CSS/@container approach. Remove the toggle-only STRINGS keys if now unreferenced (verify by grep first). Update Table.stories.tsx to demonstrate auto density (comfortable at desktop container width, compact at the mobile floor) instead of the toggle.

    Final: run the `solidstats-frontend-react-design-review` seven pillars DIFFING the reworked table geometry/controls against the binding `.design/hifi/players.jsx` (auto density, total-in-caption, no stray scroll, the pager decision). Record the hi-fi diff in the SUMMARY. Acceptance = APPROVE with the diff noted. Fix findings and re-run the gate.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && pnpm exec vitest run && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - The DensityToggle dir is deleted: `ls packages/design/src/shared/uikit/DensityToggle 2>/dev/null` returns nothing; `grep -rn 'DensityToggle' packages/design/src/index.ts` returns ZERO (the barrel export is removed).
    - `grep -rn 'DensityToggle' packages/design/src/ packages/design/tests/` returns ZERO references outside of git history (no dangling import).
    - Density derives from the @container (comfortable at desktop container width, compact below) with no user-facing toggle — assert via the Table story at desktop vs the 360px floor (the resolved ROW_H differs by container width).
    - `import { Table } from "@solid-stats/design"` still resolves; `import { DensityToggle }` no longer resolves (removed).
    - The design-review verdict is APPROVE with the `.design/hifi/players.jsx` diff recorded in the SUMMARY (auto density / total-in-caption / no-stray-scroll / real-pager all matched-or-justified).
    - `pnpm exec playwright test` + `pnpm exec vitest run` green; root `pnpm check` exit 0; `grep -rE '@tanstack/react-(table|virtual)' packages/design/src/` ZERO (D-01); 0 arbitrary values across the table slices.
  </acceptance_criteria>
  <done>DensityToggle is removed (component + story + barrel export); table density derives automatically from the @container; ROW_H + the controlled density prop are kept; the table geometry/controls pass the seven-pillar design-review diffed against the binding hi-fi, recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational data-table catalog; no untrusted input / data / network. Inherits the Phase-2 boundary set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Tampering | Removing DensityToggle leaving a dangling import / broken barrel | mitigate | The no-reference grep + `pnpm check` (tsgo) + the full Playwright matrix run as the closure gate; a dangling import fails compilation, not ships. |
| T-02-09-02 | Tampering | A stray-scrollbar regression re-appearing (the GAP-08 class) | mitigate | cls.spec now asserts `scrollHeight <= clientHeight` on both tables — a regression fails the gate. |
| T-02-09-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (D-01: no @tanstack engine; auto density is CSS/@container). |
</threat_model>

<verification>
- `cd packages/design && pnpm exec ladle build` — green.
- `pnpm exec playwright test` — full matrix green (cls.spec no-scrollbar on both tables + box-equality; Pagination real pager; loading cell shows skeleton; auto density; axe serious/critical = 0).
- `pnpm exec vitest run` — green.
- Root `pnpm check` — exit 0.
- `ls DensityToggle` empty + `grep DensityToggle index.ts` ZERO; `grep @tanstack/react-(table|virtual)` ZERO; no-arbitrary-values grep ZERO.
- design-review APPROVE with the `.design/hifi/players.jsx` diff in the SUMMARY.
</verification>

<success_criteria>
- GAP-06 closed: DensityToggle removed; auto density from @container; ROW_H + controlled density prop kept.
- GAP-07 closed: a real Pagination pager (N–M of total + disabled Next at end + Prev disabled at start).
- GAP-08 closed: border-box viewport math — no stray scroll on the table or the skeleton; cls.spec asserts no-scrollbar on both.
- GAP-11 closed: the RowStates loading cell shows the skeleton, not data.
- GAP-14 closed: few vs limit-reached read differently via the caption total.
- Design-review APPROVE diffed against the binding hi-fi; full gate green.
</success_criteria>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-09-SUMMARY.md` when done.
</output>
