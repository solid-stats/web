# Requirements: Solid Stats web — next milestone intake

**Defined:** 2026-07-04
**Status:** intake
**Previous milestone:** `v0.1` archived early as superseded, not shipped.

## Active Requirements

There are no active feature requirements for an implementation milestone yet.

The next milestone must be defined from the updated web brief and the new repo workflow before any
GSD implementation phase starts.

## Structural Requirements

- [ ] **PROC-01**: Use `.visual-prototypes/` as the active visual prototype workspace for page,
  surface, flow, role, breakpoint, or hard layout problem slices.
- [ ] **PROC-02**: Do not start production implementation for a surface until the relevant visual
  prototype has an accepted `SUMMARY.md`.
- [ ] **PROC-03**: Convert accepted prototype summaries into implementation surface specs before
  route/component work starts.
- [ ] **DS-ROOT-01**: Keep `DESIGN.md` as the single source of truth for design tokens.
- [ ] **DS-ROOT-02**: Keep `src/styles/theme.css` generated by `scripts/gen-theme.mjs`; never
  hand-edit the generated theme file.
- [ ] **LEGACY-01**: Treat `.legacy/ladle-design/` as read-only reference only. It is not active
  implementation, not part of `pnpm check`, and not a source to port directly.
- [ ] **GSD-01**: Start the next implementation milestone with fresh requirements and roadmap
  rather than resuming archived `v0.1`.

## Archived Requirements

The old `v0.1` Ladle design requirements are preserved at:

- `.planning/milestones/v0.1-superseded-REQUIREMENTS.md`

Those requirements were not completed as a shipped milestone. Their phase artifacts are preserved
for reference at `.planning/milestones/v0.1-superseded-phases/`.

## Out of Scope Until Next Milestone

| Item | Reason |
|------|--------|
| Resuming old phases 5-9 | The package-based Ladle milestone was superseded by the new visual-prototype workflow. |
| Treating `v0.1` as shipped | The milestone audit found gaps and phase 4 still had manual acceptance pending. |
| Directly porting legacy code | The archive is reference-only; production implementation must be built against the active app architecture. |
| Backend/parser/ingest work | Owned by adjacent SolidStats services. |

## Traceability

| Requirement | Status | Notes |
|-------------|--------|-------|
| PROC-01 | Pending | Starts with the first new visual prototype slice. |
| PROC-02 | Pending | Enforced per implementation phase. |
| PROC-03 | Pending | Enforced per implementation phase. |
| DS-ROOT-01 | Active | Source of truth already exists. |
| DS-ROOT-02 | Active | Generator already targets `src/styles/theme.css`. |
| LEGACY-01 | Active | `.legacy/ladle-design/README.md` defines the boundary. |
| GSD-01 | Pending | Requires a new milestone definition. |
