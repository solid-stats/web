---
phase: 01-workspace-design-system-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/design/vite.config.ts
  - packages/design/.ladle/config.mjs
  - packages/design/.ladle/components.tsx
  - packages/design/src/index.ts
  - packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx
  - packages/design/src/shared/uikit/Smoke/index.ts
autonomous: true
requirements: [WS-02, WS-04]

must_haves:
  truths:
    - "`ladle build` exits 0 and the output contains the Smoke story."
    - "The built CSS resolves the @theme tokens (e.g. a bg-surface-1 rule maps to --color-surface-1), proving tokens render on the real stack — not arbitrary values."
    - "The smoke story exercises a data-trust token utility, so DS-03's tokens are visibly wired, not just present in the file."
    - "Ladle is dark-only: the theme addon toggle is disabled and the single gunmetal palette is the only state."
    - "theme.css is imported exactly once (in .ladle/components.tsx), with fonts.css loaded so real type renders."
  artifacts:
    - path: "packages/design/vite.config.ts"
      provides: "Root Vite config Ladle auto-loads, applying @tailwindcss/vite"
      contains: "tailwindcss"
    - path: "packages/design/.ladle/config.mjs"
      provides: "Ladle config — dark-only (theme addon disabled), colocated story glob"
      contains: "defaultState"
    - path: "packages/design/.ladle/components.tsx"
      provides: "GlobalProvider importing theme.css once + fonts"
      contains: "theme.css"
    - path: "packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx"
      provides: "The one smoke story proving @theme resolves (D-07; zero components)"
      contains: "export const"
    - path: "packages/design/src/index.ts"
      provides: "The @solid-stats/design UIKIT barrel entry (empty in Phase 1)"
  key_links:
    - from: "packages/design/.ladle/components.tsx"
      to: "packages/design/src/styles/theme.css"
      via: "import \"../src/styles/theme.css\" — the ONE global import (styling.md import-once)"
      pattern: "import.*styles/theme.css"
    - from: "packages/design/vite.config.ts"
      to: "tailwindcss (@tailwindcss/vite)"
      via: "plugins: [tailwindcss()] — Ladle merges it on top of its own react + tsconfig-paths plugins"
      pattern: "plugins.*tailwindcss"
    - from: "packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx"
      to: "packages/design/src/styles/theme.css"
      via: "uses bg-surface-1 / font-display / a freshness token utility so Tailwind v4 source-scans + emits them"
      pattern: "bg-surface-1|font-display"
---

<objective>
Wire Ladle to the real Vite + Tailwind v4 stack inside `packages/design` and ship the single smoke story that proves the generated `@theme` (including the new data-trust tokens) resolves on the real stack — dark-only, colocated-story convention, self-hosted type. Zero components (D-07 / MIGRATION D4): exactly one story.

Purpose: WS-04 (Ladle on the real stack, colocated `*.stories.tsx`, tokens render) and WS-02 (the `@solid-stats/design` UIKIT entry resolves). This is the end of the walking-skeleton slice — `DESIGN.md` → generated `@theme` → Ladle renders it. The risk is the Ladle↔Vite↔Tailwind seam, not new logic (RESEARCH "spend the rigor on the seams").
Output: The Ladle config + Vite config + global provider + the one smoke story + the UIKIT barrel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md
@packages/design/src/styles/theme.css
@packages/design/src/styles/fonts.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ladle + Vite + Tailwind v4 wiring (dark-only)</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (Pattern 1 — root vite.config.ts, no Ladle plugin hook; Pattern 2 — global stylesheet imported once, dark-only is trivial; Pattern 4 — source scanning keeps story utilities alive; anti-pattern: do NOT add Vite 8 directly)
    - .planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md (D-08 Ladle inside packages/design, dark-only, no SSR; D-06 theme.css generated)
    - .claude/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (@theme consumption, theme.css import-once, no arbitrary values)
    - .claude/skills/solidstats-frontend-react-design/references/pipeline.md (Ladle-as-durable-catalog; the src/shared/uikit/<Component>/ story path; .design/CLAUDE.md rule-capture home)
    - packages/design/src/styles/theme.css + packages/design/src/styles/fonts.css (the generated @theme + the @font-face this provider imports)
  </read_first>
  <files>packages/design/vite.config.ts, packages/design/.ladle/config.mjs, packages/design/.ladle/components.tsx</files>
  <action>
    Create `packages/design/vite.config.ts` exporting a `defineConfig` with `plugins: [tailwindcss()]` (from `@tailwindcss/vite`). Ladle 5.1.1 auto-loads this file and merges its own `@vitejs/plugin-react` + `vite-tsconfig-paths` on top (RESEARCH Pattern 1) — do NOT add `@vitejs/plugin-react` or `vite` yourself.
    Create `packages/design/.ladle/config.mjs` (UserConfig): `stories: "src/**/*.stories.{ts,tsx}"`, a `defaultStory` pointing at the smoke story, and `addons.theme: { enabled: false, defaultState: "dark" }` to drop the toggle (dark-only — single-palette @theme, nothing to switch, RESEARCH Pattern 2). Optionally `addons.width: { enabled: true }`.
    Create `packages/design/.ladle/components.tsx` exporting a `GlobalProvider` named `Provider` that imports the styles ONCE — `import "../src/styles/fonts.css"` then `import "../src/styles/theme.css"` (fonts before theme so @font-face is registered) — and wraps children in a root element using @theme utilities (e.g. `bg-bg-0 text-primary font-body min-h-screen p-4`). This is the single global CSS import (styling.md import-once).
  </action>
  <verify>
    <automated>test -f packages/design/vite.config.ts && grep -q "tailwindcss" packages/design/vite.config.ts && test -f packages/design/.ladle/config.mjs && grep -q "defaultState" packages/design/.ladle/config.mjs && grep -q "enabled: false" packages/design/.ladle/config.mjs && test -f packages/design/.ladle/components.tsx && grep -q "styles/theme.css" packages/design/.ladle/components.tsx && [ "$(grep -c 'styles/theme.css' packages/design/.ladle/components.tsx)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` applies `@tailwindcss/vite` via `plugins: [tailwindcss()]` and does NOT add react/vite plugins manually.
    - `.ladle/config.mjs` disables the theme toggle (`enabled: false`) with `defaultState: "dark"` and scans `src/**/*.stories.{ts,tsx}`.
    - `.ladle/components.tsx` imports `theme.css` exactly once (and `fonts.css` before it).
    - No direct `vite` / `@vitejs/plugin-react` dependency is introduced in `packages/design`.
  </acceptance_criteria>
  <done>Ladle is wired to the real stack, dark-only, consuming the generated @theme with self-hosted fonts loaded once.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke story + UIKIT barrel (zero components)</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (Code Examples — Smoke story; Pattern 4 source scanning; Validation Map WS-04 — built CSS contains a bg-surface-1 rule / --color-surface-1)
    - .planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md (D-07 story path src/shared/uikit/<Component>/, zero components; Claude's Discretion — reconcile shared/ nesting against architecture.md and keep shared/uikit per the design-skill literal unless the package-is-shared-layer reading wins; D-05 UIKIT entry)
    - .claude/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md (FSD slice rules — a slice owns its component + story + index; the shared/uikit path resolution for D-07)
    - packages/design/src/styles/theme.css (the token utilities to exercise: surface-1, primary, font-display, tabular-nums, and the new --color-freshness-* / known/unknown/conflict tokens)
  </read_first>
  <files>packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx, packages/design/src/shared/uikit/Smoke/index.ts, packages/design/src/index.ts</files>
  <action>
    Reconcile the story path per D-07: keep the FSD `shared/` segment — `packages/design/src/shared/uikit/Smoke/` — because `architecture.md` slice rules and the design `pipeline.md` §3 both specify `src/shared/uikit/<Component>/` (the package is a workspace package, not "the shared layer"; the slice path is preserved, not flattened to `src/uikit/`). Cite the rule in the SUMMARY.
    Create `Smoke/Smoke.stories.tsx` with a single named export (a story) that renders token-driven utilities ONLY (zero real components — D-07 / MIGRATION D4): display type (`font-display`), a surfaced card (`bg-surface-1 border border-border-1 rounded-lg`), tabular-mono numerals (`font-mono tabular-nums`), the cyan primary accent, AND at least one data-trust token utility so DS-03's tokens are visibly exercised (e.g. a freshness pill using the `--color-freshness-*` tokens emitted in plan 03 — reference them via the Tailwind utility or an inline style consuming the custom property). These utilities must be real @theme tokens, never arbitrary values (styling.md). The colocated story is Tailwind-v4 source-scanned so its utilities are emitted (RESEARCH Pattern 4).
    Create `Smoke/index.ts` as the slice entrypoint (per architecture.md) and `src/index.ts` as the `@solid-stats/design` UIKIT barrel — empty in Phase 1 (a single `export {}` or a comment-only barrel; the package exports map's `"."` entry resolves to it, D-05). Do NOT export the smoke story from the public barrel — it is a catalog story, not a UIKIT component.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec ladle build && test -d packages/design/build && grep -rilq "smoke" packages/design/build 2>/dev/null && find packages/design/build -name '*.css' -exec grep -l 'surface-1' {} + | head -1 | grep -q . && test -f packages/design/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - The story lives at `packages/design/src/shared/uikit/Smoke/Smoke.stories.tsx` (shared/ segment preserved per D-07).
    - `ladle build` exits 0 and the built output references the Smoke story.
    - The built CSS contains a rule resolving a `surface-1` utility to the `--color-surface-1` token (token resolved, not arbitrary).
    - The story exercises at least one `--color-freshness-*` / data-trust token utility (DS-03 visibly wired).
    - `src/index.ts` exists as the (empty) UIKIT barrel; the smoke story is NOT exported from it.
    - The slice contains only the Smoke story — zero real components.
  </acceptance_criteria>
  <done>The single smoke story renders the generated @theme (colors, type, tabular numerals, and a data-trust token) on the real Ladle stack; `ladle build` succeeds; the UIKIT barrel resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Supply-chain / build-integrity only (no runtime surface). The boundary specific to this plan is the Vite/Ladle build config (arbitrary-code execution via config + plugins).

| Boundary | Description |
|----------|-------------|
| Ladle/Vite config → build | `vite.config.ts` + `.ladle/*` execute at build time; Ladle's bundled Vite 6 + @tailwindcss/vite process the CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Tampering | `vite.config.ts` / `.ladle/config.mjs` / `.ladle/components.tsx` (build-time code) | mitigate | Config applies only the audited `@tailwindcss/vite` plugin (plan 01 legitimacy audit); no network, no postinstall, no untrusted plugin. Ladle owns its bundled Vite 6 (no second Vite introduced). |
| T-1-08 | Tampering | font binaries loaded by the provider | accept | Static `.woff2` assets sourced from official origins (plan 02 T-1-04); no executable surface. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design exec ladle build` exits 0; output contains the Smoke story.
- Built CSS resolves a `surface-1` utility to `--color-surface-1` (token, not arbitrary).
- The smoke story exercises a `--color-freshness-*` data-trust token utility.
- `theme.css` is imported exactly once (`.ladle/components.tsx`).
</verification>

<success_criteria>
WS-04 (Ladle on the real stack, dark-only, generated @theme, colocated story, tokens render) and WS-02 (the `@solid-stats/design` UIKIT entry resolves) are satisfied. The walking-skeleton slice is complete end-to-end: DESIGN.md → @theme → Ladle render. Zero components beyond the one smoke story (D-07 / MIGRATION D4).
</success_criteria>

<output>
Create `.planning/phases/01-workspace-design-system-foundation/01-04-SUMMARY.md` when done.
</output>
