---
phase: 01-workspace-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/design/src/assets/fonts/
  - packages/design/src/styles/fonts.css
  - .planning/phases/01-workspace-design-system-foundation/TOOLCHAIN.md
autonomous: false
requirements: [WS-05]
user_setup:
  - service: vite-plus-toolchain
    why: "D-03 locked the lint/format/type-check gate as Vite+ `vp check` (Oxlint + Oxfmt + tsgo), but the exact npm package name and the `vp check` invocation are unverified (RESEARCH A1 / Pitfall 4) and `vp` is not installed. WS-05 'green across the workspace' must hold under either the `vp` path or the primitive contingency."
    dashboard_config:
      - task: "Confirm the Vite+ package name (e.g. ~`vite-plus`) exposing a `vp` binary, install it as a root dev-dep, and confirm `vp check` runs. If unavailable, the plan falls back to oxlint + oxfmt --check + tsgo --noEmit."
        location: "Vite+ docs / npm registry"

must_haves:
  truths:
    - "Either `vp check` is confirmed runnable at the workspace root, OR the primitive contingency (oxlint + oxfmt --check + tsgo --noEmit) is confirmed runnable — the resolved choice is recorded in TOOLCHAIN.md."
    - "Self-hosted Saira / IBM Plex Sans / IBM Plex Mono `.woff2` assets exist in `packages/design`, all carrying Cyrillic glyphs."
    - "`fonts.css` declares @font-face for the three families with font-display: swap and relative url() paths Vite can fingerprint."
  artifacts:
    - path: "packages/design/src/styles/fonts.css"
      provides: "@font-face declarations for the self-hosted families (QUAL-04)"
      contains: "font-display: swap"
    - path: ".planning/phases/01-workspace-design-system-foundation/TOOLCHAIN.md"
      provides: "The resolved WS-05 gate command (vp check OR primitive contingency) for plan 05 to wire"
      contains: "check command"
  key_links:
    - from: "packages/design/src/styles/fonts.css"
      to: "packages/design/src/assets/fonts/"
      via: "relative url(\"../assets/fonts/*.woff2\") so Vite resolves + fingerprints at dev and build"
      pattern: "url\\(\"\\.\\./assets/fonts"
---

<objective>
Resolve the two non-autonomous Wave-0 unknowns that gate WS-05 and the smoke render: (1) confirm/install the Vite+ `vp check` toolchain (D-03) or wire the primitive contingency, recording the resolved gate command for plan 05; (2) source the self-hosted `.woff2` font assets (Saira, IBM Plex Sans, IBM Plex Mono — all with Cyrillic) into `packages/design` and declare their `@font-face` so the smoke story shows real type (QUAL-04). Neither is autonomously resolvable, so both are checkpointed.

Purpose: WS-05 "green across the workspace" must hold under either toolchain path; QUAL-04's self-hosted-fonts intent is unmet if the smoke story falls back to system-ui. These are the floor that plans 04 (Ladle render) and 05 (gate) build on.
Output: Confirmed toolchain decision (TOOLCHAIN.md), the font assets, and `fonts.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md
@DESIGN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Resolve the Vite+ `vp check` toolchain (or contingency)</name>
  <action>Blocking human checkpoint — see what-built / how-to-verify / resume-signal below.</action>
  <what-built>D-03 locks WS-05's lint/format/type-check gate as Vite+ `vp check` (Oxlint + Oxfmt + tsgo), and WS-05 points explicitly at `solidstats-frontend-react-conventions`. But the exact npm package name and the `vp check` invocation are unverified (RESEARCH A1 / Pitfall 4) — `npx vp --version` returns "not installed" and the package name (`vite-plus`? scoped?) is unconfirmed this session. This checkpoint resolves the package + command before the gate is wired (T-1-03), with the primitive contingency baked in.</what-built>
  <how-to-verify>
    1. Confirm the Vite+ package name that exposes the `vp` binary (check the conventions skill §0 and Vite+ docs / npm). Run `gsd-tools query package-legitimacy check --ecosystem npm <confirmed-name>` on it.
    2. Install it as a root dev-dep (`pnpm add -Dw <name>`), then run `vp check` at the workspace root and confirm it runs (it may report findings — that is fine; we need it to RUN, not pass yet).
    3. CONTINGENCY (if Vite+ `vp` is unavailable or the name cannot be confirmed): per D-03's own parenthetical ("Oxlint + Oxfmt + tsgo"), fall back to the wrapped primitives directly — install `oxlint`, `@oxfmt/oxfmt` (or the current Oxfmt package), and `@typescript/native-preview` (tsgo), and confirm `oxlint && oxfmt --check . && tsgo --noEmit` runs.
    4. Record the RESOLVED gate command (the `vp check` form OR the primitive triplet) in `TOOLCHAIN.md` so plan 05 wires the exact command into the root `check` script. WS-05 "green across the workspace" must hold under whichever path was chosen.
  </how-to-verify>
  <resume-signal>Type the resolved command (e.g. "vp check" or "oxlint && oxfmt --check . && tsgo --noEmit") and confirm it runs, or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Source the self-hosted font assets (QUAL-04)</name>
  <action>Blocking human checkpoint — see what-built / how-to-verify / resume-signal below.</action>
  <what-built>QUAL-04 requires self-hosted fonts so the smoke story (and every later surface) renders real type with no FOUT/CLS. The three families — Saira (display), IBM Plex Sans (body), IBM Plex Mono (mono, tabular figures) — are named in DESIGN.md typography.fontFamilies (L57-59) and all carry Cyrillic (DESIGN.md L505), but the `.woff2` files are not in the repo (RESEARCH Environment Availability — fallback is system-ui, which misses QUAL-04 intent). The `--font-display`/`--font-body`/`--font-mono` token names already exist in @theme; this supplies the files behind them.</what-built>
  <how-to-verify>
    1. Source the `.woff2` files for the weights DESIGN.md uses: Saira 600/700 (display, L585), IBM Plex Sans 400/500/600 (body, L586), IBM Plex Mono 400/500 (mono, L587) — from Google Fonts or the official IBM Plex repo. ALL must include the Cyrillic subset (the data-trust copy is Russian — «Актуально» etc.).
    2. Place them under `packages/design/src/assets/fonts/` with stable names (e.g. `Saira-SemiBold.woff2`, `IBMPlexSans-Regular.woff2`, `IBMPlexMono-Medium.woff2`).
    3. Confirm at least one Cyrillic glyph renders (open one font, or trust the subset selection). If the assets cannot be obtained autonomously this turn, that is why this task is `autonomous: false` — provide them or approve a documented temporary system-ui fallback with a tracked follow-up.
  </how-to-verify>
  <resume-signal>Type "fonts placed" once the `.woff2` files are under packages/design/src/assets/fonts/, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Declare @font-face in fonts.css</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (Pattern 3 — self-hosted fonts inside packages/design; relative url() vs absolute /fonts 404)
    - DESIGN.md (typography.fontFamilies L56-59; weights L585-587; Cyrillic note L505)
    - .claude/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (theme.css import-once; no arbitrary values — fonts.css is imported alongside theme.css)
  </read_first>
  <files>packages/design/src/styles/fonts.css</files>
  <action>
    Create `packages/design/src/styles/fonts.css` declaring `@font-face` for each sourced family/weight: `font-family` exactly matching the names in DESIGN.md (`Saira`, `IBM Plex Sans`, `IBM Plex Mono`), the correct `font-weight` per file, `font-display: swap` (QUAL-04 / CWV), and `src: url("../assets/fonts/<file>.woff2") format("woff2")` with RELATIVE paths from this CSS file so Vite fingerprints them in the build graph (RESEARCH Pattern 3 — absolute `/fonts/...` 404s on the Ladle dev server). Do NOT inline font binaries or use a CDN. This file is imported alongside `theme.css` (the smoke-render plan wires the import order); the `--font-*` token names already exist in @theme.
  </action>
  <verify>
    <automated>test -f packages/design/src/styles/fonts.css && grep -q "font-display: swap" packages/design/src/styles/fonts.css && grep -Eq 'url\("\.\./assets/fonts/.*\.woff2"\)' packages/design/src/styles/fonts.css && grep -q "IBM Plex Mono" packages/design/src/styles/fonts.css && ls packages/design/src/assets/fonts/*.woff2 >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `fonts.css` declares @font-face for Saira, IBM Plex Sans, and IBM Plex Mono with matching `font-family` names.
    - Every declaration sets `font-display: swap`.
    - Every `src` uses a relative `url("../assets/fonts/*.woff2")` path (no absolute, no CDN).
    - At least one `.woff2` file per family exists under `packages/design/src/assets/fonts/`.
  </acceptance_criteria>
  <done>The three self-hosted families are declared with swap display and Vite-resolvable relative paths; the smoke story can render real type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Supply-chain only (no runtime surface — see plan 01 threat model). Two new boundaries this plan: the unconfirmed `vp`/Vite+ package, and the externally-sourced font binaries.

| Boundary | Description |
|----------|-------------|
| npm registry → workspace (vp) | An unverified-name package crosses into the build as the WS-05 gate |
| external font source → repo | `.woff2` binaries from Google Fonts / IBM Plex repo enter the build graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | `vp` / Vite+ install (unconfirmed package name, A1) | mitigate | Blocking human checkpoint confirms the exact package name + runs `package-legitimacy check` before install; primitive contingency (oxlint/oxfmt/tsgo, all known packages) avoids the unconfirmed name entirely. |
| T-1-04 | Tampering | self-hosted font binaries | mitigate | Source only from official origins (Google Fonts / IBM Plex repo); fonts are static assets with no executable surface; checkpoint confirms provenance + Cyrillic subset. |
</threat_model>

<verification>
- `TOOLCHAIN.md` records the resolved gate command (vp check OR the primitive triplet) and it runs at the root.
- `packages/design/src/assets/fonts/` contains `.woff2` files for all three families.
- `fonts.css` declares @font-face with `font-display: swap` and relative url() paths.
</verification>

<success_criteria>
The WS-05 toolchain path is resolved and recorded (the one blocking unknown, A1), and QUAL-04's self-hosted fonts are present and declared. Plan 05 can now wire the exact gate command; plan 04 can render real type in the smoke story.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-design-system-foundation/01-02-SUMMARY.md` when done.
</output>
