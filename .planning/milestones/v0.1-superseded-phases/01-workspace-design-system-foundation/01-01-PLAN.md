---
phase: 01-workspace-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .nvmrc
  - .node-version
  - packages/design/package.json
  - packages/design/tsconfig.json
  - packages/app/package.json
  - packages/app/tsconfig.json
  - packages/app/src/index.ts
  - pnpm-lock.yaml
autonomous: false
requirements: [WS-01, WS-02]
user_setup:
  - service: node-toolchain
    why: "The machine runs Node 24 but the workspace pins Node 25 (engines >=25 <26). pnpm install will EBADENGINE under engine-strict until Node 25 is active."
    dashboard_config:
      - task: "Install and activate Node 25 (nvm install 25 / fnm install 25). The .nvmrc / .node-version files this plan writes make it `nvm use`."
        location: "Local dev machine (nvm/fnm)"

must_haves:
  truths:
    - "A fresh `pnpm install` resolves the workspace on pnpm 11 + Node 25 with exit 0."
    - "`packages/design` is resolvable as the importable package `@solid-stats/design`."
    - "`packages/app` resolves and depends on `@solid-stats/design` via `workspace:*` but builds nothing."
    - "The four pinned root dev-deps install at exact versions (no caret drift)."
  artifacts:
    - path: "package.json"
      provides: "Private workspace root: packageManager pnpm@11.x, engines node>=25, pinned devDeps, scripts (gen-theme, lint:design, check)"
      contains: "\"private\": true"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package globs"
      contains: "packages/*"
    - path: "tsconfig.base.json"
      provides: "Strict TS baseline (exactOptionalPropertyTypes, noUncheckedIndexedAccess) that per-package tsconfig extends"
      contains: "exactOptionalPropertyTypes"
    - path: "packages/design/package.json"
      provides: "@solid-stats/design name + exports map (./theme.css + UIKIT entry)"
      contains: "@solid-stats/design"
    - path: "packages/app/package.json"
      provides: "Skeleton package depending on @solid-stats/design"
      contains: "workspace:*"
    - path: ".nvmrc"
      provides: "Node 25 pin"
      contains: "25"
  key_links:
    - from: "packages/app/package.json"
      to: "packages/design/package.json"
      via: "dependencies: { \"@solid-stats/design\": \"workspace:*\" } — proves the workspace seam resolves"
      pattern: "@solid-stats/design.*workspace"
    - from: "packages/design/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends the root base so strictness never drifts (D-02)"
      pattern: "extends.*tsconfig.base"
---

<objective>
Establish the pnpm workspace foundation every later plan and phase imports: the root private `package.json` (pnpm 11 + Node 25 pins, the four pinned dev-deps, the root scripts), `pnpm-workspace.yaml`, the strict `tsconfig.base.json`, the Node-version files, the `@solid-stats/design` package shell with its `exports` map, and the `packages/app` skeleton. A fresh `pnpm install` must resolve the whole workspace.

Purpose: This is the walking-skeleton base (WS-01, WS-02). Nothing renders or generates until the workspace resolves and `@solid-stats/design` is importable. The risk is in the seams (workspace resolution, the exports map, the engines pin), not in writing new logic.
Output: Root workspace config + both package skeletons + a clean lockfile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace config + Node/pnpm pins</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (§ Code Examples — root package.json shape, pnpm-workspace.yaml; § Standard Stack pinned versions; Pitfall 3 Node 25; Pitfall 5 run-then-check)
    - .planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md (D-01, D-02, D-03)
    - .claude/skills/solidstats-shared-ts-standards/SKILL.md (§A strict tsconfig baseline; §C the web-frontend Oxlint/vp override; §D Node 25 / pnpm 11)
  </read_first>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, .nvmrc, .node-version</files>
  <action>
    Create the root private `package.json` named `@solid-stats/web` with `"private": true`, `"type": "module"`, `"packageManager": "pnpm@11.6.0"`, `"engines": { "node": ">=25 <26", "pnpm": ">=11 <12" }` (per D-01). Add `devDependencies` pinned EXACT (no caret/tilde): `@google/design.md` `0.3.0`, `@ladle/react` `5.1.1`, `tailwindcss` `4.3.1`, `@tailwindcss/vite` `4.3.1` (per RESEARCH Standard Stack — all four legitimacy-approved). Add root `scripts`: `gen-theme` = `node scripts/gen-theme.mjs`, `lint:design` = `design.md lint DESIGN.md`, and a placeholder `check` = `pnpm gen-theme && pnpm lint:design` (the `vp check` / contingency tail is appended by plan 05 once the toolchain is resolved — do NOT hard-code `vp check` here, it is unverified per A1). `gen-theme` and `lint:design` are ROOT scripts (D-02: root owns DESIGN.md + scripts/), never package scripts.
    Create `pnpm-workspace.yaml` with `packages: ["packages/*"]` (D-01).
    Create `tsconfig.base.json` with a strict baseline per shared-ts-standards §A: `strict: true`, `exactOptionalPropertyTypes: true`, `noUncheckedIndexedAccess: true`, `module`/`moduleResolution` set for the bundler stack (`"bundler"`), `verbatimModuleSyntax`, `skipLibCheck`, `target` ES2023+. No `include` at the base — per-package tsconfig owns its globs (D-02).
    Create `.nvmrc` and `.node-version` both containing `25` (per D-01 / RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f tsconfig.base.json && node -e "const p=require('./package.json'); const d=p.devDependencies; for (const [k,v] of Object.entries({'@google/design.md':'0.3.0','@ladle/react':'5.1.1','tailwindcss':'4.3.1','@tailwindcss/vite':'4.3.1'})) { if (d[k] !== v) { console.error('BAD PIN', k, d[k]); process.exit(1); } } if (p.private !== true) process.exit(1); if (!/>=25/.test(p.engines.node)) process.exit(1); console.log('root config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` is `private: true`, pins `packageManager: pnpm@11.x` and `engines.node >=25 <26`.
    - The four dev-deps are pinned to EXACTLY `0.3.0` / `5.1.1` / `4.3.1` / `4.3.1` (string equality, no `^`/`~`).
    - Root scripts `gen-theme` and `lint:design` exist; `check` exists without a hard-coded `vp check` tail.
    - `tsconfig.base.json` sets `exactOptionalPropertyTypes` and `noUncheckedIndexedAccess` true.
    - `.nvmrc` and `.node-version` both read `25`.
  </acceptance_criteria>
  <done>Root workspace config, strict TS base, and Node/pnpm pins exist with exact-pinned dev-deps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Activate Node 25 + confirm dev-dep supply chain</name>
  <action>Blocking human checkpoint — see what-built / how-to-verify / resume-signal below.</action>
  <what-built>The root `package.json` declares `engines.node >=25 <26`, but the machine runs Node 24.16.0 (RESEARCH Pitfall 3 / Environment Availability — no fallback, the pin must not be lowered). `pnpm install` in Task 3 will EBADENGINE under engine-strict until Node 25 is active. This checkpoint also covers the supply-chain trust boundary: a human confirms the four pinned dev-deps before they are installed (T-1-01).</what-built>
  <how-to-verify>
    1. Install Node 25: `nvm install 25` (or `fnm install 25`), then `nvm use` (the `.nvmrc` = 25 written in Task 1 selects it).
    2. Confirm: `node --version` prints `v25.x`.
    3. Confirm pnpm 11: `pnpm --version` prints `11.x`.
    4. Confirm the four pinned dev-deps are the intended legitimate packages (all approved in RESEARCH § Package Legitimacy Audit — `@ladle/react` tajo/ladle, `tailwindcss`/`@tailwindcss/vite` tailwindlabs, `@google/design.md` google-labs-code): the versions in `package.json` are `0.3.0`, `5.1.1`, `4.3.1`, `4.3.1`. No `[ASSUMED]`/`[SUS]` package here is auto-approvable — confirm explicitly.
  </how-to-verify>
  <resume-signal>Type "approved" once Node 25 is active and the dev-dep pins are confirmed, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Package skeletons + install</name>
  <read_first>
    - .planning/phases/01-workspace-design-system-foundation/01-RESEARCH.md (§ Code Examples — packages/design/package.json exports map; § Open Questions #3 packages/app minimum; Recommended Project Structure)
    - .planning/phases/01-workspace-design-system-foundation/01-CONTEXT.md (D-04 app skeleton-only, D-05 exports map, D-02 per-package extends)
    - .claude/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md (FSD slice rules — the shared/uikit story-path reconcile context for later plans; here it governs the package layout)
  </read_first>
  <files>packages/design/package.json, packages/design/tsconfig.json, packages/app/package.json, packages/app/tsconfig.json, packages/app/src/index.ts</files>
  <action>
    Create `packages/design/package.json`: name `@solid-stats/design`, `version` `0.0.0`, `private: true`, `type: module`, and the `exports` map (D-05) exposing both `"./theme.css": "./src/styles/theme.css"` AND `".": { "types": "./src/index.ts", "default": "./src/index.ts" }` (the UIKIT entry — `src/index.ts` is created by the smoke-story plan, empty barrel in Phase 1). Add `react@19` + `react-dom@19` as deps (Ladle peer, RESEARCH A3) and scripts `ladle` = `ladle dev`, `ladle:build` = `ladle build`. Do NOT add `vite` directly — Ladle 5.1.1 owns its bundled Vite 6 (RESEARCH anti-pattern).
    Create `packages/design/tsconfig.json` that `extends` `../../tsconfig.base.json`, with `include` for `src` and the `.ladle` config, and `compilerOptions.jsx` `react-jsx` (D-02).
    Create `packages/app/package.json`: name `@solid-stats/app`, `version` `0.0.0`, `private: true`, `type: module`, `dependencies: { "@solid-stats/design": "workspace:*" }` and nothing else (D-04 skeleton-only — no routes, no TanStack Start). Add a `tsconfig.json` extending the base (RESEARCH Open Q #3: keep `vp check` uniform across packages) and an empty `src/index.ts` (a single `export {}` so it is a valid module).
    Run `pnpm install` from the root. Expect exit 0 and a generated `pnpm-lock.yaml` (lockfile pins the supply-chain — T-1-01 mitigation).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile 2>/dev/null || pnpm install; pnpm -r exec node -e "console.log(process.env.PNPM_PACKAGE_NAME || require('./package.json').name)" | grep -q "@solid-stats/design" && pnpm -r exec node -e "1" >/dev/null && node -e "const a=require('./packages/app/package.json'); if (a.dependencies['@solid-stats/design'] !== 'workspace:*') process.exit(1); console.log('workspace seam OK')" && test -f pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 and writes `pnpm-lock.yaml`.
    - `packages/design/package.json` `exports` has BOTH `"./theme.css"` and `"."` keys.
    - `packages/app/package.json` depends on `@solid-stats/design` via `workspace:*` and has no other deps.
    - Both per-package `tsconfig.json` files `extends` `../../tsconfig.base.json`.
    - `packages/app/src/index.ts` exists and is a valid empty module.
    - No `vite` is listed as a direct dependency of `packages/design`.
  </acceptance_criteria>
  <done>The workspace resolves on a fresh install; `@solid-stats/design` and the `packages/app` skeleton both resolve; the lockfile is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 1 has NO runtime, network, auth, or data surface (no app, no routes, no fetch — AGENTS.md frontend boundary; RESEARCH Runtime State Inventory). The only boundary is **supply-chain / build-integrity**: third-party dev-deps and the lockfile entering the workspace.

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | Four pinned dev-deps + their transitive deps cross into the build at `pnpm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | npm dev-dep installs (`@google/design.md`, `@ladle/react`, `tailwindcss`, `@tailwindcss/vite`) | mitigate | Exact-version pins (no caret) + committed `pnpm-lock.yaml` + the blocking human checkpoint (Task 2) confirming the four packages against RESEARCH § Package Legitimacy Audit (all repo-backed, no postinstall). |
| T-1-02 | Elevation of Privilege | transitive postinstall scripts | accept | RESEARCH audit confirmed none of the four direct deps run postinstall; transitive risk is low for official tailwindlabs/google-labs/tajo packages. Re-audit on version bump. |
</threat_model>

<verification>
- `pnpm install` exits 0 on Node 25 + pnpm 11.
- `pnpm-lock.yaml` is committed.
- `node -e "require.resolve('@solid-stats/design/package.json')"` resolves from the workspace.
- The four dev-deps are exact-pinned in `package.json` and present in the lockfile.
</verification>

<success_criteria>
WS-01 (workspace resolves on pnpm 11 + Node 25, fresh install succeeds) and the WS-02 package shell (`@solid-stats/design` importable, exports map with both keys) are in place. `packages/app` is a resolving skeleton (D-04).
</success_criteria>

<output>
Create `.planning/phases/01-workspace-design-system-foundation/01-01-SUMMARY.md` when done.
</output>
