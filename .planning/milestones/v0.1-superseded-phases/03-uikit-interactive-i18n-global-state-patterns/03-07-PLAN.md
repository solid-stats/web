---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 07
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
  - packages/design/src/shared/uikit/AsyncBoundary/index.ts
  - packages/design/src/shared/uikit/ToastManager/ToastManager.tsx
  - packages/design/src/shared/uikit/ToastManager/ToastManager.stories.tsx
  - packages/design/src/shared/uikit/ToastManager/index.ts
  - packages/design/src/shared/uikit/Toast/Toast.tsx
  - packages/design/src/index.ts
  - packages/design/tests/cls.spec.ts
autonomous: true
requirements: [SURF-18, KIT-06, QUAL-01, QUAL-03, QUAL-04, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "AsyncBoundary maps a discriminated state union (loading / empty / error / offline / reconnecting / stale / ready) to the right EXISTING Phase-2 primitive (Skeleton / EmptyState / ErrorState / DataTrustBanner), reserving its final height (CLS = 0) and never relying on color alone (SURF-18, SC#3, D-05)."
    - "The six named global-state patterns exist as reusable reviewed Ladle stories composing the existing primitives — none of the underlying primitives are rebuilt (D-05)."
    - "The Toast manager (createToaster) owns portal/queue/auto-dismiss/stacking and renders the existing Toast/Toast.tsx leaf as the render-prop child; the dismiss control carries toastDismiss aria (D-06, KIT-06)."
  artifacts:
    - "packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx — the state-union → primitive seam (no new heights)"
    - "packages/design/src/shared/uikit/ToastManager/ToastManager.tsx — createToaster + Toaster render-prop wrapping the existing Toast leaf"
    - "AsyncBoundary.stories.tsx (six states + forced-height cells) + ToastManager.stories.tsx (interactive trigger Playground)"
  key_links:
    - "AsyncState kind → { loading:Skeleton, empty:EmptyState, error:ErrorState, offline/reconnecting/stale:DataTrustBanner } — DataTrustBanner.BannerKind already maps the three connection states"
    - "createToaster() → <Toaster> render-prop → existing <Toast variant message action> leaf; toaster.dismiss(toast.id) on the dismiss affordance"

premises:
  - claim: "the four Phase-2 primitives (Skeleton/EmptyState/ErrorState/DataTrustBanner) are built, CLS-0, never-color-alone, and DataTrustBanner.BannerKind maps stale/offline/reconnecting"
    src: packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx#L21
    verify: "grep -nE 'stale|offline|reconnecting' packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx"
  - claim: "Toast/Toast.tsx is the styled visual leaf with variant/message/action props, built to be composed under a real manager"
    src: packages/design/src/shared/uikit/Toast/Toast.tsx#L67
    verify: "grep -n 'export function Toast' packages/design/src/shared/uikit/Toast/Toast.tsx"
  - claim: "Ark createToaster returns a toaster instance and <Toaster> takes a render prop receiving each toast (type/title/description/action) — the existing leaf slots in, no re-expression"
    src: 03-RESEARCH.md#Toast-verdict
    verify: "node -e \"require.resolve('@ark-ui/react/toast')\""
  - claim: "the AsyncBoundary reserved-height six-state cls scaffold exists RED from Plan 03-01"
    src: packages/design/tests/cls.spec.ts
    verify: "grep -n 'AsyncBoundary' packages/design/tests/cls.spec.ts"

carried_forward:
  - claim: "the render-prop toast shape (type/title/description/action) must be confirmed against the installed 5.37.2 .d.ts and mapped onto the leaf's variant/message/action (RESEARCH A3 — mechanical adaptation, not a decision re-open)"
    src: .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md#Assumptions-Log
---

<objective>
Close Phase 3: ship SURF-18 (the reusable global-state patterns + the thin `AsyncBoundary` state→primitive seam) and the deferred Toast manager (D-06, `createToaster` wrapping the existing `Toast/Toast.tsx` leaf). AsyncBoundary maps a discriminated state union to the right EXISTING Phase-2 primitive (it does not rebuild them); the Toast manager owns the portal/queue/auto-dismiss/stacking lifecycle the Phase-2 visual leaf was deliberately built to compose under.

Purpose: SURF-18 is the single state→primitive seam Phases 4–9 compose — six named states (loading / empty / error / offline / reconnecting / stale), each reserving its final height (CLS = 0) and never color-alone (SC#3, D-05); the stories demonstrate the per-component review conventions (the applicable scenario endings / data-volume states, D-07). The Toast manager completes the deferred-from-Phase-2 lifecycle (D-06) and is the last KIT-06-adjacent interactive piece.
Output: `AsyncBoundary` + `ToastManager` slices (component + stories + barrel); the AsyncBoundary reserved-height cls spec turned GREEN; Phase 3 complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/errors.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx
@packages/design/src/shared/uikit/Toast/Toast.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AsyncBoundary — the state-union → existing-primitive seam (SURF-18, D-05)</name>
  <files>packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx, packages/design/src/shared/uikit/AsyncBoundary/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/DataTrustBanner/DataTrustBanner.tsx (the BannerKind kind-switch — the exact kind→render seam to mirror; it already maps stale/offline/reconnecting and reserves a fixed height)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (### AsyncBoundary state → primitive seam — the discriminated union + the loading→Skeleton/empty→EmptyState/error→ErrorState/offline-reconnecting-stale→DataTrustBanner mapping; D-05 — primitives NOT rebuilt)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md (### AsyncBoundary — the kind-switch pattern; reserved-height already encoded in each primitive; AsyncBoundary just routes, never hardcodes a height)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/errors.md (distinguish user error by-field from system error id+contact; never a blank screen)
    - packages/design/src/index.ts (the existing exports for Skeleton/EmptyState/ErrorState/DataTrustBanner this composes)
  </read_first>
  <behavior>
    - Test (an AsyncBoundary.test.tsx DOM render, vitest): `kind: "loading"` renders the Skeleton; `"empty"` the EmptyState; `"error"` the ErrorState (system kind, carrying the id); `"offline"`/`"reconnecting"`/`"stale"` the DataTrustBanner with the matching BannerKind; `"ready"` renders its children [src: 03-RESEARCH.md#AsyncBoundary].
    - Test: AsyncBoundary does NOT introduce its own height/min-h utility — it routes to primitives that already reserve height (assert the rendered branch is the primitive, not an extra wrapper with a hardcoded h-*) [src: 03-PATTERNS.md#AsyncBoundary].
  </behavior>
  <action>
    Create `AsyncBoundary/AsyncBoundary.tsx`: a named `AsyncBoundary` taking a discriminated `state: AsyncState` union [src: 03-RESEARCH.md#AsyncBoundary] — `{ kind: "loading" } | { kind: "empty" } | { kind: "error"; id?: string } | { kind: "offline" } | { kind: "reconnecting" } | { kind: "stale" } | { kind: "ready"; children: ReactNode }` — and routing each `kind` to the matching EXISTING primitive (mirror the `DataTrustBanner` kind-switch shape, explicit branches returning the primitive, never a leaked value) [src: 03-PATTERNS.md#AsyncBoundary]: loading→`Skeleton`, empty→`EmptyState`, error→`ErrorState` (system kind, id+contact per errors.md), offline/reconnecting/stale→`DataTrustBanner` with the matching `BannerKind`, ready→`children`. AsyncBoundary NEVER hardcodes a height — the primitives already reserve their final dimensions (CLS = 0) and carry icon+text (never color-alone); the wrapper only routes [src: 03-PATTERNS.md#AsyncBoundary]. Pass-through props (skeleton density, empty/error copy as plain strings resolved in the consumer) so the seam stays presentational and i18n-free. `data-async-boundary={kind}` hook. Discretion (D-05): the discriminated union is the intended shape; this is one slice, not a per-state file set [src: 03-CONTEXT.md#Claude's-Discretion]. Add `AsyncBoundary/index.ts` exporting `AsyncBoundary` + the `AsyncState` type (the public seam Phases 4–9 import).
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- AsyncBoundary</automated>
  </verify>
  <done>AsyncBoundary routes each of the seven kinds to the correct existing primitive with no new height, reserving CLS=0 via the primitives and never color-alone; the routing contract is pinned; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: ToastManager — createToaster wrapping the existing Toast leaf (D-06)</name>
  <files>packages/design/src/shared/uikit/ToastManager/ToastManager.tsx, packages/design/src/shared/uikit/ToastManager/index.ts, packages/design/src/shared/uikit/Toast/Toast.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/Toast/Toast.tsx (the existing styled visual leaf — variant/message/action props; the comment says it is built to compose under a real manager; KEEP it presentational, add only the dismiss affordance wiring)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (### Toast: createToaster reusing the existing leaf — createToaster(options) + <Toaster toaster> render-prop; the verdict: NO re-expression; two adaptations — dismiss aria → toaster.dismiss(toast.id), and confirm the render-prop toast shape against the installed .d.ts; A3)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md (### ToastManager — the createToaster + Toaster render-prop code; colocate in Toast/ or a sibling ToastManager/ — planner's call: ToastManager/)
  </read_first>
  <action>
    Create `ToastManager/ToastManager.tsx`: `export const toaster = createToaster({ placement: "bottom-end", overlap: true, gap: 12, max: 4 })` and a `ToastViewport` rendering `<Toaster toaster={toaster}>` with a render-prop child that renders the EXISTING `<Toast variant message action>` leaf [src: 03-RESEARCH.md#Toast]. Confirm the installed `@ark-ui/react@5.37.2` render-prop `toast` shape (`type`/`title`/`description`/`action`) against its `.d.ts` and map it onto the leaf's `variant`/`message`/`action` — if a field name differs this is a mechanical adaptation, not a decision re-open (A3) [src: 03-RESEARCH.md#Assumptions-Log]. The manager owns portal/queue/auto-dismiss/stacking; the leaf stays presentational [src: 03-PATTERNS.md#ToastManager].

    In `Toast/Toast.tsx`: add ONLY the dismiss affordance wiring — a `toastDismiss`-aria'd dismiss control (icon-only, the slice takes the aria string as a prop, does not invent it) that the manager maps to `toaster.dismiss(toast.id)` [src: 03-RESEARCH.md#Toast]. Do NOT re-express the leaf's existing visual structure (the four semantic variants + icon + message + action stay). Add `ToastManager/index.ts` exporting `toaster` + `ToastViewport`.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>createToaster owns the lifecycle and the Toaster render-prop renders the existing Toast leaf (no re-expression); the dismiss control carries a localized aria and calls toaster.dismiss; the render-prop shape is mapped onto the leaf props; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: AsyncBoundary + ToastManager stories, barrel, AsyncBoundary cls spec GREEN — Phase 3 complete</name>
  <files>packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx, packages/design/src/shared/uikit/ToastManager/ToastManager.stories.tsx, packages/design/src/index.ts, packages/design/tests/cls.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/Button.stories.tsx (the StateMatrix + Playground two-mode pattern)
    - packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx (StateCell reuse, data-state-cell labels)
    - packages/design/tests/cls.spec.ts (the AsyncBoundary six-state reserved-height scaffold from Plan 03-01 — currently RED; create the matching story id; copy the boundingBox().height match oracle from the DataTrustBanner/Skeleton proofs)
    - packages/design/src/index.ts (region-comment barrel append)
  </read_first>
  <action>
    Create `AsyncBoundary.stories.tsx` (title `SURF-18 Global state / AsyncBoundary`) two-mode (D-02): a `StateMatrix` rendering ALL SIX named states (loading / empty / error / offline / reconnecting / stale) PLUS the `ready` slot — each in a `StateCell`, demonstrating the reserved-height + icon+text (never color-alone) [src: 03-UI-SPEC.md#SURF-18]; and a CLS cell rendering each state above the `ready` content of the same width so the box heights match (the story id the Wave-0 cls spec references). The six-state recipes ARE the SURF-18 deliverable (reusable patterns composing the existing primitives) — also include a `Playground` toggling `kind`. Create `ToastManager.stories.tsx` (title `KIT-06 Overlay / ToastManager`) — an interactive `Playground` with trigger buttons calling `toaster.create({ title, type })` for each variant, mounting `<ToastViewport />` (createToaster mounts its own portal — no app shell needed) [src: 03-RESEARCH.md#Toast]. Resolve all copy via `i18n._({ id })` — `emptyTableHeading`/`emptyTableBody`, `errorSystem` (with `{id}`), `loadingColdAggregate`, `offlineBanner`/`reconnectingBanner`/`staleBanner`, `toastDismiss` — and pass plain string props. RU-longest-label check at 360 floor (QUAL-05).

    Append to `src/index.ts` a region `// ---- SURF-18 Global state + Toast manager (Wave 7 / Plan 03-07) ----` exporting `AsyncBoundary` (+ the `AsyncState` type) and `toaster` + `ToastViewport`. This COMPLETES Phase 3's interactive + global-state catalog.

    Turn the Plan-03-01 AsyncBoundary cls spec block GREEN in `tests/cls.spec.ts` against the real story id: all six states reserve the SAME box height as the `ready` content slot (copy the height-match oracle from the DataTrustBanner proof) — proving CLS = 0 across the state→primitive seam [src: cls.spec.ts].
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design ladle:build && pnpm --filter @solid-stats/design test:e2e -- cls.spec.ts catalog.spec.ts keyboard.spec.ts</automated>
  </verify>
  <done>AsyncBoundary has a six-state StateMatrix + a CLS proof story + Playground; ToastManager has an interactive trigger Playground rendering the existing Toast leaf; both are graduated into the barrel; the catalog axe/44px gate and the AsyncBoundary reserved-height cls spec are GREEN — every Phase-3 requirement is covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story fixture → AsyncBoundary state / toast content | Author-controlled fixture state + copy; no untrusted input, no network/server-2/auth (v0.1 presentational Ladle). createToaster mounts a portal to document.body (styling note only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | Tampering | toast / error-state copy interpolation (incl. the error {id}) | low | accept | Strings are author-controlled fixtures via JSX (auto-escaped); the error id is a display ref, not executable; no dangerouslySetInnerHTML. |
| T-03-07-02 | Information disclosure | system-error state content | low | accept | ErrorState shows a localized ref id + contact path (errors.md), no stack/PII; this is the intended honest-error pattern, not a leak. |
| T-03-SC | Tampering | npm installs | n/a (no installs this plan) | mitigate | Deps gated by the Plan 03-01 blocking-human checkpoint. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design test` green (AsyncBoundary routing).
- `pnpm --filter @solid-stats/design test:e2e -- cls.spec.ts catalog.spec.ts keyboard.spec.ts` green — AsyncBoundary six-state reserved-height GREEN; axe + 44px on the new stories; the toast manager renders the existing leaf.
- `tsc --noEmit` green; no arbitrary values; the full Phase-3 suite green before /gsd-verify-work.
</verification>

<success_criteria>
- SURF-18 ships the six reusable global-state patterns + the AsyncBoundary state→primitive seam, reserving height (CLS = 0) and never color-alone, without rebuilding the Phase-2 primitives.
- The Toast manager (createToaster) owns the lifecycle and renders the existing Toast leaf (no re-expression), with a localized dismiss aria.
- Both are graduated into the barrel; the AsyncBoundary CLS contract is GREEN; all nine Phase-3 requirement IDs are covered.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-07-SUMMARY.md` when done.
</output>
