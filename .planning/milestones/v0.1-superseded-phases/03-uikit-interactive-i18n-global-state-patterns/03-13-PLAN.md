---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 13
type: execute
wave: 2
depends_on: [03-11]
files_modified:
  - packages/design/src/shared/uikit/Dialog/Dialog.tsx
  - packages/design/src/shared/uikit/Dialog/dialog.ts
  - packages/design/src/shared/uikit/Select/Select.tsx
  - packages/design/src/shared/uikit/Select/select.ts
  - packages/design/src/shared/uikit/Select/Select.stories.tsx
  - packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
  - packages/design/src/shared/uikit/_fixtures/strings.ts
  - packages/design/tests/overlay-form-interaction.spec.ts
autonomous: true
gap_closure: true
requirements: [KIT-06, KIT-05]
must_haves:
  truths:
    - "The Dialog close button sits in the content's top-right corner (absolute within the content) and no longer consumes a leading flex row above the title — there is no ~44px dead row above the title."
    - "An empty-options Select renders a first-class empty-state row inside the listbox (message + icon, never blank) instead of ~10px of blank padding."
    - "A clearable Select with a value renders an accessible-named clear control that resets to the placeholder."
    - "The Stepper Playground is interactive: clicking increment/decrement (and keyboard) mutates the displayed value and clamps at min/max — the value is no longer pinned."
    - "The new overlay-form-interaction regression test FAILS on the pre-fix code (dead Dialog top row; blank empty Select; no clear; pinned Stepper) and PASSES after."
    - "pnpm check + pnpm --filter @solid-stats/design test:e2e stay green."
  artifacts:
    - packages/design/src/shared/uikit/Dialog/dialog.ts
    - packages/design/src/shared/uikit/Select/Select.tsx
    - packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
    - packages/design/tests/overlay-form-interaction.spec.ts
  key_links:
    - "Dialog close → absolute top-right within content (no leading row); Select empty → in-listbox empty-state; Select clearable → Ark ClearTrigger; Stepper Playground → useState mutating the value."
---

<objective>
Close GAP-08, GAP-09, GAP-10, GAP-11 (majors) — overlay + form interaction fixes.
GAP-08: Dialog has ~44px+gap of dead space above the title because the 44px `CloseTrigger` (`self-end`) is the FIRST flex child in the `flex-col gap-4` content stack, so it occupies its own row ABOVE the title [src: 03-UAT.md#GAP-08] [src: 03-UAT-VISUAL-FINDINGS.md#GAP-D1].
GAP-09: an empty-options Select renders nothing in the listbox (~10px blank padding, no "ничего нет" message/icon) [src: 03-UAT.md#GAP-09].
GAP-10: a selected Select value cannot be cleared — no Ark `ClearTrigger` is rendered [src: 03-UAT.md#GAP-10].
GAP-11: the Stepper Playground is not interactive — `value` is a pinned arg with no `onValueChange`/state, so inc/dec/keyboard are no-ops [src: 03-UAT.md#GAP-11] [src: 03-UAT-VISUAL-FINDINGS.md#GAP-ST1].

Purpose: remove the Dialog dead row, give the empty Select an in-listbox empty state, add a Select clear control, and make the Stepper Playground actually exercisable.

Output: the four fixes + a regression test that fails on the pre-fix behaviors.
DO-NOT-TOUCH (verified working): Select typeahead, Select menu growing wider than the trigger, Select trigger placeholder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-CONTEXT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md

# CONVENTIONS — SKILL.md INDEX + named references
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/tests.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
@.agents/skills/solidstats-shared-testing-standards/SKILL.md

# Source the executor edits / depends on
@packages/design/src/shared/uikit/Dialog/Dialog.tsx
@packages/design/src/shared/uikit/Dialog/dialog.ts
@packages/design/src/shared/uikit/Select/Select.tsx
@packages/design/src/shared/uikit/Select/select.ts
@packages/design/src/shared/uikit/Select/Select.stories.tsx
@packages/design/src/shared/uikit/Stepper/Stepper.tsx
@packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
@packages/design/src/shared/uikit/_fixtures/strings.ts
</context>

<premises>
  - claim: the Dialog CloseTrigger is the first flex child (self-end) in the flex-col gap-4 content, occupying a leading row above the title
    src: Dialog.tsx#L80
    verify: grep -n "CloseTrigger\|self-end\|flex-col gap-4\|Title" packages/design/src/shared/uikit/Dialog/Dialog.tsx packages/design/src/shared/uikit/Dialog/dialog.ts
  - claim: the empty-options Select content renders no empty-state node (the listbox maps collection.items, which is empty)
    src: Select.tsx#L91
    verify: grep -n "collection.items.map\|ArkSelect.Content\|empty" packages/design/src/shared/uikit/Select/Select.tsx
  - claim: the Select renders only the trigger — no Ark ClearTrigger
    src: Select.tsx#L82
    verify: grep -n "ClearTrigger\|ArkSelect.Control" packages/design/src/shared/uikit/Select/Select.tsx
  - claim: the Stepper Playground passes `value` from the arg with no onValueChange/state, so inc/dec are no-ops
    src: Stepper.stories.tsx#L70
    verify: grep -n "Playground\|onValueChange\|useState\|value=" packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
  - claim: the Select menu correctly grows wider than the trigger (min-w + max-w:none) and typeahead works — these are DO-NOT-TOUCH
    src: 03-UAT.md#Gaps (VERIFIED-WORKING)
    verify: grep -n "min-w-(--reference-width)\|max-w" packages/design/src/shared/uikit/Select/select.ts
</premises>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — overlay+form interaction regression spec (Dialog top, Select empty/clear, Stepper interactive)</name>
  <files>packages/design/tests/overlay-form-interaction.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Dialog/Dialog.stories.tsx — a forced-open / playground Dialog story; the title node + the content node to measure the gap between the content top and the title top.
    - packages/design/src/shared/uikit/Select/Select.stories.tsx — the `empty` data-volume cell (NO_OPTIONS, updated in Task 3 to expose the empty-state node) and the NEW `clearable` cell (created in Task 4); the listbox content hooks (`data-select`, the Ark content) to assert the empty-state row + the clear control. The test targets these specific cells, which 03-13 creates — they exist by the time the test runs GREEN.
    - packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx — the Playground; the increment trigger's accessible name (`stepperIncrement` → "Увеличить"/"Increase") and the input/spinbutton to read the value.
    - @.agents/skills/solidstats-frontend-react-tests/SKILL.md — Playwright-against-Ladle; drive the interactive Playground (real clicks/keys), assert rendered DOM + measured geometry.
  </read_first>
  <behavior>
    - Test 1 (Dialog no dead top row): open the Dialog; measure the title's top relative to the content's top — assert the title sits at the content's top padding (no ~44px close-button row above it). On pre-fix code the close occupies a leading row → title pushed down → fails RED.
    - Test 2 (Select empty state): open the empty-options Select; assert the listbox contains a visible empty-state node (a message + icon, non-empty innerText), not blank padding. On pre-fix code optionCount 0 / innerText "" → fails RED.
    - Test 3 (Select clear): on a clearable Select with a value, assert an accessible-named clear control exists and clicking it resets the displayed value to the placeholder. On pre-fix code there is no ClearTrigger → fails RED.
    - Test 4 (Stepper interactive): on the Stepper Playground, read the value, click increment (by its accessible name), assert the value increased; click past max asserts it clamps. On pre-fix code the value stays pinned → fails RED.
  </behavior>
  <action>
    Create the Playwright-against-Ladle regression for GAP-08/09/10/11. Drive the Dialog/Select/Stepper Playground (and the Select empty cell) and assert: (1) no dead row above the Dialog title (title top ≈ content top padding); (2) the empty Select listbox shows a real empty-state node; (3) a clearable Select renders + honors a clear control; (4) the Stepper Playground value mutates on increment and clamps at max. [src: 03-UAT.md#GAP-08] [src: 03-UAT.md#GAP-09] [src: 03-UAT.md#GAP-10] [src: 03-UAT.md#GAP-11]

    This MUST FAIL on pre-fix code across all four. Run RED before Tasks 2–5. No `.skip`. Commit RED: `test(03-13): add failing overlay+form interaction regression`.

    No code fences in this action — describe the assertions in prose. Do NOT add assertions touching Select typeahead / menu-wider-than-trigger / trigger placeholder (DO-NOT-TOUCH).
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test:e2e -- overlay-form-interaction.spec.ts</automated>
  </verify>
  <done>The spec runs (no `.skip`) and FAILS on the current code across the four behaviors.</done>
  <acceptance_criteria>
    - All four assertions fail RED pre-fix.
    - No assertion regresses a DO-NOT-TOUCH behavior.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — Dialog close button to top-right corner (no leading row)</name>
  <files>packages/design/src/shared/uikit/Dialog/Dialog.tsx, packages/design/src/shared/uikit/Dialog/dialog.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Dialog/Dialog.tsx — the `CloseTrigger` rendered first in the content; move it to be absolutely positioned in the content's top-right, with the title first in the flow.
    - packages/design/src/shared/uikit/Dialog/dialog.ts — the `close` slot (`self-end`) + the `content` slot (`flex-col gap-4 p-6`); NOTE this file's `content`/`backdrop` motion classes were changed by 03-11 (depends_on: [03-11]) — build on the post-03-11 recipe and keep the motion policy intact.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md — tokens only; z within content; the close stays ≥44px with the canonical ring.
  </read_first>
  <action>
    Position the Dialog `CloseTrigger` in the content's top-right corner (absolute within the `relative` content) so it no longer consumes a leading flex row above the title [src: 03-UAT.md#GAP-08]. Make the content `relative`, drop the close's `self-end` leading-row placement, and absolutely place the close at the top-right inset (tokens-only spacing matching the `p-6` rhythm). Keep `p-6`/`gap-4` for the title/body stack; keep the close ≥44px with the canonical focus ring; keep the motion policy from 03-11 untouched. Ensure the title is the first in-flow child so it sits at the content's top padding. Tokens only, no arbitrary values. `pnpm check` + e2e stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- overlay-form-interaction.spec.ts</automated>
  </verify>
  <done>No dead row above the Dialog title; the close sits top-right; the Dialog test is GREEN.</done>
  <acceptance_criteria>
    - Title at content top padding; close absolute top-right within content.
    - Close ≥44px + canonical ring preserved; 03-11 motion intact.
    - No arbitrary values; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: GREEN — Select empty-listbox first-class empty state (+ the empty story cell + the selectEmpty key)</name>
  <files>packages/design/src/shared/uikit/Select/Select.tsx, packages/design/src/shared/uikit/Select/select.ts, packages/design/src/shared/uikit/Select/Select.stories.tsx, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Select/Select.tsx — the `ArkSelect.Content` mapping `collection.items`; render an empty-state row when `options.length === 0`. Add an `emptyText` string prop (resolved by the story — slice stays i18n-free).
    - packages/design/src/shared/uikit/Select/select.ts — add an `empty` slot (message + icon row, muted, ≥ comfortable padding) using tokens; do NOT alter the menu width behavior (DO-NOT-TOUCH: `min-w-(--reference-width)` + grow).
    - packages/design/src/shared/uikit/Select/Select.stories.tsx — the existing `empty` data-volume cell already passes NO_OPTIONS but renders nothing useful; this plan makes that cell expose the new empty-state node so the Task-1 test has a real story to drive. The empty story cell is CREATED/updated HERE in 03-13 (not deferred).
    - packages/design/src/shared/uikit/_fixtures/strings.ts — there is no empty-listbox copy yet; the `selectEmpty` key is REGISTERED HERE in 03-13 (RU primary + EN parity), NOT in 03-14. 03-14 does not touch this key.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md — never blank; pair the empty message with an icon (never color-alone); @.agents/.../errors.md — never a blank screen.
  </read_first>
  <action>
    Render a first-class empty-state row inside the Select listbox when `options` is empty — a message + a Lucide icon, never blank [src: 03-UAT.md#GAP-09]. Add an `emptyText` string prop (the slice stays i18n-free; the story resolves it). When `options.length === 0`, render the `empty` slot inside `ArkSelect.Content` instead of the empty `items.map`. Add the `empty` slot to `select.ts` with tokens only.

    Register the bilingual `selectEmpty` key in `strings.ts` HERE in 03-13 (RU «Ничего не найдено» / EN "Nothing found", or similar — RU primary, EN parity). Update the existing `empty` cell in `Select.stories.tsx` (the NO_OPTIONS cell) to pass `emptyText={i18n._({ id: "selectEmpty" })}` and to forced-open the empty Select so the Task-1 test can assert the rendered empty-state node. Do NOT touch the menu-wider-than-trigger behavior or typeahead or the trigger placeholder (DO-NOT-TOUCH). `pnpm check` + e2e stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- overlay-form-interaction.spec.ts</automated>
  </verify>
  <done>An empty Select shows a message+icon empty state inside the listbox; the empty-state test is GREEN.</done>
  <acceptance_criteria>
    - Empty listbox renders a non-blank message+icon row (never color-alone).
    - emptyText is a resolved prop (slice i18n-free); `selectEmpty` key added RU+EN.
    - Menu width / typeahead / placeholder untouched; no arbitrary values; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: GREEN — Select clear control (accessible-named, resets to placeholder) + the clearable story cell + the selectClear key</name>
  <files>packages/design/src/shared/uikit/Select/Select.tsx, packages/design/src/shared/uikit/Select/select.ts, packages/design/src/shared/uikit/Select/Select.stories.tsx, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Select/Select.tsx — the `ArkSelect.Control` holding the trigger; render Ark's `ClearTrigger` (with an accessible name) when a value is selected and the field is `clearable`. Add a `clearable?: boolean` prop + a `clearAria` string prop (slice i18n-free).
    - packages/design/src/shared/uikit/Select/select.ts — add a `clearTrigger` slot styled like the icon-only control (≥44px-ish or inline within the trigger, the canonical ring), tokens only; the X icon paired with the accessible name.
    - packages/design/src/shared/uikit/Select/Select.stories.tsx — there is NO clearable cell today; this task CREATES a `clearable` story cell (a clearable Select with a value) HERE in 03-13 so the GAP-10 Task-1 test has a real story to drive. Do this in 03-13, not 03-14.
    - packages/design/src/shared/uikit/_fixtures/strings.ts — the `selectClear` key is REGISTERED HERE in 03-13 (RU «Очистить» / EN "Clear", RU primary + EN parity) for the clear control's accessible name; the story passes it. 03-14 does not touch this key.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md — icon-only clear control needs an accessible name.
  </read_first>
  <action>
    Render Ark's `ClearTrigger` when a value is selected and the field is clearable [src: 03-UAT.md#GAP-10]. Add a `clearable?: boolean` prop (decide the default — recommend opt-in `clearable` so existing required Selects keep no clear; document the choice in the SUMMARY) and a `clearAria` string prop the story resolves (slice i18n-free). Render the Ark `ClearTrigger` with the `X` icon + `aria-label={clearAria}` inside the control, shown only when a value exists. Add the `clearTrigger` slot to `select.ts` (tokens only, canonical ring).

    Register the `selectClear` key in `strings.ts` HERE in 03-13 (RU+EN). CREATE a `clearable` cell in `Select.stories.tsx` — a `Select` with a preset `value` + `clearable` + `clearAria={i18n._({ id: "selectClear" })}` — that the Task-1 test drives to assert the clear control exists and resets to the placeholder. Keep typeahead / menu width / placeholder untouched. `pnpm check` + e2e stay green.

    (The Stepper.stories.tsx edit lives in Task 5 — it shares this plan's file set; do its content in Task 5.)
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- overlay-form-interaction.spec.ts</automated>
  </verify>
  <done>A clearable Select with a value renders an accessible-named clear control that resets to the placeholder; the clear test is GREEN.</done>
  <acceptance_criteria>
    - ClearTrigger rendered (accessible name) when clearable + value present; clears to placeholder.
    - clearable default documented; clearAria is a resolved prop (slice i18n-free); `selectClear` key added RU+EN.
    - Typeahead / menu width / placeholder untouched; no arbitrary values; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: GREEN — interactive Stepper Playground (useState; inc/dec/keyboard mutate + clamp)</name>
  <files>packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx — the Playground passing `value` from the arg with no state; the `incrementAria`/`decrementAria` resolution.
    - packages/design/src/shared/uikit/Stepper/Stepper.tsx — the controlled `value`/`onValueChange` contract (Ark NumberInput owns parse/clamp) the Playground must wire to real state.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md — useState + memoized handler; the story may own ephemeral demo state (this is a story, not a multi-field form, so useState is fine here — the TanStack Form rule is for real steppers, not catalog demos).
  </read_first>
  <action>
    Make the Stepper Playground interactive [src: 03-UAT.md#GAP-11]: give it real `useState` seeded from the arg, pass `value` + an `onValueChange` that updates state, so inc/dec/keyboard mutate the DISPLAYED value and Ark's min/max clamp is exercised. Keep `min`/`max` (0/99) so the clamp test passes. Keep the `incrementAria`/`decrementAria` resolution + the Field wrapper. This is a catalog Playground (ephemeral demo state) — `useState` is appropriate here (the TanStack-Form rule governs real multi-field forms, not a story demo). `pnpm check` + e2e stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- overlay-form-interaction.spec.ts</automated>
  </verify>
  <done>Clicking increment/decrement (and keyboard) changes the Playground value and clamps at min/max; the Stepper test is GREEN; `pnpm check` exits 0.</done>
  <acceptance_criteria>
    - The Playground value mutates on inc/dec/keyboard and clamps at 0/99.
    - aria names + Field wrapper preserved; `pnpm check` green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational primitive/recipe/story + test changes; no untrusted input, no network, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13-01 | Repudiation / a11y integrity | Select clear + empty state, Dialog close | low | mitigate | Clear control + empty state carry accessible names/icons (never color-alone / never unnamed); Dialog close keeps its accessible name + ≥44px ring. No package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm check` green.
- `pnpm --filter @solid-stats/design test:e2e` green (incl. overlay-form-interaction spec + the existing Dialog/Select/Stepper keyboard.spec blocks — confirm no regression to DO-NOT-TOUCH behaviors).
</verification>

<success_criteria>
GAP-08/09/10/11 closed: Dialog has no dead top row, empty Select shows an in-listbox empty state, Selects can be cleared, and the Stepper Playground is exercisable — guarded by a test that fails on the pre-fix behaviors, with no regression to verified-working Select features.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-13-SUMMARY.md` when done.
</output>
