---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
  - packages/design/tests/cls.spec.ts
  - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
autonomous: true
gap_closure: true
requirements: [SURF-18, QUAL-04]
must_haves:
  truths:
    - "The AsyncBoundary CLS test measures the ROUTED `[data-async-boundary]` primitive within the content-region group (loading/empty/error/ready), NOT the `h-64` cage — so it can no longer pass trivially."
    - "loading and ready content-region states reserve byte-for-byte equal height (the readyContent reuses the real KIT-02 Table/Skeleton geometry, closing the 1px hairline gap)."
    - "The rewritten test FAILS on the pre-fix code (the h-64-cage false-green is removed AND the 1px loading↔ready divergence is asserted) and PASSES after the fix."
    - "The SURF-18 spec's 'all six states same reserved height' claim is re-scoped to the content-region states; the status banners (offline/reconnecting/stale, 40px) are documented as a separate block role, not height-compared."
    - "pnpm check + pnpm --filter @solid-stats/design test:e2e stay green."
  artifacts:
    - packages/design/tests/cls.spec.ts
    - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
  key_links:
    - "readyContent() → the real KIT-02 Table/Skeleton geometry → loading≡ready box height → the CLS test measuring the routed primitive."
---

<objective>
Close GAP-03 (blocker): AsyncBoundary CLS is not 0 across the content-region states AND the `cls.spec.ts` AsyncBoundary block is FALSE-GREEN. Measured intrinsic content-region heights: LoadingTable 203 / Empty 222.5 / Error 234 / ReadyTable 202 — loading↔ready off by 1px (the framed Skeleton card hairline vs the story's hand-rolled ready table). The `Cls` story cages every state in `h-64` and the test measures the cage → equality passes trivially. The status BANNERS (offline/reconnecting/stale = 40px) are a DIFFERENT block role and must NOT be height-compared — the SURF-18 spec line "all six states the same reserved height as ready" over-claimed and is itself a defect [src: 03-UAT.md#GAP-03] [src: 03-UAT-VISUAL-FINDINGS.md#07/18].

Purpose: make the CLS proof real (measure the routed primitive, not the cage), make loading≡ready byte-for-byte, and re-scope the spec so the test asserts a claim that is actually true.

Output: a rewritten AsyncBoundary CLS test that fails on pre-fix code, a readyContent that reuses real KIT-02 geometry, and a corrected SURF-18 spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-CONTEXT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md

# CONVENTIONS — SKILL.md INDEX + named references (subagent reads ONLY named files)
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/tests.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
@.agents/skills/solidstats-shared-testing-standards/SKILL.md

# Source the executor edits / depends on
@packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
@packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
@packages/design/tests/cls.spec.ts
@packages/design/src/shared/uikit/Skeleton/Skeleton.tsx
@packages/design/src/shared/uikit/Table/Table.tsx
</context>

<premises>
  - claim: the AsyncBoundary CLS block measures `[data-async-cell=<state>]` (the h-64 cage div), not the routed `[data-async-boundary]` primitive, so equality is trivially true
    src: cls.spec.ts#L211
    verify: grep -n "data-async-cell\|h-64\|data-async-boundary" packages/design/tests/cls.spec.ts packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
  - claim: readyContent() is a hand-rolled table (h-11 header + h-13 rows), NOT the real KIT-02 Table/Skeleton geometry — hence the 1px loading↔ready divergence
    src: AsyncBoundary.stories.tsx#L66
    verify: grep -n "readyContent\|h-11\|h-13\|Skeleton\|Table" packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx
  - claim: AsyncBoundary routes loading→Skeleton variant="table" and ready→children; it adds no height of its own (the leaf is correct)
    src: AsyncBoundary.tsx#L99
    verify: grep -n "Skeleton\|data-async-boundary\|variant=\"table\"" packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
  - claim: the Skeleton table variant + the KIT-02 Table share a colgroup/density geometry already proven CLS-equal in the Table/Skeleton cls blocks
    src: cls.spec.ts#L77
    verify: grep -n "data-cls-table\|data-skeleton=\"table\"\|tableViewportHeight" packages/design/tests/cls.spec.ts
</premises>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — rewrite the AsyncBoundary CLS test to measure the routed primitive across content-region states</name>
  <files>packages/design/tests/cls.spec.ts</files>
  <read_first>
    - packages/design/tests/cls.spec.ts — the existing `AsyncBoundary CLS = 0` block (the false-green one measuring `[data-async-cell]`) AND the working `Table CLS = 0` block (the template: it measures `[data-table-card]` and asserts both height AND width equality plus a no-scroll overflow check). Mirror the Table block's measure-the-primitive shape.
    - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx — confirm each branch renders `<div data-async-boundary={kind}>` around the routed primitive (the real locator target).
    - @.agents/skills/solidstats-frontend-react-tests/SKILL.md — visibility is a paint assertion; box dimensions are for hit-area/geometry only. Here we DO compare box heights of the routed primitive (a layout geometry assertion), which is the legitimate use.
    - @.agents/skills/solidstats-shared-testing-standards/SKILL.md §G — a strong oracle fails on a real regression; the rewritten oracle must reject the 1px loading↔ready divergence.
  </read_first>
  <behavior>
    - Test 1 (loading≡ready): measure `[data-async-boundary="loading"]` and `[data-async-boundary="ready"]` (the ROUTED primitive, not the cage) and assert their box heights are EXACTLY equal — this is the 1px the human caught and MUST fail on the pre-fix story.
    - Test 2 (content-region group): assert the content-region states (loading/empty/error/ready) each render their routed primitive and (per the re-scoped spec decision recorded in Task 3) either all reserve the content-region min-height, OR are documented as content-sized — the test asserts the FINAL agreed contract, not the over-claim. The decision (empty/error reserve the table region min-height vs. intentionally content-sized) is recorded in Task 3 and the test encodes whichever was chosen.
    - Test 3 (banners excluded): the status banners (offline/reconnecting/stale) are NOT height-compared against the content region — the test does not assert banner==ready (that was the over-claim); if it touches them it asserts only the banner-family invariant (each renders + reserves its own h-10, never color-alone, mirroring the DataTrustBanner CLS block).
  </behavior>
  <action>
    Rewrite the `AsyncBoundary CLS = 0` describe block in `cls.spec.ts` to measure the routed `[data-async-boundary="<state>"]` primitive WITHIN the content-region group, removing the `h-64`-cage `[data-async-cell]` measurement that made it false-green [src: 03-UAT.md#GAP-03]. The new oracle:
    - asserts loading-vs-ready box-height equality EXACTLY (the 1px regression);
    - encodes the content-region contract decided in Task 3 (loading/empty/error/ready), NOT the discredited "all six states same height as ready" claim;
    - does NOT compare the offline/reconnecting/stale banners against the content region (different block role).

    The story Task 2 produces must expose the routed primitive per content-region state in a way the locator can reach (the `data-async-boundary` attribute already exists on each branch; ensure the Cls story renders the loading and ready content-region states without the equalizing `h-64` cage so the real intrinsic heights are measured).

    This test MUST FAIL on the pre-fix story+leaf: measuring the real primitives surfaces the loading(203)↔ready(202) 1px divergence, so Test 1 fails RED. Run it RED before Task 2. No `.skip`. Commit RED: `test(03-09): rewrite AsyncBoundary CLS to measure routed primitive (was false-green)`.

    Call out in the SUMMARY that this finding was a FALSE-GREEN test, fixed at the test level — not only the code.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test:e2e -- cls.spec.ts</automated>
  </verify>
  <done>The rewritten AsyncBoundary CLS block runs (no `.skip`) and FAILS on the current story/leaf with the loading↔ready 1px divergence — proving the old test was false-green.</done>
  <acceptance_criteria>
    - The test measures `[data-async-boundary]` (routed primitive), not `[data-async-cell]` (the cage).
    - The loading↔ready exact-height assertion fails RED before the fix.
    - Banners are not height-compared to the content region.
    - The other cls.spec blocks (DataTrustBanner/Skeleton/Table/StatTile/Sparkline) are untouched and still green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — readyContent reuses the real KIT-02 Table/Skeleton geometry (loading≡ready)</name>
  <files>packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx, packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.stories.tsx — the hand-rolled `readyContent()` (h-11 header + h-13 rows) and the `Cls` story (the `h-64` cage to remove from the content-region cells).
    - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx + packages/design/src/shared/uikit/Table/Table.tsx — the real table geometry (the same colgroup/density/ROW_H the Skeleton table variant reserves) so readyContent matches the loading Skeleton byte-for-byte. The Table/Skeleton CLS blocks already prove these two are height-equal.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md — tokens only, no arbitrary values; the framed-card hairline must match the Skeleton's.
  </read_first>
  <action>
    Make the `Cls` story's `ready` content reuse the SAME geometry the `loading` Skeleton reserves so loading≡ready byte-for-byte [src: 03-UAT.md#GAP-03] [src: 03-UAT-VISUAL-FINDINGS.md#GAP-A1]. Concretely: render the ready state from the real KIT-02 `Table` (the same columns/density/visibleRows the loading `Skeleton variant="table"` uses), rather than the hand-rolled `h-11/h-13` table — so the framed-card hairline that caused the 1px gap is identical on both sides. The loading state already routes to `Skeleton variant="table"`; keep that.

    Remove the equalizing `h-64` cage from the content-region cells in the `Cls` story so the test measures real intrinsic heights of the routed `[data-async-boundary]` primitive. Keep each cell's `data-async-boundary`/`data-async-cell` hook only as needed by the rewritten test's locators (coordinate with Task 1). Keep the i18n-free boundary (copy resolved in the story via `i18n._`).

    For the empty/error content-region states: implement whichever content-region contract Task 3 records (reserve the content-region table min-height for CLS=0, OR document them as intentionally content-sized). Do NOT touch the AsyncBoundary leaf routing (it correctly adds no height); only the story's ready geometry + cage change. If a `data-async-boundary` hook or a tiny prop is genuinely needed to expose the primitive for measurement, add it minimally to `AsyncBoundary.tsx` without changing routing semantics.

    Keep `pnpm check` green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- cls.spec.ts</automated>
  </verify>
  <done>The rewritten AsyncBoundary CLS test is GREEN: loading≡ready exactly; the content-region contract holds; `pnpm check` exits 0.</done>
  <acceptance_criteria>
    - readyContent renders from the real KIT-02 Table/Skeleton geometry (no hand-rolled h-11/h-13 table).
    - The `h-64` cage no longer equalizes the content-region cells under measurement.
    - loading↔ready box heights are exactly equal.
    - No arbitrary Tailwind values; tokens only; AsyncBoundary routing semantics unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Re-scope the SURF-18 spec — content-region states vs. banner block role</name>
  <files>.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md — find the SURF-18 line claiming "all six states the same reserved height as ready" (the over-claim flagged by GAP-03).
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md#07/18 — the corrected framing: content-region states (loading/empty/error/ready) are the CLS-equality set; the status banners (offline/reconnecting/stale, 40px) are a separate block role.
  </read_first>
  <action>
    Edit the SURF-18 section of `03-UI-SPEC.md` to re-scope the CLS claim [src: 03-UAT.md#GAP-03]. Replace "all six states the same reserved height as ready" with the corrected, true contract:
    - The CONTENT-REGION states (loading / empty / error / ready) are the CLS-equality set — they reserve the content region so swapping among them shifts nothing.
    - The STATUS BANNERS (offline / reconnecting / stale) are a separate block role (a 40px banner above/around content), NOT height-compared to the content region; their own CLS guarantee is the DataTrustBanner reserved-height invariant.
    Record the empty/error decision explicitly (do they reserve the content-region min-height for CLS=0, or are they intentionally content-sized?) — this is the contract Task 1's oracle and Task 2's story both encode. Use English (repo docs are English).

    Use Edit (scoped) on the spec — do NOT rewrite the whole file.
  </action>
  <verify>
    <automated>grep -n "content-region\|banner" .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md</automated>
  </verify>
  <done>The spec no longer claims "all six states same height"; it documents the content-region equality set and the banner block role separately, with the empty/error decision recorded.</done>
  <acceptance_criteria>
    - The over-claim is removed/replaced.
    - The content-region set and the banner block role are distinct in the spec.
    - The empty/error reservation decision is stated and matches Task 1/Task 2.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational story + test + doc only; no untrusted input, no network, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-01 | Tampering | test oracle integrity | low | mitigate | The rewritten oracle measures the routed primitive and fails on the 1px regression, so a future false-green cannot recur. No package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm check` green.
- `pnpm --filter @solid-stats/design test:e2e` green (all cls.spec blocks, incl. the rewritten AsyncBoundary one).
- The SUMMARY explicitly notes GAP-03 was a false-green test fixed at the test level + the code + the spec.
</verification>

<success_criteria>
GAP-03 closed: the CLS proof measures the real primitive, loading≡ready byte-for-byte, banners are correctly excluded, and the spec states a true contract — guarded by a test that fails on the pre-fix code.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-09-SUMMARY.md` when done.
</output>
