---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 14
type: execute
wave: 3
depends_on: [03-09, 03-11, 03-12, 03-13]
files_modified:
  - packages/design/src/shared/uikit/Input/Input.stories.tsx
  - packages/design/src/shared/uikit/Select/Select.stories.tsx
  - packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
  - packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx
  - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
  - packages/design/src/shared/uikit/FileUpload/fileUpload.ts
  - packages/design/src/shared/uikit/Select/select.ts
  - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
  - packages/design/src/shared/uikit/ToastManager/ToastManager.tsx
  - packages/design/src/shared/uikit/Tabs/Tabs.tsx
  - packages/design/src/shared/uikit/Tabs/tabs.ts
  - packages/design/src/shared/uikit/_fixtures/strings.ts
  - packages/design/tests/form-layout-sweep.spec.ts
autonomous: true
gap_closure: true
requirements: [QUAL-02, KIT-05, QUAL-03, KIT-06, SURF-18]
must_haves:
  truths:
    - "The four KIT-05 story 360-demo wrappers use `w-full max-w-90` (not `w-90`) so 360 + p-4 padding ≤ 360 viewport — no horizontal overflow at the mobile floor."
    - "FileUpload rows are width-bounded (truncate engages), the itemGroup caps height with overflow-y-auto, and the 'many' fixture is ~30 files (the unbounded-list defect surfaces in the catalog)."
    - "The AsyncBoundary loading state announces via aria-live/role=status; ToastManager guards against an empty title (no textless icon-only toast); the Tabs Indicator slot is driven or removed (no dead slot)."
    - "GAP-17 is RESERVE-ONLY: the FU7 four-state i18n vocabulary keys are added (not consumed) and the dead itemStatus slot is reserved/wired for v1.0 — NO lightbox/sync is built."
    - "GAP-15: the Select listbox has a viewport-aware max-width cap so a long option never overflows the 360 floor (the grow-wider-than-trigger behavior is preserved)."
    - "The new sweep regression tests FAIL on the pre-fix code (360 overflow; unbounded FileUpload list; missing loading aria-live; Select 360 overflow) and PASS after; pnpm check + e2e stay green."
  artifacts:
    - packages/design/tests/form-layout-sweep.spec.ts
    - packages/design/src/shared/uikit/_fixtures/strings.ts
  key_links:
    - "story wrappers w-full max-w-90 → no 360 overflow; FileUpload itemGroup overflow-y-auto + ~30-file fixture → bounded list; AsyncBoundary loading → aria-live; Tabs Indicator → driven or removed; FU7 i18n keys + itemStatus slot reserved (not built)."
---

<objective>
Close the remaining minors + the a11y/dead-code sweep, and RESERVE (not build) the v1.0 FileUpload items.
GAP-12: Input/Select/Stepper/FileUpload story 360-demo wrappers overflow (`w-90` (360) inside the story root `p-4` (32) = 392px at a 360 viewport); the controls are fluid `w-full` — fix the wrapper class across the four KIT-05 stories [src: 03-UAT.md#GAP-12].
GAP-13: FileUpload rows have no width bound (truncate not engaging), `itemGroup` has no capped scroll-in-card, the "many" fixture is only 3 files (masks the unbounded-list defect), and the `itemStatus` slot is dead code [src: 03-UAT.md#GAP-13].
GAP-15: the Select long-option menu has no viewport max-width cap at 360 [src: 03-UAT.md#GAP-15].
GAP-16: AsyncBoundary loading wraps an aria-hidden Skeleton with no aria-live/role=status; ToastManager coerces a non-string title to "" (icon-only textless toast); the Tabs Indicator slot is rendered but unsized (dead slot) [src: 03-UAT.md#GAP-16].
GAP-17: RESERVE the FU7 four-state i18n vocabulary + reuse the `itemStatus` slot for v1.0 — DO NOT build the lightbox/sync wiring [src: 03-UAT.md#GAP-17].

(GAP-14 — the caption-color DESIGN.md token decision + gen-theme re-export — was split into its own plan 03-15 to isolate the design-system/gen-theme workflow; it is NOT in this plan.)

Purpose: fix the layout/a11y/dead-code minors as a class and reserve (not build) the v1.0 hooks.

Output: the minors fixed + a sweep regression test failing on the pre-fix issues + the GAP-17 reserve-only additions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-CONTEXT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md

# CONVENTIONS — SKILL.md INDEX + named references
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/localization.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/tests.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
@.agents/skills/solidstats-shared-testing-standards/SKILL.md
# DESIGN — SKILL.md INDEX + the design-system reference (token decision + gen-theme workflow)
@.agents/skills/solidstats-frontend-react-design/SKILL.md
@.agents/skills/solidstats-frontend-react-design/references/design-system.md

# Source the executor edits / depends on
@packages/design/src/shared/uikit/Input/Input.stories.tsx
@packages/design/src/shared/uikit/Select/Select.stories.tsx
@packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx
@packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx
@packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
@packages/design/src/shared/uikit/FileUpload/fileUpload.ts
@packages/design/src/shared/uikit/Select/select.ts
@packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx
@packages/design/src/shared/uikit/ToastManager/ToastManager.tsx
@packages/design/src/shared/uikit/Tabs/Tabs.tsx
@packages/design/src/shared/uikit/Tabs/tabs.ts
@packages/design/src/shared/uikit/Field/field.ts
@packages/design/src/shared/uikit/_fixtures/strings.ts
@DESIGN.md
@packages/design/tests/responsive.spec.ts
@scripts/gen-theme.mjs
</context>

<premises>
  - claim: the four KIT-05 stories use a `w-90` (360) demo wrapper inside the story root `p-4`, totalling 392px at a 360 viewport; the controls themselves are w-full fluid
    src: 03-UAT-VISUAL-FINDINGS.md#GAP-I1
    verify: grep -rn "w-90" packages/design/src/shared/uikit/Input/Input.stories.tsx packages/design/src/shared/uikit/Select/Select.stories.tsx packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx
  - claim: the FileUpload itemGroup has no capped scroll, the rows are not w-full bounded, the MANY_FILES fixture is 3 files, and itemStatus is a dead slot
    src: fileUpload.ts#L116
    verify: grep -n "itemGroup\|itemStatus\|MANY_FILES\|overflow-y" packages/design/src/shared/uikit/FileUpload/fileUpload.ts packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx
  - claim: AsyncBoundary loading routes to an aria-hidden Skeleton with no aria-live/role=status; the Tabs Indicator slot is rendered but unsized
    src: 03-UAT.md#GAP-16
    verify: grep -n "aria-live\|role=\"status\"\|Skeleton\|Indicator" packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx packages/design/src/shared/uikit/Tabs/Tabs.tsx packages/design/src/shared/uikit/Tabs/tabs.ts
  - claim: ToastManager.message() coerces a non-string title to "" (a textless icon-only toast is possible)
    src: ToastManager.tsx#L55
    verify: grep -n "function message\|title\|return \"\"" packages/design/src/shared/uikit/ToastManager/ToastManager.tsx
  - claim: DESIGN.md has a caption role at text-subtle and label/caption use text-muted; theme.css is generated by gen-theme.mjs and is never hand-edited
    src: DESIGN.md#L168
    verify: grep -n "caption\|text-muted\|text-subtle" DESIGN.md
  - claim: the Select content has max-h-64 but no max-width cap; the grow-wider-than-trigger (min-w + max-w:none) is DO-NOT-TOUCH and must be preserved
    src: select.ts#L36
    verify: grep -n "max-h-64\|min-w-(--reference-width)\|max-w" packages/design/src/shared/uikit/Select/select.ts
</premises>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — sweep regression spec (360 overflow; bounded FileUpload list; loading aria-live; Select 360 cap)</name>
  <files>packages/design/tests/form-layout-sweep.spec.ts</files>
  <read_first>
    - packages/design/tests/responsive.spec.ts — the `noHorizontalScroll` helper (scrollWidth ≤ clientWidth) + the 360-width project; mirror it to assert the KIT-05 stories do not overflow at 360.
    - packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx — the `many` cell; after the fix it should hold ~30 files in a capped, scrollable itemGroup (assert the itemGroup scrolls internally without growing the page).
    - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx — the loading branch; assert an aria-live/role=status announcement region accompanies the loading skeleton.
    - packages/design/src/shared/uikit/Select/Select.stories.tsx — the long-option cell; assert the open listbox width ≤ the 360 viewport (no horizontal overflow) while still wider than the trigger.
    - @.agents/skills/solidstats-frontend-react-tests/SKILL.md — Playwright-against-Ladle; mobile-360 viewport; assert geometry + computed a11y attributes.
  </read_first>
  <behavior>
    - Test 1 (360 no overflow): at 360 viewport, load each of the four KIT-05 stories and assert the demo wrapper / story root does not scroll horizontally (scrollWidth ≤ clientWidth). On pre-fix code the `w-90`+`p-4`=392 wrapper overflows → fails RED.
    - Test 2 (FileUpload bounded list): on the `many` FileUpload cell (~30 files after fix), assert the itemGroup caps its height and scrolls internally (its scrollHeight may exceed clientHeight WITHIN the capped box) without the row/page overflowing horizontally; assert a file-name row truncates (no horizontal row overflow). On pre-fix code (3 files, unbounded) the bounded-list assertion fails RED.
    - Test 3 (loading announced): on an AsyncBoundary loading story, assert an aria-live (polite) / role=status region is present alongside the skeleton. On pre-fix code there is none → fails RED.
    - Test 4 (Select 360 cap): at 360, open a long-option Select and assert the listbox width ≤ the viewport width (no horizontal overflow) while still > the trigger width. On pre-fix code (max-w:none) a very long option overflows → fails RED.
  </behavior>
  <action>
    Create the Playwright-against-Ladle sweep regression for GAP-12/13/15/16. Use the mobile-360 viewport (mirror responsive.spec's `noHorizontalScroll`). Assert: (1) no horizontal overflow on the four KIT-05 stories at 360; (2) the FileUpload `many` list is height-capped + scrolls internally + rows truncate; (3) the AsyncBoundary loading state carries an aria-live/role=status region; (4) the open Select listbox is viewport-capped at 360 yet still wider than the trigger. [src: 03-UAT.md#GAP-12] [src: 03-UAT.md#GAP-13] [src: 03-UAT.md#GAP-15] [src: 03-UAT.md#GAP-16]

    These MUST FAIL on pre-fix code. Run RED before Tasks 2–6. No `.skip`. Commit RED: `test(03-14): add failing form-layout + a11y sweep regression`.

    No code fences in this action — describe the assertions in prose. Do NOT assert against the Select grow-wider-than-trigger / typeahead / placeholder behaviors except to confirm the cap preserves the grow (DO-NOT-TOUCH).
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test:e2e -- form-layout-sweep.spec.ts</automated>
  </verify>
  <done>The spec runs (no `.skip`) and FAILS on the current code across the four behaviors.</done>
  <acceptance_criteria>
    - All four assertions fail RED pre-fix.
    - The Select cap test confirms the grow-wider-than-trigger is still honored (not regressed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — GAP-12 fix the 360 demo wrappers across the four KIT-05 stories</name>
  <files>packages/design/src/shared/uikit/Input/Input.stories.tsx, packages/design/src/shared/uikit/Select/Select.stories.tsx, packages/design/src/shared/uikit/Stepper/Stepper.stories.tsx, packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx</files>
  <read_first>
    - each of the four stories — the `w-90` demo block (the RU-longest / 360-floor cell). Change `w-90` → `w-full max-w-90` (or drop `w-90`) so 360 + p-4 ≤ viewport. NOTE: Select.stories + Stepper.stories + FileUpload.stories were also edited by 03-13/03-12/03-10 — build on their post-edit shape.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md — tokens only; `max-w-90` is the stock scale.
  </read_first>
  <action>
    Replace the `w-90` demo wrappers with `w-full max-w-90` (or drop `w-90`) across all four KIT-05 stories so the 360-floor demo block plus the story root padding never exceeds the 360 viewport [src: 03-UAT.md#GAP-12]. Fix the class consistently (the class-level fix, not one story). Tokens only. `pnpm check` + the 360 sweep test stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- form-layout-sweep.spec.ts</automated>
  </verify>
  <done>No KIT-05 story overflows horizontally at the 360 floor; the 360 sweep test is GREEN.</done>
  <acceptance_criteria>
    - All four stories use `w-full max-w-90` (or no `w-90`); no 360 overflow.
    - No arbitrary values; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: GREEN — GAP-13 bound FileUpload rows + cap itemGroup scroll + ~30-file fixture; GAP-17 reserve itemStatus + FU7 keys</name>
  <files>packages/design/src/shared/uikit/FileUpload/FileUpload.tsx, packages/design/src/shared/uikit/FileUpload/fileUpload.ts, packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx, packages/design/src/shared/uikit/_fixtures/strings.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/FileUpload/fileUpload.ts — the `item`/`itemGroup` slots (bound the row to `w-full` so truncate engages; cap `itemGroup` height with `overflow-y-auto` + a max-h token); the DEAD `itemStatus` slot (reserve it / wire it for v1.0 FU7 — do NOT remove if reserving). NOTE: 03-10 (single preview) + 03-12 (Button row controls, deleted itemDeleteTrigger) already edited this file — build on the post-edit shape.
    - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx — the row/itemGroup render; ensure the row is width-bounded; reserve the itemStatus slot usage for v1.0 (a commented seam or an optional unused prop — NOT a built sync state).
    - packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx — bump MANY_FILES to ~30 files so the capped/scroll behavior is demonstrated.
    - packages/design/src/shared/uikit/_fixtures/strings.ts — add the FU7 four-state vocabulary keys (ожидает синхронизации / идёт синхронизация / ожидает после удаления / синхронизировано), RU primary + EN parity, as RESERVED keys (added, not consumed).
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md — capped scroll-in-card; @.agents/.../localization.md — RU/EN parity for the reserved keys.
  </read_first>
  <action>
    GAP-13: bound the FileUpload row to its field width (`w-full` so the existing `truncate` engages) and cap the `itemGroup` height with `overflow-y-auto` + a max-h token so a realistic list scrolls inside a capped window rather than growing unbounded; bump the story `MANY_FILES` fixture to ~30 files so the catalog actually demonstrates the bounded/scrolling list [src: 03-UAT.md#GAP-13]. Tokens only.

    GAP-17 (RESERVE ONLY — do NOT build): add the FU7 four-state server-sync vocabulary to `strings.ts` as RESERVED i18n keys (RU primary + EN parity: «ожидает синхронизации» / «идёт синхронизация» / «ожидает после удаления» / «синхронизировано»), added but NOT consumed; and reserve/wire the dead `itemStatus` slot so v1.0 composes it (keep it as a documented seam — e.g. an optional `itemStatus` render slot or a clearly-commented reservation). Do NOT build the lightbox (FU4) or the sync state model (FU7) — only reserve the vocabulary + slot [src: 03-UAT.md#GAP-17]. Keep the v0.1 thumbnail legible. `pnpm check` + e2e stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- form-layout-sweep.spec.ts</automated>
  </verify>
  <done>FileUpload rows truncate within a width-bounded row, the itemGroup caps + scrolls with ~30 files, the FU7 keys + itemStatus slot are reserved (not built); the bounded-list test is GREEN.</done>
  <acceptance_criteria>
    - Row `w-full`-bounded (truncate engages); itemGroup capped with overflow-y-auto + a max-h token.
    - `MANY_FILES` ~30 files.
    - FU7 keys added RU+EN (reserved, unconsumed); itemStatus slot reserved/wired — NO lightbox/sync built.
    - No arbitrary values; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: GREEN — GAP-15 Select listbox viewport max-width cap (preserve the grow)</name>
  <files>packages/design/src/shared/uikit/Select/select.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Select/select.ts — the `content` slot: `max-h-64 min-w-(--reference-width) ... ` with NO max-width. Add a viewport-aware `max-width` cap using an EXISTING token utility (a `max-w-[...]` arbitrary value is FORBIDDEN — use a stock/`@theme` `max-w-*` token such as `max-w-xs`, or a viewport-relative token utility that keeps it ≤ the mobile floor). The grow-wider-than-trigger (`min-w-(--reference-width)`) is DO-NOT-TOUCH — keep it.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md — tokens only.
  </read_first>
  <action>
    Add a viewport-aware `max-width` cap to the Select listbox `content` slot so a very long option never overflows the 360 floor, while preserving the grow-wider-than-trigger behavior (`min-w-(--reference-width)` stays) [src: 03-UAT.md#GAP-15]. Use an EXISTING token utility (a `max-w-*` from the stock/`@theme` scale, e.g. `max-w-xs`, or a viewport-relative cap that keeps it within the mobile floor). This plan must NOT edit DESIGN.md or re-export theme.css — if (and only if) no existing token expresses the cap, defer to the design-system plan 03-15 to add a token and route the Select cap through it; in that case this plan picks the nearest existing token that still keeps the listbox ≤ the 360 floor. No arbitrary values. `pnpm check` + the Select 360 cap test stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- form-layout-sweep.spec.ts</automated>
  </verify>
  <done>The open Select listbox is viewport-capped at 360 (no overflow) yet still grows wider than the trigger; the Select cap test is GREEN.</done>
  <acceptance_criteria>
    - max-width cap added via a token (no arbitrary value); grow-wider-than-trigger preserved.
    - If a new token was added, theme.css was re-generated (not hand-edited); `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: GREEN — GAP-16 a11y/dead-code sweep (loading aria-live; empty-title toast guard; Tabs Indicator driven or removed)</name>
  <files>packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx, packages/design/src/shared/uikit/ToastManager/ToastManager.tsx, packages/design/src/shared/uikit/Tabs/Tabs.tsx, packages/design/src/shared/uikit/Tabs/tabs.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/AsyncBoundary/AsyncBoundary.tsx — the `loading` branch wrapping the aria-hidden Skeleton; add a polite aria-live / role=status announcement (a visually-hidden status text the consumer resolves, slice i18n-free) so SR users are told content is loading. NOTE: 03-09 reworked this file's story geometry; the leaf routing is intact — add the loading announcement to the leaf's loading branch.
    - packages/design/src/shared/uikit/ToastManager/ToastManager.tsx — the `message()` that coerces a non-string title to ""; guard so a missing/empty title does not produce a textless icon-only toast (drop the toast, or require a non-empty message). NOTE: 03-11 changed Toast/ToastManager motion — build on that.
    - packages/design/src/shared/uikit/Tabs/Tabs.tsx + tabs.ts — the rendered-but-unsized `Indicator` slot; either drive it with a transform-only slide under motion-safe: (on the 03-11 motion token policy) or REMOVE the dead slot. Tabs already pairs cyan with the border underline (never color-alone) — keep that.
    - packages/design/src/shared/uikit/Tabs/tabs.test.ts + tooltip.test.ts — existing unit tests; keep green.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md — live regions without stealing focus; @.agents/.../performance.md — transform/opacity only if driving the Indicator.
  </read_first>
  <action>
    GAP-16 a11y/dead-code sweep [src: 03-UAT.md#GAP-16]:
    - AsyncBoundary loading: add a polite aria-live / role=status announcement region alongside the aria-hidden Skeleton so the load is announced to SR users without stealing focus; the status text arrives as a resolved string prop (slice stays i18n-free). Do NOT change the CLS geometry from 03-09 (the announcement region must not add measured height to the routed primitive — use a visually-hidden status text).
    - ToastManager: guard `message()` / the render so a missing/empty title cannot produce a textless icon-only toast (either require a non-empty message via the typed createToast contract, or skip rendering a toast with no message). Keep the dismissAria contract.
    - Tabs Indicator: either drive the `Indicator` slot with a transform-only slide under `motion-safe:` (reusing the 03-11 `--duration-*`/`--ease-*` token policy) OR remove the dead unsized slot. Pick one; document the choice. Keep the cyan+underline pairing (never color-alone) and the unclipped focus ring.
    Tokens only; transform/opacity only for any motion. `pnpm check` + e2e + the Tabs/Tooltip unit tests stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test -- tabs.test.ts && pnpm --filter @solid-stats/design test:e2e -- form-layout-sweep.spec.ts</automated>
  </verify>
  <done>Loading is announced (aria-live/role=status), an empty-title toast cannot render, and the Tabs Indicator is driven or removed; the loading-announce test is GREEN; unit tests green.</done>
  <acceptance_criteria>
    - AsyncBoundary loading carries a polite aria-live/status without adding measured height (CLS from 03-09 intact).
    - Empty/textless toast guarded.
    - Tabs Indicator driven (transform-only, motion-safe, tokens) or removed (no dead slot).
    - No arbitrary values; `pnpm check` + unit tests green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toast title input | A non-string toast title is coerced; the guard prevents a textless icon-only toast (a11y/integrity), not a security boundary. No untrusted network input, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14-01 | Repudiation / a11y integrity | empty-title toast, loading announcement, reserved i18n keys | low | mitigate | Guard the empty-title toast (no textless control); add a polite loading status (no focus steal); reserved FU7 keys carry RU+EN parity. No package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm check` green.
- `pnpm --filter @solid-stats/design test` green (tabs.test.ts, tooltip.test.ts, fileUpload.test.ts, catalogs.test.ts).
- `pnpm --filter @solid-stats/design test:e2e` green (incl. form-layout-sweep spec).
</verification>

<success_criteria>
GAP-12/13/15/16 closed and GAP-17 reserved (not built): the 360 wrappers no longer overflow, FileUpload lists are bounded + scroll, the Select menu is viewport-capped, the a11y/dead-code items are fixed, and the FU7 vocabulary + itemStatus slot are reserved for v1.0 — guarded by a sweep test that fails on the pre-fix issues. (GAP-14 caption is handled in 03-15.)
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-14-SUMMARY.md` when done.
</output>
