---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 12
type: execute
wave: 2
depends_on: [03-10]
files_modified:
  - packages/design/src/shared/uikit/Button/control.ts
  - packages/design/src/shared/uikit/Button/Button.tsx
  - packages/design/src/shared/uikit/Button/Button.stories.tsx
  - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
  - packages/design/src/shared/uikit/FileUpload/fileUpload.ts
  - packages/design/src/shared/uikit/Field/Field.tsx
  - packages/design/src/shared/uikit/Field/field.ts
  - packages/design/tests/form-affordances.spec.ts
autonomous: true
gap_closure: true
requirements: [KIT-05, KIT-07, QUAL-03]
must_haves:
  truths:
    - "An icon-only size/variant on the shared control/Button recipe exists and is catalogued in Button's StateMatrix; the FileUpload retry + Ark ItemDeleteTrigger route through Button (via asChild), and the duplicate `itemDeleteTrigger` recipe is deleted."
    - "FileUpload forwards `disabled` to the row controls — a disabled FileUpload's row controls are non-interactive (the dead `data-[disabled]` branch is now driven)."
    - "A required Field shows a visible required affordance (a `*` paired with visually-hidden 'required' text, or the fieldRequired string) driven by the existing `required` prop — never the asterisk in color alone; helperText is linked to the control via aria-describedby."
    - "The new form-affordances regression test FAILS on the pre-fix code (no required marker; row controls off-catalog / not disabled-forwarded; helperText not described) and PASSES after."
    - "pnpm check + pnpm --filter @solid-stats/design test:e2e + pnpm --filter @solid-stats/design test stay green."
  artifacts:
    - packages/design/src/shared/uikit/Button/control.ts
    - packages/design/src/shared/uikit/Field/Field.tsx
    - packages/design/tests/form-affordances.spec.ts
  key_links:
    - "control.ts icon-only variant → Button → FileUpload retry/delete (asChild) → one ≥44px ring source, no duplicate recipe."
    - "Field `required` prop → visible required marker + visually-hidden text; Field `helperText` → aria-describedby on the control."
---

<objective>
Close GAP-06 + GAP-07 (majors) — the form-control affordance hardening.
GAP-06: FileUpload's retry `<button>` and the Ark `ItemDeleteTrigger` use a bespoke `itemDeleteTrigger` recipe re-implementing the ≥44px hit area + focus ring that `control.ts` already owns; the icon-buttons are not in the Button catalog (the ambiguity that made the human think accepted files couldn't be removed). Also `disabled` is not forwarded to the row controls (dead `data-[disabled]` branch) [src: 03-UAT.md#GAP-06] [src: 03-UAT-VISUAL-FINDINGS.md#FU1].
GAP-07: a required Field has NO visible required marker (`required` only sets `aria-required` — worse than color-alone); the specced `fieldRequired` copy is never consumed; and `helperText` is not linked via `aria-describedby` (only the error is) [src: 03-UAT.md#GAP-07] [src: 03-UAT-VISUAL-FINDINGS.md#GAP-F1].

Purpose: route the FileUpload row controls through the catalogued Button (one ring/hit-area source), forward `disabled`, give required fields a visible marker, and associate helper text.

Output: an icon-only Button variant + catalogued state, refactored FileUpload row controls, a required-marker Field, and a regression test failing on the pre-fix gaps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-CONTEXT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md

# CONVENTIONS — SKILL.md INDEX + named references
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/forms.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/architecture.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/tests.md
@.agents/skills/solidstats-frontend-react-tests/SKILL.md
@.agents/skills/solidstats-shared-testing-standards/SKILL.md

# Source the executor edits / depends on
@packages/design/src/shared/uikit/Button/control.ts
@packages/design/src/shared/uikit/Button/Button.tsx
@packages/design/src/shared/uikit/Button/Button.stories.tsx
@packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
@packages/design/src/shared/uikit/FileUpload/fileUpload.ts
@packages/design/src/shared/uikit/Field/Field.tsx
@packages/design/src/shared/uikit/Field/field.ts
@packages/design/src/shared/uikit/_fixtures/strings.ts
</context>

<premises>
  - claim: FileUpload uses a bespoke `itemDeleteTrigger` recipe for the retry button + Ark ItemDeleteTrigger, duplicating control.ts's ≥44px + ring; the icon-buttons are not catalogued in Button
    src: fileUpload.ts#L124
    verify: grep -n "itemDeleteTrigger\|min-h-11\|shadow-(--shadow-ring)" packages/design/src/shared/uikit/FileUpload/fileUpload.ts
  - claim: `disabled` is not forwarded to the FileUpload row controls (the retry button + delete trigger), leaving the data-[disabled] branch dead
    src: 03-UAT-VISUAL-FINDINGS.md#FileUpload-minor
    verify: grep -n "disabled" packages/design/src/shared/uikit/FileUpload/FileUpload.tsx
  - claim: Field's `required` prop only reaches ArkField.Root (aria-required) — no visible marker; the fieldRequired string exists but is unused
    src: Field.tsx#L61
    verify: grep -n "required\|fieldRequired\|ArkField.Label" packages/design/src/shared/uikit/Field/Field.tsx packages/design/src/shared/uikit/_fixtures/strings.ts
  - claim: only the error is associated via Ark; helperText is not linked via aria-describedby
    src: 03-UAT-VISUAL-FINDINGS.md#Field-minor
    verify: grep -n "HelperText\|aria-describedby\|ErrorText" packages/design/src/shared/uikit/Field/Field.tsx
  - claim: control.ts already owns the ≥44px floor (min-h-11) + the one canonical focus ring; Button forwards arbitrary button attrs incl. aria-label and renders children
    src: control.ts#L40
    verify: grep -n "min-h-11\|shadow-(--shadow-ring)\|ButtonVariant\|ButtonSize" packages/design/src/shared/uikit/Button/control.ts
</premises>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — form-affordances regression spec (required marker + helper describedby + row controls catalogued/disabled)</name>
  <files>packages/design/tests/form-affordances.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Field/Field.stories.tsx (or the KIT-05 Field/Input stories) — a story that renders a REQUIRED field + a field with helperText; the data hooks (`data-field`, the label) to locate.
    - packages/design/src/shared/uikit/FileUpload/FileUpload.stories.tsx — the `retry` cell (accepted rows with onRetry) and the `disabled` cell; the row control hooks to assert against (after refactor the delete/retry are Buttons).
    - packages/design/src/shared/uikit/Button/Button.stories.tsx — where the icon-only Button state will be catalogued (so the test can assert it exists + is axe-clean + ≥44px via the generic catalog gate; this spec adds the Field/FileUpload-specific assertions).
    - @.agents/skills/solidstats-frontend-react-tests/SKILL.md — Playwright-against-Ladle; assert rendered DOM + computed a11y attributes, not internals.
  </read_first>
  <behavior>
    - Test 1 (required marker visible + not color-alone): on a required Field story, assert a visible required affordance is present in the label (a `*` plus visually-hidden "required" text, or the fieldRequired string) — assert the accessible text contains the required indicator (so it is not carried by color alone). On pre-fix code there is no visible marker → fails RED.
    - Test 2 (helperText associated): on a Field-with-helperText story, assert the control's `aria-describedby` includes the helperText element id (the helper is programmatically associated). On pre-fix code only the error is linked → fails RED.
    - Test 3 (row controls catalogued + disabled-forwarded): on the FileUpload `retry` cell, assert the retry + delete controls render as the shared Button (the catalogued icon-only control — assert the shared control class/`data-*` marker, ≥44px box); on the `disabled` FileUpload, assert the row controls are non-interactive (disabled/pointer-events-none). On pre-fix code the controls use the bespoke recipe and disabled is not forwarded → fails RED.
  </behavior>
  <action>
    Create the Playwright-against-Ladle regression for GAP-06 + GAP-07. Drive the relevant KIT-05 Field/FileUpload stories and assert: (1) a visible, non-color-alone required marker on a required field; (2) `aria-describedby` on the control includes the helperText id; (3) the FileUpload retry/delete row controls render through the shared Button (catalogued icon-only control, ≥44px) and are disabled when the FileUpload is disabled. [src: 03-UAT.md#GAP-06] [src: 03-UAT.md#GAP-07]

    This MUST FAIL on pre-fix code (no required marker; helper not described; bespoke off-catalog row controls; disabled not forwarded). Run RED before Tasks 2–4. No `.skip`. Commit RED: `test(03-12): add failing form-affordances regression (required marker, helper describedby, catalogued row controls)`.

    No code fences in this action — describe the assertions in prose.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test:e2e -- form-affordances.spec.ts</automated>
  </verify>
  <done>The spec runs (no `.skip`) and FAILS on the current code across the three behaviors.</done>
  <acceptance_criteria>
    - Required-marker, helper-describedby, and row-control-catalogued/disabled assertions all fail RED pre-fix.
    - Assertions read rendered DOM + computed a11y attributes (tests.md), not source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — icon-only variant on the shared control/Button + catalog it</name>
  <files>packages/design/src/shared/uikit/Button/control.ts, packages/design/src/shared/uikit/Button/Button.tsx, packages/design/src/shared/uikit/Button/Button.stories.tsx</files>
  <read_first>
    - packages/design/src/shared/uikit/Button/control.ts — the `tv()` variants (variant/size/justify/active/disabled), the `FORCED_STATE` catalog mirror (control.test.ts asserts it stays in sync — keep it consistent), and the `base` that owns `min-h-11` + the ring.
    - packages/design/src/shared/uikit/Button/Button.tsx — forwards button attrs incl. aria-label; icon-only needs an accessible name (the component does not invent one).
    - packages/design/src/shared/uikit/Button/Button.stories.tsx — the StateMatrix layout where the icon-only state gets catalogued.
    - packages/design/src/shared/uikit/Button/control.test.ts — the unit test pinning FORCED_STATE to the recipe; update it if a new variant adds forced states.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md — square icon-only control = ≥44px both axes (min-h-11 min-w-11), tokens only.
  </read_first>
  <action>
    Add an icon-only affordance to the shared `control` recipe — a size or variant that keeps the ≥44px floor on BOTH axes (square: `min-h-11 min-w-11`, centered, the canonical ring), the ghost-style hover treatment the row controls want, suitable to be driven via `asChild` onto an Ark trigger [src: 03-UAT.md#GAP-06]. Prefer adding it as a `size`/icon variant on `control` so all existing variants compose with it; keep `cursor-pointer` from `base`. Catalog the new icon-only state in `Button.stories.tsx`'s StateMatrix (so the generic catalog gate runs axe + 44px on it). If the new variant introduces forced states, update `FORCED_STATE` + `control.test.ts` to stay in sync (the test enforces this).

    Keep `Button.tsx` forwarding `aria-label`/children unchanged (icon-only needs a caller-supplied name). Tokens only, no arbitrary values. `pnpm check` + the unit test stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test -- control.test.ts</automated>
  </verify>
  <done>An icon-only ≥44px Button variant exists, is catalogued, and the control unit test (FORCED_STATE sync) is green.</done>
  <acceptance_criteria>
    - Icon-only control is square ≥44px with the canonical ring + cursor-pointer, tokens only.
    - Catalogued in Button's StateMatrix.
    - FORCED_STATE/control.test.ts kept in sync; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: GREEN — route FileUpload row controls through Button, forward disabled, delete the duplicate recipe</name>
  <files>packages/design/src/shared/uikit/FileUpload/FileUpload.tsx, packages/design/src/shared/uikit/FileUpload/fileUpload.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/FileUpload/FileUpload.tsx — the hand-rolled retry `<button className={styles.itemDeleteTrigger()}>` and the `<ArkFileUpload.ItemDeleteTrigger className={styles.itemDeleteTrigger()}>`; the `removeAria`/`retryAria` props (the accessible names to pass to Button). NOTE: this file is also edited by 03-10 (single-preview) — depends_on: [03-10], so build on the post-03-10 shape.
    - packages/design/src/shared/uikit/FileUpload/fileUpload.ts — the `itemDeleteTrigger` slot to DELETE once the controls route through Button; keep the other slots.
    - packages/design/src/shared/uikit/Button/Button.tsx — the icon-only variant from Task 2 to render the retry (a Button onClick) and the delete (Button via Ark `ItemDeleteTrigger asChild`).
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md — interactive controls go through the shared Button primitive; icon-only needs an accessible name.
  </read_first>
  <action>
    Route the FileUpload retry + delete row controls through the shared `Button` icon-only variant [src: 03-UAT.md#GAP-06]:
    - Retry: render a `Button` (icon-only variant, `aria-label={retryAria}`, `onClick={() => onRetry(file)}`) with the `RotateCcw` icon — drop the hand-rolled `<button className={styles.itemDeleteTrigger()}>`.
    - Delete: render the Ark `ItemDeleteTrigger asChild` wrapping a `Button` (icon-only, `aria-label={removeAria}`, `X` icon) so Ark keeps the delete behavior while the visual + ring come from Button.
    - Forward `disabled`: pass `disabled` to the row controls so a disabled FileUpload's retry/delete are non-interactive (driving the previously-dead `data-[disabled]` branch).
    Delete the now-unused `itemDeleteTrigger` slot from `fileUpload.ts` (the duplicate recipe). Keep the rejection-row styling + the slice's i18n-free boundary (aria names arrive as props). Tokens only. `pnpm check` + the e2e + the fileUpload unit test stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test -- fileUpload.test.ts</automated>
  </verify>
  <done>The retry + delete controls render through the shared Button (one ring/hit-area source), `disabled` is forwarded, and the duplicate recipe is gone.</done>
  <acceptance_criteria>
    - No bespoke `itemDeleteTrigger` recipe remains; row controls are Buttons (delete via Ark asChild).
    - `disabled` forwarded to the row controls.
    - Accessible names preserved (retryAria/removeAria); slice stays i18n-free; `pnpm check` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: GREEN — Field required marker (never color-alone) + helperText aria-describedby</name>
  <files>packages/design/src/shared/uikit/Field/Field.tsx, packages/design/src/shared/uikit/Field/field.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Field/Field.tsx — the `required` prop reaching only `ArkField.Root`; the `ArkField.Label` where the visible marker goes; the `ArkField.HelperText` that needs associating; the existing `requiredText`/`fieldRequired`-style prop seam (add a `requiredText` string prop the story resolves, mirroring how label/errorText arrive — the slice stays i18n-free).
    - packages/design/src/shared/uikit/Field/field.ts — add a small marker slot (a `*` + an sr-only span) using tokens (e.g. the loss/primary color paired WITH the sr-only text so it is never color-alone).
    - packages/design/src/shared/uikit/_fixtures/strings.ts — `fieldRequired` ("Обязательное поле"/"Required") is the unused copy the story will now pass as the required text.
    - @.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md — required marker must not be color-alone; helper text associated; @.agents/.../forms.md — visible labels + associated helper/errors.
  </read_first>
  <action>
    Render a visible required affordance in the Field label when `required` is set [src: 03-UAT.md#GAP-07]: a `*` paired with visually-hidden "required" text (or the `fieldRequired` string passed as a `requiredText` prop the story resolves), driven by the EXISTING `required` prop — never the asterisk in color alone (pair the color with the sr-only/visible text). Keep the slice i18n-free: add a `requiredText` string prop the story fills from `fieldRequired` (do not import i18n into the slice).

    Associate `helperText` with the control via `aria-describedby` [src: 03-UAT-VISUAL-FINDINGS.md#Field-minor]: ensure the HelperText element has an id and the control's `aria-describedby` includes it (in addition to Ark's error association). If Ark's `Field.HelperText` already wires this through `Field.Root` context, verify it actually reaches the control; if not, add the id + describedby wiring explicitly. Preserve the existing error association + aria-live.

    Add the marker slot to `field.ts` with tokens only (no arbitrary values). `pnpm check` + e2e stay green.
  </action>
  <verify>
    <automated>pnpm check && pnpm --filter @solid-stats/design test:e2e -- form-affordances.spec.ts</automated>
  </verify>
  <done>A required Field shows a visible, non-color-alone required marker; helperText is associated via aria-describedby; the Task-1 spec is GREEN; `pnpm check` exits 0.</done>
  <acceptance_criteria>
    - Visible required marker driven by `required`, paired with text (never color-alone).
    - Slice stays i18n-free (requiredText is a resolved prop).
    - helperText associated via aria-describedby alongside the error.
    - No arbitrary values; `pnpm check` green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational primitive/recipe/story + test changes; no untrusted input, no network, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12-01 | Repudiation / a11y integrity | required marker / icon-only controls | low | mitigate | Required marker paired with sr-only/visible text (never color-alone); icon-only row controls carry caller-supplied aria-labels; catalogued state runs the axe + 44px gate. No package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm check` green.
- `pnpm --filter @solid-stats/design test` green (control.test.ts, fileUpload.test.ts).
- `pnpm --filter @solid-stats/design test:e2e` green (incl. form-affordances spec + the generic catalog gate over the new Button icon state).
</verification>

<success_criteria>
GAP-06 + GAP-07 closed: row controls catalogued + disabled-forwarded with one ring source; required fields visibly marked (not color-alone) and helper text associated — guarded by a test that fails on the pre-fix gaps.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-12-SUMMARY.md` when done.
</output>
