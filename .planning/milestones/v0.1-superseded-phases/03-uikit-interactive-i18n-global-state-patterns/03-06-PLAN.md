---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - packages/design/src/shared/uikit/Menu/Menu.tsx
  - packages/design/src/shared/uikit/Menu/menu.ts
  - packages/design/src/shared/uikit/Menu/Menu.stories.tsx
  - packages/design/src/shared/uikit/Menu/index.ts
  - packages/design/src/shared/uikit/Tabs/Tabs.tsx
  - packages/design/src/shared/uikit/Tabs/tabs.ts
  - packages/design/src/shared/uikit/Tabs/Tabs.stories.tsx
  - packages/design/src/shared/uikit/Tabs/index.ts
  - packages/design/src/shared/uikit/Tooltip/Tooltip.tsx
  - packages/design/src/shared/uikit/Tooltip/tooltip.ts
  - packages/design/src/shared/uikit/Tooltip/Tooltip.stories.tsx
  - packages/design/src/shared/uikit/Tooltip/index.ts
  - packages/design/src/index.ts
  - packages/design/tests/keyboard.spec.ts
autonomous: true
requirements: [KIT-06, QUAL-01, QUAL-02, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "Menu trigger carries aria-expanded/aria-controls; arrow/Home/End keyboard moves the highlighted item; items are ≥44px; item hover is surface-3 and the active item cyan; Esc closes; no trap (KIT-06, SC#1, a11y.md)."
    - "Tabs use arrow-key roving tabindex; the active tab is cyan paired with the selected state (never color-alone); the focused tab is never obscured under a sticky bar (2.4.12); panels are aria-associated (KIT-06, SC#1)."
    - "Tooltip appears on focus as well as hover, respects prefers-reduced-motion, is never the only carrier of meaning, and is not used as a label (KIT-06, a11y.md)."
  artifacts:
    - "packages/design/src/shared/uikit/Menu/Menu.tsx + menu.ts — Ark Menu per-part recipe"
    - "packages/design/src/shared/uikit/Tabs/Tabs.tsx + tabs.ts — Ark Tabs per-part recipe (roving tabindex)"
    - "packages/design/src/shared/uikit/Tooltip/Tooltip.tsx + tooltip.ts — Ark Tooltip, focus+hover, reduced-motion"
    - "Menu/Tabs/Tooltip stories — StateMatrix forced-open + Playground"
  key_links:
    - "Menu.Trigger aria-expanded/aria-controls (Ark-provided) → forced-open cell passes open; item active cyan + check"
    - "Tabs.Root value/onValueChange → roving tabindex; active tab cyan + aria-selected; panel aria-controls"

premises:
  - claim: "Ark Menu/Tabs/Select own roving tabindex, Home/End/type-ahead/wrap, and aria-expanded/aria-controls natively"
    src: 03-RESEARCH.md#Don't-Hand-Roll
    verify: "node -e \"require.resolve('@ark-ui/react/menu'); require.resolve('@ark-ui/react/tabs'); require.resolve('@ark-ui/react/tooltip')\""
  - claim: "the overlay slice precedent (Dialog/Popover per-part recipe + forced-open) is established in Plan 03-05"
    src: packages/design/src/shared/uikit/Dialog/Dialog.tsx
    verify: "test -f packages/design/src/shared/uikit/Dialog/Dialog.tsx"
  - claim: "the Menu aria-expanded/controls + Tabs roving-tabindex spec scaffolds exist RED from Plan 03-01"
    src: packages/design/tests/keyboard.spec.ts
    verify: "grep -nE 'aria-expanded|roving' packages/design/tests/keyboard.spec.ts"

carried_forward:
  - claim: "the focused control must not be obscured by a sticky header (2.4.12) — the Tabs spec asserts the inset/visible focus, mirroring the Table GAP-10 oracle"
    src: packages/design/tests/keyboard.spec.ts
---

<objective>
Complete the overlay family (KIT-06): `Menu` (trigger menu — aria-expanded/controls, arrow keyboard, ≥44px items, cyan active), `Tabs` (roving-tabindex tab set — cyan active tab paired with selection, focus-not-obscured, aria-associated panels), and `Tooltip` (focus+hover, reduced-motion, never the only meaning carrier). Ark owns the accessible behavior; the slice adds the `tv()`-per-part recipe + aria props.

Purpose: KIT-06 needs menus, tabs, and tooltips as keyboard-accessible, focus-managed, no-trap overlays (SC#1). Ark owns roving tabindex / Home-End / type-ahead and the aria wiring — the slice adds only the `tv()`-per-part recipe + aria props (the D-01 boundary), never hand-rolling keydown handlers (Don't-Hand-Roll), and demonstrates the ×7 states / forced-open cells (D-07). This completes the interactive overlay catalog.
Output: `Menu` + `Tabs` + `Tooltip` slices (component + recipe + stories + barrel); the Menu aria-expanded/controls + Tabs roving-tabindex keyboard specs turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-PATTERNS.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/component-shape.md
@packages/design/src/shared/uikit/Dialog/Dialog.tsx
@packages/design/src/shared/uikit/Button/control.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Menu — Ark Menu per-part recipe, aria-expanded/controls, ≥44px items, cyan active</name>
  <files>packages/design/src/shared/uikit/Menu/Menu.tsx, packages/design/src/shared/uikit/Menu/menu.ts, packages/design/src/shared/uikit/Menu/index.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Dialog/Dialog.tsx (the per-part overlay shell precedent)
    - packages/design/src/shared/uikit/Popover/popover.ts (the popover-surface recipe Menu's content mirrors)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Don't-Hand-Roll — Ark Menu owns roving tabindex + Home/End/type-ahead + aria-expanded/controls; anti-pattern — className per part, asChild only to slot the shared Button into the trigger)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-06 → Menu contract: popover-style surface; aria-expanded/controls on trigger; arrow/Home/End; item hover surface-3, active cyan; ≥44px items; Esc closes; no trap)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (prefer the Ark primitive over hand-rolling option/menu keyboard handling)
  </read_first>
  <action>
    Create `Menu/menu.ts`: a `tv({ slots: { trigger, positioner, content, item, itemGroup, separator } })` over Ark Menu parts [src: 03-PATTERNS.md#tv-recipe-per-part]; content the popover surface (`bg-surface-1 border border-border-2 rounded-lg shadow-md`); items `min-h-11` (≥44px) with `hover:bg-surface-3`, the active/highlighted item `text-primary` (paired with the highlighted state — never color-alone) [src: 03-UI-SPEC.md#KIT-06]. `className` per part; `asChild` ONLY if slotting the shared `Button` into the trigger [src: 03-RESEARCH.md#Anti-Patterns-to-Avoid]. `/lite`, literal, emitted tokens.

    Create `Menu/Menu.tsx`: a named `Menu` with controlled `open`/`onOpenChange`, a `trigger` element/label, a typed `items: readonly { value: string; label: string; onSelect?: () => void }[]`, `className?`, `data-menu` hook. Render Ark `Menu.Root`/`Trigger`/`Positioner`/`Content`/`Item` — Ark provides `aria-expanded`/`aria-controls`, the roving tabindex, and Esc-close (do not hand-roll) [src: 03-RESEARCH.md#Don't-Hand-Roll]. No i18n import. Add `Menu/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Menu renders the per-part recipe over Ark Menu with a popover surface, ≥44px items, cyan active item, and Ark-provided aria-expanded/controls + keyboard; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tabs + Tooltip — roving-tabindex tab set + focus/hover reduced-motion tooltip</name>
  <files>packages/design/src/shared/uikit/Tabs/Tabs.tsx, packages/design/src/shared/uikit/Tabs/tabs.ts, packages/design/src/shared/uikit/Tabs/index.ts, packages/design/src/shared/uikit/Tooltip/Tooltip.tsx, packages/design/src/shared/uikit/Tooltip/tooltip.ts, packages/design/src/shared/uikit/Tooltip/index.ts</files>
  <read_first>
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UI-SPEC.md (### KIT-06 → Tabs contract: active tab cyan paired with selected, never color-alone; arrow roving tabindex; focused tab never obscured under a sticky bar 2.4.12; panel aria-association. Tooltip contract: not a label substitute; on focus as well as hover; respects prefers-reduced-motion; never the only meaning carrier)
    - .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-RESEARCH.md (Don't-Hand-Roll — Ark Tabs roving tabindex; a11y.md focus-not-obscured)
    - packages/design/src/shared/uikit/Button/control.ts (the canonical ring the tab/tooltip-trigger focus uses)
  </read_first>
  <behavior>
    - Test (tabs.ts pure-logic, in Tabs.stories or a tabs.test.ts): the active-tab slot resolves `text-primary` (cyan) AND the recipe pairs it with a structural selected marker (e.g. an underline/border utility) so selection is not color-alone [src: 03-UI-SPEC.md#KIT-06].
    - Test: the tooltip content recipe carries `motion-reduce:` handling (animation dropped under reduced motion) [src: a11y.md#Targets-&-motion].
  </behavior>
  <action>
    Create `Tabs/tabs.ts` + `Tabs/Tabs.tsx`: a `tv({ slots: { list, trigger, indicator, content } })` over Ark Tabs parts; the active trigger is cyan (`text-primary`/`border-primary`) paired with a structural indicator (underline/border — selection is never color-alone) [src: 03-UI-SPEC.md#KIT-06]; the canonical `focus-visible:shadow-(--shadow-ring)` ring so the focused tab is visible and not obscured under a sticky bar (2.4.12). Controlled `value`/`onValueChange`, a typed `tabs: readonly { value: string; label: string }[]`, panel children keyed by value; Ark owns the roving tabindex + panel aria-association (do not hand-roll). `data-tabs` hook; no i18n import. Add `Tabs/index.ts`.

    Create `Tooltip/tooltip.ts` + `Tooltip/Tooltip.tsx`: a `tv({ slots: { positioner, content, arrow } })` over Ark Tooltip parts; content a small `surface-1`/`border-2` surface with `motion-reduce:` dropping the animation [src: a11y.md#Targets-&-motion]. A named `Tooltip` wrapping `Tooltip.Root`/`Trigger`/`Positioner`/`Content` — appears on focus AND hover (Ark default), `content: string` (resolved), `data-tooltip` hook. Document in a head comment that a tooltip is NOT a label substitute and never the only meaning carrier (the consuming story pairs it with a visible label/icon) [src: 03-UI-SPEC.md#KIT-06]. No i18n import. Add `Tooltip/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design test -- tabs && pnpm --filter @solid-stats/design exec tsc --noEmit</automated>
  </verify>
  <done>Tabs is a roving-tabindex set with a cyan active tab paired with a structural marker and a visible focus ring; Tooltip appears on focus+hover with reduced-motion handling; the cyan-not-alone + reduced-motion contracts are pinned; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Menu/Tabs/Tooltip stories (forced-open), barrel, Menu/Tabs keyboard specs GREEN</name>
  <files>packages/design/src/shared/uikit/Menu/Menu.stories.tsx, packages/design/src/shared/uikit/Tabs/Tabs.stories.tsx, packages/design/src/shared/uikit/Tooltip/Tooltip.stories.tsx, packages/design/src/index.ts, packages/design/tests/keyboard.spec.ts</files>
  <read_first>
    - packages/design/src/shared/uikit/Dialog/Dialog.stories.tsx (the forced-open + Playground two-mode pattern just established for overlays)
    - packages/design/src/shared/uikit/_state-matrix/StateMatrix.tsx (StateCell reuse, data-state-cell labels)
    - packages/design/tests/keyboard.spec.ts (the Menu aria-expanded/controls + Tabs roving-tabindex + trap-free scaffolds from Plan 03-01 — currently RED; create the matching story ids)
    - packages/design/src/index.ts (region-comment barrel append)
  </read_first>
  <action>
    Create `Menu.stories.tsx` (title `KIT-06 Overlay / Menu`), `Tabs.stories.tsx` (title `KIT-06 Overlay / Tabs`), `Tooltip.stories.tsx` (title `KIT-06 Overlay / Tooltip`), each two-mode (D-02): a `StateMatrix` with a FORCED-OPEN cell via Ark `open`/`defaultOpen` for Menu/Tooltip and the active-tab + ×4 (where lists apply) cells for Tabs [src: 03-RESEARCH.md#Pitfall-2], plus an interactive `Playground`. Resolve copy via `i18n._({ id })` and pass plain string props; the Tooltip story pairs the tooltip with a visible label (never the only carrier). Give the forced-open Menu cell + the Tabs story stable ids matching the Wave-0 keyboard spec references. RU-longest-label check at 360 floor (QUAL-05).

    Append to `src/index.ts` a region `// ---- KIT-06 Overlay family — Menu + Tabs + Tooltip (Wave 6 / Plan 03-06) ----` exporting `Menu`, `Tabs`, `Tooltip` (+ public types). This COMPLETES the KIT-06 overlay family.

    Turn the Plan-03-01 Menu + Tabs keyboard spec blocks GREEN in `tests/keyboard.spec.ts`: Menu trigger `aria-expanded` toggles false→true and `aria-controls` points at the open menu; arrow-key moves the highlighted item; Tab does not trap. Tabs: arrow-key roving tabindex moves the active tab; the active tab carries `aria-selected`; the focused tab's ring is visible/inset and not clipped under a sticky bar (mirror the Table GAP-10 inset-ring oracle) [src: keyboard.spec.ts].
  </action>
  <verify>
    <automated>pnpm --filter @solid-stats/design ladle:build && pnpm --filter @solid-stats/design test:e2e -- keyboard.spec.ts catalog.spec.ts</automated>
  </verify>
  <done>Menu + Tabs + Tooltip each have a forced-open/active StateMatrix + Playground rendering bilingually; all three are graduated into the barrel completing KIT-06; the catalog axe/44px gate and the Menu aria-expanded/controls + Tabs roving-tabindex + focus-not-obscured keyboard specs are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story fixture → Menu items / Tabs / Tooltip content | Author-controlled fixture strings; no untrusted input, no network/server-2/auth (v0.1 presentational Ladle). Portals mount to document.body (styling note only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Tampering | menu item / tab / tooltip label interpolation | low | accept | Labels are author-controlled fixtures via JSX (auto-escaped); no dangerouslySetInnerHTML. |
| T-03-06-02 | Information disclosure | tooltip content carrying meaning | low | accept | Tooltips never carry the only meaning (the visible label/icon does); content is non-sensitive UI copy. UX/a11y discipline, not a security control. |
| T-03-SC | Tampering | npm installs | n/a (no installs this plan) | mitigate | Deps gated by the Plan 03-01 blocking-human checkpoint. |
</threat_model>

<verification>
- `pnpm --filter @solid-stats/design test:e2e -- catalog.spec.ts keyboard.spec.ts` green — axe + 44px on Menu/Tabs/Tooltip; Menu aria-expanded/controls + Tabs roving-tabindex + focus-not-obscured GREEN.
- `pnpm --filter @solid-stats/design test` green (tabs cyan-not-alone + tooltip reduced-motion).
- `tsc --noEmit` green; no arbitrary values.
</verification>

<success_criteria>
- Menu, Tabs, Tooltip complete the KIT-06 overlay family with Ark-owned keyboard/aria and the per-part recipes.
- Tabs selection is never color-alone and the focused tab is not obscured (2.4.12); Tooltip is focus+hover + reduced-motion and never the only meaning carrier.
- All three are graduated into the barrel; the overlay keyboard contracts are GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-06-SUMMARY.md` when done.
</output>
