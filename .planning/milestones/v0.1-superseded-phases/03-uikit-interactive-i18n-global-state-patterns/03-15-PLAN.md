---
phase: 03-uikit-interactive-i18n-global-state-patterns
plan: 15
type: execute
wave: 3
depends_on: [03-09, 03-11, 03-12, 03-13]
files_modified:
  - DESIGN.md
  - packages/design/src/styles/theme.css
  - packages/design/src/shared/uikit/Field/field.ts
autonomous: true
gap_closure: true
requirements: [KIT-05, QUAL-03]
must_haves:
  truths:
    - "The GAP-14 caption decision is RECORDED in DESIGN.md with rationale: either a distinct, AA-contrast caption color token is introduced (and theme.css re-exported), OR the spec-correct weight/case/tracking delta is explicitly accepted as sufficient (same color intentional)."
    - "If a caption token was added: theme.css is regenerated via scripts/gen-theme.mjs (never hand-edited), `design.md lint DESIGN.md` passes (contrast), and the Field helperText slot points at the new token — never an arbitrary value."
    - "The theme.css drift gate is green (the committed theme.css byte-matches the gen-theme.mjs output)."
    - "pnpm check stays green; no arbitrary Tailwind values introduced."
  artifacts:
    - DESIGN.md
    - packages/design/src/styles/theme.css
    - packages/design/src/shared/uikit/Field/field.ts
  key_links:
    - "DESIGN.md caption color decision → gen-theme.mjs re-export → theme.css token → Field helperText slot (if a token is added); the drift gate keeps DESIGN.md the single token source of truth."
---

<objective>
Close GAP-14 (minor) as an isolated design-system / gen-theme workflow: a Field caption (helperText) reads too similarly to its label because both share the `text-muted` color (they differ only by weight/uppercase/tracking). The spec forbids `text-subtle` for meaningful copy, so ANY change is a DESIGN.md token decision — not an arbitrary value [src: 03-UAT.md#GAP-14] [src: 03-UAT-VISUAL-FINDINGS.md#GAP-F2].

This plan was split out of 03-14 (which was over the task/file threshold) to isolate the `DESIGN.md → gen-theme.mjs → theme.css` token workflow + its drift gate from the form-layout/a11y sweep.

Purpose: record the caption-color decision in DESIGN.md (the single token source of truth) and, if a distinct token is chosen, re-export theme.css via the generator and point Field's caption at it — keeping theme.css a pure build output.

Output: a recorded DESIGN.md decision (+ a re-exported theme.css + a Field caption token, if option 1) with the drift gate green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-CONTEXT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT.md
@.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-UAT-VISUAL-FINDINGS.md

# CONVENTIONS — SKILL.md INDEX + named references
@.agents/skills/solidstats-frontend-react-conventions/SKILL.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md
@.agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md
# DESIGN — SKILL.md INDEX + the design-system reference (the DESIGN.md → @theme workflow + gates)
@.agents/skills/solidstats-frontend-react-design/SKILL.md
@.agents/skills/solidstats-frontend-react-design/references/design-system.md

# Source the executor edits / depends on
@DESIGN.md
@packages/design/src/styles/theme.css
@packages/design/src/shared/uikit/Field/field.ts
@scripts/gen-theme.mjs
</context>

<premises>
  - claim: DESIGN.md defines text-muted (#98A2B6, AA everywhere) and text-subtle (#616B80, AA-large/UI/disabled only) and a `caption` role at text-subtle; label + caption both render text-muted today
    src: DESIGN.md#L168
    verify: grep -n "caption\|text-muted\|text-subtle" DESIGN.md
  - claim: theme.css is generated by scripts/gen-theme.mjs and is NEVER hand-edited; a drift gate compares the committed theme.css against the generator output
    src: design-system.md (Export → Tailwind @theme)
    verify: grep -n "gen-theme\|theme.css" scripts/gen-theme.mjs
  - claim: the Field helperText slot uses text-muted (same color as the label) — the caption color decision lands here
    src: field.ts#L24
    verify: grep -n "helperText\|text-muted\|text-subtle" packages/design/src/shared/uikit/Field/field.ts
  - claim: field.ts is also edited by 03-12 (the required-marker slot) — this plan depends_on 03-12 and builds on the post-03-12 field.ts
    src: 03-12-PLAN.md#files_modified
    verify: grep -n "field.ts" .planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-12-PLAN.md
</premises>

<tasks>

<task type="auto">
  <name>Task 1: GAP-14 caption color — record the DESIGN.md decision; re-export + point Field at the token if a token is added</name>
  <files>DESIGN.md, packages/design/src/styles/theme.css, packages/design/src/shared/uikit/Field/field.ts</files>
  <read_first>
    - DESIGN.md — the `colors.text-muted` / `colors.text-subtle` tokens, the `caption` role (currently text-subtle), and the contrast table. The spec forbids `text-subtle` for MEANINGFUL copy, so a distinct caption color is a TOKEN decision, not an arbitrary value.
    - packages/design/src/shared/uikit/Field/field.ts — the `helperText` slot using `text-muted` (same as the label). NOTE: 03-12 added the required-marker slot to this file (depends_on: [03-12]) — build on the post-03-12 shape; do not revert it.
    - @.agents/skills/solidstats-frontend-react-design/references/design-system.md → Export → Tailwind @theme + Gates — `theme.css` is generated by `scripts/gen-theme.mjs`, NEVER hand-edited; run `design.md lint DESIGN.md` (contrast) when a color token changes.
    - scripts/gen-theme.mjs — the generator to re-run if a new caption token is added.
  </read_first>
  <action>
    Make the GAP-14 caption a recorded DESIGN.md token decision [src: 03-UAT.md#GAP-14]. Pick ONE outcome and record the rationale in DESIGN.md (English) + the SUMMARY:
    1. Introduce a distinct caption color token in DESIGN.md (a value that keeps AA contrast for meaningful copy — NOT text-subtle), re-export theme.css via `node scripts/gen-theme.mjs` (never hand-edit theme.css), run `npx @google/design.md lint DESIGN.md` to confirm contrast, and point the Field `helperText` slot at the new token (tokens only); OR
    2. Accept the spec-correct weight/case/tracking delta (label is uppercase / 600 / tracking-label; caption is normal) as sufficient separation, and record in DESIGN.md that the shared color is intentional — making NO code change beyond the recorded decision.

    Either way: NO arbitrary value; if a token is added, theme.css is regenerated (not hand-edited) and the drift gate (committed theme.css == generator output) stays green. Use Edit (scoped) on DESIGN.md — do not rewrite the whole file. `pnpm check` stays green.
  </action>
  <verify>
    <automated>pnpm check && node scripts/gen-theme.mjs && git diff --exit-code packages/design/src/styles/theme.css</automated>
  </verify>
  <done>The caption decision is recorded in DESIGN.md (a new contrast-checked token re-exported + Field pointed at it, OR the same-color delta accepted); theme.css byte-matches the generator output; `pnpm check` exits 0.</done>
  <acceptance_criteria>
    - Decision recorded in DESIGN.md with rationale.
    - If a token was added: theme.css regenerated via gen-theme.mjs (not hand-edited), `design.md lint` contrast passes, Field caption points at the token.
    - No arbitrary value; the drift gate (gen-theme output == committed theme.css) is green; 03-12's required-marker slot preserved.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | DESIGN.md token + generated theme.css + a recipe slot; no untrusted input, no network, no package installs (design.md is an existing dev tool, not a new install). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15-01 | Tampering | DESIGN.md → theme.css token pipeline | low | mitigate | theme.css stays a pure generated output (re-exported via gen-theme.mjs, never hand-edited); the drift gate enforces DESIGN.md as the single token source of truth; contrast lint gates a new color token. No package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm check` green (incl. the theme.css drift gate).
- `node scripts/gen-theme.mjs && git diff --exit-code packages/design/src/styles/theme.css` clean (generated output committed).
- `npx @google/design.md lint DESIGN.md` passes if a caption color token was added.
</verification>

<success_criteria>
GAP-14 closed: the caption-color decision is recorded in DESIGN.md as a token decision (a distinct AA-contrast token re-exported + Field pointed at it, OR the spec-correct weight/case/tracking delta accepted) — never an arbitrary value, with theme.css kept a pure generated build output.
</success_criteria>

<output>
Create `.planning/phases/03-uikit-interactive-i18n-global-state-patterns/03-15-SUMMARY.md` when done.
</output>
