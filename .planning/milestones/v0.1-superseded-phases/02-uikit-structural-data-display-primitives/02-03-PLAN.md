---
phase: 02-uikit-structural-data-display-primitives
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/design/src/shared/uikit/Badge/index.ts
  - packages/design/src/shared/uikit/Badge/Badge.tsx
  - packages/design/src/shared/uikit/Badge/Badge.stories.tsx
  - packages/design/src/shared/uikit/Pill/index.ts
  - packages/design/src/shared/uikit/Pill/Pill.tsx
  - packages/design/src/shared/uikit/Pill/Pill.stories.tsx
  - packages/design/src/shared/uikit/Skeleton/index.ts
  - packages/design/src/shared/uikit/Skeleton/Skeleton.tsx
  - packages/design/src/shared/uikit/Skeleton/Skeleton.stories.tsx
  - packages/design/src/shared/uikit/EmptyState/index.ts
  - packages/design/src/shared/uikit/EmptyState/EmptyState.tsx
  - packages/design/src/shared/uikit/EmptyState/EmptyState.stories.tsx
  - packages/design/src/shared/uikit/ErrorState/index.ts
  - packages/design/src/shared/uikit/ErrorState/ErrorState.tsx
  - packages/design/src/shared/uikit/ErrorState/ErrorState.stories.tsx
  - packages/design/src/shared/uikit/Toast/index.ts
  - packages/design/src/shared/uikit/Toast/Toast.tsx
  - packages/design/src/shared/uikit/Toast/Toast.stories.tsx
  - packages/design/src/index.ts
autonomous: true
requirements: [KIT-07, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "Skeleton reserves the exact final dimensions so skeleton→data swaps shift nothing (CLS = 0); shimmer animates opacity only and honors prefers-reduced-motion (KIT-07 / QUAL-04)"
    - "EmptyState shows h3 heading + body + actionable next step + total-count where relevant; ErrorState distinguishes system error (ref id + contact) from user error (KIT-07 / QUAL-01)"
    - "Toast is the visual primitive only (4 semantic variants, icon + label) — NO trigger/portal/queue manager (Phase 3) (KIT-07)"
    - "Badge (badge-outcome-*, badge-status-*) and Pill always render icon + label; rounded-xs for badges, rounded-full for pills only (KIT-07 / QUAL-03)"
    - "These primitives are the ×5 scenario-ending vocabulary (success/error/loading/onboarding/empty) and render that matrix per story (QUAL-01)"
    - "Every feedback story is axe-clean, keyboard-operable (Toast action ring), ≥44px targets, RU + EN from _fixtures/strings (QUAL-03 / QUAL-05)"
    - "The family graduates into the @solid-stats/design barrel"
  artifacts:
    - path: "packages/design/src/shared/uikit/Skeleton/Skeleton.tsx"
      provides: "Reserved-dimension skeleton (table/tile/text variants), opacity-only shimmer, motion-reduce static"
      contains: "motion-reduce"
    - path: "packages/design/src/shared/uikit/EmptyState/EmptyState.tsx"
      provides: "Empty state: h3 + body + action + total count"
      contains: "export function EmptyState"
    - path: "packages/design/src/shared/uikit/ErrorState/ErrorState.tsx"
      provides: "Error state: system (ref+contact) vs user variant"
      contains: "tv("
    - path: "packages/design/src/shared/uikit/Toast/Toast.tsx"
      provides: "Toast visual primitive, 4 semantic variants, icon+label, optional action"
      contains: "tv("
    - path: "packages/design/src/shared/uikit/Badge/Badge.tsx"
      provides: "Badge: outcome-win/loss + status-pending/approved/rejected, icon+label, rounded-xs"
      contains: "tv("
    - path: "packages/design/src/shared/uikit/Pill/Pill.tsx"
      provides: "Pill: rounded-full label+icon"
      contains: "rounded-full"
    - path: "packages/design/src/index.ts"
      provides: "Barrel graduating the KIT-07 feedback family"
      contains: "Skeleton"
  key_links:
    - from: "packages/design/src/shared/uikit/Skeleton/Skeleton.stories.tsx"
      to: "packages/design/src/shared/uikit/_state-matrix/index.ts"
      via: "renders the skeleton-vs-final reserved-dimension proof in a StateMatrix"
      pattern: "StateMatrix|StateCell"
    - from: "packages/design/src/shared/uikit/Badge/Badge.tsx"
      to: "packages/design/src/styles/theme.css"
      via: "consumes win/loss/warn/info semantic tokens via token utilities"
      pattern: "win|loss|warn|info"

premises:
  - claim: DESIGN.md authors badge-outcome-win/loss (L295/L302), badge-status-pending/approved/rejected (L309/L316/L323), card (L357)
    src: DESIGN.md#components
    verify: grep -nE '^\s+(badge-outcome-win|badge-outcome-loss|badge-status-pending|badge-status-approved|badge-status-rejected|card):' DESIGN.md
  - claim: Toast trigger/portal/queue manager is Phase 3 — Phase 2 ships only the toast visual primitive
    src: 02-CONTEXT.md#Deferred-Ideas
    verify: sed -n '226,239p' .planning/phases/02-uikit-structural-data-display-primitives/02-CONTEXT.md
  - claim: the FreshnessPill/TrustBadge tv() + token pattern from Plan 02 is established and reusable for Badge/Pill/Toast/ErrorState variants
    src: 02-02-SUMMARY.md
    verify: ls packages/design/src/shared/uikit/FreshnessPill

carried_forward:
  - claim: keep every class literal in tv(), all under src/, never compute class names — Tailwind v4 tree-shakes otherwise
    src: 01-04-SUMMARY.md#deviations
  - claim: skeleton must reproduce the exact colgroup+header+row dims for CLS = 0 (the table-variant skeleton pairs with KIT-02 in Plan 06)
    src: 02-RESEARCH.md#Pattern-5
---

<objective>
The KIT-07 feedback primitive family — the catalog vocabulary for the ×5 scenario endings (success / error / loading / onboarding / empty) that every later surface composes. Built second (after data-trust) because it reuses the established `tv()` + token + StateMatrix patterns and unblocks the Skeleton (table variant) that the KIT-02 data-table plan (Plan 06) depends on for its loading state. [src: 02-UI-SPEC.md#KIT-07] [src: mvp_mode_active]

Acceptance test for this slice: after it completes, a Phase-4+ surface-builder can import one more reviewed family (`Skeleton`, `EmptyState`, `ErrorState`, `Toast`, `Badge`, `Pill`) from `@solid-stats/design`.

Purpose: feedback primitives render with CLS = 0 (skeletons at exact final dimensions) and are the honest success/error/loading/onboarding/empty layer — never a blank screen, never color-alone. [src: ROADMAP.md#Phase-2-SC3]
Output: six colocated slices, each a StateMatrix + Playground story, graduated into the barrel and design-review APPROVED. Every story demonstrates the D-08 per-component review conventions (states / data-volume / scenario endings, defined click zone, axe-clean, keyboard-operable, 44px, never color-alone) [src: 02-CONTEXT.md#D-08].
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md
@.planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md
@.design/CLAUDE.md
@packages/design/src/styles/theme.css
@.planning/phases/02-uikit-structural-data-display-primitives/02-02-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

No runtime trust boundary — presentational fixture-only catalog. [src: 02-RESEARCH.md#Security-Domain]

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | RU+EN strings + error ref ids rendered in EmptyState/ErrorState/Toast | mitigate | All copy via JSX auto-escaping; never `dangerouslySetInnerHTML`; static literals from `_fixtures` (V5). [src: 02-RESEARCH.md#Security-Domain] |
| T-02-02 | Tampering | Lucide icons (trending-up/down, clock, badge-check, x-circle, semantic toast icons) | mitigate | Icons via `lucide-react` components, never raw SVG strings (V14). [src: 02-RESEARCH.md#Security-Domain] |
| — | Spoofing / Repudiation / Info-disclosure / DoS / Elevation | — | N/A | No auth/session/network/persistence/privilege this phase. [src: 02-RESEARCH.md#Security-Domain] |

V5 note: the ErrorState ref/debug id is a static fixture placeholder (`{id}`) — no real id source exists this phase; it is escaped literal text.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge + Pill (outcome/status variants, icon+label)</name>
  <files>packages/design/src/shared/uikit/Badge/index.ts, packages/design/src/shared/uikit/Badge/Badge.tsx, packages/design/src/shared/uikit/Badge/Badge.stories.tsx, packages/design/src/shared/uikit/Pill/index.ts, packages/design/src/shared/uikit/Pill/Pill.tsx, packages/design/src/shared/uikit/Pill/Pill.stories.tsx</files>
  <read_first>
    - DESIGN.md (recipes `badge-outcome-win` L295 / `badge-outcome-loss` L302; `badge-status-pending` L309 / `-approved` L316 / `-rejected` L323; icons trending-up/down, clock, badge-check, x-circle)
    - packages/design/src/shared/uikit/TrustBadge/TrustBadge.tsx (the tv() variant pattern just established — mirror it)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-07 Badge/Pill contract: rounded-xs badges, rounded-full pills, always icon+label; Copywriting Contract: Outcome badges П/пор., status vocabulary)
    - packages/design/src/shared/uikit/_state-matrix/index.ts and _fixtures/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/styling.md (no arbitrary values; rounded-xs vs rounded-full; tv())
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (never color-alone)
  </read_first>
  <behavior>
    - Badge accepts a `variant` union covering outcome-win/loss + status-pending/approved/rejected; each renders the matching Lucide icon + literal label, `rounded-xs`, the semantic token (win/loss/warn/info) — never color-alone.
    - Pill renders `rounded-full` with icon + label (the generic rounded pill; freshness is its own KIT-04 slice).
    - Stories: a Matrix (all variants as StateCells) + a Playground (args/argTypes over the variant union).
  </behavior>
  <action>
    Create the `Badge` slice as a single component with a `variant` union via `tv()` mapping to the DESIGN.md recipes (`badge-outcome-*` L295-308, `badge-status-*` L309-329) — icon + literal label, `rounded-xs`, semantic token utilities, never color-alone [src: a11y.md]. Create the `Pill` slice (`rounded-full`, icon + label). Reuse the `tv()` + token + StateMatrix pattern from Plan 02; pull labels (П/пор., Pending/Approved/Rejected) from `_fixtures/STRINGS` (RU + EN). Keep all class strings literal inside `tv()` [src: 01-04-SUMMARY.md#deviations]. Icons are `lucide-react` PascalCase locals.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts -g "badge|pill"</automated>
  </verify>
  <acceptance_criteria>
    - Badge renders every outcome + status variant with an icon + literal label (axe no serious/critical for the badge stories).
    - `rounded-xs` on badges, `rounded-full` on pills (grep confirms badges do not use `rounded-full`).
    - No arbitrary Tailwind values / raw hex (grep returns 0).
    - RU + EN labels render from `_fixtures/STRINGS`.
  </acceptance_criteria>
  <done>Badge (outcome+status, icon+label, rounded-xs) and Pill (rounded-full) are catalogued and axe-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skeleton (CLS=0), EmptyState, ErrorState, Toast (visual only)</name>
  <files>packages/design/src/shared/uikit/Skeleton/index.ts, packages/design/src/shared/uikit/Skeleton/Skeleton.tsx, packages/design/src/shared/uikit/Skeleton/Skeleton.stories.tsx, packages/design/src/shared/uikit/EmptyState/index.ts, packages/design/src/shared/uikit/EmptyState/EmptyState.tsx, packages/design/src/shared/uikit/EmptyState/EmptyState.stories.tsx, packages/design/src/shared/uikit/ErrorState/index.ts, packages/design/src/shared/uikit/ErrorState/ErrorState.tsx, packages/design/src/shared/uikit/ErrorState/ErrorState.stories.tsx, packages/design/src/shared/uikit/Toast/index.ts, packages/design/src/shared/uikit/Toast/Toast.tsx, packages/design/src/shared/uikit/Toast/Toast.stories.tsx</files>
  <read_first>
    - .planning/phases/02-uikit-structural-data-display-primitives/02-RESEARCH.md (Pattern 5 CLS-0 skeleton reproduces colgroup+header+N×ROW_H; Pattern 4 motion-reduce)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-07 Skeleton/EmptyState/ErrorState/Toast contracts; Copywriting Contract: empty-state heading/body/filtered, error system, loading cold aggregate)
    - DESIGN.md (recipe `card` L357 for ErrorState/EmptyState container; semantic tokens for Toast variants)
    - packages/design/src/shared/uikit/Badge/Badge.tsx (the tv() variant pattern)
    - packages/design/src/shared/uikit/_state-matrix/index.ts and _fixtures/index.ts
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/performance.md (reserve final dimensions; opacity-only shimmer; CLS = 0)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/errors.md (system error = ref id + contact; user error = by-field recovery; never a blank screen)
    - .agents/skills/solidstats-frontend-react-conventions/references/patterns/a11y.md (reduced-motion; Toast action accessible name + focus ring; live-region intent)
  </read_first>
  <behavior>
    - Skeleton offers reserved-dimension variants (text line, tile, table-row/header) — the table variant reproduces a colgroup + header + N×ROW_H so a later skeleton→data swap shifts nothing; shimmer animates `opacity` only and renders static under `motion-reduce:`.
    - EmptyState renders an h3 heading + body + an actionable next step + a total-count slot (e.g. "clear filters to see all N"), with a 24px Lucide icon and reserved height.
    - ErrorState has a `kind` of `system|user`: system shows a ref/debug id + contact path; user sits by the field naming the fix; both carry a recovery action (never a blank screen).
    - Toast accepts a `variant` of success/error/warn/info — the visual primitive only: surface + icon + message + optional action button (focus ring on the action). NO trigger/portal/queue manager.
    - Each story is a StateMatrix of variants + a Playground; the Skeleton story includes a skeleton-vs-final reserved-dimension proof cell.
  </behavior>
  <action>
    Create the `Skeleton` slice with reserved-dimension variants (text/tile/table) — the table variant reproduces the exact colgroup + header + N×ROW_H per RESEARCH Pattern 5 so it pairs with KIT-02's loading state (Plan 06); shimmer animates `opacity` only, static under `motion-reduce:` (performance.md). Create `EmptyState` (h3 + body + action + total-count, recipe `card` container, 24px Lucide icon, reserved height), `ErrorState` (a `kind` variant via `tv()`: system = ref id + contact, user = by-field fix; recovery action always present, errors.md), and `Toast` (a `variant` union via `tv()`: 4 semantic variants, icon + message + optional action with a focus ring) — the VISUAL primitive only, NO trigger/portal/queue manager (that is Phase 3) [src: 02-CONTEXT.md#Deferred-Ideas].

    Pull all copy (empty heading/body/filtered, error system `{id}`, loading cold-aggregate, status labels) from `_fixtures/STRINGS` (RU + EN, D-07). Reuse the `tv()`/StateMatrix patterns; keep class strings literal [src: 01-04-SUMMARY.md#deviations]. These six primitives ARE the ×5 scenario-ending vocabulary — each story renders that matrix.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test tests/catalog.spec.ts tests/cls.spec.ts -g "skeleton|emptystate|errorstate|toast"</automated>
  </verify>
  <acceptance_criteria>
    - The Skeleton story's skeleton-vs-final cells have equal `boundingBox().height` (CLS = 0) — asserted by the catalog/cls spec.
    - Skeleton shimmer animates `opacity` only (no width/height/top/left/margin animation — grep the component) and is static under reduced-motion.
    - ErrorState system variant renders a ref/debug id + a contact path; user variant names a by-field fix.
    - Toast has the 4 semantic variants with icon + label and a focusable action; NO portal/queue/trigger code exists in the slice (grep returns 0 for `createPortal`/`useToast`-style queue).
    - All four families pass axe (no serious/critical) + 44px geometry; RU + EN from `_fixtures/STRINGS`.
  </acceptance_criteria>
  <done>Skeleton (CLS=0, opacity-only, reduced-motion), EmptyState, ErrorState (system vs user), and Toast (visual only) are catalogued and axe-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Graduate the KIT-07 family into the barrel + per-family design-review</name>
  <files>packages/design/src/index.ts</files>
  <read_first>
    - packages/design/src/index.ts (the barrel after Plan 02 graduated KIT-04 — append the KIT-07 block to a distinct region)
    - .planning/phases/02-uikit-structural-data-display-primitives/02-UI-SPEC.md (KIT-07 full contract)
    - .agents/skills/solidstats-frontend-react-design-review/SKILL.md (seven pillars)
    - .agents/skills/solidstats-frontend-react-design-review/references/checklist.md (Pillar 4 states/data-volumes; Pillar 2 CLS=0; Pillar 3 a11y; Pillar 6 status vocabulary fixed set)
    - .agents/skills/solidstats-shared-review-standards/SKILL.md (report format + verdict)
  </read_first>
  <action>
    Append named re-exports of the six KIT-07 slices (`Skeleton`, `EmptyState`, `ErrorState`, `Toast`, `Badge`, `Pill`) to `packages/design/src/index.ts`, in a distinct block below the KIT-04 graduations (append-only — barrel writes serialized by wave). Keep Smoke + `_fixtures` + `_state-matrix` absent.

    Run the per-family design-review (`solidstats-frontend-react-design-review` seven pillars + checklist) against the KIT-07 stories at the project breakpoints. Confirm the status vocabulary is the fixed set (Pending/Approved/Rejected), CLS = 0 on the Skeleton, the ×5 endings present, RU + EN natural. Fix the CLASS of any finding across the family; map findings into the shared-review-standards report format.
  </action>
  <verify>
    <automated>cd packages/design && pnpm exec ladle build && pnpm exec playwright test && cd ../.. && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `import { Skeleton, EmptyState, ErrorState, Toast, Badge, Pill } from "@solid-stats/design"` resolves (barrel grep confirms the six names alongside the KIT-04 five).
    - The full `playwright test` suite is green for the KIT-07 stories (axe + 44px + keyboard + CLS = 0).
    - `pnpm check` (root) exits 0.
    - `solidstats-frontend-react-design-review` returns APPROVE for the KIT-07 family.
  </acceptance_criteria>
  <done>The KIT-07 feedback family is importable from @solid-stats/design and passes design-review.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @solid-stats/design exec playwright test` green for KIT-07 (axe = 0 serious/critical, 44px, keyboard, Skeleton CLS = 0).
- Barrel exports the six KIT-07 slices plus the KIT-04 five; helpers/Smoke absent.
- `solidstats-frontend-react-design-review` APPROVE.
- Root `pnpm check` exits 0.
</verification>

<success_criteria>
The KIT-07 feedback family (Skeleton, EmptyState, ErrorState, Toast, Badge, Pill) is catalogued as colocated Ladle stories rendering the ×5 scenario-ending vocabulary, Skeleton at exact final dimensions (CLS = 0, opacity-only, reduced-motion), Toast visual-only (no queue/portal), always icon + label, RU + EN, axe-clean / keyboard / ≥44px, graduated into the barrel and design-review APPROVED.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created this phase):
- Components: `Skeleton` (text/tile/table variants), `EmptyState`, `ErrorState` (system/user variants), `Toast` (4 semantic variants, visual only), `Badge` (outcome + status variants), `Pill`.
- Barrel exports: the six names above appended to `packages/design/src/index.ts`.
- Stories: one colocated `*.stories.tsx` per slice (Matrix + Playground).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-uikit-structural-data-display-primitives/02-03-SUMMARY.md` when done
</output>
